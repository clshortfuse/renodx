// Mechanically reconstructed from 0x3C930BB9.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler3D s11 : register(s11);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(31.875f, 0.0f, 0.0f, 0.0f);
    const float4 c1 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c2 = float4(1.0f, 0.0f, 0.5f, -2.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = (v1.xyzx) * (c2.xxxy) + (c2.yyyx);
    r1.x = dot(r0, c[32]);
    r1.x = 1.0f / (r1.x);
    r1.y = dot(r0, c[29]);
    r1.z = dot(r0, c[30]);
    r0.z = dot(r0, c[31]);
    r1 = (r1.xxxx) * (r1.yyzz);
    r2 = (r1) * (c[33].zwxy);
    r3.x = log2(abs(r2.x));
    r3.y = log2(abs(r2.y));
    r3.z = log2(abs(r2.z));
    r3.w = log2(abs(r2.w));
    r2 = (r3) * (c[34].xxxx);
    r1.x = exp2(r2.x);
    r1.z = exp2(r2.y);
    r2.x = exp2(r2.z);
    r2.y = exp2(r2.w);
    r1.xz = (r1.xz) + (r2.xy);
    r2.x = log2(abs(r1.x));
    r2.y = log2(abs(r1.z));
    r1.xz = (r2.xy) * (c[34].yy);
    r1.x = exp2(r1.x);
    r1.z = exp2(r1.z);
    r1.x = (r1.x) * (c[35].x);
    r1.x = (r1.z) * (c[35].y) + (-(r1.x));
    r2.y = c[35].y;
    r1.z = (r1.z) * (r2.y) + (-(c[34].z));
    r1.x = 1.0f / (r1.x);
    r2.w = saturate((r1.z) * (r1.x));
    r0.xy = abs(r1.yw);
    r3 = c[27];
    r3 = saturate((r0.zyxz) * (r3) + (c[28]));
    r2.x = (r3.w) * (r3.x);
    r2.yz = r3.yz;
    r3 = (r2) * (r2);
    r2 = (c[35].zzzz) * (r2) + (c[35].wwww);
    r2 = (r3) * (r2);
    r0.x = (r2.z) * (r2.y);
    r0.y = abs(c[34].w);
    r0.x = ((-(r0.y)) >= 0.0f ? (r0.x) : (r2.w));
    r0.w = (r0.z) * (r0.z);
    r0.y = dot(c[26].yz, r0.zw) + (c[26].x);
    r0.x = (r0.x) * (r0.y);
    r0.x = (r2.x) * (r0.x);
    r2 = c[36];
    r0.y = dot(r1.yw, r2.xy) + (c[25].x);
    r0.z = dot(r1.yw, r2.zw) + (c[25].z);
    r1 = tex2D(s1, r0.yz);
    r0.yzw = (r1.xyz) * (r1.xyz);
    r0.xyz = (r0.xxx) * (r0.yzw);
    r0.w = dot(v2.xyz, v2.xyz);
    r0.w = rsqrt(r0.w);
    r1.xyz = (r0.www) * (v2.xyz);
    r2.x = max(abs(r1.y), abs(r1.z));
    r3.x = max(abs(r1.x), r2.x);
    r1.w = 1.0f / (r3.x);
    r2.xyz = (r1.xyz) * (c[5].xyz);
    r2.xyz = (r2.xyz) * (r1.www) + (v5.xyz);
    r2 = tex3D(s11, r2.xyz);
    r0.xyz = (r0.xyz) * (r2.www);
    r2.xyz = (r0.xyz) * (c[23].xyz);
    r0.xyz = (r0.xyz) * (c[22].xyz);
    r0.xyz = (r0.xyz) * (c[24].xxx);
    r2.xyz = (r2.xyz) * (c[24].yyy);
    r3.x = (c[37].w) + (v4.x);
    r3.y = v4.y;
    r3.zw = frac(abs(r3.xy));
    r3.xy = float2(((r3.x) >= 0.0f ? (r3.z) : (-(r3.z))), ((r3.y) >= 0.0f ? (r3.w) : (-(r3.w))));
    r4 = tex2D(s2, r3.xy);
    r3.zw = (r4.wy) * (c1.xy) + (c1.zw);
    r3.zw = (r3.zw) * (c2.zz) + (c2.zz);
    r3.zw = (r3.zw) * (-(c2.ww)) + (-(c2.xx));
    r3.zw = (r3.zw) + (r3.zw);
    r4.xyz = (r1.zxy) * (v3.yzx);
    r4.xyz = (r1.yzx) * (v3.zxy) + (-(r4.xyz));
    r4.xyz = (r4.xyz) * (v3.www);
    r4.xyz = (r3.www) * (r4.xyz);
    r4.xyz = (r3.zzz) * (v3.xyz) + (r4.xyz);
    r4.xyz = (v2.xyz) * (r0.www) + (r4.xyz);
    r5.xyz = normalize(r4.xyz);
    r4.xyz = (c[21].xyz) + (-(v1.xyz));
    r6.xyz = normalize(r4.xyz);
    r0.w = dot(-(r6.xyz), r5.xyz);
    r0.w = (r0.w) + (r0.w);
    r4.xyz = (r5.xyz) * (-(r0.www)) + (-(r6.xyz));
    r7.xyz = normalize(v1.xyz);
    r0.w = saturate(dot(r4.xyz, -(r7.xyz)));
    r1.w = dot(r6.xyz, r7.xyz);
    r2.w = saturate(dot(r5.xyz, r6.xyz));
    r0.xyz = (r0.xyz) * (r2.www);
    r1.w = saturate((r1.w) * (c2.z) + (c2.z));
    r4.xw = c2.xw;
    r2.w = lerp(c[40].x, -(r4.w), r1.w);
    r1.w = (r1.w) + (c2.x);
    r3.z = pow(abs(r0.w), r2.w);
    r2.xyz = (r2.xyz) * (r3.zzz);
    r2.xyz = (r1.www) * (r2.xyz);
    r2.xyz = (r2.xyz) * (c[39].xxx);
    r6 = tex2D(s3, r3.xy);
    r3 = tex2D(s4, r3.xy);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r4.yzw = (r6.xyz) * (r6.xyz);
    r2.xyz = (r2.xyz) * (r4.yzw);
    r0.w = max(abs(r5.y), abs(r5.z));
    r1.w = max(abs(r5.x), r0.w);
    r4.yzw = (r5.xyz) * (c[5].xyz);
    r0.w = 1.0f / (r1.w);
    r4.yzw = (r4.yzw) * (r0.www) + (v5.xyz);
    r5 = tex3D(s11, r4.yzw);
    r4.yzw = (r5.xyz) * (r5.xyz);
    r0.xyz = (r4.yzw) * (c0.xxx) + (r0.xyz);
    r0.xyz = (r0.xyz) * (r3.xyz) + (r2.xyz);
    r2.xyz = max(r0.xyz, c2.yyy);
    r0 = (c[7]) + (-(v1.yyyy));
    r3 = (r0) * (r0);
    r5 = (c[6]) + (-(v1.xxxx));
    r3 = (r5) * (r5) + (r3);
    r6 = (c[8]) + (-(v1.zzzz));
    r3 = (r6) * (r6) + (r3);
    r7.x = rsqrt(r3.x);
    r7.y = rsqrt(r3.y);
    r7.z = rsqrt(r3.z);
    r7.w = rsqrt(r3.w);
    r3 = saturate((r3) * (c[9]) + (r4.xxxx));
    r0 = (r0) * (r7);
    r0 = (r1.yyyy) * (r0);
    r4 = (r5) * (r7);
    r5 = (r6) * (r7);
    r0 = (r4) * (r1.xxxx) + (r0);
    r0 = saturate((r5) * (r1.zzzz) + (r0));
    r0 = (r3) * (r0);
    r1.x = dot(c[10], r0);
    r1.y = dot(c[11], r0);
    r1.z = dot(c[20], r0);
    r0.xyz = (r2.xyz) * (r1.xyz) + (r2.xyz);
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[38].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c2.x;

    return oC0;
}
