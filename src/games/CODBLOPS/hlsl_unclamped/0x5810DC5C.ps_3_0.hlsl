// Mechanically reconstructed from 0x5810DC5C.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler2D s8 : register(s8);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD2;
    float4 v2 : TEXCOORD3;
    float4 v3 : TEXCOORD5;
    float4 v4 : TEXCOORD6;
    float4 v5 : TEXCOORD7;
    float4 v6 : TEXCOORD8;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    const float4 c0 = float4(2.75f, -2.0f, 0.5f, 2.0f);
    const float4 c1 = float4(60.0f, 0.25f, 1.0f, 0.0f);
    const float4 c2 = float4(75.0f, 0.5f, -0.5f, 3.68000007f);
    const float4 c3 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c4 = float4(0.00333333341f, 300.0f, -64.0301971f, 9.40301991f);
    const float4 c12 = float4(-2.0f, 3.0f, 31.875f, 100.0f);
    const float4 c13 = float4(8.0f, 0.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = (v3.xyzx) * (c1.zzzw) + (c1.wwwz);
    r1.w = dot(r0, c[33]);
    r1.w = 1.0f / (r1.w);
    r1.x = dot(r0, c[30]);
    r1.y = dot(r0, c[31]);
    r1 = (r1.wwww) * (r1.xxyy);
    r2 = (r1) * (c[34].zwxy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.z = log2(abs(r2.z));
    r2.w = log2(abs(r2.w));
    r2 = (r2) * (c[35].xxxx);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = exp2(r2.z);
    r2.w = exp2(r2.w);
    r2.xy = (r2.zw) + (r2.xy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.xy = (r2.xy) * (c[35].yy);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r1.z = (r2.x) * (c[36].x);
    r1.y = (r2.y) * (c[36].y) + (-(r1.z));
    r1.z = c[36].y;
    r1.z = (r2.y) * (r1.z) + (-(c[35].z));
    r1.y = 1.0f / (r1.y);
    r3.xy = abs(r1.xw);
    r3.z = dot(r0, c[32]);
    r4.w = saturate((r1.z) * (r1.y));
    r2 = c[28];
    r2 = saturate((r3.zyxz) * (r2) + (c[29]));
    r4.x = (r2.w) * (r2.x);
    r4.yz = r2.yz;
    r2 = (r4) * (r4);
    r4 = (c[36].zzzz) * (r4) + (c[36].wwww);
    r2 = (r2) * (r4);
    r1.y = (r2.z) * (r2.y);
    r1.z = abs(c[35].w);
    r3.w = (r3.z) * (r3.z);
    r1.y = ((-(r1.z)) >= 0.0f ? (r1.y) : (r2.w));
    r1.z = dot(c[27].yz, r3.zw) + (c[27].x);
    r1.z = (r1.y) * (r1.z);
    r5.w = (r2.x) * (r1.z);
    r2 = c[37];
    r2.x = dot(r1.xw, r2.xy) + (c[26].x);
    r2.y = dot(r1.xw, r2.zw) + (c[26].z);
    r1 = tex2D(s1, r2.xy);
    r2 = tex2D(s4, v6.xy);
    r1.w = saturate(r2.y);
    r2.xyz = c[43].xyz;
    r2.xyz = (-(r2.yzx)) + (c[44].yzx);
    r5.xyz = (r1.www) * (r2.xyz) + (c[43].yzx);
    r1.w = frac(r5.y);
    r2.w = (r5.y) + (-(r1.w));
    r4.w = (r1.w) + (c2.z);
    r1.w = c4.x;
    r1.w = (r1.w) * (c[39].w);
    r2.w = (r2.w) * (c0.y);
    r1.w = frac(abs(r1.w));
    r4.z = exp2(r2.w);
    r1.w = ((c[39].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r3.y = (r1.w) * (c1.x);
    r10.xy = (v6.xy) * (c[42].xx);
    r2.x = (r10.x) * (r4.z) + (r3.y);
    r2.z = (r4.z) * (r10.y);
    r2 = tex2D(s5, r2.xz);
    r3.xzw = (r1.www) * (c4.yzw);
    r4.xy = (r2.wy) * (c3.xy) + (c3.zw);
    r2.xy = (r10.xy) * (r4.zz) + (r3.zw);
    r2 = tex2D(s5, r2.xy);
    r2.xy = (r2.wy) * (c3.xy) + (c3.zw);
    r2.w = ((r4.w) >= 0.0f ? (c0.z) : (c0.w));
    r6.xy = (r4.xy) * (r2.xy);
    r2.w = (r4.z) * (r2.w);
    r5.y = (abs(r4.w)) + (abs(r4.w));
    r2.x = (r10.x) * (r2.w) + (r3.y);
    r2.z = (r10.y) * (r2.w);
    r4.xy = (r10.xy) * (r2.ww) + (r3.zw);
    r2 = tex2D(s5, r2.xz);
    r7.xy = (r2.wy) * (c3.xy) + (c3.zw);
    r2 = tex2D(s5, r4.xy);
    r4.xy = (r10.xy) * (c[45].xx);
    r8.xy = (c[46].xy) * (r3.xx) + (r4.xy);
    r4.xy = (c[47].xx) * (r3.xx) + (r4.xy);
    r3 = tex2D(s6, r8.xy);
    r4 = tex2D(s6, r4.xy);
    r2.z = (r3.x) + (r4.x);
    r2.z = (r2.z) + (r2.z);
    r1.w = (r1.w) * (-(c2.x)) + (r2.z);
    r1.w = (r1.w) + (c2.y);
    r1.w = frac(r1.w);
    r1.w = (r1.w) + (c2.z);
    r1.w = (abs(r1.w)) * (c2.w);
    r2.xy = (r2.wy) * (c3.xy) + (c3.zw);
    r2.w = pow(abs(r1.w), c0.x);
    r2.xy = (r2.xy) * (r7.xy) + (-(r6.xy));
    r1.w = lerp(c[48].x, c[48].y, r2.w);
    r4.xy = (r5.yy) * (r2.xy) + (r6.xy);
    r1.w = (r5.x) * (r1.w);
    r2 = tex2D(s3, r10.xy);
    r5.xy = (r2.wy) * (c3.xy) + (c3.zw);
    r2.xyz = v4.xyz;
    r3.xyz = (r2.zxy) * (v2.yzx);
    r4.xy = (r1.ww) * (r4.xy) + (r5.xy);
    r2.xyz = (r2.yzx) * (v2.zxy) + (-(r3.xyz));
    r2.xyz = (r4.yyy) * (-(r2.xyz));
    r2.xyz = (r4.xxx) * (v2.xyz) + (r2.xyz);
    r3.xyz = (r2.xyz) + (v4.xyz);
    r2.xyz = normalize(r3.xyz);
    r3.w = max(abs(r2.y), abs(r2.z));
    r3.xyz = (r1.xyz) * (r1.xyz);
    r1.w = max(abs(r2.x), r3.w);
    r1.w = 1.0f / (r1.w);
    r1.xyz = (r2.xyz) * (c[5].xyz);
    r3.xyz = (r5.www) * (r3.xyz);
    r1.xyz = (r1.xyz) * (r1.www) + (v0.xyz);
    r1 = tex3D(s11, r1.xyz);
    r1.xyz = (r3.xyz) * (r1.www);
    r3.xyz = (-(v3.xyz)) + (c[24].xyz);
    r1.w = dot(-(v3.xyz), -(v3.xyz));
    r8.xyz = normalize(r3.xyz);
    r2.w = rsqrt(r1.w);
    r4.xyz = (r1.xyz) * (c[25].xyz);
    r3.xyz = (-(v3.xyz)) * (r2.www) + (r8.xyz);
    r1.xyz = normalize(r3.xyz);
    r1.w = dot(r0, c[21]);
    r1.z = saturate(dot(r2.xyz, r1.xyz));
    r1.w = 1.0f / (r1.w);
    r1.x = dot(r0, c[10]);
    r1.y = dot(r0, c[11]);
    r3.w = pow(abs(r1.z), c12.w);
    r3.xy = (r1.ww) * (r1.xy);
    r1.x = dot(r0, c[20]);
    r0.xy = (r3.xy) * (c2.yy) + (c2.yy);
    r0 = tex2D(s2, r0.xy);
    r1.y = (r1.x) * (r1.x);
    r0.w = dot(c[8].yz, r1.xy) + (c[8].x);
    r1.xy = saturate((r1.xx) * (c[9].xy) + (c[9].zw));
    r1.w = saturate(1.0f / (r0.w));
    r0.w = ((-abs(r0.w)) >= 0.0f ? (c1.w) : (r1.w));
    r3.xy = (r1.xy) * (r1.xy);
    r5.xy = (r1.xy) * (c12.xx) + (c12.yy);
    r1.xyz = (-(v3.xyz)) + (c[6].xyz);
    r3.z = (r3.x) * (r5.x);
    r7.xyz = normalize(r1.xyz);
    r1.y = (r3.y) * (-(r5.y)) + (c1.z);
    r1.w = dot(r7.xyz, c[22].xyz);
    r0.w = (r0.w) * (r3.z);
    r1.z = saturate((r1.w) * (c[23].x) + (c[23].y));
    r1.w = (r1.z) * (r1.z);
    r1.z = (r1.z) * (c12.x) + (c12.y);
    r0.w = (r1.y) * (r0.w);
    r1.w = (r1.w) * (r1.z);
    r3.xyz = (r0.xyz) * (r0.xyz);
    r4.w = (r0.w) * (r1.w);
    r0.xy = (v1.ww) * (c[38].xy) + (v1.xy);
    r0.zw = v1.zw;
    r0 = tex2Dproj(s0, r0);
    r1.xy = (v1.ww) * (-(c[38].xy)) + (v1.xy);
    r1.zw = v1.zw;
    r1 = tex2Dproj(s0, r1);
    r0.y = r1.x;
    r1.xy = (v1.ww) * (c[38].zw) + (v1.xy);
    r1.zw = v1.zw;
    r1 = tex2Dproj(s0, r1);
    r0.z = r1.x;
    r1.xy = (v1.ww) * (-(c[38].zw)) + (v1.xy);
    r1.zw = v1.zw;
    r1 = tex2Dproj(s0, r1);
    r0.w = r1.x;
    r1.xyz = (r3.xyz) * (r4.www);
    r0.w = dot(r0, c1.yyyy);
    r0.xyz = (r4.xyz) * (r3.www);
    r1.xyz = (r1.xyz) * (r0.www);
    r1.xyz = (r1.xyz) * (c[7].xyz);
    r9.xyz = (-(v3.xyz)) * (r2.www) + (r7.xyz);
    r6.xyz = (r2.www) * (-(v3.xyz));
    r3.xyz = normalize(r9.xyz);
    r2.w = saturate(dot(r2.xyz, r3.xyz));
    r0.w = dot(-(r6.xyz), r2.xyz);
    r1.w = pow(abs(r2.w), c12.w);
    r0.w = (r0.w) + (r0.w);
    r3.xyz = (r1.www) * (r1.xyz) + (r0.xyz);
    r0.xyz = (r2.xyz) * (-(r0.www)) + (-(r6.xyz));
    r2.w = saturate(dot(r2.xyz, r6.xyz));
    r0.w = c[41].x;
    r0 = texCUBElod(s15, r0);
    r6.xyz = normalize(v4.xyz);
    r2.xyz = (r0.xyz) * (r0.xyz);
    r0.w = saturate(dot(r6.xyz, r8.xyz));
    r4.xyz = (r4.xyz) * (r0.www);
    r3.w = max(abs(r6.y), abs(r6.z));
    r1.w = saturate(dot(r6.xyz, r7.xyz));
    r0.w = max(abs(r6.x), r3.w);
    r0.xyz = (r6.xyz) * (c[5].xyz);
    r0.w = 1.0f / (r0.w);
    r1.xyz = (r1.www) * (r1.xyz) + (r4.xyz);
    r0.xyz = (r0.xyz) * (r0.www) + (v0.xyz);
    r0 = tex3D(s11, r0.xyz);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r2.xyz = (r2.xyz) * (c13.xxx) + (r3.xyz);
    r3.xyz = (r0.xyz) * (c12.zzz) + (r1.xyz);
    r1 = tex2D(s8, r10.xy);
    r0 = tex2D(s7, r10.xy);
    r0.w = (-(r2.w)) + (c1.z);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r0.z = (r0.w) * (r0.w);
    r1.xyz = (r3.xyz) * (r1.xyz);
    r0.z = (r0.z) * (r0.z);
    r2.w = (-(r0.x)) + (c1.z);
    r1.w = (r0.w) * (r0.z);
    r0.xyz = (r2.xyz) * (r5.zzz) + (-(r1.xyz));
    r0.w = saturate(lerp(r2.w, c1.z, r1.w));
    r0.xyz = (r0.www) * (r0.xyz) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v5.xyz));
    r0.w = v2.w;
    r0.xyz = (r0.www) * (r0.xyz) + (v5.xyz);
    r0.xyz = max(((r0.xyz) * (c[40].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c1.z;

    return oC0;
}
