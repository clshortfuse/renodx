// Mechanically reconstructed from 0x1A2A5F71.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(-2.0f, 3.0f, 0.5f, 1.0f);
    const float4 c1 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c2 = float4(0.25f, 1.0f, 0.0f, 8.0f);
    const float4 c3 = float4(9.99999975e-06f, 1e-15f, 1.44269502f, 0.100000001f);
    const float4 c4 = float4(31.875f, 0.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xyz = (c[6].xyz) + (-(v1.xyz));
    r0.w = dot(r0.xyz, r0.xyz);
    r0.w = rsqrt(r0.w);
    r1.x = 1.0f / (r0.w);
    r0.xyz = (r0.xyz) * (r0.www);
    r1.y = (r1.x) * (r1.x);
    r0.w = dot(c[10].yz, r1.xy) + (c[10].x);
    r1.xy = saturate((r1.xx) * (c[11].xy) + (c[11].zw));
    r1.zw = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c0.xx) + (c0.yy);
    r1.xy = (r1.zw) * (r1.xy);
    r0.w = (r0.w) * (r1.x);
    r0.w = (r1.y) * (r0.w);
    r1.x = dot(v2.xyz, v2.xyz);
    r1.x = rsqrt(r1.x);
    r1.yzw = (r1.xxx) * (v2.xyz);
    r2.x = max(abs(r1.z), abs(r1.w));
    r3.x = max(abs(r1.y), r2.x);
    r2.x = 1.0f / (r3.x);
    r2.yzw = (r1.yzw) * (c[5].xyz);
    r2.xyz = (r2.yzw) * (r2.xxx) + (v5.xyz);
    r2 = tex3D(s11, r2.xyz);
    r0.w = (r0.w) * (r2.w);
    r2.xyz = (r0.www) * (c[8].xyz);
    r3.xyz = (r0.www) * (c[7].xyz);
    r3.xyz = (r3.xyz) * (c[9].xxx);
    r2.xyz = (r2.xyz) * (c[9].yyy);
    r0.w = dot(-(v1.xyz), -(v1.xyz));
    r0.w = rsqrt(r0.w);
    r4.xyz = (-(v1.xyz)) * (r0.www) + (r0.xyz);
    r5.xyz = (r0.www) * (-(v1.xyz));
    r6.xyz = normalize(r4.xyz);
    r4.xyz = (r1.wyz) * (v3.yzx);
    r4.xyz = (r1.zwy) * (v3.zxy) + (-(r4.xyz));
    r0.w = saturate(dot(r1.yzw, r5.xyz));
    r1.yzw = (r4.xyz) * (v3.www);
    r4.xy = (c[25].xx) * (v4.xy);
    r4 = tex2D(s1, r4.xy);
    r4.xy = (r4.wy) * (c1.xy) + (c1.zw);
    r4.xy = (r4.xy) * (c0.zz) + (c0.zz);
    r4.xy = (r4.xy) * (-(c0.xx)) + (-(c0.ww));
    r1.yzw = (r1.yzw) * (r4.yyy);
    r1.yzw = (r4.xxx) * (v3.xyz) + (r1.yzw);
    r1.xyz = (v2.xyz) * (r1.xxx) + (r1.yzw);
    r4.xyz = normalize(r1.xyz);
    r1.x = saturate(dot(r4.xyz, r6.xyz));
    r1.y = (r1.x) + (r1.x);
    r1.x = (r1.x) * (r1.x) + (c3.y);
    r1.x = 1.0f / (r1.x);
    r1.z = saturate(dot(r4.xyz, r5.xyz));
    r1.w = 1.0f / (r1.z);
    r1.y = (r1.y) * (r1.w);
    r0.x = saturate(dot(r4.xyz, r0.xyz));
    r2.w = min(r1.z, r0.x);
    r0.y = (r0.w) + (r1.z);
    r0.y = (r0.y) * (c0.z);
    r1.z = max(c3.w, r0.y);
    r0.y = 1.0f / (r1.z);
    r0.y = rsqrt(r0.y);
    r0.y = 1.0f / (r0.y);
    r0.z = saturate((r1.y) * (r2.w));
    r0.z = (r0.x) * (r0.z);
    r1.yzw = (r3.xyz) * (r0.xxx);
    r0.x = rsqrt(r0.z);
    r0.x = 1.0f / (r0.x);
    r0.z = (r1.x) * (r1.x);
    r0.w = (-(r1.x)) + (c0.w);
    r3 = tex2D(s3, v4.xy);
    r1.x = (r3.x) * (c[28].x);
    r3.xyz = (r3.xyz) * (c[24].xxx);
    r2.w = max(c3.x, r1.x);
    r1.x = (r2.w) * (r2.w);
    r1.x = 1.0f / (r1.x);
    r0.w = (r0.w) * (r1.x);
    r0.w = (r0.w) * (c3.z);
    r0.w = exp2(r0.w);
    r0.z = (r0.z) * (r0.w);
    r0.x = (r0.x) * (r0.z);
    r0.xzw = (r2.xyz) * (r0.xxx);
    r0.xyz = (r0.yyy) * (r0.xzw);
    r0.xyz = (r1.xxx) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c[27].xxx);
    r0.xyz = (r0.xyz) * (c2.xxx) + (c2.yyy);
    r2.x = log2(abs(r0.x));
    r2.y = log2(abs(r0.y));
    r2.z = log2(abs(r0.z));
    r0.z = c2.z;
    r1.x = max(c[26].x, r0.z);
    r0.x = (r1.x) + (-(c0.w));
    r0.xyz = (r2.xyz) * (r0.xxx);
    r2.x = exp2(r0.x);
    r2.y = exp2(r0.y);
    r2.z = exp2(r0.z);
    r0.xyz = normalize(v1.xyz);
    r0.w = dot(r0.xyz, r4.xyz);
    r0.w = (r0.w) + (r0.w);
    r0.xyz = (r4.xyz) * (-(r0.www)) + (r0.xyz);
    r0 = texCUBE(s15, r0.xyz);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c2.www);
    r5 = tex3D(s11, v5.xyz);
    r5.xyz = (r5.xyz) * (r5.xyz);
    r0.xyz = (r0.xyz) * (r5.xyz);
    r0.xyz = (r0.xyz) * (c4.xxx);
    r0.xyz = (r3.xyz) * (r0.xyz);
    r0.xyz = (r2.xyz) * (c[26].yyy) + (r0.xyz);
    r0.xyz = (r0.xyz) * (c[29].xxx);
    r0.w = max(abs(r4.y), abs(r4.z));
    r1.x = max(abs(r4.x), r0.w);
    r2.xyz = (r4.xyz) * (c[5].xyz);
    r0.w = 1.0f / (r1.x);
    r2.xyz = (r2.xyz) * (r0.www) + (v5.xyz);
    r2 = tex3D(s11, r2.xyz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r1.xyz = (r2.xyz) * (c4.xxx) + (r1.yzw);
    r2.xy = ddx(v4.xy);
    r2.zw = ddy(v4.xy);
    r2 = tex2Dgrad(s2, v4.xy, r2.xy, r2.zw);
    r2 = (r2) * (r2);
    r2.xyz = (r1.xyz) * (r2.xyz) + (r0.xyz);
    r0 = max(r2, c2.zzzz);
    r1 = (r0.xyzx) * (c2.yyyz) + (c2.zzzy);
    oC0.w = r0.w;
    r0.x = dot(r1, c[21]);
    r0.y = dot(r1, c[22]);
    r0.z = dot(r1, c[23]);
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[20].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);

    return oC0;
}
