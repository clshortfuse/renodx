// Mechanically reconstructed from 0x975C2D94.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler2D s8 : register(s8);
sampler2D s9 : register(s9);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(-2.0f, 3.0f, 0.0f, 0.25f);
    const float4 c1 = float4(0.5f, 2.0f, -1.0f, 1.0f);
    const float4 c2 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c3 = float4(6.28318548f, 0.25f, 3.14159274f, -3.14159274f);
    const float4 c4 = float4(0.159154937f, 0.5f, -0.5f, 9.99999975e-06f);
    const float4 c12 = float4(1e-15f, 1.44269502f, 0.100000001f, 9.99999997e-07f);
    const float4 c13 = float4(2.5f, -2.5f, 0.833333313f, 1.5f);
    const float4 c14 = float4(3.5f, 1.0f, 0.600000024f, 5.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xy = (c[28].xy) * (v5.ww);
    r0.zw = c0.zz;
    r1 = (r0.xyww) + (v5);
    r0 = (-(r0)) + (v5);
    r0 = tex2Dproj(s1, r0);
    r0.y = r0.x;
    r1 = tex2Dproj(s1, r1);
    r0.x = r1.x;
    r1.xy = (c[28].zw) * (v5.ww);
    r1.zw = c0.zz;
    r2 = (r1.xyww) + (v5);
    r1 = (-(r1)) + (v5);
    r1 = tex2Dproj(s1, r1);
    r0.w = r1.x;
    r1 = tex2Dproj(s1, r2);
    r0.z = r1.x;
    r0.x = dot(r0, c0.wwww);
    r0.yzw = (c[20].xyz) + (-(v1.xyz));
    r1.x = dot(r0.yzw, r0.yzw);
    r1.x = rsqrt(r1.x);
    r0.yzw = (r0.yzw) * (r1.xxx);
    r1.x = 1.0f / (r1.x);
    r1.z = dot(r0.yzw, c[24].xyz);
    r1.z = saturate((r1.z) * (c[25].x) + (c[25].y));
    r1.z = (r1.z) * (c[25].w);
    r0.x = (r0.x) * (r1.z);
    r1.y = (r1.x) * (r1.x);
    r1.y = dot(c[26].yz, r1.xy) + (c[26].x);
    r1.xz = saturate((r1.xx) * (c[27].xy) + (c[27].zw));
    r2.xy = (r1.xz) * (r1.xz);
    r1.xz = (r1.xz) * (c0.xx) + (c0.yy);
    r1.xz = (r2.xy) * (r1.xz);
    r1.x = (r1.y) * (r1.x);
    r1.x = (r1.z) * (r1.x);
    r0.x = (r0.x) * (r1.x);
    r1.xyz = (r0.xxx) * (c[22].xyz);
    r2.xyz = (r0.xxx) * (c[21].xyz);
    r2.xyz = (r2.xyz) * (c[23].xxx);
    r1.xyz = (r1.xyz) * (c[23].yyy);
    r0.x = dot(-(v1.xyz), -(v1.xyz));
    r0.x = rsqrt(r0.x);
    r3.xyz = (-(v1.xyz)) * (r0.xxx) + (r0.yzw);
    r4.xyz = (r0.xxx) * (-(v1.xyz));
    r5.xyz = normalize(r3.xyz);
    r3 = tex2D(s4, v4.xy);
    r3.xy = (r3.wy) * (c2.xy) + (c2.zw);
    r3.xy = (r3.xy) * (c1.xx) + (c1.xx);
    r3.xy = (r3.xy) * (c1.yy) + (c1.zz);
    r6 = tex2D(s3, v4.xy);
    r3.zw = (r6.wy) * (c2.xy) + (c2.zw);
    r3.zw = (r3.zw) * (c1.xx) + (c1.xx);
    r3.zw = (r3.zw) * (c1.yy) + (c1.zz);
    r6 = tex2D(s2, v4.xy);
    r7.xyz = (r6.xyz) * (c3.xxy);
    r0.x = lerp(r6.x, r6.y, c1.x);
    r0.x = (r0.x) * (c13.x) + (c13.y);
    r6.x = c0.x;
    r6.xy = (c[29].ww) * (-(r6.xx)) + (r7.xy);
    r1.w = (r6.y) * (c4.x) + (c4.y);
    r2.w = (r6.x) + (c3.z);
    r2.w = (r2.w) * (c4.x) + (c4.y);
    r2.w = frac(r2.w);
    r2.w = (r2.w) * (c3.x) + (c3.w);
    r6.x = cos(r2.w);
    r2.w = (r6.x) * (c4.y) + (c4.z);
    r1.w = frac(r1.w);
    r1.w = (r1.w) * (c3.x) + (c3.w);
    r6.x = cos(r1.w);
    r1.w = (r6.x) * (c1.x) + (c1.x);
    r4.w = c1.w;
    r5.w = (r4.w) + (-(c[30].y));
    r5.w = saturate(r5.w);
    r1.w = (r1.w) * (r5.w);
    r2.w = (r5.w) * (r2.w) + (c1.w);
    r3.zw = (r3.zw) * (r1.ww);
    r3.xy = (r2.ww) * (r3.xy) + (r3.zw);
    r6 = tex2D(s5, v4.xy);
    r3.zw = (r6.wy) * (c2.xy) + (c2.zw);
    r3.zw = (r3.zw) * (c1.xx) + (c1.xx);
    r3.zw = (r3.zw) * (c1.yy) + (c1.zz);
    r6.xy = lerp(r3.zw, r3.xy, c[30].zz);
    r1.w = dot(v2.xyz, v2.xyz);
    r1.w = rsqrt(r1.w);
    r3.xyz = (r1.www) * (v2.xyz);
    r7.xyw = (r3.zxy) * (v3.yzx);
    r7.xyw = (r3.yzx) * (v3.zxy) + (-(r7.xyw));
    r7.xyw = (r7.xyw) * (v3.www);
    r6.yzw = (r6.yyy) * (r7.xyw);
    r6.xyz = (r6.xxx) * (v3.xyz) + (r6.yzw);
    r6.xyz = (v2.xyz) * (r1.www) + (r6.xyz);
    r8.xyz = normalize(r6.xyz);
    r1.w = saturate(dot(r8.xyz, r5.xyz));
    r2.w = (r1.w) + (r1.w);
    r1.w = (r1.w) * (r1.w) + (c12.x);
    r1.w = 1.0f / (r1.w);
    r3.w = saturate(dot(r8.xyz, r4.xyz));
    r4.x = saturate(dot(r3.xyz, r4.xyz));
    r4.x = (r3.w) + (r4.x);
    r4.x = (r4.x) * (c1.x);
    r5.x = max(c12.z, r4.x);
    r4.x = 1.0f / (r5.x);
    r4.x = rsqrt(r4.x);
    r4.x = 1.0f / (r4.x);
    r4.y = 1.0f / (r3.w);
    r2.w = (r2.w) * (r4.y);
    r0.y = saturate(dot(r8.xyz, r0.yzw));
    r4.y = min(r3.w, r0.y);
    r0.z = saturate((r2.w) * (r4.y));
    r0.z = (r0.y) * (r0.z);
    r2.xyz = (r2.xyz) * (r0.yyy);
    r0.y = rsqrt(r0.z);
    r0.y = 1.0f / (r0.y);
    r0.z = (r1.w) * (r1.w);
    r0.w = (-(r1.w)) + (c1.w);
    r5 = tex2D(s8, v4.xy);
    r1.w = max(c4.w, r5.x);
    r1.w = (r1.w) * (r1.w);
    r1.w = 1.0f / (r1.w);
    r0.w = (r0.w) * (r1.w);
    r1.w = (r1.w) * (c0.w);
    r0.w = (r0.w) * (c12.y);
    r0.w = exp2(r0.w);
    r0.z = (r0.z) * (r0.w);
    r0.y = (r0.y) * (r0.z);
    r0.yzw = (r1.xyz) * (r0.yyy);
    r0.yzw = (r4.xxx) * (r0.yzw);
    r0.yzw = (r1.www) * (r0.yzw);
    r1.xyz = normalize(v1.xyz);
    r1.y = dot(r1.xyz, r8.xyz);
    r1.z = (r1.y) + (r1.y);
    r1.y = saturate(r1.y);
    r1.y = (-(r1.y)) + (c1.w);
    r1.x = (r8.x) * (-(r1.z)) + (r1.x);
    r1.x = (r1.x) * (c[35].x);
    r1.x = (r1.x) * (c12.w);
    r1.z = (r1.y) * (r1.y);
    r1.y = (r1.y) * (r1.z);
    r5 = tex2D(s7, v4.xy);
    r1.z = (r5.w) * (c14.x) + (c14.y);
    r1.z = 1.0f / (r1.z);
    r1.y = (r1.y) * (r1.z);
    r4.xyz = lerp(r5.xyz, c1.www, r1.yyy);
    r1.xyz = (r1.xxx) * (r4.xyz);
    r0.yzw = (r0.yzw) * (c0.www) + (r1.xyz);
    r1.xyz = (-(r0.yzw)) + (c1.www);
    r0.yzw = (c[30].xxx) * (r1.xyz) + (r0.yzw);
    r1.xyz = (r7.zzz) * (r0.yzw);
    r0.yzw = (r5.xyz) * (r0.yzw);
    r5 = tex2D(s6, v4.xy);
    r4.xyz = (r5.xyz) * (r5.xyz);
    r1.xyz = (r2.xyz) * (r4.xyz) + (r1.xyz);
    r4.xyz = max(r1.xyz, c0.zzz);
    r1 = tex2D(s9, v4.xy);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r0.yzw = (r2.xyz) * (r1.xyz) + (r0.yzw);
    r1.xyz = max(r0.yzw, c0.zzz);
    r0.yzw = lerp(r1.xyz, r4.xyz, c14.zzz);
    r0.yzw = (r0.yzw) * (c[36].xyz) + (-(r1.xyz));
    r2.z = c[30].z;
    r2.x = c[37].x;
    r1.w = saturate((r2.z) * (r2.x) + (c[38].x));
    r0.x = (r1.w) * (c14.w) + (r0.x);
    r1.w = saturate((r0.x) * (c13.z));
    r0.x = (r0.x) + (-(c14.z));
    r0.x = saturate((r0.x) * (c13.w));
    r1.w = (r1.w) * (r1.w);
    r0.yzw = (r1.www) * (r0.yzw) + (r1.xyz);
    r1.xyz = lerp(r0.yzw, r4.xyz, r0.xxx);
    r0 = (c[6]) + (-(v1.yyyy));
    r2 = (r0) * (r0);
    r5 = (c[5]) + (-(v1.xxxx));
    r2 = (r5) * (r5) + (r2);
    r6 = (c[7]) + (-(v1.zzzz));
    r2 = (r6) * (r6) + (r2);
    r7.x = rsqrt(r2.x);
    r7.y = rsqrt(r2.y);
    r7.z = rsqrt(r2.z);
    r7.w = rsqrt(r2.w);
    r2 = saturate((r2) * (c[8]) + (r4.wwww));
    r0 = (r0) * (r7);
    r0 = (r3.yyyy) * (r0);
    r4 = (r5) * (r7);
    r5 = (r6) * (r7);
    r0 = (r4) * (r3.xxxx) + (r0);
    r0 = saturate((r5) * (r3.zzzz) + (r0));
    r0 = (r2) * (r0);
    r2.x = dot(c[9], r0);
    r2.y = dot(c[10], r0);
    r2.z = dot(c[11], r0);
    r0.xyz = (r1.xyz) * (r2.xyz) + (r1.xyz);
    r0.w = c1.w;
    r1.x = dot(r0, c[32]);
    r1.y = dot(r0, c[33]);
    r1.z = dot(r0, c[34]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[31].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c1.w;

    return oC0;
}
