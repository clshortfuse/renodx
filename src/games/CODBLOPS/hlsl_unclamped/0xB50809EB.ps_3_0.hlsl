// Mechanically reconstructed from 0xB50809EB.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler2D s8 : register(s8);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD7;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(0.5f, 2.0f, -1.0f, 8.0f);
    const float4 c2 = float4(31.875f, 0.0f, 0.178571001f, 4.33290005f);
    const float4 c3 = float4(2.22222233f, 3.125f, 3.4482758f, 3.0f);
    const float4 c4 = float4(4.54519987f, 5.88230991f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xyz = normalize(c[17].xyz);
    r1 = tex2D(s2, v4.xy);
    r1.xy = (r1.wy) * (c0.xy) + (c0.zw);
    r1.xy = (r1.xy) * (c1.xx) + (c1.xx);
    r1.xy = (r1.xy) * (c1.yy) + (c1.zz);
    r2 = tex2D(s1, v4.xy);
    r1.zw = (r2.wy) * (c0.xy) + (c0.zw);
    r1.zw = (r1.zw) * (c1.xx) + (c1.xx);
    r1.zw = (r1.zw) * (c1.yy) + (c1.zz);
    r0.w = saturate((c[28].x) * (v6.z));
    r2.x = (r0.w) * (c[30].x);
    r0.w = saturate(r0.w);
    r1.zw = (r1.zw) * (r2.xx);
    r1.xy = (c[29].xx) * (r1.xy) + (r1.zw);
    r1.z = dot(v2.xyz, v2.xyz);
    r1.z = rsqrt(r1.z);
    r2.xyz = (r1.zzz) * (v2.xyz);
    r3.xyz = (r2.zxy) * (v3.yzx);
    r3.xyz = (r2.yzx) * (v3.zxy) + (-(r3.xyz));
    r3.xyz = (r3.xyz) * (v3.www);
    r3.xyz = (r1.yyy) * (r3.xyz);
    r1.xyw = (r1.xxx) * (v3.xyz) + (r3.xyz);
    r1.xyz = (v2.xyz) * (r1.zzz) + (r1.xyw);
    r3.xyz = normalize(r1.xyz);
    r0.x = saturate(dot(r3.xyz, r0.xyz));
    r0.y = max(abs(r2.y), abs(r2.z));
    r1.x = max(abs(r2.x), r0.y);
    r0.y = 1.0f / (r1.x);
    r1.xyz = (r2.xyz) * (c[5].xyz);
    r1.xyz = (r1.xyz) * (r0.yyy) + (v5.xyz);
    r1 = tex3D(s11, r1.xyz);
    r1.xyz = (r1.www) * (c[18].xyz);
    r1.xyz = (r1.xyz) * (c[21].xxx);
    r0.xyz = (r0.xxx) * (r1.xyz);
    r1.x = max(abs(r3.y), abs(r3.z));
    r2.w = max(abs(r3.x), r1.x);
    r1.x = 1.0f / (r2.w);
    r1.yzw = (r3.xyz) * (c[5].xyz);
    r1.xyz = (r1.yzw) * (r1.xxx) + (v5.xyz);
    r1 = tex3D(s11, r1.xyz);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r0.xyz = (r1.xyz) * (c2.xxx) + (r0.xyz);
    r1.xyz = normalize(v1.xyz);
    r1.w = dot(r1.xyz, r3.xyz);
    r1.w = (r1.w) + (r1.w);
    r1.xyz = (r3.xyz) * (-(r1.www)) + (r1.xyz);
    r1.w = c1.w;
    r1 = texCUBElod(s15, r1);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r1.xyz = (r1.xyz) * (c1.www);
    r3 = tex3D(s11, v5.xyz);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r1.xyz = (r1.xyz) * (r3.xyz);
    r1.xyz = (r1.xyz) * (c[27].xxx);
    r1.xyz = (r1.xyz) * (c2.xxx);
    r3 = tex2D(s8, v4.xy);
    r1.xyz = (r1.xyz) * (r3.xyz);
    r3 = tex2D(s4, v4.xy);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r1.xyz = (r0.xyz) * (r3.xyz) + (r1.xyz);
    r4.xyz = max(r1.xyz, c2.yyy);
    r1 = tex2D(s3, v4.xy);
    r1.xyz = (r1.xyz) * (c1.xxx) + (-(r3.xyz));
    r5.xy = (c[34].xx) * (v4.xy);
    r5 = tex2D(s5, r5.xy);
    r1.xyz = (r5.yyy) * (r1.xyz) + (r3.xyz);
    r0.xyz = (r0.xyz) * (r1.xyz);
    r1.xyz = max(r0.xyz, c2.yyy);
    r0.xyz = (r1.xyz) * (-(c2.zzz)) + (r4.xyz);
    r3.xyz = (r1.xyz) * (c2.zzz);
    r4.xy = (c[33].xx) * (v4.xy);
    r4 = tex2D(s5, r4.xy);
    r1.w = (r4.x) + (c1.z);
    r0.w = (r0.w) * (c1.y) + (r1.w);
    r0.w = (-(r0.w)) + (-(c1.z));
    r4.xyz = saturate((r0.www) * (c3.xyz));
    r0.w = pow(abs(r4.x), c2.w);
    r0.xyz = (r0.www) * (r0.xyz) + (r3.xyz);
    r0.w = c[22].w;
    r3.xy = (r0.ww) * (c[32].xy);
    r3.xy = (r3.xy) * (c[31].xx);
    r3.xy = (v4.xy) * (c[31].xx) + (r3.xy);
    r3 = tex2D(s6, r3.xy);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r3.xyz = (r3.xyz) * (c[35].xxx) + (-(r1.xyz));
    r5 = tex2D(s7, v4.xy);
    r0.w = saturate((r5.x) * (c3.w));
    r3.xyz = (r0.www) * (r3.xyz) + (r1.xyz);
    r0.w = pow(abs(r4.y), c4.x);
    r1.w = pow(abs(r4.z), c4.y);
    r4.xyz = lerp(r3.xyz, r0.xyz, r0.www);
    r0.xyz = lerp(r1.xyz, r4.xyz, r1.www);
    r1 = (c[7]) + (-(v1.yyyy));
    r3 = (r1) * (r1);
    r4 = (c[6]) + (-(v1.xxxx));
    r3 = (r4) * (r4) + (r3);
    r5 = (c[8]) + (-(v1.zzzz));
    r3 = (r5) * (r5) + (r3);
    r6.x = rsqrt(r3.x);
    r6.y = rsqrt(r3.y);
    r6.z = rsqrt(r3.z);
    r6.w = rsqrt(r3.w);
    r7.z = c1.z;
    r3 = saturate((r3) * (c[9]) + (-(r7.zzzz)));
    r1 = (r1) * (r6);
    r1 = (r2.yyyy) * (r1);
    r4 = (r4) * (r6);
    r5 = (r5) * (r6);
    r1 = (r4) * (r2.xxxx) + (r1);
    r1 = saturate((r5) * (r2.zzzz) + (r1));
    r1 = (r3) * (r1);
    r2.x = dot(c[10], r1);
    r2.y = dot(c[11], r1);
    r2.z = dot(c[20], r1);
    r0.xyz = (r0.xyz) * (r2.xyz) + (r0.xyz);
    r0.w = -(c1.z);
    r1.x = dot(r0, c[24]);
    r1.y = dot(r0, c[25]);
    r1.z = dot(r0, c[26]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[23].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = -(c1.z);

    return oC0;
}
