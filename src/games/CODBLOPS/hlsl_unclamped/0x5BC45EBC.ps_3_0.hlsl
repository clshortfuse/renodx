// Mechanically reconstructed from 0x5BC45EBC.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(0.0f, 0.25f, 1.0f, 0.5f);
    const float4 c1 = float4(-2.0f, 3.0f, 2.0f, -1.0f);
    const float4 c2 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c3 = float4(9.99999975e-06f, 1e-15f, 1.44269502f, 0.100000001f);
    const float4 c4 = float4(9.99999997e-07f, 0.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 oC0 = 0.0f;

    r0.zw = c0.xx;
    r1 = (c[25]) * (v1.yyyy);
    r1 = (v1.xxxx) * (c[24]) + (r1);
    r1 = (v1.zzzz) * (c[26]) + (r1);
    r1 = (r1) + (c[27]);
    r0.xy = (r1.ww) * (c[28].xy);
    r2 = (r0.xyww) + (r1);
    r0 = (-(r0)) + (r1);
    r0 = tex2Dproj(s1, r0);
    r0.y = r0.x;
    r2 = tex2Dproj(s1, r2);
    r0.x = r2.x;
    r2.xy = (r1.ww) * (c[28].zw);
    r2.zw = c0.xx;
    r3 = (r1) + (r2.xyww);
    r1 = (r1) + (-(r2));
    r1 = tex2Dproj(s1, r1);
    r0.w = r1.x;
    r1 = tex2Dproj(s1, r3);
    r0.z = r1.x;
    r0.x = dot(r0, c0.yyyy);
    r1 = (v1.xyzx) * (c0.zzzx) + (c0.xxxz);
    r0.y = dot(r1, c[21]);
    r0.y = 1.0f / (r0.y);
    r0.z = dot(r1, c[10]);
    r0.w = dot(r1, c[11]);
    r1.x = dot(r1, c[20]);
    r0.yz = (r0.yy) * (r0.zw);
    r0.yz = (r0.yz) * (c0.ww) + (c0.ww);
    r2 = tex2D(s2, r0.yz);
    r0.y = (r2.x) * (r2.x);
    r1.y = (r1.x) * (r1.x);
    r0.z = dot(c[8].yz, r1.xy) + (c[8].x);
    r1.xy = saturate((r1.xx) * (c[9].xy) + (c[9].zw));
    r0.w = saturate(1.0f / (r0.z));
    r0.z = ((-abs(r0.z)) >= 0.0f ? (c0.x) : (r0.w));
    r1.zw = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c1.xx) + (c1.yy);
    r0.w = (r1.z) * (r1.x);
    r1.x = (r1.w) * (-(r1.y)) + (c0.z);
    r0.z = (r0.z) * (r0.w);
    r0.z = (r1.x) * (r0.z);
    r1.xyz = (c[5].xyz) + (-(v1.xyz));
    r2.xyz = normalize(r1.xyz);
    r0.w = dot(r2.xyz, c[22].xyz);
    r0.w = saturate((r0.w) * (c[23].x) + (c[23].y));
    r1.x = (r0.w) * (r0.w);
    r0.w = (r0.w) * (c1.x) + (c1.y);
    r0.w = (r1.x) * (r0.w);
    r0.z = (r0.z) * (r0.w);
    r0.y = (r0.y) * (r0.z);
    r0.x = (r0.x) * (r0.y);
    r0.yzw = (r0.xxx) * (c[7].xyz);
    r1.xyz = (r0.xxx) * (c[6].xyz);
    r0.x = dot(-(v1.xyz), -(v1.xyz));
    r0.x = rsqrt(r0.x);
    r3.xyz = (-(v1.xyz)) * (r0.xxx) + (r2.xyz);
    r4.xyz = (r0.xxx) * (-(v1.xyz));
    r5.xyz = normalize(r3.xyz);
    r3.xy = (c[34].xx) * (v4.xy);
    r3 = tex2D(s3, r3.xy);
    r3.xy = (r3.wy) * (c2.xy) + (c2.zw);
    r3.xy = (r3.xy) * (c0.ww) + (c0.ww);
    r3.xy = (r3.xy) * (c1.zz) + (c1.ww);
    r0.x = dot(v2.xyz, v2.xyz);
    r0.x = rsqrt(r0.x);
    r6.xyz = (r0.xxx) * (v2.xyz);
    r7.xyz = (r6.zxy) * (v3.yzx);
    r7.xyz = (r6.yzx) * (v3.zxy) + (-(r7.xyz));
    r1.w = saturate(dot(r6.xyz, r4.xyz));
    r6.xyz = (r7.xyz) * (v3.www);
    r3.yzw = (r3.yyy) * (r6.xyz);
    r3.xyz = (r3.xxx) * (v3.xyz) + (r3.yzw);
    r3.xyz = (v2.xyz) * (r0.xxx) + (r3.xyz);
    r6.xyz = normalize(r3.xyz);
    r0.x = saturate(dot(r6.xyz, r5.xyz));
    r2.w = (r0.x) + (r0.x);
    r0.x = (r0.x) * (r0.x) + (c3.y);
    r0.x = 1.0f / (r0.x);
    r3.x = saturate(dot(r6.xyz, r4.xyz));
    r3.y = 1.0f / (r3.x);
    r2.w = (r2.w) * (r3.y);
    r2.x = saturate(dot(r6.xyz, r2.xyz));
    r4.x = min(r3.x, r2.x);
    r1.w = (r1.w) + (r3.x);
    r1.w = (r1.w) * (c0.w);
    r2.y = max(c3.w, r1.w);
    r1.w = 1.0f / (r2.y);
    r1.w = rsqrt(r1.w);
    r1.w = 1.0f / (r1.w);
    r2.y = saturate((r2.w) * (r4.x));
    r2.y = (r2.x) * (r2.y);
    r1.xyz = (r1.xyz) * (r2.xxx);
    r2.x = rsqrt(r2.y);
    r2.x = 1.0f / (r2.x);
    r2.y = (r0.x) * (r0.x);
    r0.x = (-(r0.x)) + (c0.z);
    r3 = tex2D(s5, v4.xy);
    r2.z = (r3.x) * (c[37].x);
    r3.xyz = (r3.xyz) * (c[33].xxx);
    r3.w = max(c3.x, r2.z);
    r2.z = (r3.w) * (r3.w);
    r2.z = 1.0f / (r2.z);
    r0.x = (r0.x) * (r2.z);
    r0.x = (r0.x) * (c3.z);
    r0.x = exp2(r0.x);
    r0.x = (r2.y) * (r0.x);
    r0.x = (r2.x) * (r0.x);
    r0.xyz = (r0.yzw) * (r0.xxx);
    r0.xyz = (r1.www) * (r0.xyz);
    r0.xyz = (r2.zzz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c[36].xxx);
    r0.xyz = (r0.xyz) * (c0.yyy) + (c0.zzz);
    r2.x = log2(abs(r0.x));
    r2.y = log2(abs(r0.y));
    r2.z = log2(abs(r0.z));
    r0.x = c0.x;
    r1.w = max(c[35].x, r0.x);
    r0.x = (r1.w) + (-(c0.z));
    r0.xyz = (r2.xyz) * (r0.xxx);
    r2.x = exp2(r0.x);
    r2.y = exp2(r0.y);
    r2.z = exp2(r0.z);
    r0.xyz = normalize(v1.xyz);
    r0.y = dot(r0.xyz, r6.xyz);
    r0.y = (r0.y) + (r0.y);
    r0.x = (r6.x) * (-(r0.y)) + (r0.x);
    r0.x = (r0.x) * (c4.x);
    r0.xyz = (r3.xyz) * (r0.xxx);
    r0.xyz = (r2.xyz) * (c[35].yyy) + (r0.xyz);
    r0.xyz = (r0.xyz) * (c[38].xxx);
    r2.xy = ddx(v4.xy);
    r2.zw = ddy(v4.xy);
    r2 = tex2Dgrad(s4, v4.xy, r2.xy, r2.zw);
    r2 = (r2) * (r2);
    r2.xyz = (r1.xyz) * (r2.xyz) + (r0.xyz);
    r0 = max(r2, c0.xxxx);
    r1 = (r0.xyzx) * (c0.zzzx) + (c0.xxxz);
    oC0.w = r0.w;
    r0.x = dot(r1, c[30]);
    r0.y = dot(r1, c[31]);
    r0.z = dot(r1, c[32]);
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[29].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);

    return oC0;
}
