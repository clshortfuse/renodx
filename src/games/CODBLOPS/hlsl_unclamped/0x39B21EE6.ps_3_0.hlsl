// Mechanically reconstructed from 0x39B21EE6.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(-2.0f, 3.0f, 0.5f, 1.0f);
    const float4 c1 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c2 = float4(0.25f, 1.0f, 0.0f, 8.0f);
    const float4 c3 = float4(9.99999975e-06f, 1e-15f, 1.44269502f, 0.100000001f);
    const float4 c4 = float4(31.875f, 0.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xyz = (c[21].xyz) + (-(v1.xyz));
    r0.w = dot(r0.xyz, r0.xyz);
    r0.w = rsqrt(r0.w);
    r0.xyz = (r0.xyz) * (r0.www);
    r1.x = 1.0f / (r0.w);
    r0.w = dot(-(v1.xyz), -(v1.xyz));
    r0.w = rsqrt(r0.w);
    r2.xyz = (-(v1.xyz)) * (r0.www) + (r0.xyz);
    r3.xyz = (r0.www) * (-(v1.xyz));
    r4.xyz = normalize(r2.xyz);
    r1.zw = (c[32].xx) * (v4.xy);
    r2 = tex2D(s1, r1.zw);
    r1.zw = (r2.wy) * (c1.xy) + (c1.zw);
    r1.zw = (r1.zw) * (c0.zz) + (c0.zz);
    r1.zw = (r1.zw) * (-(c0.xx)) + (-(c0.ww));
    r0.w = dot(v2.xyz, v2.xyz);
    r0.w = rsqrt(r0.w);
    r2.xyz = (r0.www) * (v2.xyz);
    r5.xyz = (r2.zxy) * (v3.yzx);
    r5.xyz = (r2.yzx) * (v3.zxy) + (-(r5.xyz));
    r5.xyz = (r5.xyz) * (v3.www);
    r5.xyz = (r1.www) * (r5.xyz);
    r5.xyz = (r1.zzz) * (v3.xyz) + (r5.xyz);
    r5.xyz = (v2.xyz) * (r0.www) + (r5.xyz);
    r6.xyz = normalize(r5.xyz);
    r0.w = saturate(dot(r6.xyz, r4.xyz));
    r1.z = (r0.w) + (r0.w);
    r0.w = (r0.w) * (r0.w) + (c3.y);
    r0.w = 1.0f / (r0.w);
    r1.w = saturate(dot(r6.xyz, r3.xyz));
    r2.w = saturate(dot(r2.xyz, r3.xyz));
    r2.w = (r1.w) + (r2.w);
    r2.w = (r2.w) * (c0.z);
    r3.x = max(c3.w, r2.w);
    r2.w = 1.0f / (r3.x);
    r2.w = rsqrt(r2.w);
    r2.w = 1.0f / (r2.w);
    r3.x = 1.0f / (r1.w);
    r1.z = (r1.z) * (r3.x);
    r0.x = saturate(dot(r6.xyz, r0.xyz));
    r3.x = min(r1.w, r0.x);
    r0.y = saturate((r1.z) * (r3.x));
    r0.y = (r0.x) * (r0.y);
    r0.y = rsqrt(r0.y);
    r0.y = 1.0f / (r0.y);
    r0.z = (-(r0.w)) + (c0.w);
    r0.w = (r0.w) * (r0.w);
    r3 = tex2D(s3, v4.xy);
    r1.z = (r3.x) * (c[35].x);
    r3.xyz = (r3.xyz) * (c[31].xxx);
    r3.w = max(c3.x, r1.z);
    r1.z = (r3.w) * (r3.w);
    r1.z = 1.0f / (r1.z);
    r0.z = (r0.z) * (r1.z);
    r0.z = (r0.z) * (c3.z);
    r0.z = exp2(r0.z);
    r0.z = (r0.w) * (r0.z);
    r0.y = (r0.y) * (r0.z);
    r1.y = (r1.x) * (r1.x);
    r0.z = dot(c[25].yz, r1.xy) + (c[25].x);
    r1.xy = saturate((r1.xx) * (c[26].xy) + (c[26].zw));
    r4.xy = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c0.xx) + (c0.yy);
    r1.xy = (r4.xy) * (r1.xy);
    r0.z = (r0.z) * (r1.x);
    r0.z = (r1.y) * (r0.z);
    r0.w = max(abs(r2.y), abs(r2.z));
    r1.x = max(abs(r2.x), r0.w);
    r0.w = 1.0f / (r1.x);
    r1.xyw = (r2.xyz) * (c[5].xyz);
    r1.xyw = (r1.xyw) * (r0.www) + (v5.xyz);
    r4 = tex3D(s11, r1.xyw);
    r0.z = (r0.z) * (r4.w);
    r1.xyw = (r0.zzz) * (c[23].xyz);
    r4.xyz = (r0.zzz) * (c[22].xyz);
    r4.xyz = (r4.xyz) * (c[24].xxx);
    r0.xzw = (r0.xxx) * (r4.xyz);
    r1.xyw = (r1.xyw) * (c[24].yyy);
    r1.xyw = (r0.yyy) * (r1.xyw);
    r1.xyw = (r2.www) * (r1.xyw);
    r1.xyz = (r1.zzz) * (r1.xyw);
    r1.xyz = (r1.xyz) * (c[34].xxx);
    r1.xyz = (r1.xyz) * (c2.xxx) + (c2.yyy);
    r4.x = log2(abs(r1.x));
    r4.y = log2(abs(r1.y));
    r4.z = log2(abs(r1.z));
    r1.z = c2.z;
    r0.y = max(c[33].x, r1.z);
    r0.y = (r0.y) + (-(c0.w));
    r1.xyz = (r4.xyz) * (r0.yyy);
    r4.x = exp2(r1.x);
    r4.y = exp2(r1.y);
    r4.z = exp2(r1.z);
    r1.xyz = normalize(v1.xyz);
    r0.y = dot(r1.xyz, r6.xyz);
    r0.y = (r0.y) + (r0.y);
    r1.xyz = (r6.xyz) * (-(r0.yyy)) + (r1.xyz);
    r1 = texCUBE(s15, r1.xyz);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r1.xyz = (r1.xyz) * (c2.www);
    r5 = tex3D(s11, v5.xyz);
    r5.xyz = (r5.xyz) * (r5.xyz);
    r1.xyz = (r1.xyz) * (r5.xyz);
    r1.xyz = (r1.xyz) * (c4.xxx);
    r1.xyz = (r3.xyz) * (r1.xyz);
    r1.xyz = (r4.xyz) * (c[33].yyy) + (r1.xyz);
    r1.xyz = (r1.xyz) * (c[36].xxx);
    r0.y = max(abs(r6.y), abs(r6.z));
    r1.w = max(abs(r6.x), r0.y);
    r3.xyz = (r6.xyz) * (c[5].xyz);
    r0.y = 1.0f / (r1.w);
    r3.xyz = (r3.xyz) * (r0.yyy) + (v5.xyz);
    r3 = tex3D(s11, r3.xyz);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r0.xyz = (r3.xyz) * (c4.xxx) + (r0.xzw);
    r3.xy = ddx(v4.xy);
    r3.zw = ddy(v4.xy);
    r3 = tex2Dgrad(s2, v4.xy, r3.xy, r3.zw);
    r3 = (r3) * (r3);
    r3.xyz = (r0.xyz) * (r3.xyz) + (r1.xyz);
    r0 = max(r3, c2.zzzz);
    r1 = (c[7]) + (-(v1.yyyy));
    r3 = (r1) * (r1);
    r4 = (c[6]) + (-(v1.xxxx));
    r3 = (r4) * (r4) + (r3);
    r5 = (c[8]) + (-(v1.zzzz));
    r3 = (r5) * (r5) + (r3);
    r6.x = rsqrt(r3.x);
    r6.y = rsqrt(r3.y);
    r6.z = rsqrt(r3.z);
    r6.w = rsqrt(r3.w);
    r2.w = c0.w;
    r3 = saturate((r3) * (c[9]) + (r2.wwww));
    r1 = (r1) * (r6);
    r1 = (r2.yyyy) * (r1);
    r4 = (r4) * (r6);
    r5 = (r5) * (r6);
    r1 = (r4) * (r2.xxxx) + (r1);
    r1 = saturate((r5) * (r2.zzzz) + (r1));
    r1 = (r3) * (r1);
    r2.x = dot(c[10], r1);
    r2.y = dot(c[11], r1);
    r2.z = dot(c[20], r1);
    r1.xyz = (r0.xyz) * (r2.xyz) + (r0.xyz);
    oC0.w = r0.w;
    r1.w = c0.w;
    r0.x = dot(r1, c[28]);
    r0.y = dot(r1, c[29]);
    r0.z = dot(r1, c[30]);
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[27].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);

    return oC0;
}
