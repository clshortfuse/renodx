// Mechanically reconstructed from 0x1D636735.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler3D s11 : register(s11);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(-101.222f, 103.222f, 8.0f, 31.875f);
    const float4 c1 = float4(0.0f, 0.25f, 1.0f, 0.5f);
    const float4 c2 = float4(-2.0f, 3.0f, 2.0f, -1.0f);
    const float4 c3 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c4 = float4(1.16412354f, 1.59579468f, -0.87065506f, 0.0f);
    const float4 c12 = float4(1.16412354f, -0.813476562f, -0.391448975f, 0.529705048f);
    const float4 c13 = float4(1.16412354f, 2.01782227f, -1.08166885f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xy = lerp(c[32].xy, c[32].zw, v4.xy);
    r1 = tex2D(s4, r0.xy);
    r1.y = r1.x;
    r2 = tex2D(s5, r0.xy);
    r0 = tex2D(s3, r0.xy);
    r1.xw = (r0.xx) * (c1.zx) + (c1.xz);
    r1.z = r2.x;
    r0.y = dot(c12, r1);
    r0.x = dot(c4.xyz, r1.xyw);
    r0.z = dot(c13.xyz, r1.xzw);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c0.zzz);
    r1 = tex2D(s7, v4.xy);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r0.w = c1.w;
    r0.w = (-(r0.w)) + (c[31].x);
    r0.xyz = float3(((r0.w) >= 0.0f ? (r0.x) : (r1.x)), ((r0.w) >= 0.0f ? (r0.y) : (r1.y)), ((r0.w) >= 0.0f ? (r0.z) : (r1.z)));
    r1.zw = c1.xx;
    r2 = (c[26]) * (v1.yyyy);
    r2 = (v1.xxxx) * (c[25]) + (r2);
    r2 = (v1.zzzz) * (c[27]) + (r2);
    r2 = (r2) + (c[28]);
    r1.xy = (r2.ww) * (c[30].xy);
    r3 = (r1.xyww) + (r2);
    r1 = (-(r1)) + (r2);
    r1 = tex2Dproj(s1, r1);
    r1.y = r1.x;
    r3 = tex2Dproj(s1, r3);
    r1.x = r3.x;
    r3.xy = (r2.ww) * (c[30].zw);
    r3.zw = c1.xx;
    r4 = (r2) + (r3.xyww);
    r2 = (r2) + (-(r3));
    r2 = tex2Dproj(s1, r2);
    r1.w = r2.x;
    r2 = tex2Dproj(s1, r4);
    r1.z = r2.x;
    r0.w = dot(r1, c1.yyyy);
    r1 = (v1.xyzx) * (c1.zzzx) + (c1.xxxz);
    r2.x = dot(r1, c[22]);
    r2.x = 1.0f / (r2.x);
    r2.y = dot(r1, c[11]);
    r2.z = dot(r1, c[20]);
    r1.x = dot(r1, c[21]);
    r1.zw = (r2.xx) * (r2.yz);
    r1.zw = (r1.zw) * (c1.ww) + (c1.ww);
    r2 = tex2D(s2, r1.zw);
    r1.z = (r2.x) * (r2.x);
    r1.y = (r1.x) * (r1.x);
    r1.y = dot(c[9].yz, r1.xy) + (c[9].x);
    r1.xw = saturate((r1.xx) * (c[10].xy) + (c[10].zw));
    r2.x = saturate(1.0f / (r1.y));
    r1.y = ((-abs(r1.y)) >= 0.0f ? (c1.x) : (r2.x));
    r2.xy = (r1.xw) * (r1.xw);
    r1.xw = (r1.xw) * (c2.xx) + (c2.yy);
    r1.x = (r2.x) * (r1.x);
    r1.w = (r2.y) * (-(r1.w)) + (c1.z);
    r1.x = (r1.y) * (r1.x);
    r1.x = (r1.w) * (r1.x);
    r2.xyz = (c[6].xyz) + (-(v1.xyz));
    r3.xyz = normalize(r2.xyz);
    r1.y = dot(r3.xyz, c[23].xyz);
    r1.y = saturate((r1.y) * (c[24].x) + (c[24].y));
    r1.w = (r1.y) * (r1.y);
    r1.y = (r1.y) * (c2.x) + (c2.y);
    r1.y = (r1.w) * (r1.y);
    r1.x = (r1.x) * (r1.y);
    r1.x = (r1.z) * (r1.x);
    r0.w = (r0.w) * (r1.x);
    r1.xyz = (r0.www) * (c[8].xyz);
    r2.xyz = (r0.www) * (c[7].xyz);
    r4 = tex2D(s6, v4.xy);
    r4.xy = (r4.wy) * (c3.xy) + (c3.zw);
    r4.xy = (r4.xy) * (c1.ww) + (c1.ww);
    r4.xy = (r4.xy) * (c2.zz) + (c2.ww);
    r0.w = dot(v2.xyz, v2.xyz);
    r0.w = rsqrt(r0.w);
    r5.xyz = (r0.www) * (v2.xyz);
    r6.xyz = (r5.zxy) * (v3.yzx);
    r6.xyz = (r5.yzx) * (v3.zxy) + (-(r6.xyz));
    r6.xyz = (r6.xyz) * (v3.www);
    r4.yzw = (r4.yyy) * (r6.xyz);
    r4.xyz = (r4.xxx) * (v3.xyz) + (r4.yzw);
    r4.xyz = (v2.xyz) * (r0.www) + (r4.xyz);
    r6.xyz = normalize(r4.xyz);
    r0.w = dot(-(r3.xyz), r6.xyz);
    r0.w = (r0.w) + (r0.w);
    r4.xyz = (r6.xyz) * (-(r0.www)) + (-(r3.xyz));
    r7.xyz = normalize(v1.xyz);
    r0.w = saturate(dot(r4.xyz, -(r7.xyz)));
    r1.w = dot(r3.xyz, r7.xyz);
    r2.w = saturate(dot(r6.xyz, r3.xyz));
    r2.xyz = (r2.xyz) * (r2.www);
    r1.w = saturate((r1.w) * (c1.w) + (c1.w));
    r3.xyz = normalize(c[17].xyz);
    r2.w = dot(r3.xyz, r7.xyz);
    r2.w = saturate((r2.w) * (c1.w) + (c1.w));
    r3.w = (r2.w) * (c0.x) + (c0.y);
    r2.w = (r2.w) + (c1.z);
    r4.x = lerp(r3.w, c2.z, r1.w);
    r4.y = lerp(r2.w, c2.z, r1.w);
    r1.w = pow(abs(r0.w), r4.x);
    r1.xyz = (r1.xyz) * (r1.www);
    r1.xyz = (r4.yyy) * (r1.xyz);
    r0.w = max(abs(r5.y), abs(r5.z));
    r1.w = max(abs(r5.x), r0.w);
    r4.xyz = (r5.xyz) * (c[5].xyz);
    r0.w = 1.0f / (r1.w);
    r4.xyz = (r4.xyz) * (r0.www) + (v5.xyz);
    r4 = tex3D(s11, r4.xyz);
    r4.xyz = (r4.www) * (c[19].xyz);
    r5.xyz = (r4.www) * (c[18].xyz);
    r5.xyz = (r5.xyz) * (c[29].xxx);
    r4.xyz = (r4.xyz) * (c[29].yyy);
    r0.w = dot(-(r3.xyz), r6.xyz);
    r0.w = (r0.w) + (r0.w);
    r8.xyz = (r6.xyz) * (-(r0.www)) + (-(r3.xyz));
    r0.w = saturate(dot(r6.xyz, r3.xyz));
    r2.xyz = (r0.www) * (r5.xyz) + (r2.xyz);
    r0.w = saturate(dot(r8.xyz, -(r7.xyz)));
    r1.w = pow(abs(r0.w), r3.w);
    r3.xyz = (r4.xyz) * (r1.www);
    r1.xyz = (r3.xyz) * (r2.www) + (r1.xyz);
    r1.xyz = (r1.xyz) * (c[34].xxx);
    r0.w = max(abs(r6.y), abs(r6.z));
    r1.w = max(abs(r6.x), r0.w);
    r3.xyz = (r6.xyz) * (c[5].xyz);
    r0.w = 1.0f / (r1.w);
    r3.xyz = (r3.xyz) * (r0.www) + (v5.xyz);
    r3 = tex3D(s11, r3.xyz);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r2.xyz = (r3.xyz) * (c0.www) + (r2.xyz);
    r0.xyz = (r2.xyz) * (r0.xyz) + (r1.xyz);
    r1.xyz = max(r0.xyz, c1.xxx);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[33].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c1.z;

    return oC0;
}
