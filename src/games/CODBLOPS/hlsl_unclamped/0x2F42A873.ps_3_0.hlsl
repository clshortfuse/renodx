// Mechanically reconstructed from 0x2F42A873.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(4.0f, -0.25f, -0.5f, -0.75f);
    const float4 c1 = float4(0.125f, 2.0f, -1.0f, 9.99999975e-06f);
    const float4 c2 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c3 = float4(1e-15f, 1.44269502f, 0.100000001f, 0.0f);
    const float4 c4 = float4(0.25f, 1.0f, 8.0f, 31.875f);
    const float4 c12 = float4(1.0f, 0.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 oC0 = 0.0f;

    r0.x = (c[7].x) * (v4.x);
    r1.x = c0.x;
    r0.w = (r0.x) * (r1.x) + (c[8].x);
    r1.y = c[7].y;
    r2.zw = (v4.yy) * (r1.yy) + (c[9].yw);
    r3.xyz = (c0.yzw) + (v4.xxx);
    r4.xyz = (r3.xyz) * (c[7].xxx);
    r2.xy = (r4.yz) * (r1.xx) + (c[9].xz);
    r0.z = (r4.x) * (r1.x) + (c[8].z);
    r1.xz = float2(((r3.z) >= 0.0f ? (r2.y) : (r2.x)), ((r3.z) >= 0.0f ? (r2.w) : (r2.z)));
    r0.xy = (v4.yy) * (r1.yy) + (c[8].yw);
    r0.yz = float2(((r3.y) >= 0.0f ? (r1.x) : (r0.z)), ((r3.y) >= 0.0f ? (r1.z) : (r0.y)));
    r0.xy = float2(((r3.x) >= 0.0f ? (r0.y) : (r0.w)), ((r3.x) >= 0.0f ? (r0.z) : (r0.x)));
    r0.zw = (r0.xy) * (c[23].xx);
    r1 = tex2D(s1, r0.zw);
    r0.zw = (r1.wy) * (c2.xy) + (c2.zw);
    r0.zw = (r0.zw) * (-(c0.zz)) + (-(c0.zz));
    r1.xy = ddx(v4.xy);
    r1.xy = (r1.xy) * (c1.xx);
    r1.zw = ddy(v4.xy);
    r1.zw = (r1.zw) * (c1.xx);
    r2 = tex2Dgrad(s2, r0.xy, r1.xy, r1.zw);
    r1 = tex2Dgrad(s3, r0.xy, r1.xy, r1.zw);
    r1 = (r1) * (r1);
    r0.xy = (r2.wy) * (c2.xy) + (c2.zw);
    r0.xy = (r0.xy) * (-(c0.zz)) + (-(c0.zz));
    r0.xy = (r0.xy) * (c1.yy) + (c1.zz);
    r0.xy = (r0.zw) * (c1.yy) + (r0.xy);
    r0.xy = (r0.xy) + (c1.zz);
    r0.z = dot(v2.xyz, v2.xyz);
    r0.z = rsqrt(r0.z);
    r2.xyz = (r0.zzz) * (v2.xyz);
    r3.xyz = (r2.zxy) * (v3.yzx);
    r3.xyz = (r2.yzx) * (v3.zxy) + (-(r3.xyz));
    r3.xyz = (r3.xyz) * (v3.www);
    r3.xyz = (r0.yyy) * (r3.xyz);
    r0.xyw = (r0.xxx) * (v3.xyz) + (r3.xyz);
    r0.xyz = (v2.xyz) * (r0.zzz) + (r0.xyw);
    r3.xyz = normalize(r0.xyz);
    r0.x = dot(-(v1.xyz), -(v1.xyz));
    r0.x = rsqrt(r0.x);
    r4.xyz = normalize(c[17].xyz);
    r0.yzw = (-(v1.xyz)) * (r0.xxx) + (r4.xyz);
    r5.xyz = (r0.xxx) * (-(v1.xyz));
    r0.x = saturate(dot(r3.xyz, r4.xyz));
    r4.xyz = normalize(r0.yzw);
    r0.y = saturate(dot(r3.xyz, r4.xyz));
    r0.z = (r0.y) + (r0.y);
    r0.y = (r0.y) * (r0.y) + (c3.x);
    r0.y = 1.0f / (r0.y);
    r0.w = saturate(dot(r3.xyz, r5.xyz));
    r2.w = saturate(dot(r2.xyz, r5.xyz));
    r2.w = (r0.w) + (r2.w);
    r2.w = (r2.w) * (-(c0.z));
    r3.w = max(c3.z, r2.w);
    r2.w = 1.0f / (r3.w);
    r2.w = rsqrt(r2.w);
    r2.w = 1.0f / (r2.w);
    r3.w = 1.0f / (r0.w);
    r4.x = min(r0.w, r0.x);
    r0.z = (r0.z) * (r3.w);
    r0.z = saturate((r4.x) * (r0.z));
    r0.z = (r0.x) * (r0.z);
    r0.z = rsqrt(r0.z);
    r0.z = 1.0f / (r0.z);
    r0.w = (-(r0.y)) + (-(c1.z));
    r0.y = (r0.y) * (r0.y);
    r3.w = c1.w;
    r4.x = max(r3.w, c[26].x);
    r3.w = (r4.x) * (r4.x);
    r3.w = 1.0f / (r3.w);
    r0.w = (r0.w) * (r3.w);
    r0.w = (r0.w) * (c3.y);
    r0.w = exp2(r0.w);
    r0.y = (r0.y) * (r0.w);
    r0.y = (r0.z) * (r0.y);
    r0.z = max(abs(r2.y), abs(r2.z));
    r4.x = max(abs(r2.x), r0.z);
    r2.xyz = (r2.xyz) * (c[5].xyz);
    r0.z = 1.0f / (r4.x);
    r2.xyz = (r2.xyz) * (r0.zzz) + (v5.xyz);
    r4 = tex3D(s11, r2.xyz);
    r2.xyz = (r4.www) * (c[19].xyz);
    r4.xyz = (r4.www) * (c[18].xyz);
    r4.xyz = (r4.xyz) * (c[6].xxx);
    r0.xzw = (r0.xxx) * (r4.xyz);
    r2.xyz = (r2.xyz) * (c[6].yyy);
    r2.xyz = (r0.yyy) * (r2.xyz);
    r2.xyz = (r2.www) * (r2.xyz);
    r2.xyz = (r3.www) * (r2.xyz);
    r2.xyz = (r2.xyz) * (c[25].xxx);
    r2.xyz = (r2.xyz) * (c4.xxx) + (c4.yyy);
    r4.x = log2(abs(r2.x));
    r4.y = log2(abs(r2.y));
    r4.z = log2(abs(r2.z));
    r2.w = c3.w;
    r0.y = max(c[24].x, r2.w);
    r0.y = (r0.y) + (c1.z);
    r2.xyz = (r4.xyz) * (r0.yyy);
    r4.x = exp2(r2.x);
    r4.y = exp2(r2.y);
    r4.z = exp2(r2.z);
    r2.xyz = normalize(v1.xyz);
    r0.y = dot(r2.xyz, r3.xyz);
    r0.y = (r0.y) + (r0.y);
    r2.xyz = (r3.xyz) * (-(r0.yyy)) + (r2.xyz);
    r2 = texCUBE(s15, r2.xyz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = (r2.xyz) * (c4.zzz);
    r5 = tex3D(s11, v5.xyz);
    r5.xyz = (r5.xyz) * (r5.xyz);
    r2.xyz = (r2.xyz) * (r5.xyz);
    r2.xyz = (r2.xyz) * (c[22].xxx);
    r2.xyz = (r2.xyz) * (c4.www);
    r2.xyz = (r4.xyz) * (c[24].yyy) + (r2.xyz);
    r2.xyz = (r2.xyz) * (c[27].xxx);
    r0.y = max(abs(r3.y), abs(r3.z));
    r2.w = max(abs(r3.x), r0.y);
    r3.xyz = (r3.xyz) * (c[5].xyz);
    r0.y = 1.0f / (r2.w);
    r3.xyz = (r3.xyz) * (r0.yyy) + (v5.xyz);
    r3 = tex3D(s11, r3.xyz);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r0.xyz = (r3.xyz) * (c4.www) + (r0.xzw);
    r1.xyz = (r0.xyz) * (r1.xyz) + (r2.xyz);
    r0 = max(r1, c3.wwww);
    r1 = (r0.xyzx) * (c12.xxxy) + (c12.yyyx);
    oC0.w = r0.w;
    r0.x = dot(r1, c[11]);
    r0.y = dot(r1, c[20]);
    r0.z = dot(r1, c[21]);
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[10].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);

    return oC0;
}
