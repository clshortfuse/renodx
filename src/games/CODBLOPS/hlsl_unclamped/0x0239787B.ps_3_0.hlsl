// Mechanically reconstructed from 0x0239787B.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    const float4 c0 = float4(8.0f, 31.875f, 3.5f, 1.0f);
    const float4 c1 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c2 = float4(60.0f, -50.0f, 1.70000005f, 0.100000001f);
    const float4 c3 = float4(9.99999975e-06f, 1e-15f, 1.44269502f, 0.25f);
    const float4 c4 = float4(0.5f, 2.0f, -1.0f, 50.0f);
    const float4 c12 = float4(-5.5f, 30.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = tex2D(s4, v5.xy);
    r1 = c2;
    r0.y = (c[22].y) * (r1.x) + (r1.y);
    r0.x = (r0.x) * (c4.w) + (r0.y);
    r0.y = (c[29].x) * (r1.z) + (r1.w);
    r1.x = saturate(lerp(c4.x, r0.x, r0.y));
    r0 = tex2D(s6, v5.xy);
    r0.yz = (c[32].xx) * (v5.xy);
    r2 = tex2D(s2, r0.yz);
    r0.y = (r2.w) * (r0.x) + (-(r0.x));
    r0.x = (r1.x) * (r0.y) + (r0.x);
    r1.y = max(c3.x, r0.x);
    r0.w = (r0.x) * (c0.x);
    r1.y = (r1.y) * (r1.y);
    r1.y = 1.0f / (r1.y);
    r1.z = dot(-(v1.xyz), -(v1.xyz));
    r1.z = rsqrt(r1.z);
    r3.xyz = normalize(c[17].xyz);
    r4.xyz = (-(v1.xyz)) * (r1.zzz) + (r3.xyz);
    r5.xyz = (r1.zzz) * (-(v1.xyz));
    r6.xyz = normalize(r4.xyz);
    r1.z = dot(v2.xyz, v2.xyz);
    r1.z = rsqrt(r1.z);
    r4 = tex2D(s1, v5.xy);
    r4.xy = (r4.wy) * (c1.xy) + (c1.zw);
    r4.xy = (r4.xy) * (c4.xx) + (c4.xx);
    r4.xy = (r4.xy) * (c4.yy) + (c4.zz);
    r7.xyz = (r1.zzz) * (v2.xyz);
    r8.xyz = (r7.zxy) * (v3.yzx);
    r8.xyz = (r7.yzx) * (v3.zxy) + (-(r8.xyz));
    r8.xyz = (r8.xyz) * (v3.www);
    r4.yzw = (r4.yyy) * (r8.xyz);
    r4.xyz = (r4.xxx) * (v3.xyz) + (r4.yzw);
    r4.xyz = (v2.xyz) * (r1.zzz) + (r4.xyz);
    r8.xyz = normalize(r4.xyz);
    r1.z = saturate(dot(r8.xyz, r6.xyz));
    r1.w = (r1.z) * (r1.z) + (c3.y);
    r1.z = (r1.z) + (r1.z);
    r1.w = 1.0f / (r1.w);
    r3.w = (-(r1.w)) + (-(c4.z));
    r1.w = (r1.w) * (r1.w);
    r3.w = (r1.y) * (r3.w);
    r1.y = (r1.y) * (c3.w);
    r3.w = (r3.w) * (c3.z);
    r3.w = exp2(r3.w);
    r1.w = (r1.w) * (r3.w);
    r3.w = saturate(dot(r8.xyz, r5.xyz));
    r4.x = 1.0f / (r3.w);
    r1.z = (r1.z) * (r4.x);
    r4.x = saturate(dot(r8.xyz, r3.xyz));
    r5.w = min(r3.w, r4.x);
    r1.z = saturate((r1.z) * (r5.w));
    r1.z = (r4.x) * (r1.z);
    r1.z = rsqrt(r1.z);
    r1.z = 1.0f / (r1.z);
    r1.z = (r1.w) * (r1.z);
    r1.w = max(abs(r7.y), abs(r7.z));
    r4.y = max(abs(r7.x), r1.w);
    r1.w = 1.0f / (r4.y);
    r4.yzw = (r7.xyz) * (c[5].xyz);
    r4.yzw = (r4.yzw) * (r1.www) + (v6.xyz);
    r6 = tex3D(s11, r4.yzw);
    r4.yzw = (r6.www) * (c[19].xyz);
    r6.xyz = (r6.www) * (c[18].xyz);
    r6.xyz = (r6.xyz) * (c[21].xxx);
    r4.yzw = (r4.yzw) * (c[21].yyy);
    r4.yzw = (r1.zzz) * (r4.yzw);
    r1.z = saturate(dot(r7.xyz, r5.xyz));
    r1.z = (r3.w) + (r1.z);
    r1.z = (r1.z) * (c4.x);
    r3.w = max(c2.w, r1.z);
    r1.z = 1.0f / (r3.w);
    r1.z = rsqrt(r1.z);
    r1.z = 1.0f / (r1.z);
    r4.yzw = (r4.yzw) * (r1.zzz);
    r1.yzw = (r1.yyy) * (r4.yzw);
    r9 = tex3D(s11, v6.xyz);
    r4.yzw = (r9.xyz) * (r9.xyz);
    r9.xyz = normalize(v1.xyz);
    r3.w = dot(r9.xyz, r8.xyz);
    r5.w = (r3.w) + (r3.w);
    r3.w = saturate(r3.w);
    r3.w = (-(r3.w)) + (-(c4.z));
    r0.xyz = (r8.xyz) * (-(r5.www)) + (r9.xyz);
    r0 = texCUBElod(s15, r0);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c0.xxx);
    r0.xyz = (r4.yzw) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c[30].xxx);
    r0.xyz = (r0.xyz) * (c0.yyy);
    r9 = tex2D(s3, v5.xy);
    r9 = (r1.xxxx) * (r2) + (r9);
    r0.w = (r2.x) * (r2.w);
    r2.z = c4.z;
    r0.w = (r0.w) * (-(c[34].x)) + (-(r2.z));
    r2.x = (r9.w) * (c0.z) + (c0.w);
    r2.x = 1.0f / (r2.x);
    r2.y = (r3.w) * (r3.w);
    r2.y = (r3.w) * (r2.y);
    r2.x = (r2.x) * (r2.y);
    r4.yzw = lerp(r9.xyz, -(c4.zzz), r2.xxx);
    r0.xyz = (r0.xyz) * (r4.yzw);
    r0.xyz = (r1.yzw) * (c3.www) + (r0.xyz);
    r0.xyz = (c[22].xxx) * (-(r0.xyz)) + (r0.xyz);
    r0.xyz = (r9.xyz) * (r0.xyz);
    r1.y = dot(-(r3.xyz), r8.xyz);
    r1.y = (r1.y) + (r1.y);
    r1.yzw = (r8.xyz) * (-(r1.yyy)) + (-(r3.xyz));
    r2.x = dot(r3.xyz, -(r5.xyz));
    r1.y = saturate(dot(r1.yzw, r5.xyz));
    r1.z = saturate((r2.x) * (c4.x) + (c4.x));
    r1.z = (r1.z) + (r1.z);
    r1.z = (r1.z) * (r1.z);
    r1.w = (r1.z) * (c12.x) + (c12.y);
    r1.z = (r1.z) * (c[31].x);
    r1.z = (r1.z) * (c3.w);
    r2.x = pow(abs(r1.y), r1.w);
    r3.x = c[21].x;
    r3.xyz = (r3.xxx) * (c[18].xyz);
    r4.yzw = max(r3.xyz, -(c4.zzz));
    r2.xyw = (r2.xxx) * (r4.yzw);
    r1.yzw = (r1.zzz) * (r2.xyw);
    r2.x = max(abs(r8.y), abs(r8.z));
    r3.x = max(abs(r8.x), r2.x);
    r2.xyw = (r8.xyz) * (c[5].xyz);
    r3.x = 1.0f / (r3.x);
    r2.xyw = (r2.xyw) * (r3.xxx) + (v6.xyz);
    r3 = tex3D(s11, r2.xyw);
    r2.x = saturate(r7.z);
    r2.x = (r3.w) * (r2.x);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r1.yzw = (r1.yzw) * (r2.xxx);
    r1.yzw = (r3.xyz) * (c0.yyy) + (r1.yzw);
    r1.yzw = (r4.xxx) * (r6.xyz) + (r1.yzw);
    r3 = tex2D(s5, v5.xy);
    r2.xyw = (r3.xyz) * (r3.xyz);
    r2.xyw = (r2.xyw) * (v4.xyz);
    r3.xyz = (r2.xyw) * (c[33].xyz) + (-(r2.xyw));
    r3.xyz = (r0.www) * (r3.xyz);
    r2.xyw = (r1.xxx) * (r3.xyz) + (r2.xyw);
    r0.xyz = (r1.yzw) * (r2.xyw) + (r0.xyz);
    r1.xyz = max(r0.xyz, c12.zzz);
    r0.w = c[23].w;
    r0.x = (r0.w) * (c[28].x);
    r0.yzw = (r2.zzz) + (c[23].xyz);
    r0.xyz = (r0.xxx) * (r0.yzw) + (-(c4.zzz));
    r0.xyz = (r1.xyz) * (r0.xyz);
    r1 = (c[7]) + (-(v1.yyyy));
    r3 = (r1) * (r1);
    r4 = (c[6]) + (-(v1.xxxx));
    r3 = (r4) * (r4) + (r3);
    r5 = (c[8]) + (-(v1.zzzz));
    r3 = (r5) * (r5) + (r3);
    r6.x = rsqrt(r3.x);
    r6.y = rsqrt(r3.y);
    r6.z = rsqrt(r3.z);
    r6.w = rsqrt(r3.w);
    r2 = saturate((r3) * (c[9]) + (-(r2.zzzz)));
    r1 = (r1) * (r6);
    r1 = (r7.yyyy) * (r1);
    r3 = (r4) * (r6);
    r4 = (r5) * (r6);
    r1 = (r3) * (r7.xxxx) + (r1);
    r1 = saturate((r4) * (r7.zzzz) + (r1));
    r1 = (r2) * (r1);
    r2.x = dot(c[10], r1);
    r2.y = dot(c[11], r1);
    r2.z = dot(c[20], r1);
    r0.xyz = (r0.xyz) * (r2.xyz) + (r0.xyz);
    r0.w = -(c4.z);
    r1.x = dot(r0, c[25]);
    r1.y = dot(r0, c[26]);
    r1.z = dot(r0, c[27]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[24].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = -(c4.z);

    return oC0;
}
