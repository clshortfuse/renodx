// Mechanically reconstructed from 0xEF8D60DE.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD3;
    float4 v2 : TEXCOORD4;
    float4 v3 : TEXCOORD5;
    float4 v4 : TEXCOORD6;
    float4 v5 : TEXCOORD7;
    float4 v6 : TEXCOORD8;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(-0.5f, 0.5f, 1e-15f, 1.0f);
    const float4 c2 = float4(1.44269502f, 0.200000003f, 5.0f, 31.875f);
    const float4 c3 = float4(-2.0f, 3.0f, 0.25f, 0.100000001f);
    const float4 c4 = float4(8.0f, 0.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xy = (v6.xy) + (c1.xx);
    r8.yw = c1.yw;
    r5.xy = (r0.xy) * (c[24].xx) + (r8.yy);
    r0.xy = ddy(v6.xy);
    r3.xy = ddx(r5.xy);
    r7.xy = ddx(v6.xy);
    r8.xy = (r0.xy) * (r3.yy);
    r4.xy = ddy(r5.yx);
    r0 = v1;
    r2.xyz = (r0.yzx) * (v4.zxy);
    r1 = tex2D(s0, v6.xy);
    r1.xy = (r1.wy) * (c0.xy) + (c0.zw);
    r0.xyz = (v4.yzx) * (r0.zxy) + (-(r2.xyz));
    r6.xy = (r1.xy) * (c[23].xx);
    r2.xy = (r7.xy) * (r4.xx) + (-(r8.xy));
    r0.xyz = (-(r0.xyz)) * (r6.yyy);
    r1.xyz = (r2.yyy) * (v2.xyz);
    r0.xyz = (r6.xxx) * (v1.xyz) + (r0.xyz);
    r1.xyz = (v1.xyz) * (r2.xxx) + (r1.xyz);
    r0.xyz = (r0.xyz) + (v4.xyz);
    r1.w = (r3.y) * (r4.y);
    r2.w = dot(r0.xyz, r0.xyz);
    r1.w = (r3.x) * (r4.x) + (-(r1.w));
    r2.w = rsqrt(r2.w);
    r1.xyz = float3(((r1.w) >= 0.0f ? (r1.x) : (-(r1.x))), ((r1.w) >= 0.0f ? (r1.y) : (-(r1.y))), ((r1.w) >= 0.0f ? (r1.z) : (-(r1.z))));
    r3.xyz = (r0.xyz) * (r2.www);
    r1.w = dot(r1.xyz, r3.xyz);
    r1.xyz = (r1.www) * (-(r3.xyz)) + (r1.xyz);
    r2.xyz = normalize(r1.xyz);
    r4.xyz = (r3.zxy) * (r2.yzx);
    r1 = tex2D(s1, r5.xy);
    r6.xy = (r1.wy) * (c0.xy) + (c0.zw);
    r1.xyz = (r3.yzx) * (r2.zxy) + (-(r4.xyz));
    r3.xy = (r6.xy) * (c[25].xx);
    r1.xyz = (-(r1.xyz)) * (r3.yyy);
    r1.xyz = (r3.xxx) * (r2.xyz) + (r1.xyz);
    r1.xyz = (r0.xyz) * (r2.www) + (r1.xyz);
    r0.z = dot(-(v3.xyz), -(v3.xyz));
    r1.w = rsqrt(r0.z);
    r0.xyz = normalize(c[17].xyz);
    r10.xyz = normalize(r1.xyz);
    r2.xyz = (-(v3.xyz)) * (r1.www) + (r0.xyz);
    r3.xyz = (r1.www) * (-(v3.xyz));
    r1.xyz = normalize(r2.xyz);
    r2.w = saturate(dot(r1.xyz, r10.xyz));
    r1.z = dot(r1.xyz, r3.xyz);
    r1.w = (r2.w) + (r2.w);
    r1.z = 1.0f / (r1.z);
    r9.w = saturate(dot(r10.xyz, r0.xyz));
    r3.w = saturate(dot(r3.xyz, r10.xyz));
    r1.w = (r1.w) * (r1.z);
    r1.z = min(r3.w, r9.w);
    r1.w = saturate((r1.w) * (r1.z));
    r1.z = (r2.w) * (r2.w) + (c1.z);
    r1.w = (r9.w) * (r1.w);
    r1.w = rsqrt(r1.w);
    r2.w = 1.0f / (r1.z);
    r4.z = 1.0f / (r1.w);
    r2.z = (-(r2.w)) + (c1.w);
    r1 = tex2D(s2, r5.xy);
    r1.w = (r1.x) * (r1.x);
    r2.w = (r2.w) * (r2.w);
    r4.w = 1.0f / (r1.w);
    r1.w = (r2.z) * (r4.w);
    r2.xyz = normalize(v4.xyz);
    r1.w = (r1.w) * (c2.x);
    r4.x = max(abs(r2.y), abs(r2.z));
    r4.y = exp2(r1.w);
    r1.w = max(abs(r2.x), r4.x);
    r1.w = 1.0f / (r1.w);
    r1.xyz = (r2.xyz) * (c[5].xyz);
    r2.w = (r2.w) * (r4.y);
    r1.xyz = (r1.xyz) * (r1.www) + (v0.xyz);
    r1 = tex3D(s11, r1.xyz);
    r0.z = dot(r0.xyz, r2.xyz);
    r0.z = (r0.z) + (c2.y);
    r0.z = saturate((r0.z) * (c2.z));
    r1.z = (r0.z) * (c3.x) + (c3.y);
    r1.y = (r0.z) * (r0.z);
    r0.xyz = (r1.www) * (c[18].xyz);
    r1.z = (r1.z) * (r1.y);
    r1.w = (r4.z) * (r2.w);
    r0.xyz = (r0.xyz) * (r1.zzz);
    r2.w = saturate(dot(r3.xyz, r2.xyz));
    r1.xyz = (r1.www) * (r0.xyz);
    r1.xyz = (r4.www) * (r1.xyz);
    r1.w = dot(-(r3.xyz), r10.xyz);
    r2.xyz = (r1.xyz) * (c3.zzz);
    r1.w = (r1.w) + (r1.w);
    r1.xyz = (r10.xyz) * (-(r1.www)) + (-(r3.xyz));
    r1.w = c[22].x;
    r1 = texCUBElod(s15, r1);
    r1.w = (r3.w) + (r2.w);
    r2.w = (r1.w) * (c1.y);
    r1.w = max(c3.w, r2.w);
    r1.w = 1.0f / (r1.w);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r1.w = rsqrt(r1.w);
    r1.xyz = (r1.xyz) * (c4.xxx);
    r1.w = 1.0f / (r1.w);
    r8.xyz = (r2.xyz) * (r1.www) + (r1.xyz);
    r2 = tex2D(s4, r5.xy);
    r1 = tex2D(s3, r5.xy);
    r2.w = max(abs(r10.y), abs(r10.z));
    r1.w = max(abs(r10.x), r2.w);
    r1.w = 1.0f / (r1.w);
    r3.xyz = (r10.xyz) * (c[5].xyz);
    r9.xyz = (r2.xyz) * (c[26].xxx);
    r2.xyz = (r3.xyz) * (r1.www) + (v0.xyz);
    r3 = tex3D(s11, r2.xyz);
    r6 = (-(v3.yyyy)) + (c[7]);
    r2 = (r6) * (r6);
    r5 = (-(v3.xxxx)) + (c[6]);
    r2 = (r5) * (r5) + (r2);
    r4 = (-(v3.zzzz)) + (c[8]);
    r2 = (r4) * (r4) + (r2);
    r7.x = rsqrt(r2.x);
    r7.y = rsqrt(r2.y);
    r7.z = rsqrt(r2.z);
    r7.w = rsqrt(r2.w);
    r6 = (r6) * (r7);
    r6 = (r10.yyyy) * (r6);
    r5 = (r5) * (r7);
    r4 = (r4) * (r7);
    r5 = (r5) * (r10.xxxx) + (r6);
    r2 = saturate((r2) * (c[9]) + (r8.wwww));
    r4 = saturate((r4) * (r10.zzzz) + (r5));
    r2 = (r2) * (r4);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r4.x = dot(c[10], r2);
    r4.y = dot(c[11], r2);
    r4.z = dot(c[20], r2);
    r2.xyz = (r8.xyz) * (r9.xyz);
    r3.xyz = (r3.xyz) * (c2.www) + (r4.xyz);
    r0.xyz = (r9.www) * (r0.xyz) + (r3.xyz);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r0.xyz = (r0.xyz) * (r1.xyz) + (r2.xyz);
    r0.xyz = (r0.xyz) + (-(v5.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v5.xyz);
    r0.xyz = max(((r0.xyz) * (c[21].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c1.w;

    return oC0;
}
