// Mechanically reconstructed from 0xD91B4E6B.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);

struct PS_INPUT
{
    float4 v0 : TEXCOORD2;
    float4 v1 : TEXCOORD3;
    float4 v2 : TEXCOORD4;
    float4 v3 : TEXCOORD5;
    float4 v4 : TEXCOORD6;
    float4 v5 : TEXCOORD7;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(0.5f, 0.449999988f, 0.330000013f, 0.0900000036f);
    const float4 c2 = float4(4.0f, -2.0f, 9.99999975e-05f, 0.100000001f);
    const float4 c3 = float4(1.0f, 0.0f, 0.5f, 31.875f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 oC0 = 0.0f;

    r0.w = dot(v3.xyz, v3.xyz);
    r8.w = rsqrt(r0.w);
    r7.xyz = (r8.www) * (v3.xyz);
    r6.xyz = normalize(-(v2.xyz));
    r1.xy = (v5.xy) * (c[28].xy);
    r0 = tex2D(s2, r1.xy);
    r2 = tex2D(s1, r1.xy);
    r3 = (v2.xyzx) * (c3.xxxy) + (c3.yyyx);
    r1.w = dot(r3, c[22]);
    r1.w = 1.0f / (r1.w);
    r1.x = dot(r3, c[11]);
    r1.y = dot(r3, c[20]);
    r1 = (r1.wwww) * (r1.xxyy);
    r4 = (r1) * (c[23].zwxy);
    r4.x = log2(abs(r4.x));
    r4.y = log2(abs(r4.y));
    r4.z = log2(abs(r4.z));
    r4.w = log2(abs(r4.w));
    r4 = (r4) * (c[24].xxxx);
    r4.x = exp2(r4.x);
    r4.y = exp2(r4.y);
    r4.z = exp2(r4.z);
    r4.w = exp2(r4.w);
    r4.xy = (r4.zw) + (r4.xy);
    r4.x = log2(abs(r4.x));
    r4.y = log2(abs(r4.y));
    r4.xy = (r4.xy) * (c[24].yy);
    r4.x = exp2(r4.x);
    r4.y = exp2(r4.y);
    r4.z = dot(r3, c[21]);
    r1.z = (r4.x) * (c[25].x);
    r1.y = (r4.y) * (c[25].y) + (-(r1.z));
    r1.z = c[25].y;
    r1.z = (r4.y) * (r1.z) + (-(c[24].z));
    r1.y = 1.0f / (r1.y);
    r4.xy = abs(r1.xw);
    r5.w = saturate((r1.z) * (r1.y));
    r3 = c[9];
    r3 = saturate((r4.zyxz) * (r3) + (c[10]));
    r5.x = (r3.w) * (r3.x);
    r5.yz = r3.yz;
    r3 = (r5) * (r5);
    r5 = (c[25].zzzz) * (r5) + (c[25].wwww);
    r3 = (r3) * (r5);
    r1.y = (r3.z) * (r3.y);
    r1.z = abs(c[24].w);
    r4.w = (r4.z) * (r4.z);
    r1.y = ((-(r1.z)) >= 0.0f ? (r1.y) : (r3.w));
    r1.z = dot(c[8].yz, r4.zw) + (c[8].x);
    r1.z = (r1.y) * (r1.z);
    r2.z = (r3.x) * (r1.z);
    r3 = c[26];
    r3.x = dot(r1.xw, r3.xy) + (c[7].x);
    r3.y = dot(r1.xw, r3.zw) + (c[7].z);
    r1 = tex2D(s0, r3.xy);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r3.xyz = (r2.zzz) * (r1.xyz);
    r1 = tex2D(s12, v5.zw);
    r5.xy = (r2.wy) * (c0.xy) + (c0.zw);
    r2.xyz = v3.xyz;
    r4.xyz = (r2.zxy) * (v0.yzx);
    r5.xy = (r5.xy) * (c[29].xx);
    r2.xyz = (r2.yzx) * (v0.zxy) + (-(r4.xyz));
    r2.xyz = (r5.yyy) * (-(r2.xyz));
    r2.xyz = (r5.xxx) * (v0.xyz) + (r2.xyz);
    r2.xyz = (r2.xyz) + (v3.xyz);
    r4.xyz = (-(v2.xyz)) + (c[5].xyz);
    r5.xyz = normalize(r2.xyz);
    r2.xyz = normalize(r4.xyz);
    r1.xyz = (r3.xyz) * (r1.yyy);
    r6.w = saturate(dot(r5.xyz, r2.xyz));
    r1.w = dot(r2.xyz, r6.xyz);
    r5.w = saturate(dot(r5.xyz, r6.xyz));
    r4.xyz = (r1.xyz) * (c[6].xyz);
    r2.w = saturate((r6.w) * (-(r5.w)) + (r1.w));
    r1.w = (c[30].x) * (c[30].x);
    r1.z = c[30].x;
    r1.xy = (r1.zz) * (r1.zz) + (c1.zw);
    r2.xy = (r1.ww) * (c1.xy);
    r8.x = 1.0f / (r1.x);
    r8.y = 1.0f / (r1.y);
    r4.w = (r2.y) * (r8.y);
    r1.xy = (v5.zw) * (c3.xz);
    r3 = tex2D(s13, r1.xy);
    r1.xy = (v5.zw) * (c3.xz) + (c3.yz);
    r1 = tex2D(s13, r1.xy);
    r3.w = r1.y;
    r1.w = (r2.x) * (-(r8.x)) + (c3.x);
    r8.xy = (r3.yw) * (c2.xx) + (c2.yy);
    r3.w = (r2.w) * (r4.w);
    r2.xyz = (r8.yyy) * (v1.xyz);
    r1.y = 1.0f / (r5.w);
    r2.xyz = (r8.xxx) * (v0.xyz) + (r2.xyz);
    r7.w = saturate((r6.w) * (r1.y));
    r9.xyz = (v3.xyz) * (r8.www) + (r2.xyz);
    r2 = tex2D(s14, v5.zw);
    r10.xy = (r2.xy) * (c3.ww);
    r8.xyz = normalize(r9.xyz);
    r3.xy = (r3.xz) * (r10.xx);
    r2.z = dot(r8.xyz, r5.xyz);
    r2.w = (r2.x) * (c3.w) + (-(r3.x));
    r5.xz = (r3.xy) * (c2.xx);
    r1.xy = (r1.xz) * (r10.yy);
    r2.x = (r3.z) * (-(r10.x)) + (r2.w);
    r2.w = (r2.y) * (c3.w) + (-(r1.x));
    r5.y = (r2.x) + (r2.x);
    r2.w = (r1.z) * (-(r10.y)) + (r2.w);
    r1.xz = (r1.xy) * (c2.xx);
    r1.y = (r2.w) + (r2.w);
    r2.w = dot(r8.xyz, r6.xyz);
    r2.xyz = (r2.zzz) * (r1.xyz) + (r5.xyz);
    r1.xyz = (r2.www) * (r1.xyz) + (r5.xyz);
    r2.xyz = (r1.www) * (r2.xyz);
    r2.w = (r3.w) * (r7.w);
    r1.xyz = (r1.xyz) * (r2.xyz);
    r1.w = (r1.w) * (r6.w) + (r2.w);
    r1.x = rsqrt(r1.x);
    r1.y = rsqrt(r1.y);
    r1.z = rsqrt(r1.z);
    r3.xyz = (r4.xyz) * (r1.www) + (r2.xyz);
    r1.x = 1.0f / (r1.x);
    r1.y = 1.0f / (r1.y);
    r1.z = 1.0f / (r1.z);
    r1.w = (r5.w) * (r5.w);
    r2.w = (r5.w) * (-(r5.w)) + (c3.x);
    r2.xyz = (r4.www) * (r1.xyz);
    r1.w = ((-(r1.w)) >= 0.0f ? (c3.x) : (r2.w));
    r1.xyz = (r0.xyz) * (r0.xyz);
    r0.xyz = (r2.xyz) * (r1.www) + (r3.xyz);
    r1.w = dot(r6.xyz, r7.xyz);
    r0.xyz = (r0.xyz) * (r1.xyz) + (-(v4.xyz));
    r2.w = max(c2.z, r1.w);
    r1.x = v0.w;
    r0.xyz = (r1.xxx) * (r0.xyz) + (v4.xyz);
    r1.w = pow(abs(r2.w), c2.w);
    r0.xyz = max(((r0.xyz) * (c[27].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.w = (r0.w) + (-(c3.x));
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.w = (r1.w) * (r0.w) + (c3.x);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);

    return oC0;
}
