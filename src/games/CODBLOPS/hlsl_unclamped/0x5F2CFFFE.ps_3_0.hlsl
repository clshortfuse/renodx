// Mechanically reconstructed from 0x5F2CFFFE.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(0.100000001f, 8.0f, 31.875f, 0.0f);
    const float4 c1 = float4(0.0f, 0.25f, 1.0f, 0.5f);
    const float4 c2 = float4(-2.0f, 3.0f, 4.0f, 0.125f);
    const float4 c3 = float4(-0.25f, -0.5f, -0.75f, 9.99999975e-06f);
    const float4 c4 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c12 = float4(2.0f, -1.0f, 1e-15f, 1.44269502f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 oC0 = 0.0f;

    r0.zw = c1.xx;
    r1 = (c[26]) * (v1.yyyy);
    r1 = (v1.xxxx) * (c[25]) + (r1);
    r1 = (v1.zzzz) * (c[27]) + (r1);
    r1 = (r1) + (c[28]);
    r0.xy = (r1.ww) * (c[30].xy);
    r2 = (r0.xyww) + (r1);
    r0 = (-(r0)) + (r1);
    r0 = tex2Dproj(s1, r0);
    r0.y = r0.x;
    r2 = tex2Dproj(s1, r2);
    r0.x = r2.x;
    r2.xy = (r1.ww) * (c[30].zw);
    r2.zw = c1.xx;
    r3 = (r1) + (r2.xyww);
    r1 = (r1) + (-(r2));
    r1 = tex2Dproj(s1, r1);
    r0.w = r1.x;
    r1 = tex2Dproj(s1, r3);
    r0.z = r1.x;
    r0.x = dot(r0, c1.yyyy);
    r1 = (v1.xyzx) * (c1.zzzx) + (c1.xxxz);
    r0.y = dot(r1, c[22]);
    r0.y = 1.0f / (r0.y);
    r0.z = dot(r1, c[11]);
    r0.w = dot(r1, c[20]);
    r1.x = dot(r1, c[21]);
    r0.yz = (r0.yy) * (r0.zw);
    r0.yz = (r0.yz) * (c1.ww) + (c1.ww);
    r2 = tex2D(s2, r0.yz);
    r0.y = (r2.x) * (r2.x);
    r1.y = (r1.x) * (r1.x);
    r0.z = dot(c[9].yz, r1.xy) + (c[9].x);
    r1.xy = saturate((r1.xx) * (c[10].xy) + (c[10].zw));
    r0.w = saturate(1.0f / (r0.z));
    r0.z = ((-abs(r0.z)) >= 0.0f ? (c1.x) : (r0.w));
    r1.zw = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c2.xx) + (c2.yy);
    r0.w = (r1.z) * (r1.x);
    r1.x = (r1.w) * (-(r1.y)) + (c1.z);
    r0.z = (r0.z) * (r0.w);
    r0.z = (r1.x) * (r0.z);
    r1.xyz = (c[6].xyz) + (-(v1.xyz));
    r2.xyz = normalize(r1.xyz);
    r0.w = dot(r2.xyz, c[23].xyz);
    r0.w = saturate((r0.w) * (c[24].x) + (c[24].y));
    r1.x = (r0.w) * (r0.w);
    r0.w = (r0.w) * (c2.x) + (c2.y);
    r0.w = (r1.x) * (r0.w);
    r0.z = (r0.z) * (r0.w);
    r0.y = (r0.y) * (r0.z);
    r0.x = (r0.x) * (r0.y);
    r0.yzw = (r0.xxx) * (c[8].xyz);
    r1.xyz = (r0.xxx) * (c[7].xyz);
    r0.x = dot(-(v1.xyz), -(v1.xyz));
    r0.x = rsqrt(r0.x);
    r3.xyz = (-(v1.xyz)) * (r0.xxx) + (r2.xyz);
    r4.xyz = normalize(r3.xyz);
    r1.w = (c[31].x) * (v4.x);
    r3.z = c2.z;
    r5.w = (r1.w) * (r3.z) + (c[32].x);
    r3.y = c[31].y;
    r6.zw = (v4.yy) * (r3.yy) + (c[33].yw);
    r7.xyz = (c3.xyz) + (v4.xxx);
    r8.xyz = (r7.xyz) * (c[31].xxx);
    r6.xy = (r8.yz) * (r3.zz) + (c[33].xz);
    r5.z = (r8.x) * (r3.z) + (c[32].z);
    r3.xz = float2(((r7.z) >= 0.0f ? (r6.y) : (r6.x)), ((r7.z) >= 0.0f ? (r6.w) : (r6.z)));
    r5.xy = (v4.yy) * (r3.yy) + (c[32].yw);
    r3.xy = float2(((r7.y) >= 0.0f ? (r3.x) : (r5.z)), ((r7.y) >= 0.0f ? (r3.z) : (r5.y)));
    r3.xy = float2(((r7.x) >= 0.0f ? (r3.x) : (r5.w)), ((r7.x) >= 0.0f ? (r3.y) : (r5.x)));
    r3.zw = (r3.xy) * (c[39].xx);
    r5 = tex2D(s3, r3.zw);
    r3.zw = (r5.wy) * (c4.xy) + (c4.zw);
    r3.zw = (r3.zw) * (c1.ww) + (c1.ww);
    r5.xy = ddx(v4.xy);
    r5.xy = (r5.xy) * (c2.ww);
    r5.zw = ddy(v4.xy);
    r5.zw = (r5.zw) * (c2.ww);
    r6 = tex2Dgrad(s4, r3.xy, r5.xy, r5.zw);
    r5 = tex2Dgrad(s5, r3.xy, r5.xy, r5.zw);
    r5 = (r5) * (r5);
    r3.xy = (r6.wy) * (c4.xy) + (c4.zw);
    r3.xy = (r3.xy) * (c1.ww) + (c1.ww);
    r3.xy = (r3.xy) * (c12.xx) + (c12.yy);
    r3.xy = (r3.zw) * (-(c2.xx)) + (r3.xy);
    r3.xy = (r3.xy) + (-(c1.zz));
    r1.w = dot(v2.xyz, v2.xyz);
    r1.w = rsqrt(r1.w);
    r6.xyz = (r1.www) * (v2.xyz);
    r7.xyz = (r6.zxy) * (v3.yzx);
    r7.xyz = (r6.yzx) * (v3.zxy) + (-(r7.xyz));
    r7.xyz = (r7.xyz) * (v3.www);
    r3.yzw = (r3.yyy) * (r7.xyz);
    r3.xyz = (r3.xxx) * (v3.xyz) + (r3.yzw);
    r3.xyz = (v2.xyz) * (r1.www) + (r3.xyz);
    r7.xyz = normalize(r3.xyz);
    r1.w = saturate(dot(r7.xyz, r4.xyz));
    r3.xyz = (r0.xxx) * (-(v1.xyz));
    r2.w = saturate(dot(r7.xyz, r3.xyz));
    r3.x = saturate(dot(r6.xyz, r3.xyz));
    r3.x = (r2.w) + (r3.x);
    r3.x = (r3.x) * (c1.w);
    r4.x = max(c0.x, r3.x);
    r3.x = 1.0f / (r4.x);
    r3.x = rsqrt(r3.x);
    r3.x = 1.0f / (r3.x);
    r3.y = 1.0f / (r2.w);
    r3.z = dot(r3.yy, r1.ww) + (c1.x);
    r1.w = (r1.w) * (r1.w) + (c12.z);
    r1.w = 1.0f / (r1.w);
    r2.x = saturate(dot(r7.xyz, r2.xyz));
    r3.w = min(r2.w, r2.x);
    r2.y = saturate((r3.z) * (r3.w));
    r2.y = (r2.x) * (r2.y);
    r1.xyz = (r1.xyz) * (r2.xxx);
    r2.x = rsqrt(r2.y);
    r2.x = 1.0f / (r2.x);
    r2.y = (r1.w) * (r1.w);
    r1.w = (-(r1.w)) + (c1.z);
    r3.w = c3.w;
    r2.z = max(r3.w, c[42].x);
    r2.z = (r2.z) * (r2.z);
    r2.z = 1.0f / (r2.z);
    r1.w = (r1.w) * (r2.z);
    r1.w = (r1.w) * (c12.w);
    r1.w = exp2(r1.w);
    r1.w = (r2.y) * (r1.w);
    r1.w = (r2.x) * (r1.w);
    r0.yzw = (r0.yzw) * (r1.www);
    r4.xyz = normalize(c[17].xyz);
    r8.xyz = (-(v1.xyz)) * (r0.xxx) + (r4.xyz);
    r0.x = saturate(dot(r7.xyz, r4.xyz));
    r4.xyz = normalize(r8.xyz);
    r1.w = saturate(dot(r7.xyz, r4.xyz));
    r2.x = (r1.w) + (r1.w);
    r1.w = (r1.w) * (r1.w) + (c12.z);
    r1.w = 1.0f / (r1.w);
    r2.x = (r3.y) * (r2.x);
    r3.y = min(r2.w, r0.x);
    r2.x = saturate((r2.x) * (r3.y));
    r2.x = (r0.x) * (r2.x);
    r2.x = rsqrt(r2.x);
    r2.x = 1.0f / (r2.x);
    r2.y = (-(r1.w)) + (c1.z);
    r1.w = (r1.w) * (r1.w);
    r2.y = (r2.z) * (r2.y);
    r2.y = (r2.y) * (c12.w);
    r2.y = exp2(r2.y);
    r1.w = (r1.w) * (r2.y);
    r1.w = (r2.x) * (r1.w);
    r2.x = max(abs(r6.y), abs(r6.z));
    r3.y = max(abs(r6.x), r2.x);
    r2.xyw = (r6.xyz) * (c[5].xyz);
    r3.y = 1.0f / (r3.y);
    r2.xyw = (r2.xyw) * (r3.yyy) + (v5.xyz);
    r4 = tex3D(s11, r2.xyw);
    r2.xyw = (r4.www) * (c[19].xyz);
    r3.yzw = (r4.www) * (c[18].xyz);
    r3.yzw = (r3.yzw) * (c[29].xxx);
    r1.xyz = (r0.xxx) * (r3.yzw) + (r1.xyz);
    r2.xyw = (r2.xyw) * (c[29].yyy);
    r0.xyz = (r1.www) * (r2.xyw) + (r0.yzw);
    r0.xyz = (r3.xxx) * (r0.xyz);
    r0.xyz = (r2.zzz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c[41].xxx);
    r0.xyz = (r0.xyz) * (c1.yyy) + (c1.zzz);
    r2.x = log2(abs(r0.x));
    r2.y = log2(abs(r0.y));
    r2.z = log2(abs(r0.z));
    r0.x = c1.x;
    r1.w = max(c[40].x, r0.x);
    r0.x = (r1.w) + (-(c1.z));
    r0.xyz = (r2.xyz) * (r0.xxx);
    r2.x = exp2(r0.x);
    r2.y = exp2(r0.y);
    r2.z = exp2(r0.z);
    r0.xyz = normalize(v1.xyz);
    r0.w = dot(r0.xyz, r7.xyz);
    r0.w = (r0.w) + (r0.w);
    r0.xyz = (r7.xyz) * (-(r0.www)) + (r0.xyz);
    r0 = texCUBE(s15, r0.xyz);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c0.yyy);
    r3 = tex3D(s11, v5.xyz);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r0.xyz = (r0.xyz) * (r3.xyz);
    r0.xyz = (r0.xyz) * (c[38].xxx);
    r0.xyz = (r0.xyz) * (c0.zzz);
    r0.xyz = (r2.xyz) * (c[40].yyy) + (r0.xyz);
    r0.xyz = (r0.xyz) * (c[43].xxx);
    r0.w = max(abs(r7.y), abs(r7.z));
    r1.w = max(abs(r7.x), r0.w);
    r2.xyz = (r7.xyz) * (c[5].xyz);
    r0.w = 1.0f / (r1.w);
    r2.xyz = (r2.xyz) * (r0.www) + (v5.xyz);
    r2 = tex3D(s11, r2.xyz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r1.xyz = (r2.xyz) * (c0.zzz) + (r1.xyz);
    r5.xyz = (r1.xyz) * (r5.xyz) + (r0.xyz);
    r0 = max(r5, c1.xxxx);
    r1 = (r0.xyzx) * (c1.zzzx) + (c1.xxxz);
    oC0.w = r0.w;
    r0.x = dot(r1, c[35]);
    r0.y = dot(r1, c[36]);
    r0.z = dot(r1, c[37]);
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[34].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);

    return oC0;
}
