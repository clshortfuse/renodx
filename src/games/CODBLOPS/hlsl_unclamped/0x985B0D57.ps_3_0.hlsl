// Mechanically reconstructed from 0x985B0D57.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler2D s8 : register(s8);
sampler3D s11 : register(s11);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
    float4 v4 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(1.44269502f, 0.100000001f, 8.0f, 31.875f);
    const float4 c1 = float4(0.0f, 0.25f, 1.0f, 0.5f);
    const float4 c2 = float4(-2.0f, 3.0f, 9.99999975e-06f, 1e-15f);
    const float4 c3 = float4(1.16412354f, 1.59579468f, -0.87065506f, 0.0f);
    const float4 c4 = float4(1.16412354f, -0.813476562f, -0.391448975f, 0.529705048f);
    const float4 c12 = float4(1.16412354f, 2.01782227f, -1.08166885f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 oC0 = 0.0f;

    r0.zw = c1.xx;
    r1 = (c[26]) * (v1.yyyy);
    r1 = (v1.xxxx) * (c[25]) + (r1);
    r1 = (v1.zzzz) * (c[27]) + (r1);
    r1 = (r1) + (c[28]);
    r0.xy = (r1.ww) * (c[30].xy);
    r2 = (r0.xyww) + (r1);
    r0 = (-(r0)) + (r1);
    r0 = tex2Dproj(s1, r0);
    r0.y = r0.x;
    r2 = tex2Dproj(s1, r2);
    r0.x = r2.x;
    r2.xy = (r1.ww) * (c[30].zw);
    r2.zw = c1.xx;
    r3 = (r1) + (r2.xyww);
    r1 = (r1) + (-(r2));
    r1 = tex2Dproj(s1, r1);
    r0.w = r1.x;
    r1 = tex2Dproj(s1, r3);
    r0.z = r1.x;
    r0.x = dot(r0, c1.yyyy);
    r1 = (v1.xyzx) * (c1.zzzx) + (c1.xxxz);
    r0.y = dot(r1, c[22]);
    r0.y = 1.0f / (r0.y);
    r0.z = dot(r1, c[11]);
    r0.w = dot(r1, c[20]);
    r1.x = dot(r1, c[21]);
    r0.yz = (r0.yy) * (r0.zw);
    r0.yz = (r0.yz) * (c1.ww) + (c1.ww);
    r2 = tex2D(s2, r0.yz);
    r0.y = (r2.x) * (r2.x);
    r1.y = (r1.x) * (r1.x);
    r0.z = dot(c[9].yz, r1.xy) + (c[9].x);
    r1.xy = saturate((r1.xx) * (c[10].xy) + (c[10].zw));
    r0.w = saturate(1.0f / (r0.z));
    r0.z = ((-abs(r0.z)) >= 0.0f ? (c1.x) : (r0.w));
    r1.zw = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c2.xx) + (c2.yy);
    r0.w = (r1.z) * (r1.x);
    r1.x = (r1.w) * (-(r1.y)) + (c1.z);
    r0.z = (r0.z) * (r0.w);
    r0.z = (r1.x) * (r0.z);
    r1.xyz = (c[6].xyz) + (-(v1.xyz));
    r2.xyz = normalize(r1.xyz);
    r0.w = dot(r2.xyz, c[23].xyz);
    r0.w = saturate((r0.w) * (c[24].x) + (c[24].y));
    r1.x = (r0.w) * (r0.w);
    r0.w = (r0.w) * (c2.x) + (c2.y);
    r0.w = (r1.x) * (r0.w);
    r0.z = (r0.z) * (r0.w);
    r0.y = (r0.y) * (r0.z);
    r0.x = (r0.x) * (r0.y);
    r0.yzw = (r0.xxx) * (c[8].xyz);
    r1.xyz = (r0.xxx) * (c[7].xyz);
    r0.x = dot(-(v1.xyz), -(v1.xyz));
    r0.x = rsqrt(r0.x);
    r3.xyz = (-(v1.xyz)) * (r0.xxx) + (r2.xyz);
    r4.xyz = normalize(r3.xyz);
    r3.xyz = normalize(v2.xyz);
    r1.w = saturate(dot(r3.xyz, r4.xyz));
    r4.xyz = (r0.xxx) * (-(v1.xyz));
    r2.w = saturate(dot(r3.xyz, r4.xyz));
    r3.w = 1.0f / (r2.w);
    r4.x = dot(r3.ww, r1.ww) + (c1.x);
    r1.w = (r1.w) * (r1.w) + (c2.w);
    r1.w = 1.0f / (r1.w);
    r2.x = saturate(dot(r3.xyz, r2.xyz));
    r4.y = min(r2.w, r2.x);
    r2.y = saturate((r4.x) * (r4.y));
    r2.y = (r2.x) * (r2.y);
    r1.xyz = (r1.xyz) * (r2.xxx);
    r2.x = rsqrt(r2.y);
    r2.x = 1.0f / (r2.x);
    r4 = tex2D(s8, v3.xy);
    r2.y = (r4.x) * (r4.x);
    r4.x = max(c2.z, r2.y);
    r2.y = (r4.x) * (r4.x);
    r2.y = 1.0f / (r2.y);
    r2.z = (-(r1.w)) + (c1.z);
    r1.w = (r1.w) * (r1.w);
    r2.z = (r2.y) * (r2.z);
    r2.z = (r2.z) * (c0.x);
    r2.z = exp2(r2.z);
    r1.w = (r1.w) * (r2.z);
    r1.w = (r2.x) * (r1.w);
    r0.yzw = (r0.yzw) * (r1.www);
    r4.xyz = normalize(c[17].xyz);
    r5.xyz = (-(v1.xyz)) * (r0.xxx) + (r4.xyz);
    r0.x = saturate(dot(r3.xyz, r4.xyz));
    r4.xyz = normalize(r5.xyz);
    r1.w = saturate(dot(r3.xyz, r4.xyz));
    r2.x = (r1.w) + (r1.w);
    r1.w = (r1.w) * (r1.w) + (c2.w);
    r1.w = 1.0f / (r1.w);
    r2.x = (r3.w) * (r2.x);
    r3.w = min(r2.w, r0.x);
    r4.x = max(c0.y, r2.w);
    r2.z = 1.0f / (r4.x);
    r2.z = rsqrt(r2.z);
    r2.z = 1.0f / (r2.z);
    r2.x = saturate((r2.x) * (r3.w));
    r2.x = (r0.x) * (r2.x);
    r2.x = rsqrt(r2.x);
    r2.x = 1.0f / (r2.x);
    r2.w = (-(r1.w)) + (c1.z);
    r1.w = (r1.w) * (r1.w);
    r2.w = (r2.y) * (r2.w);
    r2.w = (r2.w) * (c0.x);
    r2.w = exp2(r2.w);
    r1.w = (r1.w) * (r2.w);
    r1.w = (r2.x) * (r1.w);
    r2.x = max(abs(r3.y), abs(r3.z));
    r4.x = max(abs(r3.x), r2.x);
    r3.xyz = (r3.xyz) * (c[5].xyz);
    r2.x = 1.0f / (r4.x);
    r3.xyz = (r3.xyz) * (r2.xxx) + (v4.xyz);
    r3 = tex3D(s11, r3.xyz);
    r4.xyz = (r3.www) * (c[19].xyz);
    r4.xyz = (r4.xyz) * (c[29].yyy);
    r0.yzw = (r1.www) * (r4.xyz) + (r0.yzw);
    r0.yzw = (r2.zzz) * (r0.yzw);
    r0.yzw = (r2.yyy) * (r0.yzw);
    r0.yzw = (r0.yzw) * (c1.yyy);
    r2 = tex2D(s6, v3.xy);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r0.yzw = (r0.yzw) * (r2.xyz);
    r2.xy = lerp(c[32].xy, c[32].zw, v3.xy);
    r4 = tex2D(s4, r2.xy);
    r4.y = r4.x;
    r5 = tex2D(s5, r2.xy);
    r2 = tex2D(s3, r2.xy);
    r4.xw = (r2.xx) * (c1.zx) + (c1.xz);
    r4.z = r5.x;
    r2.y = dot(c4, r4);
    r2.x = dot(c3.xyz, r4.xyw);
    r2.z = dot(c12.xyz, r4.xzw);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = (r2.xyz) * (c[34].xxx);
    r4 = tex2D(s7, v3.xy);
    r5 = (r4) * (r4);
    r4.w = (r4.w) * (-(r4.w)) + (c[35].x);
    r4.xyz = (r2.xyz) * (c0.zzz) + (-(r5.xyz));
    r2 = (c[31].xxxx) * (r4) + (r5);
    r0.yzw = (r0.yzw) * (r2.www);
    r4.xyz = (r3.www) * (c[18].xyz);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r4.xyz = (r4.xyz) * (c[29].xxx);
    r1.xyz = (r0.xxx) * (r4.xyz) + (r1.xyz);
    r1.xyz = (r3.xyz) * (c0.www) + (r1.xyz);
    r2.xyz = (r1.xyz) * (r2.xyz) + (r0.yzw);
    r0 = max(r2, c1.xxxx);
    oC0.w = r0.w;
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[33].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);

    return oC0;
}
