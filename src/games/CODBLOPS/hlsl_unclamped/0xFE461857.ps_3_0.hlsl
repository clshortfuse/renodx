// Mechanically reconstructed from 0xFE461857.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    const float4 c0 = float4(8.0f, 31.875f, 4.0f, 2.0f);
    const float4 c1 = float4(0.0f, 0.25f, 1.0f, 0.5f);
    const float4 c2 = float4(-2.0f, 3.0f, -28.0f, 30.0f);
    const float4 c3 = float4(0.38647899f, 0.392856985f, 0.364796013f, 15.0f);
    const float4 c4 = float4(0.0399999991f, 0.782500029f, 0.00749999983f, 1.00750005f);
    const float4 c12 = float4(81.2394867f, 17.3480244f, 37.3498383f, 59.3948402f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 oC0 = 0.0f;

    r0.zw = c1.xx;
    r1 = (c[24]) * (v1.yyyy);
    r1 = (v1.xxxx) * (c[23]) + (r1);
    r1 = (v1.zzzz) * (c[25]) + (r1);
    r1 = (r1) + (c[26]);
    r0.xy = (r1.ww) * (c[42].xy);
    r2 = (r0.xyww) + (r1);
    r0 = (-(r0)) + (r1);
    r0 = tex2Dproj(s1, r0);
    r0.y = r0.x;
    r2 = tex2Dproj(s1, r2);
    r0.x = r2.x;
    r2.xy = (r1.ww) * (c[42].zw);
    r2.zw = c1.xx;
    r3 = (r1) + (r2.xyww);
    r1 = (r1) + (-(r2));
    r1 = tex2Dproj(s1, r1);
    r0.w = r1.x;
    r1 = tex2Dproj(s1, r3);
    r0.z = r1.x;
    r0.x = dot(r0, c1.yyyy);
    r0.yzw = (c[5].xyz) + (-(v1.xyz));
    r1.xyz = normalize(r0.yzw);
    r0.y = dot(r1.xyz, c[21].xyz);
    r0.y = saturate((r0.y) * (c[22].x) + (c[22].y));
    r0.z = (r0.y) * (r0.y);
    r0.y = (r0.y) * (c2.x) + (c2.y);
    r0.y = (r0.z) * (r0.y);
    r2 = (v1.xyzx) * (c1.zzzx) + (c1.xxxz);
    r0.z = dot(r2, c[11]);
    r0.w = (r0.z) * (r0.z);
    r0.w = dot(c[7].yz, r0.zw) + (c[7].x);
    r3.xy = saturate((r0.zz) * (c[8].xy) + (c[8].zw));
    r0.z = saturate(1.0f / (r0.w));
    r0.z = ((-abs(r0.w)) >= 0.0f ? (c1.x) : (r0.z));
    r3.zw = (r3.xy) * (r3.xy);
    r3.xy = (r3.xy) * (c2.xx) + (c2.yy);
    r0.w = (r3.z) * (r3.x);
    r1.w = (r3.w) * (-(r3.y)) + (c1.z);
    r0.z = (r0.z) * (r0.w);
    r0.z = (r1.w) * (r0.z);
    r0.y = (r0.y) * (r0.z);
    r0.z = dot(r2, c[20]);
    r0.z = 1.0f / (r0.z);
    r3.x = dot(r2, c[9]);
    r3.y = dot(r2, c[10]);
    r0.zw = (r0.zz) * (r3.xy);
    r0.zw = (r0.zw) * (c1.ww) + (c1.ww);
    r3 = tex2D(s3, r0.zw);
    r0.z = (r3.x) * (r3.x);
    r0.y = (r0.y) * (r0.z);
    r0.x = (r0.x) * (r0.y);
    r0.xyz = (r0.xxx) * (c[6].xyz);
    r3.xyz = normalize(v1.xyz);
    r0.w = dot(r1.xyz, r3.xyz);
    r0.w = saturate((r0.w) * (c1.w) + (c1.w));
    r4.xyz = (c[27].xyz) + (-(v1.xyz));
    r5.xyz = normalize(r4.xyz);
    r1.w = dot(r5.xyz, r3.xyz);
    r1.w = saturate((r1.w) * (c1.w) + (c1.w));
    r3.w = (r1.w) * (c2.z) + (c2.w);
    r1.w = (r1.w) + (c1.z);
    r4.x = lerp(r3.w, -(c2.x), r0.w);
    r4.y = lerp(r1.w, -(c2.x), r0.w);
    r6.xyz = normalize(v2.xyz);
    r0.w = dot(-(r1.xyz), r6.xyz);
    r0.w = (r0.w) + (r0.w);
    r1.xyz = (r6.xyz) * (-(r0.www)) + (-(r1.xyz));
    r0.w = saturate(dot(r1.xyz, -(r3.xyz)));
    r1.x = pow(abs(r0.w), r4.x);
    r0.xyz = (r0.xyz) * (r1.xxx);
    r0.xyz = (r4.yyy) * (r0.xyz);
    r0.w = dot(-(r5.xyz), r6.xyz);
    r0.w = (r0.w) + (r0.w);
    r1.xyz = (r6.xyz) * (-(r0.www)) + (-(r5.xyz));
    r0.w = saturate(dot(r1.xyz, -(r3.xyz)));
    r1.x = pow(abs(r0.w), r3.w);
    r0.w = dot(r2, c[37]);
    r0.w = 1.0f / (r0.w);
    r1.y = dot(r2, c[34]);
    r1.z = dot(r2, c[35]);
    r2.z = dot(r2, c[36]);
    r4 = (r0.wwww) * (r1.yyzz);
    r5 = (r4) * (c[38].zwxy);
    r7.x = log2(abs(r5.x));
    r7.y = log2(abs(r5.y));
    r7.z = log2(abs(r5.z));
    r7.w = log2(abs(r5.w));
    r5 = (r7) * (c[39].xxxx);
    r1.y = exp2(r5.x);
    r1.z = exp2(r5.y);
    r4.x = exp2(r5.z);
    r4.z = exp2(r5.w);
    r1.yz = (r1.yz) + (r4.xz);
    r4.x = log2(abs(r1.y));
    r4.z = log2(abs(r1.z));
    r1.yz = (r4.xz) * (c[39].yy);
    r0.w = exp2(r1.y);
    r1.y = exp2(r1.z);
    r0.w = (r0.w) * (c[40].x);
    r0.w = (r1.y) * (c[40].y) + (-(r0.w));
    r5.y = c[40].y;
    r1.y = (r1.y) * (r5.y) + (-(c[39].z));
    r0.w = 1.0f / (r0.w);
    r5.w = saturate((r1.y) * (r0.w));
    r2.xy = abs(r4.yw);
    r7 = c[32];
    r7 = saturate((r2.zyxz) * (r7) + (c[33]));
    r5.x = (r7.w) * (r7.x);
    r5.yz = r7.yz;
    r7 = (r5) * (r5);
    r5 = (c[40].zzzz) * (r5) + (c[40].wwww);
    r5 = (r7) * (r5);
    r0.w = (r5.z) * (r5.y);
    r1.y = abs(c[39].w);
    r0.w = ((-(r1.y)) >= 0.0f ? (r0.w) : (r5.w));
    r2.w = (r2.z) * (r2.z);
    r1.y = dot(c[31].yz, r2.zw) + (c[31].x);
    r0.w = (r0.w) * (r1.y);
    r0.w = (r5.x) * (r0.w);
    r2 = c[41];
    r1.y = dot(r4.yw, r2.xy) + (c[30].x);
    r1.z = dot(r4.yw, r2.zw) + (c[30].z);
    r2 = tex2D(s2, r1.yz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = (r0.www) * (r2.xyz);
    r4 = tex2D(s12, v3.zw);
    r2.xyz = (r2.xyz) * (r4.yyy);
    r2.xyz = (r2.xyz) * (c[28].xyz);
    r2.xyz = (r2.xyz) * (c[29].yyy);
    r1.xyz = (r1.xxx) * (r2.xyz);
    r0.xyz = (r1.xyz) * (r1.www) + (r0.xyz);
    r0.w = dot(r3.xyz, r6.xyz);
    r0.w = (r0.w) + (r0.w);
    r1.xyz = (r6.xyz) * (-(r0.www)) + (r3.xyz);
    r1 = texCUBE(s15, r1.xyz);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r1.xyz = (r1.xyz) * (c0.xxx);
    r2.xy = (c1.zw) * (v3.zw);
    r2 = tex2D(s13, r2.xy);
    r3 = tex2D(s14, v3.zw);
    r0.w = (r3.x) * (c0.y);
    r2.xy = (r2.xz) * (r0.ww);
    r1.w = (r3.x) * (c0.y) + (-(r2.x));
    r2.xw = (r1.xz) * (r2.xy);
    r0.w = (r2.z) * (-(r0.w)) + (r1.w);
    r2.y = (r1.y) * (r0.w);
    r1.xyz = (r2.xyw) * (c[48].xxx);
    r1.xyz = (r1.xyz) * (c0.zwz);
    r0.xyz = (r0.xyz) * (c3.xyz) + (r1.xyz);
    r1 = c12;
    r0.w = dot(c[43].wwww, r1);
    r1.x = frac(r0.w);
    r1.w = c[43].w;
    r0.w = dot(r1.xwww, c12);
    r1.y = frac(r0.w);
    r0.w = dot(r1.xyww, c12);
    r1.z = frac(r0.w);
    r0.w = dot(r1, c12);
    r1.w = frac(r0.w);
    r2.yzw = r1.yzw;
    r0.w = dot(r1, c12);
    r2.x = frac(r0.w);
    r0.w = dot(r2, c12);
    r2.y = frac(r0.w);
    r1.x = 1.0f / (c[50].x);
    r1.y = 1.0f / (c[50].y);
    r1.xy = (v3.xy) * (r1.xy) + (r2.xy);
    r1 = tex2D(s4, r1.xy);
    r0.w = c[43].w;
    r1.w = (r0.w) * (c3.w);
    r1.w = frac(r1.w);
    r1.w = (r0.w) * (c3.w) + (-(r1.w));
    r2.y = (r1.w) * (c4.x) + (v3.y);
    r2.x = c4.y;
    r2 = tex2D(s5, r2.xy);
    r1.w = (r2.x) * (-(c4.z)) + (v3.x);
    r1.w = (r1.w) * (c4.w) + (-(v3.x));
    r2.x = (c[51].x) * (r1.w) + (v3.x);
    r1.w = (-(r2.x)) + (c1.z);
    r1.w = ((r1.w) >= 0.0f ? (c1.x) : (c1.z));
    r2.z = ((r2.x) >= 0.0f ? (c1.x) : (c1.z));
    r2.x = saturate(r2.x);
    r1.w = (r1.w) + (r2.z);
    r2.z = frac(abs(v3.y));
    r2.y = ((v3.y) >= 0.0f ? (r2.z) : (-(r2.z)));
    r2 = tex2D(s6, r2.xy);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = float3(((-(r1.w)) >= 0.0f ? (r2.x) : (c1.x)), ((-(r1.w)) >= 0.0f ? (r2.y) : (c1.x)), ((-(r1.w)) >= 0.0f ? (r2.z) : (c1.x)));
    r1.w = abs(c[43].w);
    r1.w = frac(r1.w);
    r3.y = ((c[43].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r3.x = c1.x;
    r3 = tex2D(s5, r3.xy);
    r1.w = saturate((r3.x) * (c[49].x));
    r3.xyz = lerp(r2.xyz, r1.xyz, r1.www);
    r0.w = (r0.w) * (c[52].x);
    r1.x = frac(abs(r0.w));
    r0.w = ((r0.w) >= 0.0f ? (r1.x) : (-(r1.x)));
    r1.x = (c[56].x) + (-(v3.y));
    r1.x = (r1.x) + (c1.z);
    r0.w = (r0.w) + (r1.x);
    r1.x = frac(abs(r0.w));
    r0.w = ((r0.w) >= 0.0f ? (r1.x) : (-(r1.x)));
    r1.x = pow(abs(r0.w), c[55].x);
    r1.z = c1.z;
    r0.w = (r1.x) * (-(c[54].x)) + (r1.z);
    r1.xyz = lerp(c[53].xyz, r3.xyz, r0.www);
    r0.xyz = (r0.xyz) + (r1.xyz);
    r0.w = c1.z;
    r1.x = dot(r0, c[45]);
    r1.y = dot(r0, c[46]);
    r1.z = dot(r0, c[47]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[44].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c1.z;

    return oC0;
}
