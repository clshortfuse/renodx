// Mechanically reconstructed from 0x783E7DE3.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
    float4 v4 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(1.0f, 0.0f, 0.5f, 8.0f);
    const float4 c1 = float4(-28.0f, 30.0f, 31.875f, 15.0f);
    const float4 c2 = float4(0.38647899f, 0.392856985f, 0.364796013f, 0.0399999991f);
    const float4 c3 = float4(0.782500029f, 0.00749999983f, 1.00750005f, 0.0f);
    const float4 c4 = float4(81.2394867f, 17.3480244f, 37.3498383f, 59.3948402f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = (v1.xyzx) * (c0.xxxy) + (c0.yyyx);
    r1.x = dot(r0, c[24]);
    r1.x = 1.0f / (r1.x);
    r1.y = dot(r0, c[21]);
    r1.z = dot(r0, c[22]);
    r0.z = dot(r0, c[23]);
    r1 = (r1.xxxx) * (r1.yyzz);
    r2 = (r1) * (c[25].zwxy);
    r3.x = log2(abs(r2.x));
    r3.y = log2(abs(r2.y));
    r3.z = log2(abs(r2.z));
    r3.w = log2(abs(r2.w));
    r2 = (r3) * (c[26].xxxx);
    r1.x = exp2(r2.x);
    r1.z = exp2(r2.y);
    r2.x = exp2(r2.z);
    r2.y = exp2(r2.w);
    r1.xz = (r1.xz) + (r2.xy);
    r2.x = log2(abs(r1.x));
    r2.y = log2(abs(r1.z));
    r1.xz = (r2.xy) * (c[26].yy);
    r1.x = exp2(r1.x);
    r1.z = exp2(r1.z);
    r1.x = (r1.x) * (c[27].x);
    r1.x = (r1.z) * (c[27].y) + (-(r1.x));
    r2.y = c[27].y;
    r1.z = (r1.z) * (r2.y) + (-(c[26].z));
    r1.x = 1.0f / (r1.x);
    r2.w = saturate((r1.z) * (r1.x));
    r0.xy = abs(r1.yw);
    r3 = c[11];
    r3 = saturate((r0.zyxz) * (r3) + (c[20]));
    r2.x = (r3.w) * (r3.x);
    r2.yz = r3.yz;
    r3 = (r2) * (r2);
    r2 = (c[27].zzzz) * (r2) + (c[27].wwww);
    r2 = (r3) * (r2);
    r0.x = (r2.z) * (r2.y);
    r0.y = abs(c[26].w);
    r0.x = ((-(r0.y)) >= 0.0f ? (r0.x) : (r2.w));
    r0.w = (r0.z) * (r0.z);
    r0.y = dot(c[10].yz, r0.zw) + (c[10].x);
    r0.x = (r0.x) * (r0.y);
    r0.x = (r2.x) * (r0.x);
    r2 = c[28];
    r0.y = dot(r1.yw, r2.xy) + (c[9].x);
    r0.z = dot(r1.yw, r2.zw) + (c[9].z);
    r1 = tex2D(s1, r0.yz);
    r0.yzw = (r1.xyz) * (r1.xyz);
    r0.xyz = (r0.xxx) * (r0.yzw);
    r1.xyz = normalize(v2.xyz);
    r0.w = max(abs(r1.y), abs(r1.z));
    r2.x = max(abs(r1.x), r0.w);
    r0.w = 1.0f / (r2.x);
    r2.xyz = (r1.xyz) * (c[5].xyz);
    r2.xyz = (r2.xyz) * (r0.www) + (v4.xyz);
    r2 = tex3D(s11, r2.xyz);
    r0.xyz = (r0.xyz) * (r2.www);
    r0.xyz = (r0.xyz) * (c[7].xyz);
    r0.xyz = (r0.xyz) * (c[8].yyy);
    r2.xyz = (c[6].xyz) + (-(v1.xyz));
    r3.xyz = normalize(r2.xyz);
    r0.w = dot(-(r3.xyz), r1.xyz);
    r0.w = (r0.w) + (r0.w);
    r2.xyz = (r1.xyz) * (-(r0.www)) + (-(r3.xyz));
    r4.xyz = normalize(v1.xyz);
    r0.w = saturate(dot(r2.xyz, -(r4.xyz)));
    r1.w = dot(r3.xyz, r4.xyz);
    r1.w = saturate((r1.w) * (c0.z) + (c0.z));
    r2.x = (r1.w) * (c1.x) + (c1.y);
    r1.w = (r1.w) + (c0.x);
    r3.x = pow(abs(r0.w), r2.x);
    r0.xyz = (r0.xyz) * (r3.xxx);
    r0.xyz = (r1.www) * (r0.xyz);
    r0.w = dot(r4.xyz, r1.xyz);
    r0.w = (r0.w) + (r0.w);
    r1.xyz = (r1.xyz) * (-(r0.www)) + (r4.xyz);
    r1 = texCUBE(s15, r1.xyz);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r1.xyz = (r1.xyz) * (c0.www);
    r2 = tex3D(s11, v4.xyz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r1.xyz = (r1.xyz) * (r2.xyz);
    r1.xyz = (r1.xyz) * (c[34].xxx);
    r1.xyz = (r1.xyz) * (c1.zzz);
    r0.xyz = (r0.xyz) * (c2.xyz) + (r1.xyz);
    r1 = c4;
    r0.w = dot(c[29].wwww, r1);
    r1.x = frac(r0.w);
    r1.w = c[29].w;
    r0.w = dot(r1.xwww, c4);
    r1.y = frac(r0.w);
    r0.w = dot(r1.xyww, c4);
    r1.z = frac(r0.w);
    r0.w = dot(r1, c4);
    r1.w = frac(r0.w);
    r2.yzw = r1.yzw;
    r0.w = dot(r1, c4);
    r2.x = frac(r0.w);
    r0.w = dot(r2, c4);
    r2.y = frac(r0.w);
    r1.x = 1.0f / (c[36].x);
    r1.y = 1.0f / (c[36].y);
    r1.xy = (v3.xy) * (r1.xy) + (r2.xy);
    r1 = tex2D(s2, r1.xy);
    r0.w = c[29].w;
    r1.w = (r0.w) * (c1.w);
    r1.w = frac(r1.w);
    r1.w = (r0.w) * (c1.w) + (-(r1.w));
    r2.y = (r1.w) * (c2.w) + (v3.y);
    r2.x = c3.x;
    r2 = tex2D(s3, r2.xy);
    r1.w = (r2.x) * (-(c3.y)) + (v3.x);
    r1.w = (r1.w) * (c3.z) + (-(v3.x));
    r2.x = (c[37].x) * (r1.w) + (v3.x);
    r1.w = (-(r2.x)) + (c0.x);
    r1.w = ((r1.w) >= 0.0f ? (c0.y) : (c0.x));
    r2.z = ((r2.x) >= 0.0f ? (c0.y) : (c0.x));
    r2.x = saturate(r2.x);
    r1.w = (r1.w) + (r2.z);
    r2.z = frac(abs(v3.y));
    r2.y = ((v3.y) >= 0.0f ? (r2.z) : (-(r2.z)));
    r2 = tex2D(s4, r2.xy);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = float3(((-(r1.w)) >= 0.0f ? (r2.x) : (c0.y)), ((-(r1.w)) >= 0.0f ? (r2.y) : (c0.y)), ((-(r1.w)) >= 0.0f ? (r2.z) : (c0.y)));
    r1.w = abs(c[29].w);
    r1.w = frac(r1.w);
    r3.y = ((c[29].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r3.x = c0.y;
    r3 = tex2D(s3, r3.xy);
    r1.w = saturate((r3.x) * (c[35].x));
    r3.xyz = lerp(r2.xyz, r1.xyz, r1.www);
    r0.w = (r0.w) * (c[38].x);
    r1.x = frac(abs(r0.w));
    r0.w = ((r0.w) >= 0.0f ? (r1.x) : (-(r1.x)));
    r1.x = (c[42].x) + (-(v3.y));
    r1.x = (r1.x) + (c0.x);
    r0.w = (r0.w) + (r1.x);
    r1.x = frac(abs(r0.w));
    r0.w = ((r0.w) >= 0.0f ? (r1.x) : (-(r1.x)));
    r1.x = pow(abs(r0.w), c[41].x);
    r2.x = c0.x;
    r0.w = (r1.x) * (-(c[40].x)) + (r2.x);
    r1.xyz = lerp(c[39].xyz, r3.xyz, r0.www);
    r0.xyz = (r0.xyz) + (r1.xyz);
    r0.w = c0.x;
    r1.x = dot(r0, c[31]);
    r1.y = dot(r0, c[32]);
    r1.z = dot(r0, c[33]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[30].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c0.x;

    return oC0;
}
