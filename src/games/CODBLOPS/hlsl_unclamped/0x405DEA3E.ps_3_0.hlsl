// Mechanically reconstructed from 0x405DEA3E.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);

struct PS_INPUT
{
    float4 v0 : TEXCOORD1;
    float4 v1 : TEXCOORD2;
    float4 v2 : TEXCOORD4;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    const float4 c0 = float4(1.0f, 0.0f, 0.5f, 9.99999997e-07f);
    const float4 c1 = float4(-28.0f, 30.0f, 15.0f, 0.0399999991f);
    const float4 c2 = float4(0.38647899f, 0.392856985f, 0.364796013f, 0.782500029f);
    const float4 c3 = float4(0.00749999983f, 1.00750005f, 0.0f, 0.0f);
    const float4 c4 = float4(81.2394867f, 17.3480244f, 37.3498383f, 59.3948402f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = c4;
    r0.x = dot(c[27].wwww, r0);
    r0.x = frac(r0.x);
    r0.w = c[27].w;
    r1.x = dot(r0.xwww, c4);
    r0.y = frac(r1.x);
    r1.x = dot(r0.xyww, c4);
    r0.z = frac(r1.x);
    r1.x = dot(r0, c4);
    r0.w = frac(r1.x);
    r1.yzw = r0.yzw;
    r0.x = dot(r0, c4);
    r1.x = frac(r0.x);
    r0.x = dot(r1, c4);
    r1.y = frac(r0.x);
    r0.x = 1.0f / (c[34].x);
    r0.y = 1.0f / (c[34].y);
    r0.xy = (v2.xy) * (r0.xy) + (r1.xy);
    r0 = tex2D(s2, r0.xy);
    r0.w = c[27].w;
    r1.x = (r0.w) * (c1.z);
    r1.x = frac(r1.x);
    r1.x = (r0.w) * (c1.z) + (-(r1.x));
    r1.y = (r1.x) * (c1.w) + (v2.y);
    r1.x = c2.w;
    r1 = tex2D(s3, r1.xy);
    r1.x = (r1.x) * (-(c3.x)) + (v2.x);
    r1.x = (r1.x) * (c3.y) + (-(v2.x));
    r1.x = (c[35].x) * (r1.x) + (v2.x);
    r1.z = (-(r1.x)) + (c0.x);
    r1.zw = float2(((r1.z) >= 0.0f ? (c0.y) : (c0.x)), ((r1.x) >= 0.0f ? (c0.y) : (c0.x)));
    r1.x = saturate(r1.x);
    r1.z = (r1.z) + (r1.w);
    r1.w = frac(abs(v2.y));
    r1.y = ((v2.y) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2 = tex2D(s4, r1.xy);
    r1.xyw = (r2.xyz) * (r2.xyz);
    r1.xyz = float3(((-(r1.z)) >= 0.0f ? (r1.x) : (c0.y)), ((-(r1.z)) >= 0.0f ? (r1.y) : (c0.y)), ((-(r1.z)) >= 0.0f ? (r1.w) : (c0.y)));
    r1.w = abs(c[27].w);
    r1.w = frac(r1.w);
    r2.y = ((c[27].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2.x = c0.y;
    r2 = tex2D(s3, r2.xy);
    r1.w = saturate((r2.x) * (c[33].x));
    r2.xyz = lerp(r1.xyz, r0.xyz, r1.www);
    r0.x = (r0.w) * (c[36].x);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r0.y = (c[40].x) + (-(v2.y));
    r0.y = (r0.y) + (c0.x);
    r0.x = (r0.x) + (r0.y);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r1.x = pow(abs(r0.x), c[39].x);
    r0.x = c0.x;
    r0.x = (r1.x) * (-(c[38].x)) + (r0.x);
    r1.xyz = lerp(c[37].xyz, r2.xyz, r0.xxx);
    r0.xyz = (c0.xxx) + (-(v0.xyz));
    r2.xyz = normalize(r0.xyz);
    r0.xyz = normalize(v1.xyz);
    r0.w = dot(-(r2.xyz), r0.xyz);
    r0.w = (r0.w) + (r0.w);
    r3.xyz = (r0.xyz) * (-(r0.www)) + (-(r2.xyz));
    r4.xyz = normalize(v0.xyz);
    r0.w = saturate(dot(r3.xyz, -(r4.xyz)));
    r1.w = dot(r2.xyz, r4.xyz);
    r1.w = saturate((r1.w) * (c0.z) + (c0.z));
    r2.x = (r1.w) * (c1.x) + (c1.y);
    r1.w = (r1.w) + (c0.x);
    r3.x = pow(abs(r0.w), r2.x);
    r2.xyz = c[5].xyz;
    r2.xyz = (r2.xyz) * (c[6].yyy);
    r2.xyz = (r3.xxx) * (r2.xyz);
    r2.xyz = (r1.www) * (r2.xyz);
    r0.y = dot(r4.xyz, r0.xyz);
    r0.y = (r0.y) + (r0.y);
    r0.x = (r0.x) * (-(r0.y)) + (r4.x);
    r0.x = (r0.x) * (c[32].x);
    r0.x = (r0.x) * (c0.w);
    r0.xyz = (r2.xyz) * (c2.xyz) + (r0.xxx);
    r0.xyz = (r1.xyz) + (r0.xyz);
    r1 = (v0.xyzx) * (c0.xxxy) + (c0.yyyx);
    r0.w = dot(r1, c[22]);
    r0.w = 1.0f / (r0.w);
    r2.x = dot(r1, c[11]);
    r2.y = dot(r1, c[20]);
    r1.z = dot(r1, c[21]);
    r2 = (r0.wwww) * (r2.xxyy);
    r3 = (r2) * (c[23].zwxy);
    r4.x = log2(abs(r3.x));
    r4.y = log2(abs(r3.y));
    r4.z = log2(abs(r3.z));
    r4.w = log2(abs(r3.w));
    r3 = (r4) * (c[24].xxxx);
    r2.x = exp2(r3.x);
    r2.z = exp2(r3.y);
    r3.x = exp2(r3.z);
    r3.y = exp2(r3.w);
    r2.xz = (r2.xz) + (r3.xy);
    r3.x = log2(abs(r2.x));
    r3.y = log2(abs(r2.z));
    r2.xz = (r3.xy) * (c[24].yy);
    r0.w = exp2(r2.x);
    r2.x = exp2(r2.z);
    r0.w = (r0.w) * (c[25].x);
    r0.w = (r2.x) * (c[25].y) + (-(r0.w));
    r3.y = c[25].y;
    r2.x = (r2.x) * (r3.y) + (-(c[24].z));
    r0.w = 1.0f / (r0.w);
    r3.w = saturate((r2.x) * (r0.w));
    r1.xy = abs(r2.yw);
    r4 = c[9];
    r4 = saturate((r1.zyxz) * (r4) + (c[10]));
    r3.x = (r4.w) * (r4.x);
    r3.yz = r4.yz;
    r4 = (r3) * (r3);
    r3 = (c[25].zzzz) * (r3) + (c[25].wwww);
    r3 = (r4) * (r3);
    r0.w = (r3.z) * (r3.y);
    r1.x = abs(c[24].w);
    r0.w = ((-(r1.x)) >= 0.0f ? (r0.w) : (r3.w));
    r1.w = (r1.z) * (r1.z);
    r1.x = dot(c[8].yz, r1.zw) + (c[8].x);
    r0.w = (r0.w) * (r1.x);
    r0.w = (r3.x) * (r0.w);
    r1 = c[26];
    r1.x = dot(r2.yw, r1.xy) + (c[7].x);
    r1.y = dot(r2.yw, r1.zw) + (c[7].z);
    r1 = tex2D(s1, r1.xy);
    r1.x = (r1.x) * (r1.x);
    r0.w = (r0.w) * (r1.x);
    r0.xyz = (r0.xyz) * (r0.www);
    r0.w = c0.x;
    r1.x = dot(r0, c[29]);
    r1.y = dot(r0, c[30]);
    r1.z = dot(r0, c[31]);
    r0.xyz = max(((r1.xyz) * (c[28].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c0.x;

    return oC0;
}
