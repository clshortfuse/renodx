// Mechanically reconstructed from 0x17824790.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD8;
    float4 v3 : TEXCOORD5;
    float4 v4 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(8.0f, 1.0f, 0.797884583f, 0.5f);
    const float4 c1 = float4(1.0f, 0.5f, 0.0f, 31.875f);
    const float4 c2 = float4(4.0f, -2.0f, 0.600000024f, 0.400000006f);
    const float4 c3 = float4(0.0009765625f, 0.125f, 0.25f, 0.0f);
    const float4 c4 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 r12 = 0.0f;
    float4 oC0 = 0.0f;

    r1 = (v3.xyzx) * (c1.xxxz) + (c1.zzzx);
    r0.w = dot(r1, c[23]);
    r0.w = 1.0f / (r0.w);
    r0.x = dot(r1, c[20]);
    r0.y = dot(r1, c[21]);
    r0 = (r0.wwww) * (r0.xxyy);
    r2 = (r0) * (c[24].zwxy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.z = log2(abs(r2.z));
    r2.w = log2(abs(r2.w));
    r2 = (r2) * (c[25].xxxx);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = exp2(r2.z);
    r2.w = exp2(r2.w);
    r2.xy = (r2.zw) + (r2.xy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.xy = (r2.xy) * (c[25].yy);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = dot(r1, c[22]);
    r0.z = (r2.x) * (c[26].x);
    r0.y = (r2.y) * (c[26].y) + (-(r0.z));
    r0.z = c[26].y;
    r0.z = (r2.y) * (r0.z) + (-(c[25].z));
    r0.y = 1.0f / (r0.y);
    r2.xy = abs(r0.xw);
    r3.w = saturate((r0.z) * (r0.y));
    r1 = c[10];
    r1 = saturate((r2.zyxz) * (r1) + (c[11]));
    r3.x = (r1.w) * (r1.x);
    r3.yz = r1.yz;
    r1 = (r3) * (r3);
    r3 = (c[26].zzzz) * (r3) + (c[26].wwww);
    r1 = (r1) * (r3);
    r0.y = (r1.z) * (r1.y);
    r0.z = abs(c[25].w);
    r2.w = (r2.z) * (r2.z);
    r0.y = ((-(r0.z)) >= 0.0f ? (r0.y) : (r1.w));
    r0.z = dot(c[9].yz, r2.zw) + (c[9].x);
    r0.z = (r0.y) * (r0.z);
    r2.w = (r1.x) * (r0.z);
    r1 = c[27];
    r1.x = dot(r0.xw, r1.xy) + (c[8].x);
    r1.y = dot(r0.xw, r1.zw) + (c[8].z);
    r0 = tex2D(s1, r1.xy);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r3.xyz = (r2.www) * (r0.xyz);
    r0 = tex2D(s12, v0.zw);
    r1.xyz = (-(v3.xyz)) + (c[5].xyz);
    r0.w = dot(r1.xyz, r1.xyz);
    r4.w = rsqrt(r0.w);
    r2.xyz = (r1.xyz) * (r4.www);
    r10.xyz = normalize(v1.xyz);
    r6.xyz = (r3.xyz) * (r0.yyy);
    r1.w = saturate(dot(r10.xyz, r2.xyz));
    r0 = tex2D(s3, v0.xy);
    r3.y = (r0.w) * (c0.z);
    r11.xyz = normalize(v3.xyz);
    r3.z = (r0.w) * (-(c0.z)) + (c0.y);
    r2.w = saturate(dot(r10.xyz, -(r11.xyz)));
    r3.w = (r1.w) * (r3.z) + (r3.y);
    r3.z = (r2.w) * (r3.z) + (r3.y);
    r3.w = (r3.w) * (r3.z) + (c3.x);
    r3.xyz = (r1.xyz) * (r4.www) + (-(r11.xyz));
    r3.w = 1.0f / (r3.w);
    r1.xyz = normalize(r3.xyz);
    r3.w = (r1.w) * (r3.w);
    r3.z = saturate(dot(r10.xyz, r1.xyz));
    r2.z = saturate(dot(r1.xyz, r2.xyz));
    r1.xy = (r0.ww) * (c4.xy) + (c4.zw);
    r1.z = exp2(r1.y);
    r1.y = (-(r2.z)) + (c0.y);
    r2.y = pow(abs(r3.z), r1.z);
    r2.z = (r1.y) * (r1.y);
    r1.z = (r1.z) * (c3.y) + (c3.z);
    r2.z = (r2.z) * (r2.z);
    r1.z = (r2.y) * (r1.z);
    r1.y = (r1.y) * (r2.z);
    r9.xyz = (r0.xyz) * (r0.xyz);
    r8.xyz = (r0.xyz) * (-(r0.xyz)) + (c0.yyy);
    r1.z = (r3.w) * (r1.z);
    r0.xyz = (r8.xyz) * (r1.yyy) + (r9.xyz);
    r6.w = (-(r2.w)) + (c0.y);
    r0.xyz = (r1.zzz) * (r0.xyz);
    r7.xyz = (r1.www) * (c[6].xyz);
    r0.xyz = (r0.xyz) * (c[28].www);
    r5.w = 1.0f / (r1.x);
    r0.xyz = (r0.xyz) * (c[7].xyz);
    r0.w = (r0.w) * (c0.x);
    r5.xyz = (r6.xyz) * (r0.xyz);
    r4 = tex2D(s0, v0.xy);
    r2 = tex2D(s2, v4.zw);
    r0.xy = (v0.zw) * (c0.yw);
    r1 = tex2D(s13, r0.xy);
    r0.xy = (v0.zw) * (c1.xy) + (c1.zy);
    r3 = tex2D(s13, r0.xy);
    r1.w = r3.y;
    r0.xyz = (r2.xyz) * (v4.yyy);
    r2.xy = (r1.yw) * (c2.xx) + (c2.yy);
    r0.xyz = (r0.xyz) * (r2.www) + (r4.xyz);
    r1.w = dot(r2.xy, r2.xy) + (c1.z);
    r4.xyz = (r0.xyz) * (r0.xyz);
    r0.z = exp2(-(r1.w));
    r2 = tex2D(s14, v0.zw);
    r12.xy = (r2.xy) * (c1.ww);
    r2.w = saturate((r0.z) * (c2.z) + (c2.w));
    r0.xy = (r3.xz) * (r12.yy);
    r1.w = (r2.y) * (c1.w) + (-(r0.x));
    r0.xz = (r0.xy) * (c2.xx);
    r0.y = (r3.z) * (-(r12.y)) + (r1.w);
    r1.xy = (r1.xz) * (r12.xx);
    r0.y = (r0.y) + (r0.y);
    r1.w = (r2.x) * (c1.w) + (-(r1.x));
    r2.xz = (r1.xy) * (c2.xx);
    r1.w = (r1.z) * (-(r12.x)) + (r1.w);
    r2.y = (r1.w) + (r1.w);
    r1.w = dot(r11.xyz, r10.xyz);
    r0.xyz = (r0.xyz) * (r2.www) + (r2.xyz);
    r1.w = (r1.w) + (r1.w);
    r1.xyz = (r0.xyz) * (c[28].yyy);
    r0.xyz = (r10.xyz) * (-(r1.www)) + (r11.xyz);
    r0 = texCUBElod(s15, r0);
    r0.w = (r6.w) * (r6.w);
    r0.w = (r6.w) * (r0.w);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r0.w = (r5.w) * (r0.w);
    r0.xyz = (r0.xyz) * (c[28].xxx);
    r3.xyz = (r8.xyz) * (r0.www) + (r9.xyz);
    r1.xyz = (r6.xyz) * (r7.xyz) + (r1.xyz);
    r0.xyz = (r0.xyz) * (r3.xyz);
    r1.xyz = (r4.xyz) * (r1.xyz) + (r5.xyz);
    r0.xyz = (r2.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c0.xxx) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v2.xyz));
    r0.w = v1.w;
    r0.xyz = (r0.www) * (r0.xyz) + (v2.xyz);
    r0.xyz = max(((r0.xyz) * (c[30].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c[29].w;

    return oC0;
}
