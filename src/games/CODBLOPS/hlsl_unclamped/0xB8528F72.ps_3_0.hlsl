// Mechanically reconstructed from 0xB8528F72.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
    float4 v4 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(0.5f, -28.0f, 30.0f, 1.0f);
    const float4 c1 = float4(8.0f, 31.875f, 4.0f, 2.0f);
    const float4 c2 = float4(-2.0f, 3.0f, 0.0f, 0.25f);
    const float4 c3 = float4(0.38647899f, 0.392856985f, 0.364796013f, 15.0f);
    const float4 c4 = float4(0.0399999991f, 0.782500029f, 0.00749999983f, 1.00750005f);
    const float4 c12 = float4(81.2394867f, 17.3480244f, 37.3498383f, 59.3948402f);
    const float4 c13 = float4(0.0f, 1.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xy = (c[20].xy) * (v4.ww);
    r0.zw = c2.zz;
    r1 = (r0.xyww) + (v4);
    r0 = (-(r0)) + (v4);
    r0 = tex2Dproj(s1, r0);
    r0.y = r0.x;
    r1 = tex2Dproj(s1, r1);
    r0.x = r1.x;
    r1.xy = (c[20].zw) * (v4.ww);
    r1.zw = c2.zz;
    r2 = (r1.xyww) + (v4);
    r1 = (-(r1)) + (v4);
    r1 = tex2Dproj(s1, r1);
    r0.w = r1.x;
    r1 = tex2Dproj(s1, r2);
    r0.z = r1.x;
    r0.x = dot(r0, c2.wwww);
    r1 = tex2D(s12, v3.zw);
    r0.yzw = (c[5].xyz) + (-(v1.xyz));
    r1.x = dot(r0.yzw, r0.yzw);
    r1.x = rsqrt(r1.x);
    r0.yzw = (r0.yzw) * (r1.xxx);
    r1.x = 1.0f / (r1.x);
    r1.w = dot(r0.yzw, c[8].xyz);
    r1.w = saturate((r1.w) * (c[9].x) + (c[9].y));
    r1.w = (r1.w) * (c[9].w);
    r2.x = lerp(r1.y, r0.x, r1.w);
    r1.z = (r1.x) * (r1.x);
    r0.x = dot(c[10].yz, r1.xz) + (c[10].x);
    r1.xy = saturate((r1.xx) * (c[11].xy) + (c[11].zw));
    r1.zw = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c2.xx) + (c2.yy);
    r1.xy = (r1.zw) * (r1.xy);
    r0.x = (r0.x) * (r1.x);
    r0.x = (r1.y) * (r0.x);
    r0.x = (r2.x) * (r0.x);
    r1.xyz = (r0.xxx) * (c[6].xyz);
    r1.xyz = (r1.xyz) * (c[7].yyy);
    r2.xyz = normalize(v2.xyz);
    r0.x = dot(-(r0.yzw), r2.xyz);
    r0.x = (r0.x) + (r0.x);
    r3.xyz = (r2.xyz) * (-(r0.xxx)) + (-(r0.yzw));
    r4.xyz = normalize(v1.xyz);
    r0.x = saturate(dot(r3.xyz, -(r4.xyz)));
    r0.y = dot(r0.yzw, r4.xyz);
    r0.y = saturate((r0.y) * (c0.x) + (c0.x));
    r0.z = (r0.y) * (c0.y) + (c0.z);
    r0.y = (r0.y) + (c0.w);
    r1.w = pow(abs(r0.x), r0.z);
    r0.xzw = (r1.xyz) * (r1.www);
    r0.xyz = (r0.yyy) * (r0.xzw);
    r0.w = dot(r4.xyz, r2.xyz);
    r0.w = (r0.w) + (r0.w);
    r1.xyz = (r2.xyz) * (-(r0.www)) + (r4.xyz);
    r1 = texCUBE(s15, r1.xyz);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r1.xyz = (r1.xyz) * (c1.xxx);
    r2.xy = (c0.wx) * (v3.zw);
    r2 = tex2D(s13, r2.xy);
    r3 = tex2D(s14, v3.zw);
    r0.w = (r3.x) * (c1.y);
    r2.xy = (r2.xz) * (r0.ww);
    r1.w = (r3.x) * (c1.y) + (-(r2.x));
    r2.xw = (r1.xz) * (r2.xy);
    r0.w = (r2.z) * (-(r0.w)) + (r1.w);
    r2.y = (r1.y) * (r0.w);
    r1.xyz = (r2.xyw) * (c[26].xxx);
    r1.xyz = (r1.xyz) * (c1.zwz);
    r0.xyz = (r0.xyz) * (c3.xyz) + (r1.xyz);
    r1 = c12;
    r0.w = dot(c[21].wwww, r1);
    r1.x = frac(r0.w);
    r1.w = c[21].w;
    r0.w = dot(r1.xwww, c12);
    r1.y = frac(r0.w);
    r0.w = dot(r1.xyww, c12);
    r1.z = frac(r0.w);
    r0.w = dot(r1, c12);
    r1.w = frac(r0.w);
    r2.yzw = r1.yzw;
    r0.w = dot(r1, c12);
    r2.x = frac(r0.w);
    r0.w = dot(r2, c12);
    r2.y = frac(r0.w);
    r1.x = 1.0f / (c[28].x);
    r1.y = 1.0f / (c[28].y);
    r1.xy = (v3.xy) * (r1.xy) + (r2.xy);
    r1 = tex2D(s2, r1.xy);
    r0.w = c[21].w;
    r1.w = (r0.w) * (c3.w);
    r1.w = frac(r1.w);
    r1.w = (r0.w) * (c3.w) + (-(r1.w));
    r2.y = (r1.w) * (c4.x) + (v3.y);
    r2.x = c4.y;
    r2 = tex2D(s3, r2.xy);
    r1.w = (r2.x) * (-(c4.z)) + (v3.x);
    r1.w = (r1.w) * (c4.w) + (-(v3.x));
    r2.x = (c[29].x) * (r1.w) + (v3.x);
    r1.w = (-(r2.x)) + (c0.w);
    r1.w = ((r1.w) >= 0.0f ? (c13.x) : (c13.y));
    r2.z = ((r2.x) >= 0.0f ? (c13.x) : (c13.y));
    r2.x = saturate(r2.x);
    r1.w = (r1.w) + (r2.z);
    r2.z = frac(abs(v3.y));
    r2.y = ((v3.y) >= 0.0f ? (r2.z) : (-(r2.z)));
    r2 = tex2D(s4, r2.xy);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = float3(((-(r1.w)) >= 0.0f ? (r2.x) : (c2.z)), ((-(r1.w)) >= 0.0f ? (r2.y) : (c2.z)), ((-(r1.w)) >= 0.0f ? (r2.z) : (c2.z)));
    r1.w = abs(c[21].w);
    r1.w = frac(r1.w);
    r3.y = ((c[21].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r3.x = c2.z;
    r3 = tex2D(s3, r3.xy);
    r1.w = saturate((r3.x) * (c[27].x));
    r3.xyz = lerp(r2.xyz, r1.xyz, r1.www);
    r0.w = (r0.w) * (c[30].x);
    r1.x = frac(abs(r0.w));
    r0.w = ((r0.w) >= 0.0f ? (r1.x) : (-(r1.x)));
    r1.x = (c[34].x) + (-(v3.y));
    r1.x = (r1.x) + (c0.w);
    r0.w = (r0.w) + (r1.x);
    r1.x = frac(abs(r0.w));
    r0.w = ((r0.w) >= 0.0f ? (r1.x) : (-(r1.x)));
    r1.x = pow(abs(r0.w), c[33].x);
    r0.w = c0.w;
    r0.w = (r1.x) * (-(c[32].x)) + (r0.w);
    r1.xyz = lerp(c[31].xyz, r3.xyz, r0.www);
    r0.xyz = (r0.xyz) + (r1.xyz);
    r0.w = c0.w;
    r1.x = dot(r0, c[23]);
    r1.y = dot(r0, c[24]);
    r1.z = dot(r0, c[25]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[22].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c0.w;

    return oC0;
}
