// Mechanically reconstructed from 0xCCF0A2C0.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler2D s8 : register(s8);
sampler3D s11 : register(s11);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
    float4 v4 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(1.44269502f, 0.100000001f, 8.0f, 31.875f);
    const float4 c1 = float4(0.0f, 0.25f, 1.0f, 0.5f);
    const float4 c2 = float4(-2.0f, 3.0f, 9.99999975e-06f, 1e-15f);
    const float4 c3 = float4(1.16412354f, 1.59579468f, -0.87065506f, 0.0f);
    const float4 c4 = float4(1.16412354f, -0.813476562f, -0.391448975f, 0.529705048f);
    const float4 c12 = float4(1.16412354f, 2.01782227f, -1.08166885f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 oC0 = 0.0f;

    r0.zw = c1.xx;
    r1 = (c[33]) * (v1.yyyy);
    r1 = (v1.xxxx) * (c[32]) + (r1);
    r1 = (v1.zzzz) * (c[34]) + (r1);
    r1 = (r1) + (c[35]);
    r0.xy = (r1.ww) * (c[37].xy);
    r2 = (r0.xyww) + (r1);
    r0 = (-(r0)) + (r1);
    r0 = tex2Dproj(s1, r0);
    r0.y = r0.x;
    r2 = tex2Dproj(s1, r2);
    r0.x = r2.x;
    r2.xy = (r1.ww) * (c[37].zw);
    r2.zw = c1.xx;
    r3 = (r1) + (r2.xyww);
    r1 = (r1) + (-(r2));
    r1 = tex2Dproj(s1, r1);
    r0.w = r1.x;
    r1 = tex2Dproj(s1, r3);
    r0.z = r1.x;
    r0.x = dot(r0, c1.yyyy);
    r1 = (v1.xyzx) * (c1.zzzx) + (c1.xxxz);
    r0.y = dot(r1, c[29]);
    r0.y = 1.0f / (r0.y);
    r0.z = dot(r1, c[26]);
    r0.w = dot(r1, c[27]);
    r1.x = dot(r1, c[28]);
    r0.yz = (r0.yy) * (r0.zw);
    r0.yz = (r0.yz) * (c1.ww) + (c1.ww);
    r2 = tex2D(s2, r0.yz);
    r0.y = (r2.x) * (r2.x);
    r1.y = (r1.x) * (r1.x);
    r0.z = dot(c[24].yz, r1.xy) + (c[24].x);
    r1.xy = saturate((r1.xx) * (c[25].xy) + (c[25].zw));
    r0.w = saturate(1.0f / (r0.z));
    r0.z = ((-abs(r0.z)) >= 0.0f ? (c1.x) : (r0.w));
    r1.zw = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c2.xx) + (c2.yy);
    r0.w = (r1.z) * (r1.x);
    r1.x = (r1.w) * (-(r1.y)) + (c1.z);
    r0.z = (r0.z) * (r0.w);
    r0.z = (r1.x) * (r0.z);
    r1.xyz = (c[21].xyz) + (-(v1.xyz));
    r2.xyz = normalize(r1.xyz);
    r0.w = dot(r2.xyz, c[30].xyz);
    r0.w = saturate((r0.w) * (c[31].x) + (c[31].y));
    r1.x = (r0.w) * (r0.w);
    r0.w = (r0.w) * (c2.x) + (c2.y);
    r0.w = (r1.x) * (r0.w);
    r0.z = (r0.z) * (r0.w);
    r0.y = (r0.y) * (r0.z);
    r0.x = (r0.x) * (r0.y);
    r0.yzw = (r0.xxx) * (c[23].xyz);
    r1.xyz = (r0.xxx) * (c[22].xyz);
    r0.x = dot(-(v1.xyz), -(v1.xyz));
    r0.x = rsqrt(r0.x);
    r3.xyz = (-(v1.xyz)) * (r0.xxx) + (r2.xyz);
    r4.xyz = normalize(r3.xyz);
    r3.xyz = normalize(v2.xyz);
    r1.w = saturate(dot(r3.xyz, r4.xyz));
    r4.xyz = (r0.xxx) * (-(v1.xyz));
    r2.w = saturate(dot(r3.xyz, r4.xyz));
    r3.w = 1.0f / (r2.w);
    r4.x = dot(r3.ww, r1.ww) + (c1.x);
    r1.w = (r1.w) * (r1.w) + (c2.w);
    r1.w = 1.0f / (r1.w);
    r2.x = saturate(dot(r3.xyz, r2.xyz));
    r4.y = min(r2.w, r2.x);
    r2.y = saturate((r4.x) * (r4.y));
    r2.y = (r2.x) * (r2.y);
    r1.xyz = (r1.xyz) * (r2.xxx);
    r2.x = rsqrt(r2.y);
    r2.x = 1.0f / (r2.x);
    r4 = tex2D(s8, v3.xy);
    r2.y = (r4.x) * (r4.x);
    r4.x = max(c2.z, r2.y);
    r2.y = (r4.x) * (r4.x);
    r2.y = 1.0f / (r2.y);
    r2.z = (-(r1.w)) + (c1.z);
    r1.w = (r1.w) * (r1.w);
    r2.z = (r2.y) * (r2.z);
    r2.z = (r2.z) * (c0.x);
    r2.z = exp2(r2.z);
    r1.w = (r1.w) * (r2.z);
    r1.w = (r2.x) * (r1.w);
    r0.yzw = (r0.yzw) * (r1.www);
    r4.xyz = normalize(c[17].xyz);
    r5.xyz = (-(v1.xyz)) * (r0.xxx) + (r4.xyz);
    r0.x = saturate(dot(r3.xyz, r4.xyz));
    r4.xyz = normalize(r5.xyz);
    r1.w = saturate(dot(r3.xyz, r4.xyz));
    r2.x = (r1.w) + (r1.w);
    r1.w = (r1.w) * (r1.w) + (c2.w);
    r1.w = 1.0f / (r1.w);
    r2.x = (r3.w) * (r2.x);
    r3.w = min(r2.w, r0.x);
    r4.x = max(c0.y, r2.w);
    r2.z = 1.0f / (r4.x);
    r2.z = rsqrt(r2.z);
    r2.z = 1.0f / (r2.z);
    r2.x = saturate((r2.x) * (r3.w));
    r2.x = (r0.x) * (r2.x);
    r2.x = rsqrt(r2.x);
    r2.x = 1.0f / (r2.x);
    r2.w = (-(r1.w)) + (c1.z);
    r1.w = (r1.w) * (r1.w);
    r2.w = (r2.y) * (r2.w);
    r2.w = (r2.w) * (c0.x);
    r2.w = exp2(r2.w);
    r1.w = (r1.w) * (r2.w);
    r1.w = (r2.x) * (r1.w);
    r2.x = max(abs(r3.y), abs(r3.z));
    r4.x = max(abs(r3.x), r2.x);
    r2.x = 1.0f / (r4.x);
    r4.xyz = (r3.xyz) * (c[5].xyz);
    r4.xyz = (r4.xyz) * (r2.xxx) + (v4.xyz);
    r4 = tex3D(s11, r4.xyz);
    r5.xyz = (r4.www) * (c[19].xyz);
    r5.xyz = (r5.xyz) * (c[36].yyy);
    r0.yzw = (r1.www) * (r5.xyz) + (r0.yzw);
    r0.yzw = (r2.zzz) * (r0.yzw);
    r0.yzw = (r2.yyy) * (r0.yzw);
    r0.yzw = (r0.yzw) * (c1.yyy);
    r2 = tex2D(s6, v3.xy);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r0.yzw = (r0.yzw) * (r2.xyz);
    r2.xy = lerp(c[39].xy, c[39].zw, v3.xy);
    r5 = tex2D(s4, r2.xy);
    r5.y = r5.x;
    r6 = tex2D(s5, r2.xy);
    r2 = tex2D(s3, r2.xy);
    r5.xw = (r2.xx) * (c1.zx) + (c1.xz);
    r5.z = r6.x;
    r2.y = dot(c4, r5);
    r2.x = dot(c3.xyz, r5.xyw);
    r2.z = dot(c12.xyz, r5.xzw);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = (r2.xyz) * (c[41].xxx);
    r5 = tex2D(s7, v3.xy);
    r6 = (r5) * (r5);
    r5.w = (r5.w) * (-(r5.w)) + (c[42].x);
    r5.xyz = (r2.xyz) * (c0.zzz) + (-(r6.xyz));
    r2 = (c[38].xxxx) * (r5) + (r6);
    r0.yzw = (r0.yzw) * (r2.www);
    r5.xyz = (r4.www) * (c[18].xyz);
    r4.xyz = (r4.xyz) * (r4.xyz);
    r5.xyz = (r5.xyz) * (c[36].xxx);
    r1.xyz = (r0.xxx) * (r5.xyz) + (r1.xyz);
    r1.xyz = (r4.xyz) * (c0.www) + (r1.xyz);
    r2.xyz = (r1.xyz) * (r2.xyz) + (r0.yzw);
    r0 = max(r2, c1.xxxx);
    r1 = (c[7]) + (-(v1.yyyy));
    r2 = (r1) * (r1);
    r4 = (c[6]) + (-(v1.xxxx));
    r2 = (r4) * (r4) + (r2);
    r5 = (c[8]) + (-(v1.zzzz));
    r2 = (r5) * (r5) + (r2);
    r6.x = rsqrt(r2.x);
    r6.y = rsqrt(r2.y);
    r6.z = rsqrt(r2.z);
    r6.w = rsqrt(r2.w);
    r7.z = c1.z;
    r2 = saturate((r2) * (c[9]) + (r7.zzzz));
    r1 = (r1) * (r6);
    r1 = (r3.yyyy) * (r1);
    r4 = (r4) * (r6);
    r5 = (r5) * (r6);
    r1 = (r4) * (r3.xxxx) + (r1);
    r1 = saturate((r5) * (r3.zzzz) + (r1));
    r1 = (r2) * (r1);
    r2.x = dot(c[10], r1);
    r2.y = dot(c[11], r1);
    r2.z = dot(c[20], r1);
    r0.xyz = (r0.xyz) * (r2.xyz) + (r0.xyz);
    oC0.w = r0.w;
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[40].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);

    return oC0;
}
