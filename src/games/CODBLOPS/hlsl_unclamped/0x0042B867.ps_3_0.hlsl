// Mechanically reconstructed from 0x0042B867.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD8;
    float4 v3 : TEXCOORD2;
    float4 v4 : TEXCOORD3;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD6;
    float4 v7 : COLOR1;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(2.0f, -1.0f, 0.200000003f, 0.0f);
    const float4 c2 = float4(-0.5f, 0.600000024f, 0.400000006f, 8.0f);
    const float4 c3 = float4(31.875f, 4.0f, -2.0f, 0.0009765625f);
    const float4 c4 = float4(0.797884583f, 1.0f, 0.5f, 0.0f);
    const float4 c8 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    const float4 c9 = float4(0.125f, 0.25f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xy = (v0.zw) * (c4.yz);
    r0 = tex2D(s13, r0.xy);
    r1.xy = (v0.zw) * (c4.yz) + (c4.wz);
    r2 = tex2D(s13, r1.xy);
    r0.w = r2.y;
    r1 = tex2D(s1, v0.xy);
    r5.xy = (r1.wy) * (c0.xy) + (c0.zw);
    r1 = tex2D(s3, v6.zw);
    r3.xy = (r1.wy) * (c0.xy) + (c0.zw);
    r1 = (c1.xxxx) * (v7) + (c1.yyyy);
    r4.x = dot(r3.xy, r1.xy) + (c1.w);
    r4.y = dot(r3.xy, r1.zw) + (c1.w);
    r3 = tex2D(s2, v6.zw);
    r7.w = (r3.w) * (v6.y) + (c2.x);
    r1.xy = (r0.yw) * (c3.yy) + (c3.zz);
    r5.xy = float2(((r7.w) >= 0.0f ? (r4.x) : (r5.x)), ((r7.w) >= 0.0f ? (r4.y) : (r5.y)));
    r0.y = dot(r1.xy, r1.xy) + (c1.w);
    r0.w = dot(r5.xy, r5.xy) + (c1.w);
    r0.y = exp2(-(r0.y));
    r0.w = exp2(-(r0.w));
    r0.y = (r0.y) * (c2.y) + (c2.z);
    r0.w = (r0.w) * (c2.y) + (c2.z);
    r2.w = (r0.y) * (r0.w);
    r0.y = dot(r1.xy, r5.xy) + (c1.w);
    r1 = tex2D(s14, v0.zw);
    r6.xy = (r1.xy) * (c3.xx);
    r1.z = saturate((r0.y) * (r2.w) + (r2.w));
    r2.xy = (r2.xz) * (r6.yy);
    r0.y = (r1.y) * (c3.x) + (-(r2.x));
    r4.xz = (r2.xy) * (c3.yy);
    r1.w = (r2.z) * (-(r6.y)) + (r0.y);
    r0.xy = (r0.xz) * (r6.xx);
    r4.y = (r1.w) + (r1.w);
    r1.w = (r1.x) * (c3.x) + (-(r0.x));
    r1.xyz = (r1.zzz) * (r4.xyz);
    r0.z = (r0.z) * (-(r6.x)) + (r1.w);
    r7.xz = (r0.xy) * (c3.yy);
    r7.y = (r0.z) + (r0.z);
    r1.xyz = (r7.xyz) * (r0.www) + (r1.xyz);
    r6 = tex2D(s4, v0.xy);
    r0 = v1;
    r0.xyz = (r5.xxx) * (v4.xyz) + (r0.xyz);
    r2.xyz = (r5.yyy) * (v3.xyz) + (r0.xyz);
    r0.xyz = normalize(r2.xyz);
    r9.xyz = float3(((r7.w) >= 0.0f ? (c[5].x) : (r6.y)), ((r7.w) >= 0.0f ? (c[5].y) : (r6.y)), ((r7.w) >= 0.0f ? (c[5].w) : (r6.y)));
    r3.w = saturate(dot(r0.xyz, c[17].xyz));
    r4.xyz = (r1.xyz) * (r9.yyy);
    r2.xyz = (r3.www) * (c[18].xyz);
    r1 = tex2D(s12, v0.zw);
    r8.xyz = (r1.yyy) * (r2.xyz) + (r4.xyz);
    r2 = tex2D(s5, v6.zw);
    r4.w = r6.w;
    r5 = tex2D(s0, v0.xy);
    r4.xyz = lerp(r5.xyz, c1.zzz, r6.xxx);
    r5.xyz = (r6.xxx) * (r5.xyz);
    r2 = float4(((r7.w) >= 0.0f ? (r2.x) : (r4.x)), ((r7.w) >= 0.0f ? (r2.y) : (r4.y)), ((r7.w) >= 0.0f ? (r2.z) : (r4.z)), ((r7.w) >= 0.0f ? (r2.w) : (r4.w)));
    r1.w = dot(v5.xyz, v5.xyz);
    r4.xyz = float3(((r7.w) >= 0.0f ? (r3.x) : (r5.x)), ((r7.w) >= 0.0f ? (r3.y) : (r5.y)), ((r7.w) >= 0.0f ? (r3.z) : (r5.z)));
    r3.z = rsqrt(r1.w);
    r3.y = (r2.w) * (c4.x);
    r6.xyz = (r3.zzz) * (v5.xyz);
    r1.x = (r2.w) * (-(c4.x)) + (c4.y);
    r1.z = saturate(dot(r0.xyz, -(r6.xyz)));
    r1.w = (r3.w) * (r1.x) + (r3.y);
    r1.x = (r1.z) * (r1.x) + (r3.y);
    r5.xyz = (v5.xyz) * (-(r3.zzz)) + (c[17].xyz);
    r1.w = (r1.w) * (r1.x) + (c3.w);
    r4.w = (-(r1.z)) + (-(c1.y));
    r1.w = 1.0f / (r1.w);
    r1.x = (r3.w) * (r1.w);
    r3.xyz = normalize(r5.xyz);
    r5.w = saturate(dot(r0.xyz, r3.xyz));
    r5.xy = (r2.ww) * (c8.xy) + (c8.zw);
    r1.w = saturate(dot(r3.xyz, c[17].xyz));
    r1.z = exp2(r5.y);
    r3.w = 1.0f / (r5.x);
    r1.w = (-(r1.w)) + (-(c1.y));
    r3.y = pow(abs(r5.w), r1.z);
    r3.z = (r1.w) * (r1.w);
    r1.z = (r1.z) * (c9.x) + (c9.y);
    r3.z = (r3.z) * (r3.z);
    r1.z = (r3.y) * (r1.z);
    r1.w = (r1.w) * (r3.z);
    r5.xyz = (r2.xyz) * (r2.xyz);
    r3.xyz = (r2.xyz) * (-(r2.xyz)) + (-(c1.yyy));
    r1.z = (r1.x) * (r1.z);
    r2.xyz = (r3.xyz) * (r1.www) + (r5.xyz);
    r1.w = (r2.w) * (c2.w);
    r2.xyz = (r1.zzz) * (r2.xyz);
    r2.xyz = (r9.zzz) * (r2.xyz);
    r1.z = dot(r6.xyz, r0.xyz);
    r2.xyz = (r2.xyz) * (c[19].xyz);
    r1.z = (r1.z) + (r1.z);
    r2.xyz = (r1.yyy) * (r2.xyz);
    r1.xyz = (r0.xyz) * (-(r1.zzz)) + (r6.xyz);
    r1 = texCUBElod(s15, r1);
    r0.z = (r4.w) * (r4.w);
    r1.w = (r4.w) * (r0.z);
    r0.xyz = (r1.xyz) * (r1.xyz);
    r1.w = (r3.w) * (r1.w);
    r0.xyz = (r9.xxx) * (r0.xyz);
    r3.xyz = (r3.xyz) * (r1.www) + (r5.xyz);
    r1.xyz = (r4.xyz) * (r4.xyz);
    r0.xyz = (r0.xyz) * (r3.xyz);
    r1.xyz = (r1.xyz) * (r8.xyz) + (r2.xyz);
    r0.xyz = (r7.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c2.www) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v2.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v2.xyz);
    r0.xyz = max(((r0.xyz) * (c[7].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c[6].w;

    return oC0;
}
