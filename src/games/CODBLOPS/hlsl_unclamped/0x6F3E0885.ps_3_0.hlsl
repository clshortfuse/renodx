// Mechanically reconstructed from 0x6F3E0885.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD8;
    float4 v3 : TEXCOORD5;
    float4 v4 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(-1.0f, 1.0f, 0.200000003f, 8.0f);
    const float4 c1 = float4(31.875f, 4.0f, -2.0f, 0.0009765625f);
    const float4 c2 = float4(0.600000024f, 0.400000006f, 0.125f, 0.25f);
    const float4 c3 = float4(0.797884583f, 1.0f, 0.5f, 0.0f);
    const float4 c4 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    const float4 c12 = float4(-2.0f, 3.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 r12 = 0.0f;
    float4 r13 = 0.0f;
    float4 oC0 = 0.0f;

    r2.xyz = (-(v3.xyz)) + (c[20].xyz);
    r3.y = dot(r2.xyz, r2.xyz);
    r0.w = rsqrt(r3.y);
    r3.x = 1.0f / (r0.w);
    r1.xy = saturate((r3.xx) * (c[24].xy) + (c[24].zw));
    r0.xy = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c12.xx) + (c12.yy);
    r0.z = dot(c[23].yz, r3.xy) + (c[23].x);
    r0.xy = (r0.xy) * (r1.xy);
    r0.z = (r0.z) * (r0.x);
    r2.w = (r0.y) * (r0.z);
    r1 = tex2D(s12, v0.zw);
    r7.xyz = normalize(v3.xyz);
    r0.xyz = (r2.xyz) * (r0.www) + (-(r7.xyz));
    r4.xyz = (r2.xyz) * (r0.www);
    r2.xyz = normalize(r0.xyz);
    r0.w = saturate(dot(r2.xyz, r4.xyz));
    r1.w = (-(r0.w)) + (c0.y);
    r0.w = (r1.w) * (r1.w);
    r1.z = (r0.w) * (r0.w);
    r0 = tex2D(s1, v4.zw);
    r0.xyz = (r0.xyz) + (c0.xxx);
    r1.x = (r1.w) * (r1.z);
    r13.xyz = (v4.yyy) * (r0.xyz) + (c0.yyy);
    r3.xyz = (r13.xyz) * (c0.zzz);
    r9.xyz = normalize(v1.xyz);
    r11.xyz = (r3.xyz) * (r3.xyz);
    r1.w = saturate(dot(r9.xyz, r4.xyz));
    r0 = tex2D(s2, v0.xy);
    r3.w = (r0.w) * (c3.x);
    r1.z = (r0.w) * (-(c3.x)) + (c3.y);
    r0.z = saturate(dot(r9.xyz, -(r7.xyz)));
    r0.x = (r1.w) * (r1.z) + (r3.w);
    r1.z = (r0.z) * (r1.z) + (r3.w);
    r10.xyz = (r3.xyz) * (-(r3.xyz)) + (c0.yyy);
    r0.x = (r0.x) * (r1.z) + (c1.w);
    r1.z = 1.0f / (r0.x);
    r3.xy = (r0.ww) * (c4.xy) + (c4.zw);
    r3.w = saturate(dot(r9.xyz, r2.xyz));
    r0.x = exp2(r3.y);
    r2.z = pow(abs(r3.w), r0.x);
    r0.x = (r0.x) * (c2.z) + (c2.w);
    r1.z = (r1.w) * (r1.z);
    r0.x = (r2.z) * (r0.x);
    r2.xyz = (r10.xyz) * (r1.xxx) + (r11.xyz);
    r1.z = (r1.z) * (r0.x);
    r0.x = (r2.w) * (r1.y);
    r1.xyz = (r2.xyz) * (r1.zzz);
    r0.z = (-(r0.z)) + (c0.y);
    r1.xyz = (r0.yyy) * (r1.xyz);
    r12.xyz = (r1.www) * (c[21].xyz);
    r1.xyz = (r1.xyz) * (c[22].xyz);
    r5.w = 1.0f / (r3.x);
    r6.xyz = (r0.xxx) * (r1.xyz);
    r1 = tex2D(s0, v0.xy);
    r2.xy = (v0.zw) * (c3.yz);
    r2 = tex2D(s13, r2.xy);
    r3.xy = (v0.zw) * (c3.yz) + (c3.wz);
    r4 = tex2D(s13, r3.xy);
    r2.w = r4.y;
    r3.xy = (r2.yw) * (c1.yy) + (c1.zz);
    r1.w = dot(r3.xy, r3.xy) + (c3.w);
    r3 = tex2D(s14, v0.zw);
    r8.xy = (r3.xy) * (c1.xx);
    r1.w = exp2(-(r1.w));
    r4.xy = (r4.xz) * (r8.yy);
    r1.w = saturate((r1.w) * (c2.x) + (c2.y));
    r2.w = (r3.y) * (c1.x) + (-(r4.x));
    r5.xz = (r4.xy) * (c1.yy);
    r2.xy = (r2.xz) * (r8.xx);
    r3.w = (r4.z) * (-(r8.y)) + (r2.w);
    r2.w = (r3.x) * (c1.x) + (-(r2.x));
    r5.y = (r3.w) + (r3.w);
    r2.w = (r2.z) * (-(r8.x)) + (r2.w);
    r8.xz = (r2.xy) * (c1.yy);
    r8.y = (r2.w) + (r2.w);
    r2.xyz = (r13.xyz) * (r1.xyz);
    r1.xyz = (r5.xyz) * (r1.www) + (r8.xyz);
    r5.xyz = (r2.xyz) * (r2.xyz);
    r1.xyz = (r0.yyy) * (r1.xyz);
    r1.xyz = (r0.xxx) * (r12.xyz) + (r1.xyz);
    r0.x = dot(r7.xyz, r9.xyz);
    r6.xyz = (r5.xyz) * (r1.xyz) + (r6.xyz);
    r0.x = (r0.x) + (r0.x);
    r1.xyz = (r9.xyz) * (-(r0.xxx)) + (r7.xyz);
    r1.w = (r0.w) * (c0.w);
    r1 = texCUBElod(s15, r1);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r7.xyz = (r0.yyy) * (r1.xyz);
    r0.w = (r0.z) * (r0.z);
    r4.w = (r0.z) * (r0.w);
    r3 = (-(v3.yyyy)) + (c[6]);
    r0 = (r3) * (r3);
    r2 = (-(v3.xxxx)) + (c[5]);
    r0 = (r2) * (r2) + (r0);
    r1 = (-(v3.zzzz)) + (c[7]);
    r4.w = (r5.w) * (r4.w);
    r0 = (r1) * (r1) + (r0);
    r10.xyz = (r10.xyz) * (r4.www) + (r11.xyz);
    r4.x = rsqrt(r0.x);
    r4.y = rsqrt(r0.y);
    r4.z = rsqrt(r0.z);
    r4.w = rsqrt(r0.w);
    r7.xyz = (r7.xyz) * (r10.xyz);
    r3 = (r3) * (r4);
    r3 = (r9.yyyy) * (r3);
    r2 = (r2) * (r4);
    r1 = (r1) * (r4);
    r2 = (r2) * (r9.xxxx) + (r3);
    r3.z = c0.y;
    r0 = saturate((r0) * (c[8]) + (r3.zzzz));
    r1 = saturate((r1) * (r9.zzzz) + (r2));
    r2.xyz = (r8.xyz) * (r7.xyz);
    r0 = (r0) * (r1);
    r2.xyz = (r2.xyz) * (c0.www) + (r6.xyz);
    r1.x = dot(c[9], r0);
    r1.y = dot(c[10], r0);
    r1.z = dot(c[11], r0);
    r0.xyz = (r5.xyz) * (r1.xyz) + (r2.xyz);
    r0.xyz = (r0.xyz) + (-(v2.xyz));
    r0.w = v1.w;
    r0.xyz = (r0.www) * (r0.xyz) + (v2.xyz);
    r0.xyz = max(((r0.xyz) * (c[25].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c0.y;

    return oC0;
}
