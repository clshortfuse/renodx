// Mechanically reconstructed from 0x935A392F.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    const float4 c0 = float4(8.0f, 31.875f, 4.0f, 2.0f);
    const float4 c1 = float4(0.0f, 0.25f, 1.0f, 0.5f);
    const float4 c2 = float4(-2.0f, 3.0f, -28.0f, 30.0f);
    const float4 c3 = float4(0.38647899f, 0.392856985f, 0.364796013f, 15.0f);
    const float4 c4 = float4(0.0399999991f, 0.782500029f, 0.00749999983f, 1.00750005f);
    const float4 c12 = float4(81.2394867f, 17.3480244f, 37.3498383f, 59.3948402f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = c12;
    r0.x = dot(c[40].wwww, r0);
    r0.x = frac(r0.x);
    r0.w = c[40].w;
    r1.x = dot(r0.xwww, c12);
    r0.y = frac(r1.x);
    r1.x = dot(r0.xyww, c12);
    r0.z = frac(r1.x);
    r1.x = dot(r0, c12);
    r0.w = frac(r1.x);
    r1.yzw = r0.yzw;
    r0.x = dot(r0, c12);
    r1.x = frac(r0.x);
    r0.x = dot(r1, c12);
    r1.y = frac(r0.x);
    r0.x = 1.0f / (c[47].x);
    r0.y = 1.0f / (c[47].y);
    r0.xy = (v3.xy) * (r0.xy) + (r1.xy);
    r0 = tex2D(s3, r0.xy);
    r0.w = c[40].w;
    r1.x = (r0.w) * (c3.w);
    r1.x = frac(r1.x);
    r1.x = (r0.w) * (c3.w) + (-(r1.x));
    r1.y = (r1.x) * (c4.x) + (v3.y);
    r1.x = c4.y;
    r1 = tex2D(s4, r1.xy);
    r1.x = (r1.x) * (-(c4.z)) + (v3.x);
    r1.x = (r1.x) * (c4.w) + (-(v3.x));
    r1.x = (c[48].x) * (r1.x) + (v3.x);
    r1.z = (-(r1.x)) + (c1.z);
    r1.zw = float2(((r1.z) >= 0.0f ? (c1.x) : (c1.z)), ((r1.x) >= 0.0f ? (c1.x) : (c1.z)));
    r1.x = saturate(r1.x);
    r1.z = (r1.z) + (r1.w);
    r1.w = frac(abs(v3.y));
    r1.y = ((v3.y) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2 = tex2D(s5, r1.xy);
    r1.xyw = (r2.xyz) * (r2.xyz);
    r1.xyz = float3(((-(r1.z)) >= 0.0f ? (r1.x) : (c1.x)), ((-(r1.z)) >= 0.0f ? (r1.y) : (c1.x)), ((-(r1.z)) >= 0.0f ? (r1.w) : (c1.x)));
    r1.w = abs(c[40].w);
    r1.w = frac(r1.w);
    r2.y = ((c[40].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2.x = c1.x;
    r2 = tex2D(s4, r2.xy);
    r1.w = saturate((r2.x) * (c[46].x));
    r2.xyz = lerp(r1.xyz, r0.xyz, r1.www);
    r0.x = (r0.w) * (c[49].x);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r0.y = (c[53].x) + (-(v3.y));
    r0.y = (r0.y) + (c1.z);
    r0.x = (r0.x) + (r0.y);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r1.x = pow(abs(r0.x), c[52].x);
    r0.z = c1.z;
    r0.x = (r1.x) * (-(c[51].x)) + (r0.z);
    r1.xyz = lerp(c[50].xyz, r2.xyz, r0.xxx);
    r2.zw = c1.xx;
    r3 = (c[31]) * (v1.yyyy);
    r3 = (v1.xxxx) * (c[30]) + (r3);
    r3 = (v1.zzzz) * (c[32]) + (r3);
    r3 = (r3) + (c[33]);
    r2.xy = (r3.ww) * (c[39].xy);
    r4 = (r2.xyww) + (r3);
    r2 = (-(r2)) + (r3);
    r2 = tex2Dproj(s1, r2);
    r2.y = r2.x;
    r4 = tex2Dproj(s1, r4);
    r2.x = r4.x;
    r4.xy = (r3.ww) * (c[39].zw);
    r4.zw = c1.xx;
    r5 = (r3) + (r4.xyww);
    r3 = (r3) + (-(r4));
    r3 = tex2Dproj(s1, r3);
    r2.w = r3.x;
    r3 = tex2Dproj(s1, r5);
    r2.z = r3.x;
    r0.x = dot(r2, c1.yyyy);
    r2 = (v1.xyzx) * (c1.zzzx) + (c1.xxxz);
    r0.y = dot(r2, c[27]);
    r0.y = 1.0f / (r0.y);
    r3.x = dot(r2, c[24]);
    r3.y = dot(r2, c[25]);
    r2.x = dot(r2, c[26]);
    r0.yw = (r0.yy) * (r3.xy);
    r0.yw = (r0.yw) * (c1.ww) + (c1.ww);
    r3 = tex2D(s2, r0.yw);
    r0.y = (r3.x) * (r3.x);
    r2.y = (r2.x) * (r2.x);
    r0.w = dot(c[22].yz, r2.xy) + (c[22].x);
    r2.xy = saturate((r2.xx) * (c[23].xy) + (c[23].zw));
    r1.w = saturate(1.0f / (r0.w));
    r0.w = ((-abs(r0.w)) >= 0.0f ? (c1.x) : (r1.w));
    r2.zw = (r2.xy) * (r2.xy);
    r2.xy = (r2.xy) * (c2.xx) + (c2.yy);
    r1.w = (r2.z) * (r2.x);
    r2.x = (r2.w) * (-(r2.y)) + (c1.z);
    r0.w = (r0.w) * (r1.w);
    r0.w = (r2.x) * (r0.w);
    r2.xyz = (c[20].xyz) + (-(v1.xyz));
    r3.xyz = normalize(r2.xyz);
    r1.w = dot(r3.xyz, c[28].xyz);
    r1.w = saturate((r1.w) * (c[29].x) + (c[29].y));
    r2.x = (r1.w) * (r1.w);
    r1.w = (r1.w) * (c2.x) + (c2.y);
    r1.w = (r2.x) * (r1.w);
    r0.w = (r0.w) * (r1.w);
    r0.y = (r0.y) * (r0.w);
    r0.x = (r0.x) * (r0.y);
    r0.xyw = (r0.xxx) * (c[21].xyz);
    r2.xyz = (c[34].xyz) + (-(v1.xyz));
    r1.w = dot(r2.xyz, r2.xyz);
    r1.w = rsqrt(r1.w);
    r2.xyz = (r2.xyz) * (r1.www);
    r4.x = 1.0f / (r1.w);
    r5.xyz = normalize(v1.xyz);
    r1.w = dot(r2.xyz, r5.xyz);
    r1.w = saturate((r1.w) * (c1.w) + (c1.w));
    r2.w = (r1.w) * (c2.z) + (c2.w);
    r1.w = (r1.w) + (c1.z);
    r3.w = dot(r3.xyz, r5.xyz);
    r3.w = saturate((r3.w) * (c1.w) + (c1.w));
    r4.z = lerp(r2.w, -(c2.x), r3.w);
    r4.w = lerp(r1.w, -(c2.x), r3.w);
    r6.xyz = normalize(v2.xyz);
    r3.w = dot(-(r3.xyz), r6.xyz);
    r3.w = (r3.w) + (r3.w);
    r3.xyz = (r6.xyz) * (-(r3.www)) + (-(r3.xyz));
    r3.x = saturate(dot(r3.xyz, -(r5.xyz)));
    r5.w = pow(abs(r3.x), r4.z);
    r0.xyw = (r0.xyw) * (r5.www);
    r0.xyw = (r4.www) * (r0.xyw);
    r4.y = (r4.x) * (r4.x);
    r3.x = dot(c[37].yz, r4.xy) + (c[37].x);
    r3.yz = saturate((r4.xx) * (c[38].xy) + (c[38].zw));
    r4.xy = (r3.yz) * (r3.yz);
    r3.yz = (r3.yz) * (c2.xx) + (c2.yy);
    r3.yz = (r4.xy) * (r3.yz);
    r3.x = (r3.x) * (r3.y);
    r3.x = (r3.z) * (r3.x);
    r4 = tex2D(s12, v3.zw);
    r3.x = (r3.x) * (r4.y);
    r3.xyz = (r3.xxx) * (c[35].xyz);
    r3.xyz = (r3.xyz) * (c[36].yyy);
    r3.w = dot(-(r2.xyz), r6.xyz);
    r3.w = (r3.w) + (r3.w);
    r2.xyz = (r6.xyz) * (-(r3.www)) + (-(r2.xyz));
    r2.x = saturate(dot(r2.xyz, -(r5.xyz)));
    r3.w = pow(abs(r2.x), r2.w);
    r2.xyz = (r3.xyz) * (r3.www);
    r0.xyw = (r2.xyz) * (r1.www) + (r0.xyw);
    r1.w = dot(r5.xyz, r6.xyz);
    r1.w = (r1.w) + (r1.w);
    r2.xyz = (r6.xyz) * (-(r1.www)) + (r5.xyz);
    r2 = texCUBE(s15, r2.xyz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = (r2.xyz) * (c0.xxx);
    r3.xy = (c1.zw) * (v3.zw);
    r3 = tex2D(s13, r3.xy);
    r4 = tex2D(s14, v3.zw);
    r1.w = (r4.x) * (c0.y);
    r3.xy = (r3.xz) * (r1.ww);
    r2.w = (r4.x) * (c0.y) + (-(r3.x));
    r3.xw = (r2.xz) * (r3.xy);
    r1.w = (r3.z) * (-(r1.w)) + (r2.w);
    r3.y = (r2.y) * (r1.w);
    r2.xyz = (r3.xyw) * (c[45].xxx);
    r2.xyz = (r2.xyz) * (c0.zwz);
    r0.xyw = (r0.xyw) * (c3.xyz) + (r2.xyz);
    r0.xyw = (r1.xyz) + (r0.xyw);
    r1 = (c[6]) + (-(v1.yyyy));
    r2 = (r1) * (r1);
    r3 = (c[5]) + (-(v1.xxxx));
    r2 = (r3) * (r3) + (r2);
    r4 = (c[7]) + (-(v1.zzzz));
    r2 = (r4) * (r4) + (r2);
    r5.x = rsqrt(r2.x);
    r5.y = rsqrt(r2.y);
    r5.z = rsqrt(r2.z);
    r5.w = rsqrt(r2.w);
    r2 = saturate((r2) * (c[8]) + (r0.zzzz));
    r1 = (r1) * (r5);
    r1 = (r6.yyyy) * (r1);
    r3 = (r3) * (r5);
    r4 = (r4) * (r5);
    r1 = (r3) * (r6.xxxx) + (r1);
    r1 = saturate((r4) * (r6.zzzz) + (r1));
    r1 = (r2) * (r1);
    r2.x = dot(c[9], r1);
    r2.y = dot(c[10], r1);
    r2.z = dot(c[11], r1);
    r0.xyz = (r0.xyw) * (r2.xyz) + (r0.xyw);
    r0.w = c1.z;
    r1.x = dot(r0, c[42]);
    r1.y = dot(r0, c[43]);
    r1.z = dot(r0, c[44]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[41].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c1.z;

    return oC0;
}
