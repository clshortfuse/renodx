// Mechanically reconstructed from 0x6B32FB51.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD1;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD3;
    float4 v3 : TEXCOORD4;
    float4 v4 : TEXCOORD5;
    float4 v5 : TEXCOORD6;
    float4 v6 : TEXCOORD7;
    float4 v7 : TEXCOORD8;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c0 = float4(0.25f, 1.0f, 0.0f, 0.5f);
    const float4 c1 = float4(0.00048828125f, -0.000122070312f, 0.0f, 0.75f);
    const float4 c3 = float4(4.0f, -3.0f, -4.0f, 1e-15f);
    const float4 c4 = float4(-2.0f, 3.0f, 0.000244140625f, 0.0f);
    const float4 c12 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c13 = float4(1.44269502f, 0.200000003f, 5.0f, 31.875f);
    const float4 c14 = float4(0.100000001f, 8.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xy = (v7.xy) + (-(c0.ww));
    r11.yw = c0.yw;
    r10.xy = (r0.xy) * (c[39].xx) + (r11.ww);
    r2.xy = ddx(r10.xy);
    r3.xy = ddy(r10.yx);
    r0.xy = ddy(v7.xy);
    r1.xy = (r2.yy) * (r0.xy);
    r0.xy = ddx(v7.xy);
    r1.xy = (r0.xy) * (r3.xx) + (-(r1.xy));
    r0.w = (r2.y) * (r3.y);
    r0.xyz = (r1.yyy) * (v3.xyz);
    r1.w = (r2.x) * (r3.x) + (-(r0.w));
    r1.xyz = (v2.xyz) * (r1.xxx) + (r0.xyz);
    r0 = tex2D(s3, v7.xy);
    r3.xy = (r0.wy) * (c12.xy) + (c12.zw);
    r0 = v2;
    r2.xyz = (r0.yzx) * (v5.zxy);
    r3.xy = (r3.xy) * (c[38].xx);
    r0.xyz = (v5.yzx) * (r0.zxy) + (-(r2.xyz));
    r2.xyz = float3(((r1.w) >= 0.0f ? (r1.x) : (-(r1.x))), ((r1.w) >= 0.0f ? (r1.y) : (-(r1.y))), ((r1.w) >= 0.0f ? (r1.z) : (-(r1.z))));
    r0.xyz = (r3.yyy) * (-(r0.xyz));
    r1.w = dot(-(v4.xyz), -(v4.xyz));
    r0.xyz = (r3.xxx) * (v2.xyz) + (r0.xyz);
    r6.w = rsqrt(r1.w);
    r0.xyz = (r0.xyz) + (v5.xyz);
    r9.xyz = normalize(v5.xyz);
    r1.w = dot(r0.xyz, r0.xyz);
    r8.xyz = (r6.www) * (-(v4.xyz));
    r2.w = rsqrt(r1.w);
    r1 = tex2D(s4, r10.xy);
    r3.xyz = (r0.xyz) * (r2.www);
    r1.z = dot(r2.xyz, r3.xyz);
    r4.xyz = (r1.zzz) * (-(r3.xyz)) + (r2.xyz);
    r2.xyz = normalize(r4.xyz);
    r4.xy = (r1.wy) * (c12.xy) + (c12.zw);
    r1.xyz = (r3.zxy) * (r2.yzx);
    r4.xy = (r4.xy) * (c[40].xx);
    r1.xyz = (r3.yzx) * (r2.zxy) + (-(r1.xyz));
    r9.w = saturate(dot(r8.xyz, r9.xyz));
    r1.xyz = (r4.yyy) * (-(r1.xyz));
    r1.xyz = (r4.xxx) * (r2.xyz) + (r1.xyz);
    r1.w = max(abs(r9.y), abs(r9.z));
    r1.xyz = (r0.xyz) * (r2.www) + (r1.xyz);
    r0.z = max(abs(r9.x), r1.w);
    r1.w = 1.0f / (r0.z);
    r0.xyz = (r9.xyz) * (c[5].xyz);
    r7.xyz = normalize(r1.xyz);
    r0.xyz = (r0.xyz) * (r1.www) + (v1.xyz);
    r1 = tex3D(s11, r0.xyz);
    r0.xyz = (-(v4.xyz)) + (c[21].xyz);
    r6.xyz = normalize(r0.xyz);
    r7.w = dot(r6.xyz, r9.xyz);
    if ((c0.y) >= (v0.w))
    {
        r2 = (v0.xyzx) * (c0.yyyz);
        r1 = (r2) + (-(c1.xyzz));
        r1 = tex2Dlod(s0, r1);
        r1.w = r1.x;
        r3 = (r2) + (c4.zzww);
        r3 = tex2Dlod(s0, r3);
        r1.x = r3.x;
        r3 = (r2) + (-(c4.zzww));
        r3 = tex2Dlod(s0, r3);
        r1.y = r3.x;
        r2 = (r2) + (c1.xyzz);
        r2 = tex2Dlod(s0, r2);
        r1.z = r2.x;
        r11.w = dot(r1, c0.xxxx);
        if ((c1.w) < (v0.w))
        {
            r0.xy = (v0.xy) * (c[2].ww) + (c[2].xy);
            r1.xy = (r0.xy) + (c4.zz);
            r1.zw = (v0.zx) * (c0.yz);
            r1 = tex2Dlod(s0, r1);
            r2.xy = (r0.xy) + (-(c4.zz));
            r2.zw = (v0.zx) * (c0.yz);
            r4 = tex2Dlod(s0, r2);
            r2.xy = (r0.xy) + (c1.xy);
            r2.zw = (v0.zx) * (c0.yz);
            r3 = tex2Dlod(s0, r2);
            r2.xy = (r0.xy) + (-(c1.xy));
            r2.zw = (v0.zx) * (c0.yz);
            r2 = tex2Dlod(s0, r2);
            r1.y = r4.x;
            r1.z = r3.x;
            r1.w = r2.x;
            r0.z = dot(r1, c0.xxxx);
            r0.y = (-(r11.w)) + (r0.z);
            r0.z = (v0.w) * (c3.x) + (c3.y);
            r11.w = (r0.z) * (r0.y) + (r11.w);
        }
    }
    else
    {
        r0.z = (v0.w) + (c3.z);
        r0.z = ((r0.z) >= 0.0f ? (c0.z) : (c0.y));
        if ((r0.z) != (-(r0.z)))
        {
            r0.xy = (v0.xy) * (c[2].ww) + (c[2].xy);
            r2.xy = (r0.xy) + (c4.zz);
            r2.zw = (v0.zz) * (c0.yz);
            r2 = tex2Dlod(s0, r2);
            r3.xy = (r0.xy) + (-(c4.zz));
            r3.zw = (v0.zz) * (c0.yz);
            r5 = tex2Dlod(s0, r3);
            r3.xy = (r0.xy) + (c1.xy);
            r3.zw = (v0.zz) * (c0.yz);
            r4 = tex2Dlod(s0, r3);
            r3.xy = (r0.xy) + (-(c1.xy));
            r3.zw = (v0.zz) * (c0.yz);
            r3 = tex2Dlod(s0, r3);
            r2.y = r5.x;
            r2.z = r4.x;
            r2.w = r3.x;
            r0.x = dot(r2, c0.xxxx);
            r0.z = saturate((v0.w) + (-(c4.y)));
            r0.y = (r1.w) + (-(r0.x));
            r0.z = (r0.z) * (r0.y) + (r0.x);
        }
        else
        {
            r0.z = r1.w;
        }
        r11.w = r0.z;
    }
    r0.z = (r7.w) + (c13.y);
    r0.y = saturate((r0.z) * (c13.z));
    r0.x = (r0.y) * (r0.y);
    r0.z = dot(r6.xyz, c[29].xyz);
    r0.y = (r0.y) * (c4.x) + (c4.y);
    r0.z = saturate((r0.z) * (c[30].x) + (c[30].y));
    r7.w = (r0.x) * (r0.y);
    r0.y = (r0.z) * (c4.x) + (c4.y);
    r0.z = (r0.z) * (r0.z);
    r1 = (v4.yyyy) * (c[32]);
    r10.w = (r0.y) * (r0.z);
    r1 = (v4.xxxx) * (c[31]) + (r1);
    r2.xyz = (-(v4.xyz)) * (r6.www) + (r6.xyz);
    r1 = (v4.zzzz) * (c[33]) + (r1);
    r0.xyz = normalize(r2.xyz);
    r4 = (r1) + (c[34]);
    r1.w = dot(r0.xyz, r8.xyz);
    r3.zw = r4.zw;
    r8.w = 1.0f / (r1.w);
    r5.zw = r3.zw;
    r4.z = saturate(dot(r0.xyz, r7.xyz));
    r2.zw = r5.zw;
    r1.xy = (r4.ww) * (-(c[35].zw)) + (r4.xy);
    r1.zw = r2.zw;
    r1 = tex2Dproj(s1, r1);
    r1.w = r1.x;
    r5.xy = (r3.ww) * (-(c[35].xy)) + (r4.xy);
    r5 = tex2Dproj(s1, r5);
    r1.y = r5.x;
    r3.xy = (r3.ww) * (c[35].xy) + (r4.xy);
    r5 = tex2Dproj(s1, r3);
    r1.x = r5.x;
    r2.xy = (r3.ww) * (c[35].zw) + (r4.xy);
    r3 = tex2Dproj(s1, r2);
    r2 = (v4.xyzx) * (c0.yyyz) + (c0.zzzy);
    r1.z = r3.x;
    r0.z = dot(r2, c[28]);
    r3.w = 1.0f / (r0.z);
    r3.x = dot(r2, c[27]);
    r0.x = dot(r2, c[25]);
    r3.y = (r3.x) * (r3.x);
    r0.y = dot(r2, c[26]);
    r0.z = dot(c[23].yz, r3.xy) + (c[23].x);
    r2.w = saturate(1.0f / (r0.z));
    r2.xy = saturate((r3.xx) * (c[24].xy) + (c[24].zw));
    r3.xy = (r2.xy) * (c4.xx) + (c4.yy);
    r2.xy = (r2.xy) * (r2.xy);
    r0.z = ((-abs(r0.z)) >= 0.0f ? (c0.z) : (r2.w));
    r2.w = (r3.x) * (r2.x);
    r0.z = (r0.z) * (r2.w);
    r2.w = (r2.y) * (-(r3.y)) + (c0.y);
    r0.xy = (r3.ww) * (r0.xy);
    r0.z = (r0.z) * (r2.w);
    r3.w = (r10.w) * (r0.z);
    r0.xy = (r0.xy) * (c0.ww) + (c0.ww);
    r2 = tex2D(s2, r0.xy);
    r0.xyz = (r2.xyz) * (r2.xyz);
    r1.z = dot(r1, c0.xxxx);
    r0.xyz = (r3.www) * (r0.xyz);
    r1.w = (r4.z) + (r4.z);
    r0.xyz = (r1.zzz) * (r0.xyz);
    r3.w = (r8.w) * (r1.w);
    r0.xyz = (r0.xyz) * (c[22].xyz);
    r0.xyz = (r7.www) * (r0.xyz);
    r2.xyz = normalize(c[17].xyz);
    r1.w = dot(r2.xyz, r9.xyz);
    r1.xyz = (-(v4.xyz)) * (r6.www) + (r2.xyz);
    r3.xyz = normalize(r1.xyz);
    r1.w = (r1.w) + (c13.y);
    r1.z = dot(r3.xyz, r8.xyz);
    r1.w = saturate((r1.w) * (c13.z));
    r2.w = 1.0f / (r1.z);
    r1.y = (r1.w) * (r1.w);
    r1.z = (r1.w) * (c4.x) + (c4.y);
    r1.w = (r4.z) * (r4.z) + (c3.w);
    r5.w = (r1.y) * (r1.z);
    r4.w = 1.0f / (r1.w);
    r1 = tex2D(s5, r10.xy);
    r1.z = (r1.x) * (r1.x);
    r1.w = (r4.w) * (r4.w);
    r8.w = 1.0f / (r1.z);
    r10.w = saturate(dot(r8.xyz, r7.xyz));
    r6.w = saturate(dot(r7.xyz, r6.xyz));
    r1.y = (-(r4.w)) + (c0.y);
    r1.z = min(r10.w, r6.w);
    r1.y = (r8.w) * (r1.y);
    r1.z = saturate((r3.w) * (r1.z));
    r1.y = (r1.y) * (c13.x);
    r1.z = (r6.w) * (r1.z);
    r1.y = exp2(r1.y);
    r1.z = rsqrt(r1.z);
    r1.w = (r1.w) * (r1.y);
    r1.z = 1.0f / (r1.z);
    r10.z = (r1.w) * (r1.z);
    r1.w = saturate(dot(r3.xyz, r7.xyz));
    r1.z = (r1.w) + (r1.w);
    r1.w = (r1.w) * (r1.w) + (c3.w);
    r1.z = (r2.w) * (r1.z);
    r1.y = 1.0f / (r1.w);
    r1.w = (r1.y) * (r1.y);
    r7.w = saturate(dot(r7.xyz, r2.xyz));
    r1.y = (-(r1.y)) + (c0.y);
    r1.x = min(r10.w, r7.w);
    r1.y = (r8.w) * (r1.y);
    r1.z = saturate((r1.z) * (r1.x));
    r1.y = (r1.y) * (c13.x);
    r1.z = (r7.w) * (r1.z);
    r1.y = exp2(r1.y);
    r1.z = rsqrt(r1.z);
    r1.w = (r1.w) * (r1.y);
    r1.z = 1.0f / (r1.z);
    r9.z = (r1.w) * (r1.z);
    r4 = (-(v4.yyyy)) + (c[7]);
    r3 = (-(v4.xxxx)) + (c[6]);
    r1 = (r4) * (r4);
    r1 = (r3) * (r3) + (r1);
    r2 = (-(v4.zzzz)) + (c[8]);
    r5.xyz = (r11.www) * (c[18].xyz);
    r1 = (r2) * (r2) + (r1);
    r6.xyz = (r5.www) * (r5.xyz);
    r5.x = rsqrt(r1.x);
    r5.y = rsqrt(r1.y);
    r5.z = rsqrt(r1.z);
    r5.w = rsqrt(r1.w);
    r9.xyz = (r9.zzz) * (r6.xyz);
    r4 = (r4) * (r5);
    r4 = (r7.yyyy) * (r4);
    r3 = (r3) * (r5);
    r2 = (r2) * (r5);
    r3 = (r3) * (r7.xxxx) + (r4);
    r2 = saturate((r2) * (r7.zzzz) + (r3));
    r1 = saturate((r1) * (c[9]) + (r11.yyyy));
    r3.xyz = (r10.zzz) * (r0.xyz) + (r9.xyz);
    r1 = (r2) * (r1);
    r4.z = dot(c[20], r1);
    r2.w = (r9.w) + (r10.w);
    r2.xyz = (r8.www) * (r3.xyz);
    r3.w = (r2.w) * (c0.w);
    r3.xyz = (r2.xyz) * (c0.xxx);
    r2.w = max(c14.x, r3.w);
    r2.z = 1.0f / (r2.w);
    r2.w = dot(-(r8.xyz), r7.xyz);
    r3.w = rsqrt(r2.z);
    r2.z = (r2.w) + (r2.w);
    r2.w = c[37].x;
    r2.xyz = (r7.xyz) * (-(r2.zzz)) + (-(r8.xyz));
    r2 = texCUBElod(s15, r2);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.w = 1.0f / (r3.w);
    r2.xyz = (r2.xyz) * (c14.yyy);
    r2.xyz = (r3.xyz) * (r2.www) + (r2.xyz);
    r3.w = max(abs(r7.y), abs(r7.z));
    r2.w = max(abs(r7.x), r3.w);
    r4.x = dot(c[10], r1);
    r2.w = 1.0f / (r2.w);
    r3.xyz = (r7.xyz) * (c[5].xyz);
    r4.y = dot(c[11], r1);
    r1.xyz = (r3.xyz) * (r2.www) + (v1.xyz);
    r1 = tex3D(s11, r1.xyz);
    r3.xyz = (r1.xyz) * (r1.xyz);
    r1.xyz = (r7.www) * (r6.xyz);
    r3.xyz = (r3.xyz) * (c13.www) + (r4.xyz);
    r0.xyz = (r6.www) * (r0.xyz) + (r1.xyz);
    r1 = tex2D(s7, r10.xy);
    r1.xyz = (r1.xyz) * (c[41].xxx);
    r0.xyz = (r3.xyz) + (r0.xyz);
    r2.xyz = (r2.xyz) * (r1.xyz);
    r1 = tex2D(s6, r10.xy);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r0.xyz = (r0.xyz) * (r1.xyz) + (r2.xyz);
    r0.xyz = (r0.xyz) + (-(v6.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v6.xyz);
    r0.xyz = max(((r0.xyz) * (c[36].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c0.y;

    return oC0;
}
