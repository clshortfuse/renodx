// Mechanically reconstructed from 0x48F89EE8.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler3D s11 : register(s11);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    const float4 c0 = float4(-0.5f, 1.0f, 0.0f, 31.875f);
    const float4 c1 = float4(-2.0f, 3.0f, 0.25f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xyz = (-(v5.xyz)) + (c[35].xyz);
    r3.y = dot(r0.xyz, r0.xyz);
    r1.w = rsqrt(r3.y);
    r3.x = 1.0f / (r1.w);
    r2.xy = saturate((r3.xx) * (c[40].xy) + (c[40].zw));
    r1.xy = (r2.xy) * (r2.xy);
    r2.xy = (r2.xy) * (c1.xx) + (c1.yy);
    r0.w = dot(c[39].yz, r3.xy) + (c[39].x);
    r1.xy = (r1.xy) * (r2.xy);
    r0.w = (r0.w) * (r1.x);
    r2.xyz = (r0.xyz) * (r1.www);
    r3.w = (r1.y) * (r0.w);
    r0.xy = (v4.ww) * (c[41].xy) + (v4.xy);
    r0.zw = v4.zw;
    r0 = tex2Dproj(s1, r0);
    r1.xy = (v4.ww) * (-(c[41].xy)) + (v4.xy);
    r1.zw = v4.zw;
    r1 = tex2Dproj(s1, r1);
    r0.y = r1.x;
    r1.xy = (v4.ww) * (c[41].zw) + (v4.xy);
    r1.zw = v4.zw;
    r1 = tex2Dproj(s1, r1);
    r0.z = r1.x;
    r1.xy = (v4.ww) * (-(c[41].zw)) + (v4.xy);
    r1.zw = v4.zw;
    r1 = tex2Dproj(s1, r1);
    r0.w = r1.x;
    r3.z = dot(r0, c1.zzzz);
    r9.xyz = normalize(v2.xyz);
    r0.w = dot(r2.xyz, c[37].xyz);
    r1.w = max(abs(r9.y), abs(r9.z));
    r0.w = saturate((r0.w) * (c[38].x) + (c[38].y));
    r0.z = max(abs(r9.x), r1.w);
    r1.w = 1.0f / (r0.z);
    r0.xyz = (r9.xyz) * (c[5].xyz);
    r0.w = (r0.w) * (c[38].w);
    r0.xyz = (r0.xyz) * (r1.www) + (v6.xyz);
    r1 = tex3D(s11, r0.xyz);
    r2.w = lerp(r1.w, r3.z, r0.w);
    r0 = tex2D(s0, v1.xy);
    r1.w = (r0.w) * (v0.w) + (c0.x);
    r0 = (r0.xyzx) * (c0.yyyz) + (c0.zzzy);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r0 = float4(((r1.w) >= 0.0f ? (r0.x) : (c0.z)), ((r1.w) >= 0.0f ? (r0.y) : (c0.z)), ((r1.w) >= 0.0f ? (r0.z) : (c0.z)), ((r1.w) >= 0.0f ? (r0.w) : (c0.z)));
    r2.w = (r3.w) * (r2.w);
    r1.xyz = (r1.xyz) * (r0.www);
    r3.xyz = (r1.xyz) * (c0.www);
    r1 = (v5.yyyy) * (c[32]);
    r2.z = saturate(dot(r2.xyz, r9.xyz));
    r1 = (v5.xxxx) * (c[31]) + (r1);
    r2.xyz = (r2.zzz) * (c[36].xyz);
    r1 = (v5.zzzz) * (c[33]) + (r1);
    r6.xyz = (r2.www) * (r2.xyz) + (r3.xyz);
    r2 = (r1) + (c[34]);
    r4.xy = (r2.ww) * (c[42].xy) + (r2.xy);
    r4.zw = r2.zw;
    r1 = tex2Dproj(s1, r4);
    r3.zw = r4.zw;
    r2.zw = r3.zw;
    r5.xy = (r4.ww) * (-(c[42].zw)) + (r2.xy);
    r5.zw = r2.zw;
    r5 = tex2Dproj(s1, r5);
    r1.w = r5.x;
    r3.xy = (r4.ww) * (-(c[42].xy)) + (r2.xy);
    r2.xy = (r4.ww) * (c[42].zw) + (r2.xy);
    r3 = tex2Dproj(s1, r3);
    r1.y = r3.x;
    r3 = tex2Dproj(s1, r2);
    r2 = (v5.xyzx) * (c0.yyyz) + (c0.zzzy);
    r1.z = dot(r2, c[28]);
    r3.w = 1.0f / (r1.z);
    r4.x = dot(r2, c[25]);
    r4.y = dot(r2, c[26]);
    r1.z = r3.x;
    r4.xy = (r3.ww) * (r4.xy);
    r3.x = dot(r2, c[27]);
    r2.xy = (r4.xy) * (-(c0.xx)) + (-(c0.xx));
    r2 = tex2D(s2, r2.xy);
    r3.y = (r3.x) * (r3.x);
    r2.w = dot(c[23].yz, r3.xy) + (c[23].x);
    r3.xy = saturate((r3.xx) * (c[24].xy) + (c[24].zw));
    r3.w = saturate(1.0f / (r2.w));
    r2.w = ((-abs(r2.w)) >= 0.0f ? (c0.z) : (r3.w));
    r5.xy = (r3.xy) * (r3.xy);
    r7.xy = (r3.xy) * (c1.xx) + (c1.yy);
    r4.xyz = (-(v5.xyz)) + (c[21].xyz);
    r4.w = (r5.x) * (r7.x);
    r3.xyz = normalize(r4.xyz);
    r4.z = (r5.y) * (-(r7.y)) + (c0.y);
    r3.w = dot(r3.xyz, c[29].xyz);
    r2.w = (r2.w) * (r4.w);
    r4.w = saturate((r3.w) * (c[30].x) + (c[30].y));
    r3.w = (r4.w) * (r4.w);
    r4.w = (r4.w) * (c1.x) + (c1.y);
    r2.w = (r4.z) * (r2.w);
    r3.w = (r3.w) * (r4.w);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.w = (r2.w) * (r3.w);
    r1.w = dot(r1, c1.zzzz);
    r1.xyz = (r2.xyz) * (r2.www);
    r5.w = saturate(dot(r3.xyz, r9.xyz));
    r7.xyz = (r1.www) * (r1.xyz);
    r0.xyz = (r0.xyz) * (v0.xyz);
    r4 = (-(v5.yyyy)) + (c[7]);
    r1 = (r4) * (r4);
    r3 = (-(v5.xxxx)) + (c[6]);
    r1 = (r3) * (r3) + (r1);
    r2 = (-(v5.zzzz)) + (c[8]);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r1 = (r2) * (r2) + (r1);
    r8.xyz = (r5.www) * (c[22].xyz);
    r5.x = rsqrt(r1.x);
    r5.y = rsqrt(r1.y);
    r5.z = rsqrt(r1.z);
    r5.w = rsqrt(r1.w);
    r8.xyz = (r0.xyz) * (r8.xyz);
    r4 = (r4) * (r5);
    r4 = (r9.yyyy) * (r4);
    r3 = (r3) * (r5);
    r2 = (r2) * (r5);
    r3 = (r3) * (r9.xxxx) + (r4);
    r4.z = c0.y;
    r1 = saturate((r1) * (c[9]) + (r4.zzzz));
    r2 = saturate((r2) * (r9.zzzz) + (r3));
    r3.xyz = (r7.xyz) * (r8.xyz);
    r1 = (r1) * (r2);
    r3.xyz = (r0.xyz) * (r6.xyz) + (r3.xyz);
    r2.x = dot(c[10], r1);
    r2.y = dot(c[11], r1);
    r2.z = dot(c[20], r1);
    r0.xyz = (r0.xyz) * (r2.xyz) + (r3.xyz);
    r0.xyz = (r0.xyz) + (-(v3.xyz));
    r1.x = v2.w;
    r0.xyz = (r1.xxx) * (r0.xyz) + (v3.xyz);
    r0.xyz = max(((r0.xyz) * (c[43].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.w = rsqrt(r0.w);
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = 1.0f / (r0.w);

    return oC0;
}
