// Mechanically reconstructed from 0x9501DD09.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler2D s8 : register(s8);
sampler2D s9 : register(s9);
sampler2D s10 : register(s10);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(3.14159274f, 0.159154937f, 0.5f, -6.28318548f);
    const float4 c2 = float4(2.0f, -1.0f, 6.28318548f, 0.25f);
    const float4 c3 = float4(0.5f, -0.5f, 9.99999975e-06f, 1e-15f);
    const float4 c4 = float4(1.44269502f, 0.100000001f, 9.99999997e-07f, 0.600000024f);
    const float4 c12 = float4(1.0f, 0.0f, 0.25f, 0.5f);
    const float4 c13 = float4(3.5f, 1.0f, 2.5f, -2.5f);
    const float4 c14 = float4(5.0f, 0.833333313f, 1.5f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = (v1.xyzx) * (c12.xxxy) + (c12.yyyx);
    r1.x = dot(r0, c[24]);
    r1.x = 1.0f / (r1.x);
    r1.y = dot(r0, c[21]);
    r1.z = dot(r0, c[22]);
    r0.z = dot(r0, c[23]);
    r1 = (r1.xxxx) * (r1.yyzz);
    r2 = (r1) * (c[25].zwxy);
    r3.x = log2(abs(r2.x));
    r3.y = log2(abs(r2.y));
    r3.z = log2(abs(r2.z));
    r3.w = log2(abs(r2.w));
    r2 = (r3) * (c[26].xxxx);
    r1.x = exp2(r2.x);
    r1.z = exp2(r2.y);
    r2.x = exp2(r2.z);
    r2.y = exp2(r2.w);
    r1.xz = (r1.xz) + (r2.xy);
    r2.x = log2(abs(r1.x));
    r2.y = log2(abs(r1.z));
    r1.xz = (r2.xy) * (c[26].yy);
    r1.x = exp2(r1.x);
    r1.z = exp2(r1.z);
    r1.x = (r1.x) * (c[27].x);
    r1.x = (r1.z) * (c[27].y) + (-(r1.x));
    r2.y = c[27].y;
    r1.z = (r1.z) * (r2.y) + (-(c[26].z));
    r1.x = 1.0f / (r1.x);
    r2.w = saturate((r1.z) * (r1.x));
    r0.xy = abs(r1.yw);
    r3 = c[11];
    r3 = saturate((r0.zyxz) * (r3) + (c[20]));
    r2.x = (r3.w) * (r3.x);
    r2.yz = r3.yz;
    r3 = (r2) * (r2);
    r2 = (c[27].zzzz) * (r2) + (c[27].wwww);
    r2 = (r3) * (r2);
    r0.x = (r2.z) * (r2.y);
    r0.y = abs(c[26].w);
    r0.x = ((-(r0.y)) >= 0.0f ? (r0.x) : (r2.w));
    r0.w = (r0.z) * (r0.z);
    r0.y = dot(c[10].yz, r0.zw) + (c[10].x);
    r0.x = (r0.x) * (r0.y);
    r0.x = (r2.x) * (r0.x);
    r1.xz = c[9].xz;
    r0.y = dot(r1.yw, c[28].xy) + (r1.x);
    r0.z = dot(r1.yw, c[28].zw) + (r1.z);
    r1 = tex2D(s2, r0.yz);
    r0.yzw = (r1.xyz) * (r1.xyz);
    r0.xyz = (r0.xxx) * (r0.yzw);
    r1.xy = (c[29].xy) * (v5.ww);
    r1.zw = c12.yy;
    r2 = (r1.xyww) + (v5);
    r1 = (-(r1)) + (v5);
    r1 = tex2Dproj(s1, r1);
    r1.y = r1.x;
    r2 = tex2Dproj(s1, r2);
    r1.x = r2.x;
    r2.xy = (c[29].zw) * (v5.ww);
    r2.zw = c12.yy;
    r3 = (r2.xyww) + (v5);
    r2 = (-(r2)) + (v5);
    r2 = tex2Dproj(s1, r2);
    r1.w = r2.x;
    r2 = tex2Dproj(s1, r3);
    r1.z = r2.x;
    r0.w = dot(r1, c12.zzzz);
    r0.w = (r0.w) * (c[9].w);
    r0.xyz = (r0.xyz) * (r0.www);
    r1.xyz = (r0.xyz) * (c[7].xyz);
    r0.xyz = (r0.xyz) * (c[6].xyz);
    r0.xyz = (r0.xyz) * (c[8].xxx);
    r1.xyz = (r1.xyz) * (c[8].yyy);
    r2 = tex2D(s5, v4.xy);
    r2.xy = (r2.wy) * (c0.xy) + (c0.zw);
    r2.xy = (r2.xy) * (c12.ww) + (c12.ww);
    r2.xy = (r2.xy) * (c2.xx) + (c2.yy);
    r3 = tex2D(s4, v4.xy);
    r2.zw = (r3.wy) * (c0.xy) + (c0.zw);
    r2.zw = (r2.zw) * (c12.ww) + (c12.ww);
    r2.zw = (r2.zw) * (c2.xx) + (c2.yy);
    r3 = tex2D(s3, v4.xy);
    r4.xyz = (r3.xyz) * (c2.zzw);
    r0.w = lerp(r3.x, r3.y, c12.w);
    r0.w = (r0.w) * (c13.z) + (c13.w);
    r3.x = c2.x;
    r3.xy = (c[30].ww) * (r3.xx) + (r4.xy);
    r1.w = (r3.y) * (c1.y) + (c1.z);
    r3.x = (r3.x) + (c1.x);
    r3.x = (r3.x) * (c1.y) + (c1.z);
    r3.x = frac(r3.x);
    r3.x = (r3.x) * (-(c1.w)) + (-(c1.x));
    r5.x = cos(r3.x);
    r3.x = (r5.x) * (c3.x) + (c3.y);
    r1.w = frac(r1.w);
    r1.w = (r1.w) * (-(c1.w)) + (-(c1.x));
    r5.x = cos(r1.w);
    r1.w = (r5.x) * (c12.w) + (c12.w);
    r4.x = c12.x;
    r3.y = (r4.x) + (-(c[31].y));
    r3.y = saturate(r3.y);
    r1.w = (r1.w) * (r3.y);
    r3.x = (r3.y) * (r3.x) + (c12.x);
    r2.zw = (r2.zw) * (r1.ww);
    r2.xy = (r3.xx) * (r2.xy) + (r2.zw);
    r3 = tex2D(s6, v4.xy);
    r2.zw = (r3.wy) * (c0.xy) + (c0.zw);
    r2.zw = (r2.zw) * (c12.ww) + (c12.ww);
    r2.zw = (r2.zw) * (c2.xx) + (c2.yy);
    r3.xy = lerp(r2.zw, r2.xy, c[31].zz);
    r1.w = dot(v2.xyz, v2.xyz);
    r1.w = rsqrt(r1.w);
    r2.xyz = (r1.www) * (v2.xyz);
    r4.xyw = (r2.zxy) * (v3.yzx);
    r4.xyw = (r2.yzx) * (v3.zxy) + (-(r4.xyw));
    r4.xyw = (r4.xyw) * (v3.www);
    r3.yzw = (r3.yyy) * (r4.xyw);
    r3.xyz = (r3.xxx) * (v3.xyz) + (r3.yzw);
    r3.xyz = (v2.xyz) * (r1.www) + (r3.xyz);
    r5.xyz = normalize(r3.xyz);
    r3.xyz = (c[5].xyz) + (-(v1.xyz));
    r6.xyz = normalize(r3.xyz);
    r1.w = dot(-(v1.xyz), -(v1.xyz));
    r1.w = rsqrt(r1.w);
    r3.xyz = (-(v1.xyz)) * (r1.www) + (r6.xyz);
    r2.w = saturate(dot(r5.xyz, r6.xyz));
    r4.xyw = (r1.www) * (-(v1.xyz));
    r6.xyz = normalize(r3.xyz);
    r1.w = saturate(dot(r5.xyz, r6.xyz));
    r3.x = (r1.w) + (r1.w);
    r1.w = (r1.w) * (r1.w) + (c3.w);
    r1.w = 1.0f / (r1.w);
    r3.y = saturate(dot(r5.xyz, r4.xyw));
    r2.x = saturate(dot(r2.xyz, r4.xyw));
    r2.x = (r3.y) + (r2.x);
    r2.x = (r2.x) * (c12.w);
    r3.z = max(c4.y, r2.x);
    r2.x = 1.0f / (r3.z);
    r2.x = rsqrt(r2.x);
    r2.x = 1.0f / (r2.x);
    r2.y = 1.0f / (r3.y);
    r4.x = min(r3.y, r2.w);
    r2.y = (r3.x) * (r2.y);
    r2.y = saturate((r4.x) * (r2.y));
    r2.y = (r2.w) * (r2.y);
    r0.xyz = (r0.xyz) * (r2.www);
    r2.y = rsqrt(r2.y);
    r2.y = 1.0f / (r2.y);
    r2.z = (r1.w) * (r1.w);
    r1.w = (-(r1.w)) + (c12.x);
    r3 = tex2D(s9, v4.xy);
    r2.w = max(c3.z, r3.x);
    r2.w = (r2.w) * (r2.w);
    r2.w = 1.0f / (r2.w);
    r1.w = (r1.w) * (r2.w);
    r2.w = (r2.w) * (c12.z);
    r1.w = (r1.w) * (c4.x);
    r1.w = exp2(r1.w);
    r1.w = (r2.z) * (r1.w);
    r1.w = (r2.y) * (r1.w);
    r1.xyz = (r1.xyz) * (r1.www);
    r1.xyz = (r2.xxx) * (r1.xyz);
    r1.xyz = (r2.www) * (r1.xyz);
    r2.xyz = normalize(v1.xyz);
    r1.w = dot(r2.xyz, r5.xyz);
    r2.y = (r1.w) + (r1.w);
    r1.w = saturate(r1.w);
    r1.w = (-(r1.w)) + (c12.x);
    r2.x = (r5.x) * (-(r2.y)) + (r2.x);
    r2.x = (r2.x) * (c[36].x);
    r2.x = (r2.x) * (c4.z);
    r2.y = (r1.w) * (r1.w);
    r1.w = (r1.w) * (r2.y);
    r3 = tex2D(s8, v4.xy);
    r2.y = (r3.w) * (c13.x) + (c13.y);
    r2.y = 1.0f / (r2.y);
    r1.w = (r1.w) * (r2.y);
    r2.yzw = lerp(r3.xyz, c12.xxx, r1.www);
    r2.xyz = (r2.xxx) * (r2.yzw);
    r1.xyz = (r1.xyz) * (c12.zzz) + (r2.xyz);
    r2.xyz = (-(r1.xyz)) + (c12.xxx);
    r1.xyz = (c[31].xxx) * (r2.xyz) + (r1.xyz);
    r2.xyz = (r4.zzz) * (r1.xyz);
    r1.xyz = (r3.xyz) * (r1.xyz);
    r3 = tex2D(s7, v4.xy);
    r3.xyz = (r3.xyz) * (r3.xyz);
    r2.xyz = (r0.xyz) * (r3.xyz) + (r2.xyz);
    r3.xyz = max(r2.xyz, c12.yyy);
    r2 = tex2D(s10, v4.xy);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r0.xyz = (r0.xyz) * (r2.xyz) + (r1.xyz);
    r1.xyz = max(r0.xyz, c12.yyy);
    r0.xyz = lerp(r1.xyz, r3.xyz, c4.www);
    r0.xyz = (r0.xyz) * (c[37].xyz) + (-(r1.xyz));
    r2.z = c[31].z;
    r2.x = c[38].x;
    r1.w = saturate((r2.z) * (r2.x) + (c[39].x));
    r0.w = (r1.w) * (c14.x) + (r0.w);
    r1.w = saturate((r0.w) * (c14.y));
    r0.w = (r0.w) + (-(c4.w));
    r0.w = saturate((r0.w) * (c14.z));
    r1.w = (r1.w) * (r1.w);
    r0.xyz = (r1.www) * (r0.xyz) + (r1.xyz);
    r1.xyz = lerp(r0.xyz, r3.xyz, r0.www);
    r1.w = c12.x;
    r0.x = dot(r1, c[33]);
    r0.y = dot(r1, c[34]);
    r0.z = dot(r1, c[35]);
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[32].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c12.x;

    return oC0;
}
