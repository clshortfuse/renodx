// Mechanically reconstructed from 0x1AD42810.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD3;
    float4 v2 : TEXCOORD4;
    float4 v3 : TEXCOORD5;
    float4 v4 : TEXCOORD6;
    float4 v5 : TEXCOORD7;
    float4 v6 : TEXCOORD8;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(1.0f, 0.0f, -0.5f, 0.5f);
    const float4 c2 = float4(1e-15f, 1.44269502f, 0.200000003f, 5.0f);
    const float4 c3 = float4(-2.0f, 3.0f, 31.875f, 0.25f);
    const float4 c4 = float4(0.100000001f, 8.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 oC0 = 0.0f;

    r1 = (v3.xyzx) * (c1.xxxy) + (c1.yyyx);
    r0.w = dot(r1, c[23]);
    r0.w = 1.0f / (r0.w);
    r0.x = dot(r1, c[20]);
    r0.y = dot(r1, c[21]);
    r0 = (r0.wwww) * (r0.xxyy);
    r2 = (r0) * (c[24].zwxy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.z = log2(abs(r2.z));
    r2.w = log2(abs(r2.w));
    r2 = (r2) * (c[25].xxxx);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = exp2(r2.z);
    r2.w = exp2(r2.w);
    r2.xy = (r2.zw) + (r2.xy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.xy = (r2.xy) * (c[25].yy);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = dot(r1, c[22]);
    r0.z = (r2.x) * (c[26].x);
    r0.y = (r2.y) * (c[26].y) + (-(r0.z));
    r0.z = c[26].y;
    r0.z = (r2.y) * (r0.z) + (-(c[25].z));
    r0.y = 1.0f / (r0.y);
    r2.xy = abs(r0.xw);
    r3.w = saturate((r0.z) * (r0.y));
    r1 = c[10];
    r1 = saturate((r2.zyxz) * (r1) + (c[11]));
    r3.x = (r1.w) * (r1.x);
    r3.yz = r1.yz;
    r1 = (r3) * (r3);
    r3 = (c[26].zzzz) * (r3) + (c[26].wwww);
    r1 = (r1) * (r3);
    r0.y = (r1.z) * (r1.y);
    r0.z = abs(c[25].w);
    r2.w = (r2.z) * (r2.z);
    r0.y = ((-(r0.z)) >= 0.0f ? (r0.y) : (r1.w));
    r0.z = dot(c[9].yz, r2.zw) + (c[9].x);
    r0.z = (r0.y) * (r0.z);
    r2.w = (r1.x) * (r0.z);
    r1 = c[27];
    r1.x = dot(r0.xw, r1.xy) + (c[8].x);
    r1.y = dot(r0.xw, r1.zw) + (c[8].z);
    r0 = tex2D(s0, r1.xy);
    r5.xyz = normalize(v4.xyz);
    r1.w = max(abs(r5.y), abs(r5.z));
    r1.xyz = (r0.xyz) * (r0.xyz);
    r0.w = max(abs(r5.x), r1.w);
    r0.w = 1.0f / (r0.w);
    r0.xyz = (r5.xyz) * (c[5].xyz);
    r1.xyz = (r2.www) * (r1.xyz);
    r0.xyz = (r0.xyz) * (r0.www) + (v0.xyz);
    r0 = tex3D(s11, r0.xyz);
    r0.xyz = (r1.xyz) * (r0.www);
    r2.xyz = (r0.xyz) * (c[7].xyz);
    r0.xyz = (-(v3.xyz)) + (c[6].xyz);
    r1.xy = (v6.xy) + (c1.zz);
    r6.xyz = normalize(r0.xyz);
    r0.x = c1.w;
    r8.xy = (r1.xy) * (c[31].xx) + (r0.xx);
    r0.xy = ddy(v6.xy);
    r4.xy = ddx(r8.xy);
    r10.xy = ddx(v6.xy);
    r11.xy = (r0.xy) * (r4.yy);
    r7.xy = ddy(r8.yx);
    r0 = v1;
    r3.xyz = (r0.yzx) * (v4.zxy);
    r1 = tex2D(s1, v6.xy);
    r1.xy = (r1.wy) * (c0.xy) + (c0.zw);
    r0.xyz = (v4.yzx) * (r0.zxy) + (-(r3.xyz));
    r9.xy = (r1.xy) * (c[30].xx);
    r3.xy = (r10.xy) * (r7.xx) + (-(r11.xy));
    r0.xyz = (-(r0.xyz)) * (r9.yyy);
    r1.xyz = (r3.yyy) * (v2.xyz);
    r0.xyz = (r9.xxx) * (v1.xyz) + (r0.xyz);
    r1.xyz = (v1.xyz) * (r3.xxx) + (r1.xyz);
    r0.xyz = (r0.xyz) + (v4.xyz);
    r1.w = (r4.y) * (r7.y);
    r2.w = dot(r0.xyz, r0.xyz);
    r1.w = (r4.x) * (r7.x) + (-(r1.w));
    r2.w = rsqrt(r2.w);
    r1.xyz = float3(((r1.w) >= 0.0f ? (r1.x) : (-(r1.x))), ((r1.w) >= 0.0f ? (r1.y) : (-(r1.y))), ((r1.w) >= 0.0f ? (r1.z) : (-(r1.z))));
    r4.xyz = (r0.xyz) * (r2.www);
    r1.w = dot(r6.xyz, r5.xyz);
    r3.w = dot(r1.xyz, r4.xyz);
    r1.w = (r1.w) + (c2.z);
    r1.xyz = (r3.www) * (-(r4.xyz)) + (r1.xyz);
    r4.w = saturate((r1.w) * (c2.w));
    r3.xyz = normalize(r1.xyz);
    r3.w = (r4.w) * (c3.x) + (c3.y);
    r7.xyz = (r4.zxy) * (r3.yzx);
    r1 = tex2D(s2, r8.xy);
    r9.xy = (r1.wy) * (c0.xy) + (c0.zw);
    r1.xyz = (r4.yzx) * (r3.zxy) + (-(r7.xyz));
    r4.xy = (r9.xy) * (c[32].xx);
    r1.w = (r4.w) * (r4.w);
    r1.xyz = (-(r1.xyz)) * (r4.yyy);
    r5.w = (r3.w) * (r1.w);
    r1.xyz = (r4.xxx) * (r3.xyz) + (r1.xyz);
    r0.xyz = (r0.xyz) * (r2.www) + (r1.xyz);
    r1.w = dot(-(v3.xyz), -(v3.xyz));
    r3.xyz = normalize(r0.xyz);
    r0.z = rsqrt(r1.w);
    r1.xyz = (-(v3.xyz)) * (r0.zzz) + (r6.xyz);
    r4.xyz = (r0.zzz) * (-(v3.xyz));
    r3.w = saturate(dot(r3.xyz, r6.xyz));
    r0.xyz = normalize(r1.xyz);
    r1.w = saturate(dot(r0.xyz, r3.xyz));
    r0.x = dot(r0.xyz, r4.xyz);
    r0.y = (r1.w) + (r1.w);
    r0.z = (r1.w) * (r1.w) + (c2.x);
    r0.x = 1.0f / (r0.x);
    r4.w = saturate(dot(r4.xyz, r3.xyz));
    r0.y = (r0.y) * (r0.x);
    r0.x = min(r4.w, r3.w);
    r0.x = saturate((r0.y) * (r0.x));
    r0.z = 1.0f / (r0.z);
    r1 = tex2D(s3, r8.xy);
    r1.w = (r1.x) * (r1.x);
    r0.y = (-(r0.z)) + (c1.x);
    r2.w = 1.0f / (r1.w);
    r0.x = (r3.w) * (r0.x);
    r0.y = (r0.y) * (r2.w);
    r0.x = rsqrt(r0.x);
    r0.y = (r0.y) * (c2.y);
    r0.z = (r0.z) * (r0.z);
    r0.y = exp2(r0.y);
    r1.z = 1.0f / (r0.x);
    r1.w = (r0.z) * (r0.y);
    r0.xyz = (r2.xyz) * (r5.www);
    r1.w = (r1.z) * (r1.w);
    r1.xyz = (r0.xyz) * (r1.www);
    r1.w = dot(-(r4.xyz), r3.xyz);
    r1.xyz = (r2.www) * (r1.xyz);
    r1.w = (r1.w) + (r1.w);
    r2.xyz = (r1.xyz) * (c3.www);
    r1.xyz = (r3.xyz) * (-(r1.www)) + (-(r4.xyz));
    r2.w = saturate(dot(r4.xyz, r5.xyz));
    r1.w = c[29].x;
    r1 = texCUBElod(s15, r1);
    r1.w = (r4.w) + (r2.w);
    r2.w = (r1.w) * (c1.w);
    r1.w = max(c4.x, r2.w);
    r1.w = 1.0f / (r1.w);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r1.w = rsqrt(r1.w);
    r1.xyz = (r1.xyz) * (c4.yyy);
    r1.w = 1.0f / (r1.w);
    r4.xyz = (r2.xyz) * (r1.www) + (r1.xyz);
    r2 = tex2D(s5, r8.xy);
    r1 = tex2D(s4, r8.xy);
    r2.w = max(abs(r3.y), abs(r3.z));
    r1.w = max(abs(r3.x), r2.w);
    r3.xyz = (r3.xyz) * (c[5].xyz);
    r1.w = 1.0f / (r1.w);
    r5.xyz = (r2.xyz) * (c[33].xxx);
    r2.xyz = (r3.xyz) * (r1.www) + (v0.xyz);
    r2 = tex3D(s11, r2.xyz);
    r3.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = (r4.xyz) * (r5.xyz);
    r3.xyz = (r3.xyz) * (c3.zzz);
    r0.xyz = (r3.www) * (r0.xyz) + (r3.xyz);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r0.xyz = (r0.xyz) * (r1.xyz) + (r2.xyz);
    r0.xyz = (r0.xyz) + (-(v5.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v5.xyz);
    r0.xyz = max(((r0.xyz) * (c[28].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c1.x;

    return oC0;
}
