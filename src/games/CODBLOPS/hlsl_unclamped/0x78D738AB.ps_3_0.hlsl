// Mechanically reconstructed from 0x78D738AB.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler2D s8 : register(s8);
sampler2D s9 : register(s9);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(6.28318548f, 0.25f, 3.14159274f, -3.14159274f);
    const float4 c2 = float4(0.159154937f, 0.5f, -0.5f, 9.99999975e-06f);
    const float4 c3 = float4(0.5f, 2.0f, -1.0f, 1.0f);
    const float4 c4 = float4(1e-15f, 1.44269502f, 0.100000001f, 8.0f);
    const float4 c12 = float4(31.875f, 3.5f, 1.0f, 12.0f);
    const float4 c13 = float4(-0.00537109375f, 30.0f, 0.000244140625f, 0.0f);
    const float4 c14 = float4(0.600000024f, 2.5f, -2.5f, 5.0f);
    const float4 c15 = float4(0.833333313f, 1.5f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = tex2D(s3, v4.xy);
    r0.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r0.xy = (r0.xy) * (c3.xx) + (c3.xx);
    r0.xy = (r0.xy) * (c3.yy) + (c3.zz);
    r1 = tex2D(s2, v4.xy);
    r0.zw = (r1.wy) * (c0.xy) + (c0.zw);
    r0.zw = (r0.zw) * (c3.xx) + (c3.xx);
    r0.zw = (r0.zw) * (c3.yy) + (c3.zz);
    r1 = tex2D(s1, v4.xy);
    r2.xyz = (r1.xyz) * (c1.xxy);
    r2.w = lerp(r1.x, r1.y, c3.x);
    r1.x = (r2.w) * (c14.y) + (c14.z);
    r1.yw = c3.yw;
    r1.yz = (c[7].ww) * (r1.yy) + (r2.xy);
    r1.z = (r1.z) * (c2.x) + (c2.y);
    r1.y = (r1.y) + (c1.z);
    r1.y = (r1.y) * (c2.x) + (c2.y);
    r1.y = frac(r1.y);
    r1.y = (r1.y) * (c1.x) + (c1.w);
    r3.x = cos(r1.y);
    r1.y = (r3.x) * (c2.y) + (c2.z);
    r1.z = frac(r1.z);
    r1.z = (r1.z) * (c1.x) + (c1.w);
    r3.x = cos(r1.z);
    r1.z = (r3.x) * (c3.x) + (c3.x);
    r1.w = (r1.w) + (-(c[8].y));
    r1.w = saturate(r1.w);
    r1.z = (r1.z) * (r1.w);
    r1.y = (r1.w) * (r1.y) + (c3.w);
    r0.zw = (r0.zw) * (r1.zz);
    r0.xy = (r1.yy) * (r0.xy) + (r0.zw);
    r3 = tex2D(s4, v4.xy);
    r0.zw = (r3.wy) * (c0.xy) + (c0.zw);
    r0.zw = (r0.zw) * (c3.xx) + (c3.xx);
    r0.zw = (r0.zw) * (c3.yy) + (c3.zz);
    r1.yz = lerp(r0.zw, r0.xy, c[8].zz);
    r0.x = dot(v2.xyz, v2.xyz);
    r0.x = rsqrt(r0.x);
    r0.yzw = (r0.xxx) * (v2.zxy);
    r2.xyw = (r0.yzw) * (v3.yzx);
    r2.xyw = (r0.wyz) * (v3.zxy) + (-(r2.xyw));
    r2.xyw = (r2.xyw) * (v3.www);
    r2.xyw = (r1.zzz) * (r2.xyw);
    r1.yzw = (r1.yyy) * (v3.xyz) + (r2.xyw);
    r1.yzw = (v2.xyz) * (r0.xxx) + (r1.yzw);
    r3.xyz = normalize(r1.yzw);
    r0.x = dot(-(v1.xyz), -(v1.xyz));
    r0.x = rsqrt(r0.x);
    r4.xyz = normalize(c[17].xyz);
    r1.yzw = (-(v1.xyz)) * (r0.xxx) + (r4.xyz);
    r2.xyw = (r0.xxx) * (-(v1.xyz));
    r5.xyz = normalize(r1.yzw);
    r0.x = saturate(dot(r3.xyz, r5.xyz));
    r1.y = (r0.x) * (r0.x) + (c4.x);
    r0.x = (r0.x) + (r0.x);
    r1.y = 1.0f / (r1.y);
    r1.z = (-(r1.y)) + (c3.w);
    r1.y = (r1.y) * (r1.y);
    r5 = tex2D(s7, v4.xy);
    r1.w = max(c2.w, r5.x);
    r5.w = (r5.x) * (c4.w);
    r1.w = (r1.w) * (r1.w);
    r1.w = 1.0f / (r1.w);
    r1.z = (r1.z) * (r1.w);
    r1.w = (r1.w) * (c1.y);
    r1.z = (r1.z) * (c4.y);
    r1.z = exp2(r1.z);
    r1.y = (r1.y) * (r1.z);
    r1.z = saturate(dot(r3.xyz, r2.xyw));
    r3.w = 1.0f / (r1.z);
    r0.x = (r0.x) * (r3.w);
    r3.w = saturate(dot(r3.xyz, r4.xyz));
    r4.w = min(r1.z, r3.w);
    r0.x = saturate((r0.x) * (r4.w));
    r0.x = (r3.w) * (r0.x);
    r0.x = rsqrt(r0.x);
    r0.x = 1.0f / (r0.x);
    r0.x = (r1.y) * (r0.x);
    r1.y = max(abs(r0.w), abs(r0.y));
    r4.w = max(abs(r0.z), r1.y);
    r1.y = 1.0f / (r4.w);
    r6.xyz = (r0.zwy) * (c[5].xyz);
    r6.xyz = (r6.xyz) * (r1.yyy) + (v5.xyz);
    r6 = tex3D(s11, r6.xyz);
    r6.xyz = (r6.www) * (c[19].xyz);
    r7.xyz = (r6.www) * (c[18].xyz);
    r7.xyz = (r7.xyz) * (c[6].xxx);
    r6.xyz = (r6.xyz) * (c[6].yyy);
    r6.xyz = (r0.xxx) * (r6.xyz);
    r0.x = saturate(dot(r0.zwy, r2.xyw));
    r0.y = saturate(r0.y);
    r0.x = (r1.z) + (r0.x);
    r0.x = (r0.x) * (c3.x);
    r1.y = max(c4.z, r0.x);
    r0.x = 1.0f / (r1.y);
    r0.x = rsqrt(r0.x);
    r0.x = 1.0f / (r0.x);
    r0.xzw = (r6.xyz) * (r0.xxx);
    r0.xzw = (r1.www) * (r0.xzw);
    r6.xyz = normalize(v1.xyz);
    r1.y = dot(r6.xyz, r3.xyz);
    r1.z = (r1.y) + (r1.y);
    r1.y = saturate(r1.y);
    r1.y = (-(r1.y)) + (c3.w);
    r5.xyz = (r3.xyz) * (-(r1.zzz)) + (r6.xyz);
    r5 = texCUBElod(s15, r5);
    r5.xyz = (r5.xyz) * (r5.xyz);
    r5.xyz = (r5.xyz) * (c4.www);
    r6 = tex3D(s11, v5.xyz);
    r6.xyz = (r6.xyz) * (r6.xyz);
    r5.xyz = (r5.xyz) * (r6.xyz);
    r5.xyz = (r5.xyz) * (c[21].xxx);
    r5.xyz = (r5.xyz) * (c12.xxx);
    r1.z = (r1.y) * (r1.y);
    r1.y = (r1.y) * (r1.z);
    r6 = tex2D(s6, v4.xy);
    r1.z = (r6.w) * (c12.y) + (c12.z);
    r1.z = 1.0f / (r1.z);
    r1.y = (r1.y) * (r1.z);
    r8.xyz = lerp(r6.xyz, c3.www, r1.yyy);
    r1.yzw = (r5.xyz) * (r8.xyz);
    r0.xzw = (r0.xzw) * (c1.yyy) + (r1.yzw);
    r1.yzw = (-(r0.xzw)) + (c3.www);
    r0.xzw = (c[8].xxx) * (r1.yzw) + (r0.xzw);
    r1.yzw = (r6.xyz) * (r0.xzw);
    r0.xzw = (r2.zzz) * (r0.xzw);
    r2.z = dot(-(r4.xyz), r3.xyz);
    r2.z = (r2.z) + (r2.z);
    r5.xyz = (r3.xyz) * (-(r2.zzz)) + (-(r4.xyz));
    r2.z = dot(r4.xyz, -(r2.xyw));
    r2.x = saturate(dot(r5.xyz, r2.xyw));
    r2.y = saturate((r2.z) * (c3.x) + (c3.x));
    r2.y = (r2.y) + (r2.y);
    r4.x = pow(abs(r2.y), c12.w);
    r2.y = (r4.x) * (c13.x) + (c13.y);
    r4.y = pow(abs(r2.x), r2.y);
    r2.x = c[6].x;
    r2.xyz = (r2.xxx) * (c[18].xyz);
    r5.xyz = max(r2.xyz, c3.www);
    r2.xyz = (r4.yyy) * (r5.xyz);
    r5 = tex2D(s9, v4.xy);
    r2.w = (r5.x) * (c[25].x);
    r2.w = (r4.x) * (r2.w);
    r2.w = (r2.w) * (c13.z);
    r2.xyz = (r2.xyz) * (r2.www);
    r2.w = max(abs(r3.y), abs(r3.z));
    r4.x = max(abs(r3.x), r2.w);
    r3.xyz = (r3.xyz) * (c[5].xyz);
    r2.w = 1.0f / (r4.x);
    r3.xyz = (r3.xyz) * (r2.www) + (v5.xyz);
    r4 = tex3D(s11, r3.xyz);
    r0.y = (r0.y) * (r4.w);
    r3.xyz = (r4.xyz) * (r4.xyz);
    r3.xyz = (r3.xyz) * (c12.xxx);
    r2.xyz = (r2.xyz) * (r0.yyy) + (r3.xyz);
    r3.xyz = (r3.www) * (r7.xyz) + (r3.xyz);
    r2.xyz = (r3.www) * (r7.xyz) + (r2.xyz);
    r4 = tex2D(s8, v4.xy);
    r4.xyz = (r4.xyz) * (r4.xyz);
    r1.yzw = (r2.xyz) * (r4.xyz) + (r1.yzw);
    r2.xyz = max(r1.yzw, c13.www);
    r4 = tex2D(s5, v4.xy);
    r1.yzw = (r4.xyz) * (r4.xyz);
    r0.xyz = (r3.xyz) * (r1.yzw) + (r0.xzw);
    r1.yzw = max(r0.xyz, c13.www);
    r0.xyz = lerp(r2.xyz, r1.yzw, c14.xxx);
    r0.xyz = (r0.xyz) * (c[22].xyz) + (-(r2.xyz));
    r3.z = c[8].z;
    r3.x = c[23].x;
    r0.w = saturate((r3.z) * (r3.x) + (c[24].x));
    r0.w = (r0.w) * (c14.w) + (r1.x);
    r1.x = saturate((r0.w) * (c15.x));
    r0.w = (r0.w) + (-(c14.x));
    r0.w = saturate((r0.w) * (c15.y));
    r1.x = (r1.x) * (r1.x);
    r0.xyz = (r1.xxx) * (r0.xyz) + (r2.xyz);
    r2.xyz = lerp(r0.xyz, r1.yzw, r0.www);
    r2.w = c3.w;
    r0.x = dot(r2, c[10]);
    r0.y = dot(r2, c[11]);
    r0.z = dot(r2, c[20]);
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[9].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c3.w;

    return oC0;
}
