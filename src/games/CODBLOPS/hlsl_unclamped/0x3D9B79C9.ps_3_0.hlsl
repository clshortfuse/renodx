// Mechanically reconstructed from 0x3D9B79C9.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);

struct PS_INPUT
{
    float4 v0 : TEXCOORD5;
    float4 v1 : TEXCOORD6;
    float4 v2 : TEXCOORD7;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD9;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c1 = float4(6.28318548f, -3.14159274f, 0.0f, 1.0f);
    const float4 c2 = float4(-0.5f, 0.5f, 1.0f, 1.52587891e-05f);
    const float4 c3 = float4(0.707106769f, 0.5f, 0.25f, 0.75f);
    const float4 c4 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 oC0 = 0.0f;

    r1 = (v2.xyzx) * (c1.wwwz) + (c1.zzzw);
    r0.w = dot(r1, c[22]);
    r0.w = 1.0f / (r0.w);
    r0.x = dot(r1, c[11]);
    r0.y = dot(r1, c[20]);
    r0 = (r0.wwww) * (r0.xxyy);
    r2 = (r0) * (c[23].zwxy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.z = log2(abs(r2.z));
    r2.w = log2(abs(r2.w));
    r2 = (r2) * (c[24].xxxx);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = exp2(r2.z);
    r2.w = exp2(r2.w);
    r2.xy = (r2.zw) + (r2.xy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.xy = (r2.xy) * (c[24].yy);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = dot(r1, c[21]);
    r0.z = (r2.x) * (c[25].x);
    r0.y = (r2.y) * (c[25].y) + (-(r0.z));
    r0.z = c[25].y;
    r0.z = (r2.y) * (r0.z) + (-(c[24].z));
    r0.y = 1.0f / (r0.y);
    r2.xy = abs(r0.xw);
    r3.w = saturate((r0.z) * (r0.y));
    r1 = c[9];
    r1 = saturate((r2.zyxz) * (r1) + (c[10]));
    r3.x = (r1.w) * (r1.x);
    r3.yz = r1.yz;
    r1 = (r3) * (r3);
    r3 = (c[25].zzzz) * (r3) + (c[25].wwww);
    r1 = (r1) * (r3);
    r0.y = (r1.z) * (r1.y);
    r0.z = abs(c[24].w);
    r2.w = (r2.z) * (r2.z);
    r0.y = ((-(r0.z)) >= 0.0f ? (r0.y) : (r1.w));
    r0.z = dot(c[8].yz, r2.zw) + (c[8].x);
    r0.z = (r0.y) * (r0.z);
    r2.w = (r1.x) * (r0.z);
    r1 = c[26];
    r1.x = dot(r0.xw, r1.xy) + (c[7].x);
    r1.y = dot(r0.xw, r1.zw) + (c[7].z);
    r0 = tex2D(s0, r1.xy);
    r1.xyz = v0.xyz;
    r2.xyz = (r1.yzx) * (v3.zxy);
    r1.xyz = (v3.yzx) * (r1.zxy) + (-(r2.xyz));
    r2.xyz = (r1.xyz) * (v0.www);
    r1 = tex2D(s3, v4.xy);
    r3.xy = (r1.wy) * (c4.xy) + (c4.zw);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r1.xyz = (r2.xyz) * (r3.yyy);
    r0.xyz = (r2.www) * (r0.xyz);
    r1.xyz = (r3.xxx) * (v0.xyz) + (r1.xyz);
    r0.xyz = (r0.xyz) * (c[6].xyz);
    r3.xyz = (r1.xyz) + (v3.xyz);
    r1.xyz = normalize(-(v2.xyz));
    r2.xyz = normalize(r3.xyz);
    r4.xyz = (-(v2.xyz)) + (c[5].xyz);
    r0.w = dot(r1.xyz, r2.xyz);
    r3.xyz = normalize(r4.xyz);
    r0.w = (r0.w) * (-(r0.w)) + (c1.w);
    r1.w = saturate(dot(r1.xyz, r3.xyz));
    r0.w = rsqrt(r0.w);
    r1.xyz = (r0.xyz) * (r1.www);
    r1.w = 1.0f / (r0.w);
    r0.w = saturate(dot(r2.xyz, r3.xyz));
    r1.w = saturate((r1.w) * (c3.z));
    r1.xyz = (r1.xyz) * (r1.www);
    r0.xyz = (r0.xyz) * (r0.www);
    r3.xyz = (r0.xyz) * (c3.www) + (r1.xyz);
    r0 = tex2D(s4, v4.xy);
    r1.xy = (v4.xy) + (c2.xx);
    r1.z = c2.y;
    r1.xy = (r1.xy) * (c[31].xy) + (r1.zz);
    r1 = tex2D(s1, r1.xy);
    r5.xy = (r1.xy) * (c[32].xx);
    r1.w = (r5.y) * (c2.z) + (c2.y);
    r1.xyz = ddy(v4.xyx);
    r1.w = frac(r1.w);
    r4.xyz = (r1.xyy) * (r1.xyz);
    r2.w = (r1.w) * (c1.x) + (c1.y);
    r2.xyz = ddx(v4.xyx);
    r1.xy = float2(cos(r2.w), sin(r2.w));
    r1.w = (r5.x) * (r5.x);
    r1.xyz = (r1.xyx) * (r1.xyy);
    r2.xyz = (r2.xyz) * (r2.xyy) + (r4.xyz);
    r1.z = (r1.w) * (r1.z);
    r1.xy = (r1.ww) * (r1.xy) + (r1.ww);
    r2.xyz = (r1.xyz) * (c2.www) + (r2.xyz);
    r1.x = rsqrt(r2.x);
    r1.y = rsqrt(r2.y);
    r5.x = 1.0f / (r1.x);
    r5.y = 1.0f / (r1.y);
    r1.w = (r5.y) * (r5.x);
    r0.xyz = saturate((r3.xyz) * (r0.xyz));
    r1.w = 1.0f / (r1.w);
    r1.w = (r2.z) * (r1.w);
    r3.xy = frac(v4.xy);
    r1.x = ((-abs(r2.z)) >= 0.0f ? (c1.z) : (r1.w));
    r4.xy = (r3.xy) + (c2.xx);
    r1.y = dot(v1.xyz, v1.xyz);
    r1.w = dot(r4.xy, r4.xy) + (c2.x);
    r1.z = (r1.y) * (r1.w);
    r1.w = dot(v1.xy, r4.xy) + (c1.z);
    r2.z = (-(r2.x)) + (r2.y);
    r1.w = (r1.w) * (r1.w) + (-(r1.z));
    r1.z = (r1.x) * (-(r1.x)) + (c1.w);
    r1.w = rsqrt(r1.w);
    r1.z = rsqrt(r1.z);
    r1.w = 1.0f / (r1.w);
    r1.y = 1.0f / (r1.y);
    r1.w = dot(v1.xy, -(r4.xy)) + (r1.w);
    r1.z = 1.0f / (r1.z);
    r2.w = (r1.y) * (r1.w);
    r1.yw = c1.wz;
    r3.xy = (r2.ww) * (v1.xy);
    r1 = float4(((r2.z) >= 0.0f ? (r1.x) : (r1.y)), ((r2.z) >= 0.0f ? (r1.y) : (r1.x)), ((r2.z) >= 0.0f ? (r1.z) : (r1.w)), ((r2.z) >= 0.0f ? (r1.w) : (r1.z)));
    r2.xy = ddx(r3.xy);
    r4.xy = (v1.xy) * (r2.ww) + (r4.xy);
    r2.xy = (r5.xy) * (r1.xy) + (r2.xy);
    r1.xy = ddy(r3.xy);
    r2.xy = (r2.xy) * (c[33].xy);
    r1.xy = (r5.xy) * (r1.zw) + (r1.xy);
    r2.xy = (r2.xy) * (c3.xx);
    r3.xy = (r1.xy) * (c[33].xy);
    r1.xy = (r4.xy) * (c[33].xy);
    r3.xy = (r3.xy) * (c3.xx);
    r1.xy = (r1.xy) * (c3.xx) + (c3.yy);
    r1 = tex2Dgrad(s2, r1.xy, r2.xy, r3.xy);
    r0.xyz = (r1.xyz) * (-(c[34].xyz)) + (r0.xyz);
    r2.xyz = (r1.xyz) * (c[34].xyz);
    r1.x = c1.w;
    r1.w = (r1.w) * (-(c[34].w)) + (r1.x);
    r1.xyz = normalize(v2.xyz);
    r0.xyz = (r0.www) * (r0.xyz) + (r2.xyz);
    r1.z = dot(c[27].xyz, r1.xyz);
    r2.w = saturate((c[29].y) * (r1.z) + (c[29].x));
    r1.xyz = c[0].xyz;
    r1.xyz = (-(r1.xyz)) + (c[28].xyz);
    r0.w = (-(r0.w)) + (c1.w);
    r1.xyz = (r2.www) * (r1.xyz) + (c[0].xyz);
    r0.w = (r1.w) * (-(r0.w)) + (c1.w);
    r1.xyz = (r1.xyz) * (c[0].www);
    r0.xyz = (r0.xyz) * (r0.www) + (-(r1.xyz));
    r0.xyz = (v1.www) * (r0.xyz) + (r1.xyz);
    r0.xyz = max(((r0.xyz) * (c[30].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.w = r0.w;
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);

    return oC0;
}
