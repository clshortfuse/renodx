// Mechanically reconstructed from 0x17BEB16E.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD2;
    float4 v5 : TEXCOORD3;
    float4 v6 : TEXCOORD5;
    float4 v7 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c0 = float4(-0.5f, 1.0f, 0.0f, 8.0f);
    const float4 c1 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c2 = float4(31.875f, 0.797884583f, -2.0f, 3.0f);
    const float4 c3 = float4(0.797884583f, 1.0f, 0.0009765625f, 0.0f);
    const float4 c4 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    const float4 c12 = float4(0.125f, 0.25f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 r12 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = tex2D(s1, v1.xy);
    r1.xy = (r0.wy) * (c1.xy) + (c1.zw);
    r0 = tex2D(s0, v1.xy);
    r8.z = (r0.w) * (v0.w) + (c0.x);
    r5.xyz = (-(v6.xyz)) + (c[6].xyz);
    r2.xy = float2(((r8.z) >= 0.0f ? (r1.x) : (c0.z)), ((r8.z) >= 0.0f ? (r1.y) : (c0.z)));
    r8.y = dot(r5.xyz, r5.xyz);
    r1 = v2;
    r1.xyz = (r2.xxx) * (v5.xyz) + (r1.xyz);
    r4.w = rsqrt(r8.y);
    r1.xyz = (r2.yyy) * (v4.xyz) + (r1.xyz);
    r4.xyz = (r5.xyz) * (r4.www);
    r7.xyz = normalize(r1.xyz);
    r8.w = saturate(dot(r7.xyz, r4.xyz));
    r2 = tex2D(s2, v1.xy);
    r3 = float4(((r8.z) >= 0.0f ? (r2.x) : (c0.z)), ((r8.z) >= 0.0f ? (r2.y) : (c0.z)), ((r8.z) >= 0.0f ? (r2.z) : (c0.z)), ((r8.z) >= 0.0f ? (r2.w) : (c0.y)));
    r2.z = (r3.w) * (c2.y);
    r11.xyz = normalize(v6.xyz);
    r1.y = (r3.w) * (-(c3.x)) + (c3.y);
    r1.x = saturate(dot(r7.xyz, -(r11.xyz)));
    r1.z = (r8.w) * (r1.y) + (r2.z);
    r1.y = (r1.x) * (r1.y) + (r2.z);
    r7.w = (-(r1.x)) + (c0.y);
    r1.z = (r1.z) * (r1.y) + (c3.z);
    r1.z = 1.0f / (r1.z);
    r2.xyz = (r5.xyz) * (r4.www) + (-(r11.xyz));
    r2.w = (r8.w) * (r1.z);
    r1.xyz = normalize(r2.xyz);
    r8.x = 1.0f / (r4.w);
    r4.w = saturate(dot(r7.xyz, r1.xyz));
    r1.z = saturate(dot(r1.xyz, r4.xyz));
    r2.xy = (r3.ww) * (c4.xy) + (c4.zw);
    r1.y = exp2(r2.y);
    r1.z = (-(r1.z)) + (c0.y);
    r2.z = pow(abs(r4.w), r1.y);
    r1.x = (r1.z) * (r1.z);
    r1.y = (r1.y) * (c12.x) + (c12.y);
    r1.x = (r1.x) * (r1.x);
    r1.y = (r2.z) * (r1.y);
    r1.z = (r1.z) * (r1.x);
    r10.xyz = (r3.xyz) * (r3.xyz);
    r9.xyz = (r3.xyz) * (-(r3.xyz)) + (c0.yyy);
    r2.w = (r2.w) * (r1.y);
    r1.xyz = (r9.xyz) * (r1.zzz) + (r10.xyz);
    r1.xyz = (r2.www) * (r1.xyz);
    r2.y = c0.z;
    r6.xyz = float3(((r8.z) >= 0.0f ? (c[20].x) : (r2.y)), ((r8.z) >= 0.0f ? (c[20].y) : (r2.y)), ((r8.z) >= 0.0f ? (c[20].w) : (r2.y)));
    r6.w = 1.0f / (r2.x);
    r1.xyz = (r1.xyz) * (r6.zzz);
    r2 = tex2D(s4, v1.xy);
    r1.xyz = (r1.xyz) * (r2.www);
    r12.xy = c[9].xy;
    r3.xyz = (r12.yyy) * (c[8].xyz);
    r3.w = (r3.w) * (c0.w);
    r1.xyz = (r1.xyz) * (r3.xyz);
    r3.z = dot(c[10].yz, r8.xy) + (c[10].x);
    r4.xy = saturate((r8.xx) * (c[11].xy) + (c[11].zw));
    r3.xy = (r4.xy) * (r4.xy);
    r4.xy = (r4.xy) * (c2.zz) + (c2.ww);
    r4.xy = (r3.xy) * (r4.xy);
    r4.w = max(abs(r7.y), abs(r7.z));
    r4.z = (r3.z) * (r4.x);
    r3.z = max(abs(r7.x), r4.w);
    r4.w = 1.0f / (r3.z);
    r3.xyz = (r7.xyz) * (c[5].xyz);
    r5.w = (r4.y) * (r4.z);
    r3.xyz = (r3.xyz) * (r4.www) + (v7.xyz);
    r4 = tex3D(s11, r3.xyz);
    r4.w = (r5.w) * (r4.w);
    r3.xy = (v1.xy) * (c[21].xy);
    r5 = tex2D(s3, r3.xy);
    r3.xyz = (r5.xyz) + (c0.xxx);
    r0.xyz = saturate((r3.xyz) * (r0.www) + (r0.xyz));
    r0.w = c0.y;
    r3.xyz = (r4.xyz) * (r4.xyz);
    r0 = float4(((r8.z) >= 0.0f ? (r0.x) : (c0.z)), ((r8.z) >= 0.0f ? (r0.y) : (c0.z)), ((r8.z) >= 0.0f ? (r0.z) : (c0.z)), ((r8.z) >= 0.0f ? (r0.w) : (c0.z)));
    r4.xyz = (r1.xyz) * (r4.www);
    r0.xyz = (r0.xyz) * (v0.xyz);
    r1.xyz = (r0.xyz) * (r0.xyz);
    r0.xyz = (r6.yyy) * (r3.xyz);
    r0.xyz = (r2.www) * (r0.xyz);
    r2.w = dot(r11.xyz, r7.xyz);
    r8.xyz = (r0.xyz) * (c2.xxx);
    r0.z = (r2.w) + (r2.w);
    r5.xyz = (r12.xxx) * (c[7].xyz);
    r3.xyz = (r7.xyz) * (-(r0.zzz)) + (r11.xyz);
    r3 = texCUBElod(s15, r3);
    r0.xyz = (r3.xyz) * (r3.xyz);
    r7.xyz = (r8.www) * (r5.xyz);
    r5.xyz = (r0.xyz) * (c0.www);
    r3 = tex3D(s11, v7.xyz);
    r2.w = (r7.w) * (r7.w);
    r0.xyz = (r3.xyz) * (r3.xyz);
    r2.w = (r7.w) * (r2.w);
    r0.xyz = (r5.xyz) * (r0.xyz);
    r2.w = (r6.w) * (r2.w);
    r0.xyz = (r0.xyz) * (c2.xxx);
    r5.xyz = (r9.xyz) * (r2.www) + (r10.xyz);
    r3.xyz = (r4.www) * (r7.xyz) + (r8.xyz);
    r0.xyz = (r0.xyz) * (r5.xyz);
    r1.xyz = (r1.xyz) * (r3.xyz) + (r4.xyz);
    r0.xyz = (r6.xxx) * (r0.xyz);
    r2.xyz = (r0.xyz) * (r2.xyz) + (r1.xyz);
    r2.w = c0.y;
    r0.x = dot(r2, c[23]);
    r0.y = dot(r2, c[24]);
    r0.z = dot(r2, c[25]);
    r0.xyz = (r0.xyz) + (-(v3.xyz));
    r0.xyz = (r1.www) * (r0.xyz) + (v3.xyz);
    r0.xyz = max(((r0.xyz) * (c[22].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.w = rsqrt(r0.w);
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = 1.0f / (r0.w);

    return oC0;
}
