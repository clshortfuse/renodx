// Mechanically reconstructed from 0x0F4D6751.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
    float4 v4 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(0.5f, -28.0f, 30.0f, 1.0f);
    const float4 c1 = float4(8.0f, 31.875f, 4.0f, 2.0f);
    const float4 c2 = float4(-2.0f, 3.0f, 0.0f, 0.25f);
    const float4 c3 = float4(0.38647899f, 0.392856985f, 0.364796013f, 15.0f);
    const float4 c4 = float4(0.0399999991f, 0.782500029f, 0.00749999983f, 1.00750005f);
    const float4 c12 = float4(81.2394867f, 17.3480244f, 37.3498383f, 59.3948402f);
    const float4 c13 = float4(0.0f, 1.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = c12;
    r0.x = dot(c[28].wwww, r0);
    r0.x = frac(r0.x);
    r0.w = c[28].w;
    r1.x = dot(r0.xwww, c12);
    r0.y = frac(r1.x);
    r1.x = dot(r0.xyww, c12);
    r0.z = frac(r1.x);
    r1.x = dot(r0, c12);
    r0.w = frac(r1.x);
    r1.yzw = r0.yzw;
    r0.x = dot(r0, c12);
    r1.x = frac(r0.x);
    r0.x = dot(r1, c12);
    r1.y = frac(r0.x);
    r0.x = 1.0f / (c[35].x);
    r0.y = 1.0f / (c[35].y);
    r0.xy = (v3.xy) * (r0.xy) + (r1.xy);
    r0 = tex2D(s2, r0.xy);
    r0.w = c[28].w;
    r1.x = (r0.w) * (c3.w);
    r1.x = frac(r1.x);
    r1.x = (r0.w) * (c3.w) + (-(r1.x));
    r1.y = (r1.x) * (c4.x) + (v3.y);
    r1.x = c4.y;
    r1 = tex2D(s3, r1.xy);
    r1.x = (r1.x) * (-(c4.z)) + (v3.x);
    r1.x = (r1.x) * (c4.w) + (-(v3.x));
    r1.x = (c[36].x) * (r1.x) + (v3.x);
    r1.z = (-(r1.x)) + (c0.w);
    r1.zw = float2(((r1.z) >= 0.0f ? (c13.x) : (c13.y)), ((r1.x) >= 0.0f ? (c13.x) : (c13.y)));
    r1.x = saturate(r1.x);
    r1.z = (r1.z) + (r1.w);
    r1.w = frac(abs(v3.y));
    r1.y = ((v3.y) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2 = tex2D(s4, r1.xy);
    r1.xyw = (r2.xyz) * (r2.xyz);
    r1.xyz = float3(((-(r1.z)) >= 0.0f ? (r1.x) : (c2.z)), ((-(r1.z)) >= 0.0f ? (r1.y) : (c2.z)), ((-(r1.z)) >= 0.0f ? (r1.w) : (c2.z)));
    r1.w = abs(c[28].w);
    r1.w = frac(r1.w);
    r2.y = ((c[28].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2.x = c2.z;
    r2 = tex2D(s3, r2.xy);
    r1.w = saturate((r2.x) * (c[34].x));
    r2.xyz = lerp(r1.xyz, r0.xyz, r1.www);
    r0.x = (r0.w) * (c[37].x);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r0.y = (c[41].x) + (-(v3.y));
    r0.y = (r0.y) + (c0.w);
    r0.x = (r0.x) + (r0.y);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r1.x = pow(abs(r0.x), c[40].x);
    r0.w = c0.w;
    r0.x = (r1.x) * (-(c[39].x)) + (r0.w);
    r1.xyz = lerp(c[38].xyz, r2.xyz, r0.xxx);
    r2.xy = (c[27].xy) * (v4.ww);
    r2.zw = c2.zz;
    r3 = (r2.xyww) + (v4);
    r2 = (-(r2)) + (v4);
    r2 = tex2Dproj(s1, r2);
    r2.y = r2.x;
    r3 = tex2Dproj(s1, r3);
    r2.x = r3.x;
    r3.xy = (c[27].zw) * (v4.ww);
    r3.zw = c2.zz;
    r4 = (r3.xyww) + (v4);
    r3 = (-(r3)) + (v4);
    r3 = tex2Dproj(s1, r3);
    r2.w = r3.x;
    r3 = tex2Dproj(s1, r4);
    r2.z = r3.x;
    r0.x = dot(r2, c2.wwww);
    r2 = tex2D(s12, v3.zw);
    r2.xzw = (c[20].xyz) + (-(v1.xyz));
    r0.y = dot(r2.xzw, r2.xzw);
    r0.y = rsqrt(r0.y);
    r2.xzw = (r2.xzw) * (r0.yyy);
    r0.y = 1.0f / (r0.y);
    r1.w = dot(r2.xzw, c[23].xyz);
    r1.w = saturate((r1.w) * (c[24].x) + (c[24].y));
    r1.w = (r1.w) * (c[24].w);
    r3.x = lerp(r2.y, r0.x, r1.w);
    r0.z = (r0.y) * (r0.y);
    r0.x = dot(c[25].yz, r0.yz) + (c[25].x);
    r0.yz = saturate((r0.yy) * (c[26].xy) + (c[26].zw));
    r3.yz = (r0.yz) * (r0.yz);
    r0.yz = (r0.yz) * (c2.xx) + (c2.yy);
    r0.yz = (r3.yz) * (r0.yz);
    r0.x = (r0.x) * (r0.y);
    r0.x = (r0.z) * (r0.x);
    r0.x = (r3.x) * (r0.x);
    r0.xyz = (r0.xxx) * (c[21].xyz);
    r0.xyz = (r0.xyz) * (c[22].yyy);
    r3.xyz = normalize(v1.xyz);
    r1.w = dot(r2.xzw, r3.xyz);
    r1.w = saturate((r1.w) * (c0.x) + (c0.x));
    r2.y = (r1.w) * (c0.y) + (c0.z);
    r1.w = (r1.w) + (c0.w);
    r4.xyz = normalize(v2.xyz);
    r3.w = dot(-(r2.xzw), r4.xyz);
    r3.w = (r3.w) + (r3.w);
    r2.xzw = (r4.xyz) * (-(r3.www)) + (-(r2.xzw));
    r2.x = saturate(dot(r2.xzw, -(r3.xyz)));
    r3.w = pow(abs(r2.x), r2.y);
    r0.xyz = (r0.xyz) * (r3.www);
    r0.xyz = (r1.www) * (r0.xyz);
    r1.w = dot(r3.xyz, r4.xyz);
    r1.w = (r1.w) + (r1.w);
    r2.xyz = (r4.xyz) * (-(r1.www)) + (r3.xyz);
    r2 = texCUBE(s15, r2.xyz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = (r2.xyz) * (c1.xxx);
    r3.xy = (c0.wx) * (v3.zw);
    r3 = tex2D(s13, r3.xy);
    r5 = tex2D(s14, v3.zw);
    r1.w = (r5.x) * (c1.y);
    r3.xy = (r3.xz) * (r1.ww);
    r2.w = (r5.x) * (c1.y) + (-(r3.x));
    r3.xw = (r2.xz) * (r3.xy);
    r1.w = (r3.z) * (-(r1.w)) + (r2.w);
    r3.y = (r2.y) * (r1.w);
    r2.xyz = (r3.xyw) * (c[33].xxx);
    r2.xyz = (r2.xyz) * (c1.zwz);
    r0.xyz = (r0.xyz) * (c3.xyz) + (r2.xyz);
    r0.xyz = (r1.xyz) + (r0.xyz);
    r1 = (c[6]) + (-(v1.yyyy));
    r2 = (r1) * (r1);
    r3 = (c[5]) + (-(v1.xxxx));
    r2 = (r3) * (r3) + (r2);
    r5 = (c[7]) + (-(v1.zzzz));
    r2 = (r5) * (r5) + (r2);
    r6.x = rsqrt(r2.x);
    r6.y = rsqrt(r2.y);
    r6.z = rsqrt(r2.z);
    r6.w = rsqrt(r2.w);
    r2 = saturate((r2) * (c[8]) + (r0.wwww));
    r1 = (r1) * (r6);
    r1 = (r4.yyyy) * (r1);
    r3 = (r3) * (r6);
    r5 = (r5) * (r6);
    r1 = (r3) * (r4.xxxx) + (r1);
    r1 = saturate((r5) * (r4.zzzz) + (r1));
    r1 = (r2) * (r1);
    r2.x = dot(c[9], r1);
    r2.y = dot(c[10], r1);
    r2.z = dot(c[11], r1);
    r0.xyz = (r0.xyz) * (r2.xyz) + (r0.xyz);
    r0.w = c0.w;
    r1.x = dot(r0, c[30]);
    r1.y = dot(r0, c[31]);
    r1.z = dot(r0, c[32]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[29].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c0.w;

    return oC0;
}
