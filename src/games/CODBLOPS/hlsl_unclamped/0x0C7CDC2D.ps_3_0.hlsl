// Mechanically reconstructed from 0x0C7CDC2D.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD2;
    float4 v4 : TEXCOORD3;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD6;
    float4 v7 : COLOR1;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c1 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c2 = float4(0.200000003f, 0.600000024f, 0.400000006f, 8.0f);
    const float4 c3 = float4(31.875f, 4.0f, -2.0f, 0.0009765625f);
    const float4 c4 = float4(0.125f, 0.25f, -2.0f, 3.0f);
    const float4 c12 = float4(0.797884583f, 1.0f, 0.5f, 0.0f);
    const float4 c13 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    const float4 c14 = float4(2.0f, -1.0f, 0.0f, 1.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 r12 = 0.0f;
    float4 r13 = 0.0f;
    float4 r14 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = tex2D(s1, v1.xy);
    r2.xy = (r0.wy) * (c1.xy) + (c1.zw);
    r0 = tex2D(s4, v6.xy);
    r3.xy = (r0.wy) * (c1.xy) + (c1.zw);
    r0 = (c14.xxxx) * (v7) + (c14.yyyy);
    r1.x = dot(r3.xy, r0.xy) + (c14.z);
    r1.y = dot(r3.xy, r0.zw) + (c14.z);
    r0 = tex2D(s2, v6.xy);
    r0.w = (r0.w) * (v0.y);
    r12.xy = lerp(r2.xy, r1.xy, r0.ww);
    r4.xyz = (-(v5.xyz)) + (c[5].xyz);
    r1.xyz = v2.xyz;
    r1.xyz = (r12.xxx) * (v4.xyz) + (r1.xyz);
    r6.y = dot(r4.xyz, r4.xyz);
    r1.xyz = (r12.yyy) * (v3.xyz) + (r1.xyz);
    r4.w = rsqrt(r6.y);
    r9.xyz = normalize(r1.xyz);
    r3.xyz = (r4.xyz) * (r4.www);
    r5.w = saturate(dot(r9.xyz, r3.xyz));
    r2 = tex2D(s5, v1.xy);
    r1 = tex2D(s6, v6.xy);
    r13.xy = lerp(r2.wy, r1.wy, r0.ww);
    r1.x = (r13.x) * (c12.x);
    r5.xyz = normalize(v5.xyz);
    r1.y = (r13.x) * (-(c12.x)) + (c12.y);
    r1.z = saturate(dot(r9.xyz, -(r5.xyz)));
    r1.w = (r5.w) * (r1.y) + (r1.x);
    r1.y = (r1.z) * (r1.y) + (r1.x);
    r1.w = (r1.w) * (r1.y) + (c3.w);
    r1.w = 1.0f / (r1.w);
    r6.w = (-(r1.z)) + (c14.w);
    r3.w = (r5.w) * (r1.w);
    r11.xyz = (r5.www) * (c[6].xyz);
    r2.xyz = (r4.xyz) * (r4.www) + (-(r5.xyz));
    r6.x = 1.0f / (r4.w);
    r1.xyz = normalize(r2.xyz);
    r2.z = saturate(dot(r9.xyz, r1.xyz));
    r2.xy = (r13.xx) * (c13.xy) + (c13.zw);
    r1.y = saturate(dot(r1.xyz, r3.xyz));
    r2.w = exp2(r2.y);
    r5.w = 1.0f / (r2.x);
    r1.z = pow(abs(r2.z), r2.w);
    r1.w = (r2.w) * (c4.x) + (c4.y);
    r2.w = (-(r1.y)) + (c14.w);
    r2.z = (r1.z) * (r1.w);
    r2.y = (r2.w) * (r2.w);
    r1 = tex2D(s3, v6.zw);
    r1.xyz = (r1.xyz) + (c14.yyy);
    r1.w = (r2.y) * (r2.y);
    r10.xyz = (v0.zzz) * (r1.xyz) + (c14.www);
    r2.w = (r2.w) * (r1.w);
    r1.xyz = (r10.xyz) * (c2.xxx);
    r8.xyz = (r1.xyz) * (r1.xyz);
    r7.xyz = (r1.xyz) * (-(r1.xyz)) + (c14.www);
    r1.w = (r3.w) * (r2.z);
    r1.xyz = (r7.xyz) * (r2.www) + (r8.xyz);
    r1.xyz = (r1.www) * (r1.xyz);
    r3.xy = saturate((r6.xx) * (c[9].xy) + (c[9].zw));
    r2.xy = (r3.xy) * (r3.xy);
    r3.xy = (r3.xy) * (c4.zz) + (c4.ww);
    r1.w = dot(c[8].yz, r6.xy) + (c[8].x);
    r2.xy = (r2.xy) * (r3.xy);
    r1.xyz = (r13.yyy) * (r1.xyz);
    r1.w = (r1.w) * (r2.x);
    r6.xyz = (r1.xyz) * (c[7].xyz);
    r7.w = (r2.y) * (r1.w);
    r1 = tex2D(s12, v1.zw);
    r2.xy = (v1.zw) * (c12.yz);
    r2 = tex2D(s13, r2.xy);
    r3.xy = (v1.zw) * (c12.yz) + (c12.wz);
    r4 = tex2D(s13, r3.xy);
    r2.w = r4.y;
    r3.xy = (r2.yw) * (c3.yy) + (c3.zz);
    r1.w = dot(r12.xy, r12.xy) + (c14.z);
    r1.z = dot(r3.xy, r3.xy) + (c14.z);
    r1.w = exp2(-(r1.w));
    r1.z = exp2(-(r1.z));
    r1.w = (r1.w) * (c2.y) + (c2.z);
    r1.x = (r1.z) * (c2.y) + (c2.z);
    r1.z = dot(r3.xy, r12.xy) + (c14.z);
    r1.x = (r1.w) * (r1.x);
    r3 = tex2D(s14, v1.zw);
    r14.xy = (r3.xy) * (c3.xx);
    r1.x = saturate((r1.z) * (r1.x) + (r1.x));
    r4.xy = (r4.xz) * (r14.yy);
    r1.z = (r3.y) * (c3.x) + (-(r4.x));
    r12.xz = (r4.xy) * (c3.yy);
    r1.z = (r4.z) * (-(r14.y)) + (r1.z);
    r2.xy = (r2.xz) * (r14.xx);
    r12.y = (r1.z) + (r1.z);
    r1.z = (r3.x) * (c3.x) + (-(r2.x));
    r4.xyz = (r1.xxx) * (r12.xyz);
    r1.z = (r2.z) * (-(r14.x)) + (r1.z);
    r3.xz = (r2.xy) * (c3.yy);
    r3.y = (r1.z) + (r1.z);
    r2.xyz = (r3.xyz) * (r1.www) + (r4.xyz);
    r1.w = (r7.w) * (r1.y);
    r1.xyz = (r13.yyy) * (r2.xyz);
    r6.xyz = (r6.xyz) * (r1.www);
    r4.xyz = (r1.www) * (r11.xyz) + (r1.xyz);
    r1 = tex2D(s0, v1.xy);
    r2.xyz = lerp(r1.xyz, r0.xyz, r0.www);
    r0.xyz = (r10.xyz) * (r2.xyz);
    r0.w = dot(r5.xyz, r9.xyz);
    r2.xyz = (r0.xyz) * (r0.xyz);
    r0.w = (r0.w) + (r0.w);
    r0.xyz = (r9.xyz) * (-(r0.www)) + (r5.xyz);
    r0.w = (r13.x) * (c2.w);
    r0 = texCUBElod(s15, r0);
    r0.w = (r6.w) * (r6.w);
    r0.w = (r6.w) * (r0.w);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r0.w = (r5.w) * (r0.w);
    r0.xyz = (r13.yyy) * (r0.xyz);
    r1.xyz = (r7.xyz) * (r0.www) + (r8.xyz);
    r2.xyz = (r2.xyz) * (r4.xyz) + (r6.xyz);
    r0.xyz = (r0.xyz) * (r1.xyz);
    r0.w = dot(c[10].xyz, r5.xyz);
    r1.xyz = (r3.xyz) * (r0.xyz);
    r0.w = saturate((c[20].y) * (r0.w) + (c[20].x));
    r0.xyz = c[0].xyz;
    r0.xyz = (-(r0.xyz)) + (c[11].xyz);
    r1.xyz = (r1.xyz) * (c2.www) + (r2.xyz);
    r0.xyz = (r0.www) * (r0.xyz) + (c[0].xyz);
    r1.xyz = (r0.xyz) * (-(c[0].www)) + (r1.xyz);
    r1.xyz = (r1.xyz) * (v2.www);
    r0.xyz = (r0.xyz) * (c[0].www) + (r1.xyz);
    r0.xyz = max(((r0.xyz) * (c[22].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c[21].w;

    return oC0;
}
