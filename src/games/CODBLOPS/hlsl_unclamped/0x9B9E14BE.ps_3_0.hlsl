// Mechanically reconstructed from 0x9B9E14BE.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD2;
    float4 v5 : TEXCOORD3;
    float4 v6 : TEXCOORD5;
    float4 v7 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(-0.5f, 1.0f, -1.0f, 0.200000003f);
    const float4 c2 = float4(0.0f, 0.600000024f, 0.400000006f, 8.0f);
    const float4 c3 = float4(0.125f, 0.25f, -2.0f, 3.0f);
    const float4 c4 = float4(31.875f, 4.0f, -2.0f, 0.0009765625f);
    const float4 c11 = float4(0.797884583f, 1.0f, 0.5f, 0.0f);
    const float4 c12 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 oC0 = 0.0f;

    r1 = tex2D(s0, v1.xy);
    r0 = tex2D(s2, v7.xy);
    r3.w = (r0.w) * (v0.y) + (c1.x);
    r2 = tex2D(s3, v7.zw);
    r2.xyz = (r2.xyz) + (c1.zzz);
    r1.xyz = float3(((r3.w) >= 0.0f ? (r0.x) : (r1.x)), ((r3.w) >= 0.0f ? (r0.y) : (r1.y)), ((r3.w) >= 0.0f ? (r0.z) : (r1.z)));
    r0.xyz = (v0.zzz) * (r2.xyz) + (c1.yyy);
    r1.xyz = (r1.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c1.www);
    r5.xyz = (r1.xyz) * (r1.xyz);
    r1.xyz = (-(v6.xyz)) + (c[5].xyz);
    r9.xyz = (r0.xyz) * (r0.xyz);
    r4.y = dot(r1.xyz, r1.xyz);
    r0.w = rsqrt(r4.y);
    r10.xyz = normalize(v6.xyz);
    r8.xyz = (r0.xyz) * (-(r0.xyz)) + (c1.yyy);
    r0.xyz = (r1.xyz) * (r0.www) + (-(r10.xyz));
    r1.xyz = (r1.xyz) * (r0.www);
    r3.xyz = normalize(r0.xyz);
    r4.x = 1.0f / (r0.w);
    r1.w = saturate(dot(r3.xyz, r1.xyz));
    r0 = tex2D(s1, v1.xy);
    r11.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r1.w = (-(r1.w)) + (c1.y);
    r0 = v2;
    r0.xyz = (r11.xxx) * (v5.xyz) + (r0.xyz);
    r2.w = (r1.w) * (r1.w);
    r2.xyz = (r11.yyy) * (v4.xyz) + (r0.xyz);
    r2.w = (r2.w) * (r2.w);
    r0.xyz = normalize(r2.xyz);
    r2.z = (r1.w) * (r2.w);
    r4.w = saturate(dot(r0.xyz, r1.xyz));
    r1 = tex2D(s4, v1.xy);
    r2.y = (r1.w) * (c11.x);
    r2.w = (r1.w) * (-(c11.x)) + (c11.y);
    r1.x = saturate(dot(r0.xyz, -(r10.xyz)));
    r1.z = (r4.w) * (r2.w) + (r2.y);
    r2.w = (r1.x) * (r2.w) + (r2.y);
    r2.xyz = (r8.xyz) * (r2.zzz) + (r9.xyz);
    r1.z = (r1.z) * (r2.w) + (c4.w);
    r8.w = (-(r1.x)) + (c1.y);
    r1.z = 1.0f / (r1.z);
    r1.x = (r4.w) * (r1.z);
    r6.xy = (r1.ww) * (c12.xy) + (c12.zw);
    r3.z = saturate(dot(r0.xyz, r3.xyz));
    r1.z = exp2(r6.y);
    r2.w = pow(abs(r3.z), r1.z);
    r1.z = (r1.z) * (c3.x) + (c3.y);
    r7.xyz = (r4.www) * (c[6].xyz);
    r1.z = (r2.w) * (r1.z);
    r7.w = 1.0f / (r6.x);
    r1.z = (r1.x) * (r1.z);
    r2.xyz = (r2.xyz) * (r1.zzz);
    r6.w = ((r3.w) >= 0.0f ? (c1.y) : (r1.y));
    r1.w = (r1.w) * (c2.w);
    r3.xy = saturate((r4.xx) * (c[9].xy) + (c[9].zw));
    r1.xy = (r3.xy) * (r3.xy);
    r3.xy = (r3.xy) * (c3.zz) + (c3.ww);
    r2.w = dot(c[8].yz, r4.xy) + (c[8].x);
    r3.xy = (r1.xy) * (r3.xy);
    r1.xyz = (r2.xyz) * (r6.www);
    r2.w = (r2.w) * (r3.x);
    r1.xyz = (r1.xyz) * (c[7].xyz);
    r3.w = (r3.y) * (r2.w);
    r2 = tex2D(s12, v1.zw);
    r5.w = (r3.w) * (r2.y);
    r6.xyz = (r1.xyz) * (r5.www);
    r1.xy = (v1.zw) * (-(c1.zx));
    r2 = tex2D(s13, r1.xy);
    r1.xy = (v1.zw) * (c11.yz) + (c11.wz);
    r4 = tex2D(s13, r1.xy);
    r2.w = r4.y;
    r1.z = dot(r11.xy, r11.xy) + (c2.x);
    r1.xy = (r2.yw) * (c4.yy) + (c4.zz);
    r2.w = exp2(-(r1.z));
    r1.z = dot(r1.xy, r1.xy) + (c2.x);
    r4.w = (r2.w) * (c2.y) + (c2.z);
    r2.w = exp2(-(r1.z));
    r1.z = dot(r1.xy, r11.xy) + (c2.x);
    r1.y = (r2.w) * (c2.y) + (c2.z);
    r3 = tex2D(s14, v1.zw);
    r11.xy = (r3.xy) * (c4.xx);
    r2.w = (r4.w) * (r1.y);
    r1.xy = (r4.xz) * (r11.yy);
    r2.w = saturate((r1.z) * (r2.w) + (r2.w));
    r2.y = (r3.y) * (c4.x) + (-(r1.x));
    r1.xz = (r1.xy) * (c4.yy);
    r1.y = (r4.z) * (-(r11.y)) + (r2.y);
    r1.y = (r1.y) + (r1.y);
    r2.xy = (r2.xz) * (r11.xx);
    r1.xyz = (r2.www) * (r1.xyz);
    r2.w = (r3.x) * (c4.x) + (-(r2.x));
    r3.xz = (r2.xy) * (c4.yy);
    r2.w = (r2.z) * (-(r11.x)) + (r2.w);
    r3.y = (r2.w) + (r2.w);
    r2.w = dot(r10.xyz, r0.xyz);
    r1.xyz = (r3.xyz) * (r4.www) + (r1.xyz);
    r2.w = (r2.w) + (r2.w);
    r4.xyz = (r6.www) * (r1.xyz);
    r1.xyz = (r0.xyz) * (-(r2.www)) + (r10.xyz);
    r1 = texCUBElod(s15, r1);
    r0.z = (r8.w) * (r8.w);
    r1.w = (r8.w) * (r0.z);
    r0.xyz = (r1.xyz) * (r1.xyz);
    r1.w = (r7.w) * (r1.w);
    r0.xyz = (r6.www) * (r0.xyz);
    r2.xyz = (r8.xyz) * (r1.www) + (r9.xyz);
    r1.xyz = (r5.www) * (r7.xyz) + (r4.xyz);
    r0.xyz = (r0.xyz) * (r2.xyz);
    r1.xyz = (r5.xyz) * (r1.xyz) + (r6.xyz);
    r0.xyz = (r3.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c2.www) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v3.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v3.xyz);
    r0.xyz = max(((r0.xyz) * (c[10].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c1.y;

    return oC0;
}
