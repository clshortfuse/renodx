// Mechanically reconstructed from 0x99E2B7B8.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(-0.5f, 1.0f, 0.0f, 0.200000003f);
    const float4 c1 = float4(8.0f, 0.797884583f, 1.0f, 0.5f);
    const float4 c2 = float4(1.0f, 0.5f, 0.0f, 31.875f);
    const float4 c3 = float4(4.0f, -2.0f, 0.600000024f, 0.400000006f);
    const float4 c4 = float4(0.0009765625f, 0.125f, 0.25f, 0.0f);
    const float4 c12 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    const float4 c13 = float4(-2.0f, 3.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 oC0 = 0.0f;

    r1 = tex2D(s0, v1.xy);
    r0 = (r1.xyzx) * (c0.yyyz) + (c0.zzzy);
    r4.w = (r1.w) * (v0.w) + (c0.x);
    r0 = float4(((r4.w) >= 0.0f ? (r0.x) : (c0.z)), ((r4.w) >= 0.0f ? (r0.y) : (c0.z)), ((r4.w) >= 0.0f ? (r0.z) : (c0.z)), ((r4.w) >= 0.0f ? (r0.w) : (c0.z)));
    r1.xy = (v4.ww) * (c[20].xy) + (v4.xy);
    r1.zw = v4.zw;
    r1 = tex2Dproj(s1, r1);
    r2.xy = (v4.ww) * (-(c[20].xy)) + (v4.xy);
    r2.zw = v4.zw;
    r2 = tex2Dproj(s1, r2);
    r1.y = r2.x;
    r2.xy = (v4.ww) * (c[20].zw) + (v4.xy);
    r2.zw = v4.zw;
    r2 = tex2Dproj(s1, r2);
    r1.z = r2.x;
    r2.xy = (v4.ww) * (-(c[20].zw)) + (v4.xy);
    r2.zw = v4.zw;
    r2 = tex2Dproj(s1, r2);
    r1.w = r2.x;
    r6.xyz = (r0.xyz) * (v0.xyz);
    r3.w = dot(r1, c4.zzzz);
    r1 = tex2D(s12, v1.zw);
    r0.xyz = (-(v5.xyz)) + (c[5].xyz);
    r4.y = dot(r0.xyz, r0.xyz);
    r1.z = rsqrt(r4.y);
    r2.xyz = (r0.xyz) * (r1.zzz);
    r1.w = dot(r2.xyz, c[8].xyz);
    r1.w = saturate((r1.w) * (c[9].x) + (c[9].y));
    r1.w = (r1.w) * (c[9].w);
    r4.x = 1.0f / (r1.z);
    r2.w = lerp(r1.y, r3.w, r1.w);
    r3.xy = saturate((r4.xx) * (c[11].xy) + (c[11].zw));
    r1.xy = (r3.xy) * (r3.xy);
    r3.xy = (r3.xy) * (c13.xx) + (c13.yy);
    r1.w = dot(c[10].yz, r4.xy) + (c[10].x);
    r1.xy = (r1.xy) * (r3.xy);
    r1.w = (r1.w) * (r1.x);
    r7.xyz = normalize(v5.xyz);
    r1.w = (r1.y) * (r1.w);
    r0.xyz = (r0.xyz) * (r1.zzz) + (-(r7.xyz));
    r3.xyz = normalize(r0.xyz);
    r0.xyz = normalize(v2.xyz);
    r9.z = (r2.w) * (r1.w);
    r3.w = saturate(dot(r0.xyz, r3.xyz));
    r1 = tex2D(s2, v1.xy);
    r4.xy = (r1.ww) * (c0.zy) + (c0.wz);
    r1.w = saturate(dot(r3.xyz, r2.xyz));
    r3.xy = float2(((r4.w) >= 0.0f ? (r4.x) : (c0.z)), ((r4.w) >= 0.0f ? (r4.y) : (c0.y)));
    r1.z = saturate(dot(r0.xyz, r2.xyz));
    r2.xy = (r3.yy) * (c12.xy) + (c12.zw);
    r7.w = ((r4.w) >= 0.0f ? (r1.y) : (c0.z));
    r2.w = exp2(r2.y);
    r1.x = pow(abs(r3.w), r2.w);
    r1.y = (r2.w) * (c4.y) + (c4.z);
    r8.w = 1.0f / (r2.x);
    r2.w = (r1.x) * (r1.y);
    r2.y = (r3.y) * (c1.y);
    r1.x = (r3.y) * (-(c1.y)) + (c1.z);
    r1.y = (r1.z) * (r1.x) + (r2.y);
    r2.z = saturate(dot(r0.xyz, -(r7.xyz)));
    r1.x = (r2.z) * (r1.x) + (r2.y);
    r9.w = (-(r2.z)) + (c0.y);
    r1.y = (r1.y) * (r1.x) + (c4.x);
    r1.w = (-(r1.w)) + (c0.y);
    r1.x = 1.0f / (r1.y);
    r1.y = (r1.w) * (r1.w);
    r2.z = (r1.z) * (r1.x);
    r2.y = (r1.y) * (r1.y);
    r1.xyz = (r1.zzz) * (c[6].xyz);
    r1.w = (r1.w) * (r2.y);
    r6.w = (r3.x) * (r3.x);
    r5.w = (r3.x) * (-(r3.x)) + (c0.y);
    r2.w = (r2.w) * (r2.z);
    r2.z = (r5.w) * (r1.w) + (r6.w);
    r1.w = (r3.y) * (c1.x);
    r2.w = (r2.w) * (r2.z);
    r3.w = (r7.w) * (r2.w);
    r2.xy = (v1.zw) * (c1.zw);
    r2 = tex2D(s13, r2.xy);
    r3.xy = (v1.zw) * (c2.xy) + (c2.zy);
    r4 = tex2D(s13, r3.xy);
    r2.w = r4.y;
    r3.xyz = (r3.www) * (c[7].xyz);
    r8.xy = (r2.yw) * (c3.xx) + (c3.yy);
    r5.xyz = (r9.zzz) * (r3.xyz);
    r2.w = dot(r8.xy, r8.xy) + (c0.z);
    r3 = tex2D(s14, v1.zw);
    r9.xy = (r3.xy) * (c2.ww);
    r2.w = exp2(-(r2.w));
    r4.xy = (r4.xz) * (r9.yy);
    r2.w = saturate((r2.w) * (c3.z) + (c3.w));
    r3.w = (r3.y) * (c2.w) + (-(r4.x));
    r8.xz = (r4.xy) * (c3.xx);
    r2.xy = (r2.xz) * (r9.xx);
    r3.z = (r4.z) * (-(r9.y)) + (r3.w);
    r3.w = (r3.x) * (c2.w) + (-(r2.x));
    r8.y = (r3.z) + (r3.z);
    r3.w = (r2.z) * (-(r9.x)) + (r3.w);
    r2.xz = (r2.xy) * (c3.xx);
    r2.y = (r3.w) + (r3.w);
    r3.xyz = (r8.xyz) * (r2.www) + (r2.xyz);
    r2.w = dot(r7.xyz, r0.xyz);
    r3.xyz = (r7.www) * (r3.xyz);
    r2.w = (r2.w) + (r2.w);
    r3.xyz = (r9.zzz) * (r1.xyz) + (r3.xyz);
    r1.xyz = (r0.xyz) * (-(r2.www)) + (r7.xyz);
    r1 = texCUBElod(s15, r1);
    r0.z = (r9.w) * (r9.w);
    r1.w = (r9.w) * (r0.z);
    r0.xyz = (r1.xyz) * (r1.xyz);
    r1.w = (r8.w) * (r1.w);
    r0.xyz = (r7.www) * (r0.xyz);
    r1.w = (r5.w) * (r1.w) + (r6.w);
    r1.xyz = (r6.xyz) * (r6.xyz);
    r0.xyz = (r0.xyz) * (r1.www);
    r1.xyz = (r1.xyz) * (r3.xyz) + (r5.xyz);
    r0.xyz = (r2.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c1.xxx) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v3.xyz));
    r1.x = v2.w;
    r0.xyz = (r1.xxx) * (r0.xyz) + (v3.xyz);
    r0.xyz = max(((r0.xyz) * (c[22].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.w = (r0.w) * (c[21].w);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);

    return oC0;
}
