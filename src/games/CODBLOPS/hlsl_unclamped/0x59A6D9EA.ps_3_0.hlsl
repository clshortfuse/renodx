// Mechanically reconstructed from 0x59A6D9EA.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD3;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(0.5f, 2.0f, -1.0f, 9.99999975e-06f);
    const float4 c1 = float4(-2.0f, 3.0f, 0.0f, 0.25f);
    const float4 c2 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c3 = float4(0.25f, 1.0f, 0.0f, 0.0f);
    const float4 c4 = float4(1e-15f, 1.44269502f, 0.100000001f, 9.99999997e-07f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xy = (c[28].xy) * (v5.ww);
    r0.zw = c1.zz;
    r1 = (r0.xyww) + (v5);
    r0 = (-(r0)) + (v5);
    r0 = tex2Dproj(s1, r0);
    r0.y = r0.x;
    r1 = tex2Dproj(s1, r1);
    r0.x = r1.x;
    r1.xy = (c[28].zw) * (v5.ww);
    r1.zw = c1.zz;
    r2 = (r1.xyww) + (v5);
    r1 = (-(r1)) + (v5);
    r1 = tex2Dproj(s1, r1);
    r0.w = r1.x;
    r1 = tex2Dproj(s1, r2);
    r0.z = r1.x;
    r0.x = dot(r0, c1.wwww);
    r0.yzw = (c[20].xyz) + (-(v1.xyz));
    r1.x = dot(r0.yzw, r0.yzw);
    r1.x = rsqrt(r1.x);
    r0.yzw = (r0.yzw) * (r1.xxx);
    r1.x = 1.0f / (r1.x);
    r1.z = dot(r0.yzw, c[24].xyz);
    r1.z = saturate((r1.z) * (c[25].x) + (c[25].y));
    r1.z = (r1.z) * (c[25].w);
    r0.x = (r0.x) * (r1.z);
    r1.y = (r1.x) * (r1.x);
    r1.y = dot(c[26].yz, r1.xy) + (c[26].x);
    r1.xz = saturate((r1.xx) * (c[27].xy) + (c[27].zw));
    r2.xy = (r1.xz) * (r1.xz);
    r1.xz = (r1.xz) * (c1.xx) + (c1.yy);
    r1.xz = (r2.xy) * (r1.xz);
    r1.x = (r1.y) * (r1.x);
    r1.x = (r1.z) * (r1.x);
    r0.x = (r0.x) * (r1.x);
    r1.xyz = (r0.xxx) * (c[22].xyz);
    r2.xyz = (r0.xxx) * (c[21].xyz);
    r2.xyz = (r2.xyz) * (c[23].xxx);
    r1.xyz = (r1.xyz) * (c[23].yyy);
    r0.x = dot(-(v1.xyz), -(v1.xyz));
    r0.x = rsqrt(r0.x);
    r3.xyz = (-(v1.xyz)) * (r0.xxx) + (r0.yzw);
    r4.xyz = (r0.xxx) * (-(v1.xyz));
    r5.xyz = normalize(r3.xyz);
    r3.xy = (c[34].xx) * (v4.xy);
    r3 = tex2D(s2, r3.xy);
    r3.xy = (r3.wy) * (c2.xy) + (c2.zw);
    r3.xy = (r3.xy) * (c0.xx) + (c0.xx);
    r3.xy = (r3.xy) * (c0.yy) + (c0.zz);
    r0.x = dot(v2.xyz, v2.xyz);
    r0.x = rsqrt(r0.x);
    r6.xyz = (r0.xxx) * (v2.xyz);
    r7.xyz = (r6.zxy) * (v3.yzx);
    r7.xyz = (r6.yzx) * (v3.zxy) + (-(r7.xyz));
    r7.xyz = (r7.xyz) * (v3.www);
    r3.yzw = (r3.yyy) * (r7.xyz);
    r3.xyz = (r3.xxx) * (v3.xyz) + (r3.yzw);
    r3.xyz = (v2.xyz) * (r0.xxx) + (r3.xyz);
    r7.xyz = normalize(r3.xyz);
    r0.x = saturate(dot(r7.xyz, r5.xyz));
    r1.w = (r0.x) + (r0.x);
    r0.x = (r0.x) * (r0.x) + (c4.x);
    r0.x = 1.0f / (r0.x);
    r2.w = saturate(dot(r7.xyz, r4.xyz));
    r3.x = saturate(dot(r6.xyz, r4.xyz));
    r3.x = (r2.w) + (r3.x);
    r3.x = (r3.x) * (c0.x);
    r4.x = max(c4.z, r3.x);
    r3.x = 1.0f / (r4.x);
    r3.x = rsqrt(r3.x);
    r3.x = 1.0f / (r3.x);
    r3.y = 1.0f / (r2.w);
    r1.w = (r1.w) * (r3.y);
    r0.y = saturate(dot(r7.xyz, r0.yzw));
    r3.y = min(r2.w, r0.y);
    r0.z = saturate((r1.w) * (r3.y));
    r0.z = (r0.y) * (r0.z);
    r2.xyz = (r2.xyz) * (r0.yyy);
    r0.y = rsqrt(r0.z);
    r0.y = 1.0f / (r0.y);
    r0.z = (r0.x) * (r0.x);
    r0.x = (-(r0.x)) + (-(c0.z));
    r4 = tex2D(s4, v4.xy);
    r0.w = (r4.x) * (c[37].x);
    r3.yzw = (r4.xyz) * (c[33].xxx);
    r1.w = max(c0.w, r0.w);
    r0.w = (r1.w) * (r1.w);
    r0.w = 1.0f / (r0.w);
    r0.x = (r0.x) * (r0.w);
    r0.x = (r0.x) * (c4.y);
    r0.x = exp2(r0.x);
    r0.x = (r0.z) * (r0.x);
    r0.x = (r0.y) * (r0.x);
    r0.xyz = (r1.xyz) * (r0.xxx);
    r0.xyz = (r3.xxx) * (r0.xyz);
    r0.xyz = (r0.www) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c[36].xxx);
    r0.xyz = (r0.xyz) * (c3.xxx) + (c3.yyy);
    r1.x = log2(abs(r0.x));
    r1.y = log2(abs(r0.y));
    r1.z = log2(abs(r0.z));
    r0.z = c1.z;
    r1.w = max(c[35].x, r0.z);
    r0.x = (r1.w) + (c0.z);
    r0.xyz = (r1.xyz) * (r0.xxx);
    r1.x = exp2(r0.x);
    r1.y = exp2(r0.y);
    r1.z = exp2(r0.z);
    r0.xyz = normalize(v1.xyz);
    r0.y = dot(r0.xyz, r7.xyz);
    r0.y = (r0.y) + (r0.y);
    r0.x = (r7.x) * (-(r0.y)) + (r0.x);
    r0.x = (r0.x) * (c4.w);
    r0.xyz = (r3.yzw) * (r0.xxx);
    r0.xyz = (r1.xyz) * (c[35].yyy) + (r0.xyz);
    r0.xyz = (r0.xyz) * (c[38].xxx);
    r1.xy = ddx(v4.xy);
    r1.zw = ddy(v4.xy);
    r1 = tex2Dgrad(s3, v4.xy, r1.xy, r1.zw);
    r1 = (r1) * (r1);
    r1.xyz = (r2.xyz) * (r1.xyz) + (r0.xyz);
    r0 = max(r1, c1.zzzz);
    r1 = (c[6]) + (-(v1.yyyy));
    r2 = (r1) * (r1);
    r3 = (c[5]) + (-(v1.xxxx));
    r2 = (r3) * (r3) + (r2);
    r4 = (c[7]) + (-(v1.zzzz));
    r2 = (r4) * (r4) + (r2);
    r5.x = rsqrt(r2.x);
    r5.y = rsqrt(r2.y);
    r5.z = rsqrt(r2.z);
    r5.w = rsqrt(r2.w);
    r7.z = c0.z;
    r2 = saturate((r2) * (c[8]) + (-(r7.zzzz)));
    r1 = (r1) * (r5);
    r1 = (r6.yyyy) * (r1);
    r3 = (r3) * (r5);
    r4 = (r4) * (r5);
    r1 = (r3) * (r6.xxxx) + (r1);
    r1 = saturate((r4) * (r6.zzzz) + (r1));
    r1 = (r2) * (r1);
    r2.x = dot(c[9], r1);
    r2.y = dot(c[10], r1);
    r2.z = dot(c[11], r1);
    r1.xyz = (r0.xyz) * (r2.xyz) + (r0.xyz);
    oC0.w = r0.w;
    r1.w = -(c0.z);
    r0.x = dot(r1, c[30]);
    r0.y = dot(r1, c[31]);
    r0.z = dot(r1, c[32]);
    r0.w = v1.w;
    r1.xyz = lerp(v0.xyz, r0.xyz, r0.www);
    r0.xyz = max(((r1.xyz) * (c[29].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);

    return oC0;
}
