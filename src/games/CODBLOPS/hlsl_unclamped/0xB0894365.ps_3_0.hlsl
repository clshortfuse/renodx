// Mechanically reconstructed from 0xB0894365.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD8;
    float4 v3 : TEXCOORD2;
    float4 v4 : TEXCOORD3;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(0.600000024f, 0.400000006f, 8.0f, 0.797884583f);
    const float4 c2 = float4(1.0f, 0.5f, -0.0f, 31.875f);
    const float4 c3 = float4(0.0009765625f, 0.125f, 0.25f, 0.0f);
    const float4 c4 = float4(4.0f, -2.0f, 0.797884583f, 1.0f);
    const float4 c11 = float4(-1.0f, 1.0f, 0.200000003f, -0.0f);
    const float4 c12 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    const float4 c13 = float4(-2.0f, 3.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 r12 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = tex2D(s0, v0.xy);
    r1 = tex2D(s2, v6.zw);
    r1.xyz = (r1.xyz) + (c11.xxx);
    r2.xyz = (v6.yyy) * (r1.xyz) + (c11.yyy);
    r1.xyz = (r0.xyz) * (r2.xyz);
    r0.xyz = (r2.xyz) * (c11.zzz);
    r5.xyz = (r1.xyz) * (r1.xyz);
    r1.xyz = (-(v5.xyz)) + (c[5].xyz);
    r9.xyz = (r0.xyz) * (r0.xyz);
    r6.y = dot(r1.xyz, r1.xyz);
    r0.w = rsqrt(r6.y);
    r10.xyz = normalize(v5.xyz);
    r8.xyz = (r0.xyz) * (-(r0.xyz)) + (c11.yyy);
    r0.xyz = (r1.xyz) * (r0.www) + (-(r10.xyz));
    r1.xyz = (r1.xyz) * (r0.www);
    r3.xyz = normalize(r0.xyz);
    r6.x = 1.0f / (r0.w);
    r1.w = saturate(dot(r3.xyz, r1.xyz));
    r0 = tex2D(s1, v0.xy);
    r11.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r1.w = (-(r1.w)) + (c11.y);
    r0 = v1;
    r0.xyz = (r11.xxx) * (v4.xyz) + (r0.xyz);
    r2.w = (r1.w) * (r1.w);
    r2.xyz = (r11.yyy) * (v3.xyz) + (r0.xyz);
    r2.w = (r2.w) * (r2.w);
    r0.xyz = normalize(r2.xyz);
    r2.z = (r1.w) * (r2.w);
    r3.w = saturate(dot(r0.xyz, r1.xyz));
    r1 = tex2D(s3, v0.xy);
    r2.y = (r1.w) * (c1.w);
    r2.w = (r1.w) * (-(c4.z)) + (c4.w);
    r1.x = saturate(dot(r0.xyz, -(r10.xyz)));
    r1.z = (r3.w) * (r2.w) + (r2.y);
    r2.w = (r1.x) * (r2.w) + (r2.y);
    r2.xyz = (r8.xyz) * (r2.zzz) + (r9.xyz);
    r1.z = (r1.z) * (r2.w) + (c3.x);
    r5.w = (-(r1.x)) + (c11.y);
    r1.z = 1.0f / (r1.z);
    r2.w = (r3.w) * (r1.z);
    r4.xy = (r1.ww) * (c12.xy) + (c12.zw);
    r3.y = saturate(dot(r0.xyz, r3.xyz));
    r3.z = exp2(r4.y);
    r1.x = pow(abs(r3.y), r3.z);
    r1.z = (r3.z) * (c3.y) + (c3.z);
    r7.xyz = (r3.www) * (c[6].xyz);
    r1.z = (r1.x) * (r1.z);
    r1.x = 1.0f / (r4.x);
    r1.z = (r2.w) * (r1.z);
    r2.xyz = (r2.xyz) * (r1.zzz);
    r4.xy = saturate((r6.xx) * (c[9].xy) + (c[9].zw));
    r3.xy = (r4.xy) * (r4.xy);
    r4.xy = (r4.xy) * (c13.xx) + (c13.yy);
    r1.z = dot(c[8].yz, r6.xy) + (c[8].x);
    r4.xy = (r3.xy) * (r4.xy);
    r2.xyz = (r1.yyy) * (r2.xyz);
    r1.z = (r1.z) * (r4.x);
    r3.xyz = (r2.xyz) * (c[7].xyz);
    r1.z = (r4.y) * (r1.z);
    r2 = tex2D(s12, v0.zw);
    r1.z = (r1.z) * (r2.y);
    r2.xy = (v0.zw) * (c2.xy);
    r2 = tex2D(s13, r2.xy);
    r4.xy = (v0.zw) * (c2.xy) + (c2.zy);
    r4 = tex2D(s13, r4.xy);
    r2.w = r4.y;
    r6.xyz = (r3.xyz) * (r1.zzz);
    r3.xy = (r2.yw) * (c4.xx) + (c4.yy);
    r2.w = dot(r11.xy, r11.xy) + (c11.w);
    r2.y = dot(r3.xy, r3.xy) + (c11.w);
    r2.w = exp2(-(r2.w));
    r2.y = exp2(-(r2.y));
    r2.w = (r2.w) * (c1.x) + (c1.y);
    r3.w = (r2.y) * (c1.x) + (c1.y);
    r2.y = dot(r3.xy, r11.xy) + (c11.w);
    r4.w = (r2.w) * (r3.w);
    r3 = tex2D(s14, v0.zw);
    r12.xy = (r3.xy) * (c2.ww);
    r3.z = saturate((r2.y) * (r4.w) + (r4.w));
    r4.xy = (r4.xz) * (r12.yy);
    r2.y = (r3.y) * (c2.w) + (-(r4.x));
    r11.xz = (r4.xy) * (c4.xx);
    r3.w = (r4.z) * (-(r12.y)) + (r2.y);
    r2.xy = (r2.xz) * (r12.xx);
    r11.y = (r3.w) + (r3.w);
    r3.w = (r3.x) * (c2.w) + (-(r2.x));
    r4.xyz = (r3.zzz) * (r11.xyz);
    r2.z = (r2.z) * (-(r12.x)) + (r3.w);
    r3.xz = (r2.xy) * (c4.xx);
    r3.y = (r2.z) + (r2.z);
    r2.xyz = (r3.xyz) * (r2.www) + (r4.xyz);
    r2.w = dot(r10.xyz, r0.xyz);
    r4.xyz = (r1.yyy) * (r2.xyz);
    r2.w = (r2.w) + (r2.w);
    r2.xyz = (r0.xyz) * (-(r2.www)) + (r10.xyz);
    r2.w = (r1.w) * (c1.z);
    r2 = texCUBElod(s15, r2);
    r0.z = (r5.w) * (r5.w);
    r1.w = (r5.w) * (r0.z);
    r0.xyz = (r2.xyz) * (r2.xyz);
    r1.w = (r1.x) * (r1.w);
    r0.xyz = (r1.yyy) * (r0.xyz);
    r2.xyz = (r8.xyz) * (r1.www) + (r9.xyz);
    r1.xyz = (r1.zzz) * (r7.xyz) + (r4.xyz);
    r0.xyz = (r0.xyz) * (r2.xyz);
    r1.xyz = (r5.xyz) * (r1.xyz) + (r6.xyz);
    r0.xyz = (r3.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c1.zzz) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v2.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v2.xyz);
    r0.xyz = max(((r0.xyz) * (c[10].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c11.y;

    return oC0;
}
