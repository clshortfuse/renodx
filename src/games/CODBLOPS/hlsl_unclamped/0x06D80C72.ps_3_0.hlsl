// Mechanically reconstructed from 0x06D80C72.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD8;
    float4 v3 : TEXCOORD2;
    float4 v4 : TEXCOORD3;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD6;
    float4 v7 : COLOR1;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(2.0f, -1.0f, 0.0f, -0.5f);
    const float4 c2 = float4(0.600000024f, 0.400000006f, 8.0f, 0.797884583f);
    const float4 c3 = float4(0.959999979f, 0.0399999991f, 31.875f, 4.0f);
    const float4 c4 = float4(0.0009765625f, 0.125f, 0.25f, 0.0f);
    const float4 c12 = float4(4.0f, -2.0f, 0.797884583f, 1.0f);
    const float4 c13 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 oC0 = 0.0f;

    r1 = (v5.xyzx) * (-(c1.yyyz)) + (-(c1.zzzy));
    r0.w = dot(r1, c[23]);
    r0.w = 1.0f / (r0.w);
    r0.x = dot(r1, c[20]);
    r0.y = dot(r1, c[21]);
    r0 = (r0.wwww) * (r0.xxyy);
    r2 = (r0) * (c[24].zwxy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.z = log2(abs(r2.z));
    r2.w = log2(abs(r2.w));
    r2 = (r2) * (c[25].xxxx);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = exp2(r2.z);
    r2.w = exp2(r2.w);
    r2.xy = (r2.zw) + (r2.xy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.xy = (r2.xy) * (c[25].yy);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = dot(r1, c[22]);
    r0.z = (r2.x) * (c[26].x);
    r0.y = (r2.y) * (c[26].y) + (-(r0.z));
    r0.z = c[26].y;
    r0.z = (r2.y) * (r0.z) + (-(c[25].z));
    r0.y = 1.0f / (r0.y);
    r2.xy = abs(r0.xw);
    r3.w = saturate((r0.z) * (r0.y));
    r1 = c[10];
    r1 = saturate((r2.zyxz) * (r1) + (c[11]));
    r3.x = (r1.w) * (r1.x);
    r3.yz = r1.yz;
    r1 = (r3) * (r3);
    r3 = (c[26].zzzz) * (r3) + (c[26].wwww);
    r1 = (r1) * (r3);
    r0.y = (r1.z) * (r1.y);
    r0.z = abs(c[25].w);
    r2.w = (r2.z) * (r2.z);
    r0.y = ((-(r0.z)) >= 0.0f ? (r0.y) : (r1.w));
    r0.z = dot(c[9].yz, r2.zw) + (c[9].x);
    r0.z = (r0.y) * (r0.z);
    r2.w = (r1.x) * (r0.z);
    r1 = c[27];
    r1.x = dot(r0.xw, r1.xy) + (c[8].x);
    r1.y = dot(r0.xw, r1.zw) + (c[8].z);
    r0 = tex2D(s2, r1.xy);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r1.xyz = (r2.www) * (r0.xyz);
    r0 = tex2D(s12, v0.zw);
    r3.xyz = (-(v5.xyz)) + (c[5].xyz);
    r0.w = dot(r3.xyz, r3.xyz);
    r0.w = rsqrt(r0.w);
    r6.xyz = normalize(v5.xyz);
    r2.xyz = (r3.xyz) * (r0.www) + (-(r6.xyz));
    r8.xyz = (r3.xyz) * (r0.www);
    r5.xyz = normalize(r2.xyz);
    r0.w = saturate(dot(r5.xyz, r8.xyz));
    r7.xyz = (r1.xyz) * (r0.yyy);
    r4.w = (-(r0.w)) + (-(c1.y));
    r0 = tex2D(s1, v0.xy);
    r4.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r0 = tex2D(s4, v6.zw);
    r1.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r0 = (c1.xxxx) * (v7) + (c1.yyyy);
    r0.x = dot(r1.xy, r0.xy) + (c1.z);
    r0.y = dot(r1.xy, r0.zw) + (c1.z);
    r2 = tex2D(s6, v6.zw);
    r0.z = r2.w;
    r3 = tex2D(s5, v0.xy);
    r4.z = r3.w;
    r1 = tex2D(s3, v6.zw);
    r8.w = (r1.w) * (v6.y) + (c1.w);
    r0.w = (r4.w) * (r4.w);
    r9.xyz = float3(((r8.w) >= 0.0f ? (r0.x) : (r4.x)), ((r8.w) >= 0.0f ? (r0.y) : (r4.y)), ((r8.w) >= 0.0f ? (r0.z) : (r4.z)));
    r1.w = (r0.w) * (r0.w);
    r0 = v1;
    r0.xyz = (r9.xxx) * (v4.xyz) + (r0.xyz);
    r1.w = (r4.w) * (r1.w);
    r4.xyz = (r9.yyy) * (v3.xyz) + (r0.xyz);
    r2.w = (r1.w) * (c3.x) + (c3.y);
    r0.xyz = normalize(r4.xyz);
    r2.x = saturate(dot(r0.xyz, r8.xyz));
    r3.z = (r9.z) * (c2.w);
    r2.z = (r9.z) * (-(c12.z)) + (c12.w);
    r3.w = saturate(dot(r0.xyz, -(r6.xyz)));
    r1.w = (r2.x) * (r2.z) + (r3.z);
    r2.z = (r3.w) * (r2.z) + (r3.z);
    r7.w = (-(r3.w)) + (-(c1.y));
    r1.w = (r1.w) * (r2.z) + (c4.x);
    r2.z = 1.0f / (r1.w);
    r4.xy = (r9.zz) * (c13.xy) + (c13.zw);
    r4.w = saturate(dot(r0.xyz, r5.xyz));
    r1.w = exp2(r4.y);
    r3.w = pow(abs(r4.w), r1.w);
    r1.w = (r1.w) * (c4.y) + (c4.z);
    r2.z = (r2.x) * (r2.z);
    r1.w = (r3.w) * (r1.w);
    r8.xyz = (r2.xxx) * (c[6].xyz);
    r1.w = (r2.z) * (r1.w);
    r6.w = 1.0f / (r4.x);
    r1.w = (r2.w) * (r1.w);
    r5.w = ((r8.w) >= 0.0f ? (r2.y) : (r3.y));
    r2.xy = (v0.zw) * (-(c1.yw));
    r2 = tex2D(s13, r2.xy);
    r3.xy = (v0.zw) * (-(c1.yw)) + (-(c1.zw));
    r4 = tex2D(s13, r3.xy);
    r2.w = r4.y;
    r4.w = (r1.w) * (r5.w);
    r3.xy = (r2.yw) * (c12.xx) + (c12.yy);
    r1.w = dot(r9.xy, r9.xy) + (c1.z);
    r2.w = dot(r3.xy, r3.xy) + (c1.z);
    r1.w = exp2(-(r1.w));
    r2.w = exp2(-(r2.w));
    r1.w = (r1.w) * (c2.x) + (c2.y);
    r2.y = (r2.w) * (c2.x) + (c2.y);
    r2.w = dot(r3.xy, r9.xy) + (c1.z);
    r2.y = (r1.w) * (r2.y);
    r3 = tex2D(s14, v0.zw);
    r9.xy = (r3.xy) * (c3.zz);
    r3.w = saturate((r2.w) * (r2.y) + (r2.y));
    r4.xy = (r4.xz) * (r9.yy);
    r2.w = (r3.y) * (c3.z) + (-(r4.x));
    r5.xz = (r4.xy) * (c3.ww);
    r2.w = (r4.z) * (-(r9.y)) + (r2.w);
    r2.xy = (r2.xz) * (r9.xx);
    r5.y = (r2.w) + (r2.w);
    r2.w = (r3.x) * (c3.z) + (-(r2.x));
    r5.xyz = (r3.www) * (r5.xyz);
    r2.w = (r2.z) * (-(r9.x)) + (r2.w);
    r3.xz = (r2.xy) * (c3.ww);
    r3.y = (r2.w) + (r2.w);
    r4.xyz = (r4.www) * (c[7].xyz);
    r2.xyz = (r3.xyz) * (r1.www) + (r5.xyz);
    r5.xyz = (r7.xyz) * (r4.xyz);
    r2.xyz = (r5.www) * (r2.xyz);
    r1.w = (r9.z) * (c2.z);
    r4.xyz = (r7.xyz) * (r8.xyz) + (r2.xyz);
    r2 = tex2D(s0, v0.xy);
    r2.w = dot(r6.xyz, r0.xyz);
    r2.w = (r2.w) + (r2.w);
    r2.xyz = float3(((r8.w) >= 0.0f ? (r1.x) : (r2.x)), ((r8.w) >= 0.0f ? (r1.y) : (r2.y)), ((r8.w) >= 0.0f ? (r1.z) : (r2.z)));
    r1.xyz = (r0.xyz) * (-(r2.www)) + (r6.xyz);
    r1 = texCUBElod(s15, r1);
    r0.z = (r7.w) * (r7.w);
    r1.w = (r7.w) * (r0.z);
    r0.xyz = (r1.xyz) * (r1.xyz);
    r1.w = (r6.w) * (r1.w);
    r0.xyz = (r5.www) * (r0.xyz);
    r1.w = (r1.w) * (c3.x) + (c3.y);
    r1.xyz = (r2.xyz) * (r2.xyz);
    r0.xyz = (r0.xyz) * (r1.www);
    r1.xyz = (r1.xyz) * (r4.xyz) + (r5.xyz);
    r0.xyz = (r3.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c2.zzz) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v2.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v2.xyz);
    r0.xyz = max(((r0.xyz) * (c[28].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = -(c1.y);

    return oC0;
}
