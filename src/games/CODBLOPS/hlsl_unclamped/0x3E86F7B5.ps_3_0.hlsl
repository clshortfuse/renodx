// Mechanically reconstructed from 0x3E86F7B5.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD2;
    float4 v4 : TEXCOORD3;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD6;
    float4 v7 : COLOR1;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c1 = float4(2.0f, -1.0f, -0.5f, 1.0f);
    const float4 c2 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c3 = float4(-0.0f, 0.600000024f, 0.400000006f, 8.0f);
    const float4 c4 = float4(0.797884583f, 0.959999979f, 0.0399999991f, 31.875f);
    const float4 c9 = float4(0.0009765625f, 0.125f, 0.25f, 0.0f);
    const float4 c10 = float4(1.0f, 0.5f, -0.0f, 4.0f);
    const float4 c11 = float4(4.0f, -2.0f, 0.797884583f, 1.0f);
    const float4 c12 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xy = (v1.zw) * (-(c1.yz));
    r1 = tex2D(s13, r0.xy);
    r0.xy = (v1.zw) * (c10.xy) + (c10.zy);
    r3 = tex2D(s13, r0.xy);
    r1.w = r3.y;
    r0 = tex2D(s1, v1.xy);
    r5.xy = (r0.wy) * (c2.xy) + (c2.zw);
    r0 = tex2D(s4, v6.zw);
    r2.xy = (r0.wy) * (c2.xy) + (c2.zw);
    r0 = (c1.xxxx) * (v7) + (c1.yyyy);
    r4.x = dot(r2.xy, r0.xy) + (c3.x);
    r4.y = dot(r2.xy, r0.zw) + (c3.x);
    r0 = tex2D(s3, v6.zw);
    r5.w = (r0.w) * (v0.z);
    r2.xy = (r1.yw) * (c11.xx) + (c11.yy);
    r6.xy = lerp(r5.xy, r4.xy, r5.ww);
    r1.w = dot(r2.xy, r2.xy) + (c3.x);
    r0.w = dot(r6.xy, r6.xy) + (c3.x);
    r1.w = exp2(-(r1.w));
    r0.w = exp2(-(r0.w));
    r1.w = (r1.w) * (c3.y) + (c3.z);
    r0.w = (r0.w) * (c3.y) + (c3.z);
    r1.y = (r1.w) * (r0.w);
    r1.w = dot(r2.xy, r6.xy) + (c3.x);
    r2 = tex2D(s14, v1.zw);
    r5.xy = (r2.xy) * (c4.ww);
    r2.w = saturate((r1.w) * (r1.y) + (r1.y));
    r3.xy = (r3.xz) * (r5.yy);
    r1.w = (r2.y) * (c4.w) + (-(r3.x));
    r4.xz = (r3.xy) * (c10.ww);
    r1.w = (r3.z) * (-(r5.y)) + (r1.w);
    r1.xy = (r1.xz) * (r5.xx);
    r4.y = (r1.w) + (r1.w);
    r1.w = (r2.x) * (c4.w) + (-(r1.x));
    r2.xyz = (r2.www) * (r4.xyz);
    r1.w = (r1.z) * (-(r5.x)) + (r1.w);
    r4.xz = (r1.xy) * (c10.ww);
    r4.y = (r1.w) + (r1.w);
    r3 = tex2D(s5, v1.xy);
    r1 = tex2D(s2, v6.xy);
    r6.w = (r1.w) * (v0.y) + (c1.z);
    r5.xyz = (r4.xyz) * (r0.www) + (r2.xyz);
    r0.w = ((r6.w) >= 0.0f ? (c1.w) : (r3.y));
    r2 = tex2D(s6, v6.zw);
    r3.xyz = v2.xyz;
    r3.xyz = (r6.xxx) * (v4.xyz) + (r3.xyz);
    r3.xyz = (r6.yyy) * (v3.xyz) + (r3.xyz);
    r1.w = lerp(r0.w, r2.y, r5.w);
    r7.xyz = normalize(r3.xyz);
    r0.w = lerp(r3.w, r2.w, r5.w);
    r3.w = saturate(dot(r7.xyz, c[17].xyz));
    r6.xyz = (r5.xyz) * (r1.www);
    r5.xyz = (r3.www) * (c[18].xyz);
    r2 = tex2D(s12, v1.zw);
    r2.w = dot(v5.xyz, v5.xyz);
    r4.w = rsqrt(r2.w);
    r7.w = (r0.w) * (c4.x);
    r3.xyz = (r4.www) * (v5.xyz);
    r2.x = (r0.w) * (-(c11.z)) + (c11.w);
    r2.z = saturate(dot(r7.xyz, -(r3.xyz)));
    r2.w = (r3.w) * (r2.x) + (r7.w);
    r2.x = (r2.z) * (r2.x) + (r7.w);
    r9.xyz = (v5.xyz) * (-(r4.www)) + (c[17].xyz);
    r2.w = (r2.w) * (r2.x) + (c9.x);
    r2.w = 1.0f / (r2.w);
    r8.xyz = normalize(r9.xyz);
    r3.w = (r3.w) * (r2.w);
    r4.w = saturate(dot(r7.xyz, r8.xyz));
    r2.x = saturate(dot(r8.xyz, c[17].xyz));
    r8.xy = (r0.ww) * (c12.xy) + (c12.zw);
    r2.w = exp2(r8.y);
    r2.x = (-(r2.x)) + (c1.w);
    r7.w = pow(abs(r4.w), r2.w);
    r4.w = (r2.x) * (r2.x);
    r2.w = (r2.w) * (c9.y) + (c9.z);
    r4.w = (r4.w) * (r4.w);
    r2.w = (r7.w) * (r2.w);
    r2.x = (r2.x) * (r4.w);
    r2.w = (r3.w) * (r2.w);
    r2.x = (r2.x) * (c4.y) + (c4.z);
    r5.xyz = (r2.yyy) * (r5.xyz) + (r6.xyz);
    r2.w = (r2.w) * (r2.x);
    r4.w = (-(r2.z)) + (c1.w);
    r2.w = (r1.w) * (r2.w);
    r0.w = (r0.w) * (c3.w);
    r6.xyz = (r2.www) * (c[19].xyz);
    r3.w = 1.0f / (r8.x);
    r6.xyz = (r2.yyy) * (r6.xyz);
    r2 = tex2D(s0, v1.xy);
    r2.xyz = float3(((r6.w) >= 0.0f ? (r1.x) : (r2.x)), ((r6.w) >= 0.0f ? (r1.y) : (r2.y)), ((r6.w) >= 0.0f ? (r1.z) : (r2.z)));
    r2.w = dot(r3.xyz, r7.xyz);
    r1.xyz = lerp(r2.xyz, r0.xyz, r5.www);
    r0.z = (r2.w) + (r2.w);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r0.xyz = (r7.xyz) * (-(r0.zzz)) + (r3.xyz);
    r0 = texCUBElod(s15, r0);
    r0.w = (r4.w) * (r4.w);
    r0.w = (r4.w) * (r0.w);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r0.w = (r3.w) * (r0.w);
    r0.xyz = (r1.www) * (r0.xyz);
    r0.w = (r0.w) * (c4.y) + (c4.z);
    r2.xyz = (r1.xyz) * (r5.xyz) + (r6.xyz);
    r0.xyz = (r0.xyz) * (r0.www);
    r0.w = dot(c[5].xyz, r3.xyz);
    r1.xyz = (r4.xyz) * (r0.xyz);
    r0.w = saturate((c[7].y) * (r0.w) + (c[7].x));
    r0.xyz = c[0].xyz;
    r0.xyz = (-(r0.xyz)) + (c[6].xyz);
    r1.xyz = (r1.xyz) * (c3.www) + (r2.xyz);
    r0.xyz = (r0.www) * (r0.xyz) + (c[0].xyz);
    r1.xyz = (r0.xyz) * (-(c[0].www)) + (r1.xyz);
    r1.xyz = (r1.xyz) * (v2.www);
    r0.xyz = (r0.xyz) * (c[0].www) + (r1.xyz);
    r0.xyz = max(((r0.xyz) * (c[8].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c1.w;

    return oC0;
}
