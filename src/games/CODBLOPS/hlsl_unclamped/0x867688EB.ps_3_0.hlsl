// Mechanically reconstructed from 0x867688EB.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD2;
    float4 v5 : TEXCOORD3;
    float4 v6 : TEXCOORD5;
    float4 v7 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(-0.5f, -1.0f, 1.0f, 0.200000003f);
    const float4 c2 = float4(-0.0f, 0.600000024f, 0.400000006f, 8.0f);
    const float4 c3 = float4(31.875f, 4.0f, -2.0f, 0.0009765625f);
    const float4 c4 = float4(0.125f, 0.25f, -2.0f, 3.0f);
    const float4 c11 = float4(0.797884583f, 1.0f, 0.5f, -0.0f);
    const float4 c12 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 r12 = 0.0f;
    float4 oC0 = 0.0f;

    r1 = tex2D(s0, v1.xy);
    r0 = tex2D(s2, v7.xy);
    r3.w = (r0.w) * (v0.y) + (c1.x);
    r2 = tex2D(s3, v7.zw);
    r2.xyz = (r2.xyz) + (c1.yyy);
    r1.xyz = float3(((r3.w) >= 0.0f ? (r0.x) : (r1.x)), ((r3.w) >= 0.0f ? (r0.y) : (r1.y)), ((r3.w) >= 0.0f ? (r0.z) : (r1.z)));
    r0.xyz = (v0.zzz) * (r2.xyz) + (c1.zzz);
    r1.xyz = (r1.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c1.www);
    r2.xyz = (-(v6.xyz)) + (c[5].xyz);
    r4.xyz = (r1.xyz) * (r1.xyz);
    r12.y = dot(r2.xyz, r2.xyz);
    r1.w = rsqrt(r12.y);
    r10.xyz = normalize(v6.xyz);
    r9.xyz = (r0.xyz) * (r0.xyz);
    r1.xyz = (r2.xyz) * (r1.www) + (-(r10.xyz));
    r5.xyz = (r2.xyz) * (r1.www);
    r3.xyz = normalize(r1.xyz);
    r8.xyz = (r0.xyz) * (-(r0.xyz)) + (c1.zzz);
    r0.w = saturate(dot(r3.xyz, r5.xyz));
    r12.x = 1.0f / (r1.w);
    r1.w = (-(r0.w)) + (c1.z);
    r0 = tex2D(s1, v1.xy);
    r7.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r1.z = (r1.w) * (r1.w);
    r0 = v2;
    r0.xyz = (r7.xxx) * (v5.xyz) + (r0.xyz);
    r2.w = (r1.z) * (r1.z);
    r1.xyz = (r7.yyy) * (v4.xyz) + (r0.xyz);
    r4.w = (r1.w) * (r2.w);
    r0.xyz = normalize(r1.xyz);
    r2 = tex2D(s4, v1.xy);
    r1 = tex2D(s5, v7.xy);
    r11.xy = float2(((r3.w) >= 0.0f ? (r1.w) : (r2.w)), ((r3.w) >= 0.0f ? (r1.y) : (r2.y)));
    r3.w = saturate(dot(r0.xyz, r5.xyz));
    r1.y = (r11.x) * (c11.x);
    r1.z = (r11.x) * (-(c11.x)) + (c11.y);
    r2.w = saturate(dot(r0.xyz, -(r10.xyz)));
    r1.w = (r3.w) * (r1.z) + (r1.y);
    r2.z = (r2.w) * (r1.z) + (r1.y);
    r1.xyz = (r8.xyz) * (r4.www) + (r9.xyz);
    r1.w = (r1.w) * (r2.z) + (c3.w);
    r6.w = (-(r2.w)) + (c1.z);
    r1.w = 1.0f / (r1.w);
    r2.w = (r3.w) * (r1.w);
    r2.xy = (r11.xx) * (c12.xy) + (c12.zw);
    r3.z = saturate(dot(r0.xyz, r3.xyz));
    r1.w = exp2(r2.y);
    r2.z = pow(abs(r3.z), r1.w);
    r1.w = (r1.w) * (c4.x) + (c4.y);
    r6.xyz = (r3.www) * (c[6].xyz);
    r1.w = (r2.z) * (r1.w);
    r5.w = 1.0f / (r2.x);
    r1.w = (r2.w) * (r1.w);
    r1.xyz = (r1.xyz) * (r1.www);
    r3.xy = saturate((r12.xx) * (c[9].xy) + (c[9].zw));
    r2.xy = (r3.xy) * (r3.xy);
    r3.xy = (r3.xy) * (c4.zz) + (c4.ww);
    r1.w = dot(c[8].yz, r12.xy) + (c[8].x);
    r3.xy = (r2.xy) * (r3.xy);
    r1.xyz = (r11.yyy) * (r1.xyz);
    r1.w = (r1.w) * (r3.x);
    r2.xyz = (r1.xyz) * (c[7].xyz);
    r2.w = (r3.y) * (r1.w);
    r1 = tex2D(s12, v1.zw);
    r4.w = (r2.w) * (r1.y);
    r1.xy = (v1.zw) * (-(c1.yx));
    r1 = tex2D(s13, r1.xy);
    r3.xy = (v1.zw) * (c11.yz) + (c11.wz);
    r3 = tex2D(s13, r3.xy);
    r1.w = r3.y;
    r5.xyz = (r2.xyz) * (r4.www);
    r2.xy = (r1.yw) * (c3.yy) + (c3.zz);
    r1.w = dot(r7.xy, r7.xy) + (c2.x);
    r1.y = dot(r2.xy, r2.xy) + (c2.x);
    r1.w = exp2(-(r1.w));
    r1.y = exp2(-(r1.y));
    r1.w = (r1.w) * (c2.y) + (c2.z);
    r2.w = (r1.y) * (c2.y) + (c2.z);
    r1.y = dot(r2.xy, r7.xy) + (c2.x);
    r3.w = (r1.w) * (r2.w);
    r2 = tex2D(s14, v1.zw);
    r12.xy = (r2.xy) * (c3.xx);
    r2.z = saturate((r1.y) * (r3.w) + (r3.w));
    r3.xy = (r3.xz) * (r12.yy);
    r1.y = (r2.y) * (c3.x) + (-(r3.x));
    r7.xz = (r3.xy) * (c3.yy);
    r2.w = (r3.z) * (-(r12.y)) + (r1.y);
    r1.xy = (r1.xz) * (r12.xx);
    r7.y = (r2.w) + (r2.w);
    r2.w = (r2.x) * (c3.x) + (-(r1.x));
    r3.xyz = (r2.zzz) * (r7.xyz);
    r1.z = (r1.z) * (-(r12.x)) + (r2.w);
    r2.xz = (r1.xy) * (c3.yy);
    r2.y = (r1.z) + (r1.z);
    r1.xyz = (r2.xyz) * (r1.www) + (r3.xyz);
    r1.w = dot(r10.xyz, r0.xyz);
    r7.xyz = (r11.yyy) * (r1.xyz);
    r1.w = (r1.w) + (r1.w);
    r1.xyz = (r0.xyz) * (-(r1.www)) + (r10.xyz);
    r1.w = (r11.x) * (c2.w);
    r1 = texCUBElod(s15, r1);
    r0.z = (r6.w) * (r6.w);
    r1.w = (r6.w) * (r0.z);
    r0.xyz = (r1.xyz) * (r1.xyz);
    r1.w = (r5.w) * (r1.w);
    r0.xyz = (r11.yyy) * (r0.xyz);
    r3.xyz = (r8.xyz) * (r1.www) + (r9.xyz);
    r1.xyz = (r4.www) * (r6.xyz) + (r7.xyz);
    r0.xyz = (r0.xyz) * (r3.xyz);
    r1.xyz = (r4.xyz) * (r1.xyz) + (r5.xyz);
    r0.xyz = (r2.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c2.www) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v3.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v3.xyz);
    r0.xyz = max(((r0.xyz) * (c[10].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c1.z;

    return oC0;
}
