// Mechanically reconstructed from 0x5E25A19B.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD2;
    float4 v5 : TEXCOORD3;
    float4 v6 : TEXCOORD5;
    float4 v7 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(-0.5f, 1.0f, 0.0f, 8.0f);
    const float4 c2 = float4(31.875f, 0.797884583f, 1.0f, 0.0009765625f);
    const float4 c3 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    const float4 c4 = float4(0.125f, 0.25f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 r12 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = tex2D(s1, v1.xy);
    r1.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r0 = tex2D(s0, v1.xy);
    r8.z = (r0.w) * (v0.w) + (c1.x);
    r8.xy = c[21].xy;
    r5.xyz = (r8.yyy) * (c[19].xyz);
    r2.xy = float2(((r8.z) >= 0.0f ? (r1.x) : (c1.z)), ((r8.z) >= 0.0f ? (r1.y) : (c1.z)));
    r2.w = dot(v6.xyz, v6.xyz);
    r1 = v2;
    r1.xyz = (r2.xxx) * (v5.xyz) + (r1.xyz);
    r3.x = rsqrt(r2.w);
    r2.xyz = (r2.yyy) * (v4.xyz) + (r1.xyz);
    r1.xyz = (r3.xxx) * (v6.xyz);
    r10.xyz = normalize(r2.xyz);
    r2 = tex2D(s2, v1.xy);
    r2 = float4(((r8.z) >= 0.0f ? (r2.x) : (c1.z)), ((r8.z) >= 0.0f ? (r2.y) : (c1.z)), ((r8.z) >= 0.0f ? (r2.z) : (c1.z)), ((r8.z) >= 0.0f ? (r2.w) : (c1.y)));
    r3.z = saturate(dot(r10.xyz, -(r1.xyz)));
    r4.z = (r2.w) * (c2.y);
    r3.w = (r2.w) * (-(c2.y)) + (c2.z);
    r8.w = saturate(dot(r10.xyz, c[17].xyz));
    r3.y = (r3.z) * (r3.w) + (r4.z);
    r3.w = (r8.w) * (r3.w) + (r4.z);
    r4.xyz = (v6.xyz) * (-(r3.xxx)) + (c[17].xyz);
    r3.w = (r3.w) * (r3.y) + (c2.w);
    r6.w = (-(r3.z)) + (c1.y);
    r3.w = 1.0f / (r3.w);
    r4.w = (r8.w) * (r3.w);
    r3.xyz = normalize(r4.xyz);
    r5.w = saturate(dot(r10.xyz, r3.xyz));
    r4.xy = (r2.ww) * (c3.xy) + (c3.zw);
    r3.w = saturate(dot(r3.xyz, c[17].xyz));
    r4.z = exp2(r4.y);
    r7.w = 1.0f / (r4.x);
    r3.z = (-(r3.w)) + (c1.y);
    r3.x = pow(abs(r5.w), r4.z);
    r3.y = (r3.z) * (r3.z);
    r3.w = (r4.z) * (c4.x) + (c4.y);
    r3.y = (r3.y) * (r3.y);
    r3.w = (r3.x) * (r3.w);
    r3.z = (r3.z) * (r3.y);
    r11.xyz = (r2.xyz) * (r2.xyz);
    r7.xyz = (r2.xyz) * (-(r2.xyz)) + (c1.yyy);
    r3.w = (r4.w) * (r3.w);
    r2.xyz = (r7.xyz) * (r3.zzz) + (r11.xyz);
    r2.xyz = (r3.www) * (r2.xyz);
    r12.yz = c1.yz;
    r9.xyz = float3(((r8.z) >= 0.0f ? (c[22].x) : (r12.z)), ((r8.z) >= 0.0f ? (c[22].y) : (r12.z)), ((r8.z) >= 0.0f ? (c[22].w) : (r12.z)));
    r4.w = (r2.w) * (c1.w);
    r2.xyz = (r2.xyz) * (r9.zzz);
    r3 = tex2D(s4, v1.xy);
    r5.w = max(abs(r10.y), abs(r10.z));
    r4.xyz = (r2.xyz) * (r3.www);
    r2.w = max(abs(r10.x), r5.w);
    r2.w = 1.0f / (r2.w);
    r2.xyz = (r10.xyz) * (c[5].xyz);
    r4.xyz = (r5.xyz) * (r4.xyz);
    r2.xyz = (r2.xyz) * (r2.www) + (v7.xyz);
    r2 = tex3D(s11, r2.xyz);
    r6.xyz = (r4.xyz) * (r2.www);
    r4.xy = (v1.xy) * (c[23].xy);
    r5 = tex2D(s3, r4.xy);
    r4.xyz = (r5.xyz) + (c1.xxx);
    r0.xyz = saturate((r4.xyz) * (r0.www) + (r0.xyz));
    r0.w = c1.y;
    r0 = float4(((r8.z) >= 0.0f ? (r0.x) : (c1.z)), ((r8.z) >= 0.0f ? (r0.y) : (c1.z)), ((r8.z) >= 0.0f ? (r0.z) : (c1.z)), ((r8.z) >= 0.0f ? (r0.w) : (c1.z)));
    r2.xyz = (r2.xyz) * (r2.xyz);
    r0.xyz = (r0.xyz) * (v0.xyz);
    r2.xyz = (r9.yyy) * (r2.xyz);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r2.xyz = (r3.www) * (r2.xyz);
    r5.xyz = (r2.xyz) * (c2.xxx);
    r3.w = dot(r1.xyz, r10.xyz);
    r2.xyz = (r8.xxx) * (c[18].xyz);
    r3.w = (r3.w) + (r3.w);
    r2.xyz = (r8.www) * (r2.xyz);
    r4.xyz = (r10.xyz) * (-(r3.www)) + (r1.xyz);
    r4 = texCUBElod(s15, r4);
    r1.xyz = (r4.xyz) * (r4.xyz);
    r5.xyz = (r2.www) * (r2.xyz) + (r5.xyz);
    r4.xyz = (r1.xyz) * (c1.www);
    r2 = tex3D(s11, v7.xyz);
    r1.xyz = (r2.xyz) * (r2.xyz);
    r8.xyz = (r0.xyz) * (r5.xyz) + (r6.xyz);
    r1.xyz = (r4.xyz) * (r1.xyz);
    r1.xyz = (r1.xyz) * (c2.xxx);
    r2.w = (r6.w) * (r6.w);
    r3.w = (r6.w) * (r2.w);
    r6 = (-(v6.yyyy)) + (c[7]);
    r2 = (r6) * (r6);
    r5 = (-(v6.xxxx)) + (c[6]);
    r2 = (r5) * (r5) + (r2);
    r4 = (-(v6.zzzz)) + (c[8]);
    r3.w = (r7.w) * (r3.w);
    r2 = (r4) * (r4) + (r2);
    r11.xyz = (r7.xyz) * (r3.www) + (r11.xyz);
    r7.x = rsqrt(r2.x);
    r7.y = rsqrt(r2.y);
    r7.z = rsqrt(r2.z);
    r7.w = rsqrt(r2.w);
    r1.xyz = (r1.xyz) * (r11.xyz);
    r6 = (r6) * (r7);
    r6 = (r10.yyyy) * (r6);
    r5 = (r5) * (r7);
    r4 = (r4) * (r7);
    r5 = (r5) * (r10.xxxx) + (r6);
    r2 = saturate((r2) * (c[9]) + (r12.yyyy));
    r4 = saturate((r4) * (r10.zzzz) + (r5));
    r1.xyz = (r9.xxx) * (r1.xyz);
    r2 = (r2) * (r4);
    r3.xyz = (r1.xyz) * (r3.xyz) + (r8.xyz);
    r1.x = dot(c[10], r2);
    r1.y = dot(c[11], r2);
    r1.z = dot(c[20], r2);
    r2.xyz = (r0.xyz) * (r1.xyz) + (r3.xyz);
    r2.w = c1.y;
    r0.x = dot(r2, c[25]);
    r0.y = dot(r2, c[26]);
    r0.z = dot(r2, c[27]);
    r0.xyz = (r0.xyz) + (-(v3.xyz));
    r0.xyz = (r1.www) * (r0.xyz) + (v3.xyz);
    r0.xyz = max(((r0.xyz) * (c[24].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.w = rsqrt(r0.w);
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = 1.0f / (r0.w);

    return oC0;
}
