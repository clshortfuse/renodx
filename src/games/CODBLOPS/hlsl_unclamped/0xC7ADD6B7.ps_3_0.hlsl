// Mechanically reconstructed from 0xC7ADD6B7.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler2D s8 : register(s8);

struct PS_INPUT
{
    float4 v0 : TEXCOORD1;
    float4 v1 : TEXCOORD2;
    float4 v2 : TEXCOORD4;
    float4 v3 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    const float4 c0 = float4(0.0f, 0.25f, -1.0f, 1.0f);
    const float4 c1 = float4(9.99999975e-06f, 1e-15f, 1.44269502f, 0.100000001f);
    const float4 c2 = float4(1.16412354f, 1.59579468f, -0.87065506f, 8.0f);
    const float4 c3 = float4(1.16412354f, -0.813476562f, -0.391448975f, 0.529705048f);
    const float4 c4 = float4(1.16412354f, 2.01782227f, -1.08166885f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = (v0.xyzx) * (c0.wwwx) + (c0.xxxw);
    r1.x = dot(r0, c[23]);
    r1.x = 1.0f / (r1.x);
    r1.y = dot(r0, c[20]);
    r1.z = dot(r0, c[21]);
    r0.z = dot(r0, c[22]);
    r1 = (r1.xxxx) * (r1.yyzz);
    r2 = (r1) * (c[24].zwxy);
    r3.x = log2(abs(r2.x));
    r3.y = log2(abs(r2.y));
    r3.z = log2(abs(r2.z));
    r3.w = log2(abs(r2.w));
    r2 = (r3) * (c[25].xxxx);
    r1.x = exp2(r2.x);
    r1.z = exp2(r2.y);
    r2.x = exp2(r2.z);
    r2.y = exp2(r2.w);
    r1.xz = (r1.xz) + (r2.xy);
    r2.x = log2(abs(r1.x));
    r2.y = log2(abs(r1.z));
    r1.xz = (r2.xy) * (c[25].yy);
    r1.x = exp2(r1.x);
    r1.z = exp2(r1.z);
    r1.x = (r1.x) * (c[26].x);
    r1.x = (r1.z) * (c[26].y) + (-(r1.x));
    r2.y = c[26].y;
    r1.z = (r1.z) * (r2.y) + (-(c[25].z));
    r1.x = 1.0f / (r1.x);
    r2.w = saturate((r1.z) * (r1.x));
    r0.xy = abs(r1.yw);
    r3 = c[10];
    r3 = saturate((r0.zyxz) * (r3) + (c[11]));
    r2.x = (r3.w) * (r3.x);
    r2.yz = r3.yz;
    r3 = (r2) * (r2);
    r2 = (c[26].zzzz) * (r2) + (c[26].wwww);
    r2 = (r3) * (r2);
    r0.x = (r2.z) * (r2.y);
    r0.y = abs(c[25].w);
    r0.x = ((-(r0.y)) >= 0.0f ? (r0.x) : (r2.w));
    r0.w = (r0.z) * (r0.z);
    r0.y = dot(c[9].yz, r0.zw) + (c[9].x);
    r0.x = (r0.x) * (r0.y);
    r0.x = (r2.x) * (r0.x);
    r2.xzw = c[8].xzw;
    r0.y = dot(r1.yw, c[27].xy) + (r2.x);
    r0.z = dot(r1.yw, c[27].zw) + (r2.z);
    r1 = tex2D(s2, r0.yz);
    r0.y = (r1.x) * (r1.x);
    r0.x = (r0.x) * (r0.y);
    r1.xy = (c[28].xy) * (v3.ww);
    r1.zw = c0.xx;
    r3 = (r1.xyww) + (v3);
    r1 = (-(r1)) + (v3);
    r1 = tex2Dproj(s1, r1);
    r1.y = r1.x;
    r3 = tex2Dproj(s1, r3);
    r1.x = r3.x;
    r3.xy = (c[28].zw) * (v3.ww);
    r3.zw = c0.xx;
    r4 = (r3.xyww) + (v3);
    r3 = (-(r3)) + (v3);
    r3 = tex2Dproj(s1, r3);
    r1.w = r3.x;
    r3 = tex2Dproj(s1, r4);
    r1.z = r3.x;
    r0.y = dot(r1, c0.yyyy);
    r0.y = (r0.y) + (c0.z);
    r0.y = (r2.w) * (r0.y) + (c0.w);
    r0.x = (r0.x) * (r0.y);
    r0.yzw = (c0.www) + (-(v0.xyz));
    r1.xyz = normalize(r0.yzw);
    r0.y = dot(-(v0.xyz), -(v0.xyz));
    r0.y = rsqrt(r0.y);
    r2.xyz = (-(v0.xyz)) * (r0.yyy) + (r1.xyz);
    r0.yzw = (r0.yyy) * (-(v0.xyz));
    r3.xyz = normalize(r2.xyz);
    r2.xyz = normalize(v1.xyz);
    r1.w = saturate(dot(r2.xyz, r3.xyz));
    r2.w = (r1.w) + (r1.w);
    r1.w = (r1.w) * (r1.w) + (c1.y);
    r1.w = 1.0f / (r1.w);
    r0.y = saturate(dot(r2.xyz, r0.yzw));
    r0.z = saturate(dot(r2.xyz, r1.xyz));
    r0.w = 1.0f / (r0.y);
    r0.w = (r2.w) * (r0.w);
    r1.x = min(r0.y, r0.z);
    r1.y = max(c1.w, r0.y);
    r0.y = 1.0f / (r1.y);
    r0.y = rsqrt(r0.y);
    r0.y = 1.0f / (r0.y);
    r0.w = saturate((r0.w) * (r1.x));
    r0.w = (r0.z) * (r0.w);
    r0.w = rsqrt(r0.w);
    r0.w = 1.0f / (r0.w);
    r1.x = (r1.w) * (r1.w);
    r1.y = (-(r1.w)) + (c0.w);
    r2 = tex2D(s8, v2.xy);
    r1.z = (r2.x) * (r2.x);
    r2.x = max(c1.x, r1.z);
    r1.z = (r2.x) * (r2.x);
    r1.z = 1.0f / (r1.z);
    r1.y = (r1.y) * (r1.z);
    r1.y = (r1.y) * (c1.z);
    r1.y = exp2(r1.y);
    r1.x = (r1.x) * (r1.y);
    r0.w = (r0.w) * (r1.x);
    r1.xy = c[7].xy;
    r2.xyz = (r1.yyy) * (c[6].xyz);
    r2.xyz = (r0.www) * (r2.xyz);
    r2.xyz = (r0.yyy) * (r2.xyz);
    r1.yzw = (r1.zzz) * (r2.xyz);
    r1.yzw = (r1.yzw) * (c0.yyy);
    r2 = tex2D(s6, v2.xy);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r1.yzw = (r1.yzw) * (r2.xyz);
    r0.yw = lerp(c[30].xy, c[30].zw, v2.xy);
    r2 = tex2D(s4, r0.yw);
    r2.y = r2.x;
    r3 = tex2D(s5, r0.yw);
    r4 = tex2D(s3, r0.yw);
    r2.xw = (r4.xx) * (c0.wx) + (c0.xw);
    r2.z = r3.x;
    r3.y = dot(c3, r2);
    r3.x = dot(c2.xyz, r2.xyw);
    r3.z = dot(c4.xyz, r2.xzw);
    r2.xyz = (r3.xyz) * (r3.xyz);
    r2.xyz = (r2.xyz) * (c[32].xxx);
    r3 = tex2D(s7, v2.xy);
    r4 = (r3) * (r3);
    r3.w = (r3.w) * (-(r3.w)) + (c[33].x);
    r3.xyz = (r2.xyz) * (c2.www) + (-(r4.xyz));
    r2 = (c[29].xxxx) * (r3) + (r4);
    r1.yzw = (r1.yzw) * (r2.www);
    r3.xyz = (r1.xxx) * (c[5].xyz);
    r0.yzw = (r0.zzz) * (r3.xyz);
    r2.xyz = (r0.yzw) * (r2.xyz) + (r1.yzw);
    r1 = max(r2, c0.xxxx);
    r0.xyz = (r0.xxx) * (r1.xyz);
    r0.xyz = (r1.www) * (r0.xyz);
    oC0.w = r1.w;
    r0.xyz = max(((r0.xyz) * (c[31].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);

    return oC0;
}
