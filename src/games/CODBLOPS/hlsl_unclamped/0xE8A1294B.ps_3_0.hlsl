// Mechanically reconstructed from 0xE8A1294B.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
    float4 v4 : TEXCOORD5;
    float4 v5 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(-2.0f, 3.0f, 0.0f, 0.25f);
    const float4 c1 = float4(0.5f, -28.0f, 30.0f, 1.0f);
    const float4 c2 = float4(0.38647899f, 0.392856985f, 0.364796013f, 0.782500029f);
    const float4 c3 = float4(8.0f, 31.875f, 15.0f, 0.0399999991f);
    const float4 c4 = float4(0.00749999983f, 1.00750005f, 0.0f, 1.0f);
    const float4 c12 = float4(81.2394867f, 17.3480244f, 37.3498383f, 59.3948402f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = c12;
    r0.x = dot(c[29].wwww, r0);
    r0.x = frac(r0.x);
    r0.w = c[29].w;
    r1.x = dot(r0.xwww, c12);
    r0.y = frac(r1.x);
    r1.x = dot(r0.xyww, c12);
    r0.z = frac(r1.x);
    r1.x = dot(r0, c12);
    r0.w = frac(r1.x);
    r1.yzw = r0.yzw;
    r0.x = dot(r0, c12);
    r1.x = frac(r0.x);
    r0.x = dot(r1, c12);
    r1.y = frac(r0.x);
    r0.x = 1.0f / (c[36].x);
    r0.y = 1.0f / (c[36].y);
    r0.xy = (v3.xy) * (r0.xy) + (r1.xy);
    r0 = tex2D(s2, r0.xy);
    r0.w = c[29].w;
    r1.x = (r0.w) * (c3.z);
    r1.x = frac(r1.x);
    r1.x = (r0.w) * (c3.z) + (-(r1.x));
    r1.y = (r1.x) * (c3.w) + (v3.y);
    r1.x = c2.w;
    r1 = tex2D(s3, r1.xy);
    r1.x = (r1.x) * (-(c4.x)) + (v3.x);
    r1.x = (r1.x) * (c4.y) + (-(v3.x));
    r1.x = (c[37].x) * (r1.x) + (v3.x);
    r1.z = (-(r1.x)) + (c1.w);
    r1.zw = float2(((r1.z) >= 0.0f ? (c4.z) : (c4.w)), ((r1.x) >= 0.0f ? (c4.z) : (c4.w)));
    r1.x = saturate(r1.x);
    r1.z = (r1.z) + (r1.w);
    r1.w = frac(abs(v3.y));
    r1.y = ((v3.y) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2 = tex2D(s4, r1.xy);
    r1.xyw = (r2.xyz) * (r2.xyz);
    r1.xyz = float3(((-(r1.z)) >= 0.0f ? (r1.x) : (c0.z)), ((-(r1.z)) >= 0.0f ? (r1.y) : (c0.z)), ((-(r1.z)) >= 0.0f ? (r1.w) : (c0.z)));
    r1.w = abs(c[29].w);
    r1.w = frac(r1.w);
    r2.y = ((c[29].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2.x = c0.z;
    r2 = tex2D(s3, r2.xy);
    r1.w = saturate((r2.x) * (c[35].x));
    r2.xyz = lerp(r1.xyz, r0.xyz, r1.www);
    r0.x = (r0.w) * (c[38].x);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r0.y = (c[42].x) + (-(v3.y));
    r0.y = (r0.y) + (c1.w);
    r0.x = (r0.x) + (r0.y);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r1.x = pow(abs(r0.x), c[41].x);
    r0.w = c1.w;
    r0.x = (r1.x) * (-(c[40].x)) + (r0.w);
    r1.xyz = lerp(c[39].xyz, r2.xyz, r0.xxx);
    r0.xyz = (c[21].xyz) + (-(v1.xyz));
    r1.w = dot(r0.xyz, r0.xyz);
    r1.w = rsqrt(r1.w);
    r2.x = 1.0f / (r1.w);
    r0.xyz = (r0.xyz) * (r1.www);
    r2.y = (r2.x) * (r2.x);
    r1.w = dot(c[26].yz, r2.xy) + (c[26].x);
    r2.xy = saturate((r2.xx) * (c[27].xy) + (c[27].zw));
    r2.zw = (r2.xy) * (r2.xy);
    r2.xy = (r2.xy) * (c0.xx) + (c0.yy);
    r2.xy = (r2.zw) * (r2.xy);
    r1.w = (r1.w) * (r2.x);
    r1.w = (r2.y) * (r1.w);
    r2.xy = (c[28].xy) * (v5.ww);
    r2.zw = c0.zz;
    r3 = (r2.xyww) + (v5);
    r2 = (-(r2)) + (v5);
    r2 = tex2Dproj(s1, r2);
    r2.y = r2.x;
    r3 = tex2Dproj(s1, r3);
    r2.x = r3.x;
    r3.xy = (c[28].zw) * (v5.ww);
    r3.zw = c0.zz;
    r4 = (r3.xyww) + (v5);
    r3 = (-(r3)) + (v5);
    r3 = tex2Dproj(s1, r3);
    r2.w = r3.x;
    r3 = tex2Dproj(s1, r4);
    r2.z = r3.x;
    r2.x = dot(r2, c0.wwww);
    r2.y = dot(r0.xyz, c[24].xyz);
    r2.y = saturate((r2.y) * (c[25].x) + (c[25].y));
    r2.y = (r2.y) * (c[25].w);
    r3.xyz = normalize(v2.xyz);
    r2.z = max(abs(r3.y), abs(r3.z));
    r4.x = max(abs(r3.x), r2.z);
    r2.z = 1.0f / (r4.x);
    r4.xyz = (r3.xyz) * (c[5].xyz);
    r4.xyz = (r4.xyz) * (r2.zzz) + (v4.xyz);
    r4 = tex3D(s11, r4.xyz);
    r3.w = lerp(r4.w, r2.x, r2.y);
    r1.w = (r1.w) * (r3.w);
    r2.xyz = (r1.www) * (c[22].xyz);
    r2.xyz = (r2.xyz) * (c[23].yyy);
    r1.w = dot(-(r0.xyz), r3.xyz);
    r1.w = (r1.w) + (r1.w);
    r4.xyz = (r3.xyz) * (-(r1.www)) + (-(r0.xyz));
    r5.xyz = normalize(v1.xyz);
    r1.w = saturate(dot(r4.xyz, -(r5.xyz)));
    r0.x = dot(r0.xyz, r5.xyz);
    r0.x = saturate((r0.x) * (c1.x) + (c1.x));
    r0.y = (r0.x) * (c1.y) + (c1.z);
    r0.x = (r0.x) + (c1.w);
    r2.w = pow(abs(r1.w), r0.y);
    r2.xyz = (r2.xyz) * (r2.www);
    r0.xyz = (r0.xxx) * (r2.xyz);
    r1.w = dot(r5.xyz, r3.xyz);
    r1.w = (r1.w) + (r1.w);
    r2.xyz = (r3.xyz) * (-(r1.www)) + (r5.xyz);
    r2 = texCUBE(s15, r2.xyz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = (r2.xyz) * (c3.xxx);
    r4 = tex3D(s11, v4.xyz);
    r4.xyz = (r4.xyz) * (r4.xyz);
    r2.xyz = (r2.xyz) * (r4.xyz);
    r2.xyz = (r2.xyz) * (c[34].xxx);
    r2.xyz = (r2.xyz) * (c3.yyy);
    r0.xyz = (r0.xyz) * (c2.xyz) + (r2.xyz);
    r0.xyz = (r1.xyz) + (r0.xyz);
    r1 = (c[7]) + (-(v1.yyyy));
    r2 = (r1) * (r1);
    r4 = (c[6]) + (-(v1.xxxx));
    r2 = (r4) * (r4) + (r2);
    r5 = (c[8]) + (-(v1.zzzz));
    r2 = (r5) * (r5) + (r2);
    r6.x = rsqrt(r2.x);
    r6.y = rsqrt(r2.y);
    r6.z = rsqrt(r2.z);
    r6.w = rsqrt(r2.w);
    r2 = saturate((r2) * (c[9]) + (r0.wwww));
    r1 = (r1) * (r6);
    r1 = (r3.yyyy) * (r1);
    r4 = (r4) * (r6);
    r5 = (r5) * (r6);
    r1 = (r4) * (r3.xxxx) + (r1);
    r1 = saturate((r5) * (r3.zzzz) + (r1));
    r1 = (r2) * (r1);
    r2.x = dot(c[10], r1);
    r2.y = dot(c[11], r1);
    r2.z = dot(c[20], r1);
    r0.xyz = (r0.xyz) * (r2.xyz) + (r0.xyz);
    r0.w = c1.w;
    r1.x = dot(r0, c[31]);
    r1.y = dot(r0, c[32]);
    r1.z = dot(r0, c[33]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[30].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c1.w;

    return oC0;
}
