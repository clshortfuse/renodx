// Mechanically reconstructed from 0xE0144DA2.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD2;
    float4 v5 : TEXCOORD3;
    float4 v6 : TEXCOORD5;
    float4 v7 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(8.0f, 0.797884583f, 0.959999979f, 0.0399999991f);
    const float4 c2 = float4(1.0f, 0.5f, 0.0f, 31.875f);
    const float4 c3 = float4(0.0009765625f, 0.125f, 0.25f, 0.0f);
    const float4 c4 = float4(4.0f, -2.0f, 0.797884583f, 1.0f);
    const float4 c11 = float4(1.0f, 0.0f, 0.600000024f, 0.400000006f);
    const float4 c12 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    const float4 c13 = float4(-2.0f, 3.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xyz = (-(v6.xyz)) + (c[5].xyz);
    r5.y = dot(r0.xyz, r0.xyz);
    r0.w = rsqrt(r5.y);
    r8.xyz = normalize(v6.xyz);
    r2.xyz = (r0.xyz) * (r0.www);
    r0.xyz = (r0.xyz) * (r0.www) + (-(r8.xyz));
    r5.x = 1.0f / (r0.w);
    r3.xyz = normalize(r0.xyz);
    r0 = tex2D(s1, v1.xy);
    r0.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r1 = tex2D(s2, v7.xy);
    r4.w = (r1.w) * (v0.y);
    r0.w = saturate(dot(r3.xyz, r2.xyz));
    r9.xy = (r4.ww) * (-(r0.xy)) + (r0.xy);
    r1.w = (-(r0.w)) + (c11.x);
    r0 = v2;
    r0.xyz = (r9.xxx) * (v5.xyz) + (r0.xyz);
    r2.w = (r1.w) * (r1.w);
    r4.xyz = (r9.yyy) * (v4.xyz) + (r0.xyz);
    r2.w = (r2.w) * (r2.w);
    r0.xyz = normalize(r4.xyz);
    r4.y = (r1.w) * (r2.w);
    r4.z = saturate(dot(r0.xyz, r2.xyz));
    r2 = tex2D(s4, v1.xy);
    r3.w = (r2.w) * (c1.y);
    r2.z = (r2.w) * (-(c4.z)) + (c4.w);
    r2.x = saturate(dot(r0.xyz, -(r8.xyz)));
    r1.w = (r4.z) * (r2.z) + (r3.w);
    r3.w = (r2.x) * (r2.z) + (r3.w);
    r2.z = (r4.y) * (c1.z) + (c1.w);
    r1.w = (r1.w) * (r3.w) + (c3.x);
    r8.w = (-(r2.x)) + (c11.x);
    r1.w = 1.0f / (r1.w);
    r2.x = (r4.z) * (r1.w);
    r4.xy = (r2.ww) * (c12.xy) + (c12.zw);
    r5.w = saturate(dot(r0.xyz, r3.xyz));
    r1.w = exp2(r4.y);
    r3.w = pow(abs(r5.w), r1.w);
    r1.w = (r1.w) * (c3.y) + (c3.z);
    r7.xyz = (r4.zzz) * (c[6].xyz);
    r1.w = (r3.w) * (r1.w);
    r7.w = 1.0f / (r4.x);
    r1.w = (r2.x) * (r1.w);
    r2.z = (r2.z) * (r1.w);
    r6.w = lerp(r2.y, c11.x, r4.w);
    r1.w = (r2.w) * (c1.x);
    r3.xy = saturate((r5.xx) * (c[9].xy) + (c[9].zw));
    r2.xy = (r3.xy) * (r3.xy);
    r3.xy = (r3.xy) * (c13.xx) + (c13.yy);
    r2.w = dot(c[8].yz, r5.xy) + (c[8].x);
    r2.xy = (r2.xy) * (r3.xy);
    r2.z = (r2.z) * (r6.w);
    r2.w = (r2.w) * (r2.x);
    r4.xyz = (r2.zzz) * (c[7].xyz);
    r3.w = (r2.y) * (r2.w);
    r2 = tex2D(s12, v1.zw);
    r5.w = (r3.w) * (r2.y);
    r2 = tex2D(s0, v1.xy);
    r3.xyz = lerp(r2.xyz, r1.xyz, r4.www);
    r2 = tex2D(s3, v7.zw);
    r1.xyz = (r2.xyz) * (v0.zzz);
    r6.xyz = (r4.xyz) * (r5.www);
    r1.xyz = (r1.xyz) * (r2.www) + (r3.xyz);
    r5.xyz = (r1.xyz) * (r1.xyz);
    r1.xy = (v1.zw) * (c2.xy);
    r2 = tex2D(s13, r1.xy);
    r1.xy = (v1.zw) * (c2.xy) + (c2.zy);
    r4 = tex2D(s13, r1.xy);
    r2.w = r4.y;
    r1.z = dot(r9.xy, r9.xy) + (c11.y);
    r1.xy = (r2.yw) * (c4.xx) + (c4.yy);
    r2.w = exp2(-(r1.z));
    r1.z = dot(r1.xy, r1.xy) + (c11.y);
    r4.w = (r2.w) * (c11.z) + (c11.w);
    r2.w = exp2(-(r1.z));
    r1.z = dot(r1.xy, r9.xy) + (c11.y);
    r1.y = (r2.w) * (c11.z) + (c11.w);
    r3 = tex2D(s14, v1.zw);
    r9.xy = (r3.xy) * (c2.ww);
    r2.w = (r4.w) * (r1.y);
    r1.xy = (r4.xz) * (r9.yy);
    r2.w = saturate((r1.z) * (r2.w) + (r2.w));
    r2.y = (r3.y) * (c2.w) + (-(r1.x));
    r1.xz = (r1.xy) * (c4.xx);
    r1.y = (r4.z) * (-(r9.y)) + (r2.y);
    r1.y = (r1.y) + (r1.y);
    r2.xy = (r2.xz) * (r9.xx);
    r1.xyz = (r2.www) * (r1.xyz);
    r2.w = (r3.x) * (c2.w) + (-(r2.x));
    r3.xz = (r2.xy) * (c4.xx);
    r2.w = (r2.z) * (-(r9.x)) + (r2.w);
    r3.y = (r2.w) + (r2.w);
    r2.w = dot(r8.xyz, r0.xyz);
    r1.xyz = (r3.xyz) * (r4.www) + (r1.xyz);
    r2.w = (r2.w) + (r2.w);
    r2.xyz = (r6.www) * (r1.xyz);
    r1.xyz = (r0.xyz) * (-(r2.www)) + (r8.xyz);
    r1 = texCUBElod(s15, r1);
    r0.z = (r8.w) * (r8.w);
    r1.w = (r8.w) * (r0.z);
    r0.xyz = (r1.xyz) * (r1.xyz);
    r1.w = (r7.w) * (r1.w);
    r0.xyz = (r6.www) * (r0.xyz);
    r1.w = (r1.w) * (c1.z) + (c1.w);
    r1.xyz = (r5.www) * (r7.xyz) + (r2.xyz);
    r0.xyz = (r0.xyz) * (r1.www);
    r1.xyz = (r5.xyz) * (r1.xyz) + (r6.xyz);
    r0.xyz = (r3.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c1.xxx) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v3.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v3.xyz);
    r0.xyz = max(((r0.xyz) * (c[10].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c11.x;

    return oC0;
}
