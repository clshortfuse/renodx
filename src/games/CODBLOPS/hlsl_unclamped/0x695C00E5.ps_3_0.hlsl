// Mechanically reconstructed from 0x695C00E5.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler3D s11 : register(s11);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
    float4 v4 : TEXCOORD5;
    float4 v5 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(0.0f, 0.25f, 1.0f, 0.5f);
    const float4 c1 = float4(-2.0f, 3.0f, -28.0f, 30.0f);
    const float4 c2 = float4(0.38647899f, 0.392856985f, 0.364796013f, 0.782500029f);
    const float4 c3 = float4(8.0f, 31.875f, 15.0f, 0.0399999991f);
    const float4 c4 = float4(0.00749999983f, 1.00750005f, 0.0f, 0.0f);
    const float4 c12 = float4(81.2394867f, 17.3480244f, 37.3498383f, 59.3948402f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 oC0 = 0.0f;

    r0.xyz = (c[28].xyz) + (-(v1.xyz));
    r0.w = dot(r0.xyz, r0.xyz);
    r0.w = rsqrt(r0.w);
    r1.x = 1.0f / (r0.w);
    r0.xyz = (r0.xyz) * (r0.www);
    r1.y = (r1.x) * (r1.x);
    r0.w = dot(c[33].yz, r1.xy) + (c[33].x);
    r1.xy = saturate((r1.xx) * (c[34].xy) + (c[34].zw));
    r1.zw = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c1.xx) + (c1.yy);
    r1.xy = (r1.zw) * (r1.xy);
    r0.w = (r0.w) * (r1.x);
    r0.w = (r1.y) * (r0.w);
    r1.xy = (c[35].xy) * (v5.ww);
    r1.zw = c0.xx;
    r2 = (r1.xyww) + (v5);
    r1 = (-(r1)) + (v5);
    r1 = tex2Dproj(s1, r1);
    r1.y = r1.x;
    r2 = tex2Dproj(s1, r2);
    r1.x = r2.x;
    r2.xy = (c[35].zw) * (v5.ww);
    r2.zw = c0.xx;
    r3 = (r2.xyww) + (v5);
    r2 = (-(r2)) + (v5);
    r2 = tex2Dproj(s1, r2);
    r1.w = r2.x;
    r2 = tex2Dproj(s1, r3);
    r1.z = r2.x;
    r1.x = dot(r1, c0.yyyy);
    r1.y = dot(r0.xyz, c[31].xyz);
    r1.y = saturate((r1.y) * (c[32].x) + (c[32].y));
    r1.y = (r1.y) * (c[32].w);
    r2.xyz = normalize(v2.xyz);
    r1.z = max(abs(r2.y), abs(r2.z));
    r3.x = max(abs(r2.x), r1.z);
    r1.z = 1.0f / (r3.x);
    r3.xyz = (r2.xyz) * (c[5].xyz);
    r3.xyz = (r3.xyz) * (r1.zzz) + (v4.xyz);
    r3 = tex3D(s11, r3.xyz);
    r2.w = lerp(r3.w, r1.x, r1.y);
    r0.w = (r0.w) * (r2.w);
    r1.xyz = (r0.www) * (c[29].xyz);
    r1.xyz = (r1.xyz) * (c[30].yyy);
    r0.w = dot(-(r0.xyz), r2.xyz);
    r0.w = (r0.w) + (r0.w);
    r3.xyz = (r2.xyz) * (-(r0.www)) + (-(r0.xyz));
    r4.xyz = normalize(v1.xyz);
    r0.w = saturate(dot(r3.xyz, -(r4.xyz)));
    r0.x = dot(r0.xyz, r4.xyz);
    r0.x = saturate((r0.x) * (c0.w) + (c0.w));
    r0.y = (r0.x) * (c1.z) + (c1.w);
    r0.x = (r0.x) + (c0.z);
    r1.w = pow(abs(r0.w), r0.y);
    r1.xyz = (r1.xyz) * (r1.www);
    r3.zw = c0.xx;
    r5 = (c[25]) * (v1.yyyy);
    r5 = (v1.xxxx) * (c[24]) + (r5);
    r5 = (v1.zzzz) * (c[26]) + (r5);
    r5 = (r5) + (c[27]);
    r3.xy = (r5.ww) * (c[36].xy);
    r6 = (r3.xyww) + (r5);
    r3 = (-(r3)) + (r5);
    r3 = tex2Dproj(s1, r3);
    r3.y = r3.x;
    r6 = tex2Dproj(s1, r6);
    r3.x = r6.x;
    r6.xy = (r5.ww) * (c[36].zw);
    r6.zw = c0.xx;
    r7 = (r5) + (r6.xyww);
    r5 = (r5) + (-(r6));
    r5 = tex2Dproj(s1, r5);
    r3.w = r5.x;
    r5 = tex2Dproj(s1, r7);
    r3.z = r5.x;
    r0.z = dot(r3, c0.yyyy);
    r3 = (v1.xyzx) * (c0.zzzx) + (c0.xxxz);
    r0.w = dot(r3, c[21]);
    r0.w = 1.0f / (r0.w);
    r5.x = dot(r3, c[10]);
    r5.y = dot(r3, c[11]);
    r3.x = dot(r3, c[20]);
    r3.zw = (r0.ww) * (r5.xy);
    r3.zw = (r3.zw) * (c0.ww) + (c0.ww);
    r5 = tex2D(s2, r3.zw);
    r0.w = (r5.x) * (r5.x);
    r3.y = (r3.x) * (r3.x);
    r1.w = dot(c[8].yz, r3.xy) + (c[8].x);
    r3.xy = saturate((r3.xx) * (c[9].xy) + (c[9].zw));
    r2.w = saturate(1.0f / (r1.w));
    r1.w = ((-abs(r1.w)) >= 0.0f ? (c0.x) : (r2.w));
    r3.zw = (r3.xy) * (r3.xy);
    r3.xy = (r3.xy) * (c1.xx) + (c1.yy);
    r2.w = (r3.z) * (r3.x);
    r3.x = (r3.w) * (-(r3.y)) + (c0.z);
    r1.w = (r1.w) * (r2.w);
    r1.w = (r3.x) * (r1.w);
    r3.xyz = (c[6].xyz) + (-(v1.xyz));
    r5.xyz = normalize(r3.xyz);
    r2.w = dot(r5.xyz, c[22].xyz);
    r2.w = saturate((r2.w) * (c[23].x) + (c[23].y));
    r3.x = (r2.w) * (r2.w);
    r2.w = (r2.w) * (c1.x) + (c1.y);
    r2.w = (r3.x) * (r2.w);
    r1.w = (r1.w) * (r2.w);
    r0.w = (r0.w) * (r1.w);
    r0.z = (r0.z) * (r0.w);
    r3.xyz = (r0.zzz) * (c[7].xyz);
    r0.z = dot(-(r5.xyz), r2.xyz);
    r0.z = (r0.z) + (r0.z);
    r6.xyz = (r2.xyz) * (-(r0.zzz)) + (-(r5.xyz));
    r0.z = dot(r5.xyz, r4.xyz);
    r0.z = saturate((r0.z) * (c0.w) + (c0.w));
    r0.w = saturate(dot(r6.xyz, -(r4.xyz)));
    r1.w = lerp(r0.y, -(c1.x), r0.z);
    r2.w = lerp(r0.x, -(c1.x), r0.z);
    r3.w = pow(abs(r0.w), r1.w);
    r0.yzw = (r3.xyz) * (r3.www);
    r0.yzw = (r2.www) * (r0.yzw);
    r0.xyz = (r1.xyz) * (r0.xxx) + (r0.yzw);
    r0.w = dot(r4.xyz, r2.xyz);
    r0.w = (r0.w) + (r0.w);
    r1.xyz = (r2.xyz) * (-(r0.www)) + (r4.xyz);
    r1 = texCUBE(s15, r1.xyz);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r1.xyz = (r1.xyz) * (c3.xxx);
    r2 = tex3D(s11, v4.xyz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r1.xyz = (r1.xyz) * (r2.xyz);
    r1.xyz = (r1.xyz) * (c[42].xxx);
    r1.xyz = (r1.xyz) * (c3.yyy);
    r0.xyz = (r0.xyz) * (c2.xyz) + (r1.xyz);
    r1 = c12;
    r0.w = dot(c[37].wwww, r1);
    r1.x = frac(r0.w);
    r1.w = c[37].w;
    r0.w = dot(r1.xwww, c12);
    r1.y = frac(r0.w);
    r0.w = dot(r1.xyww, c12);
    r1.z = frac(r0.w);
    r0.w = dot(r1, c12);
    r1.w = frac(r0.w);
    r2.yzw = r1.yzw;
    r0.w = dot(r1, c12);
    r2.x = frac(r0.w);
    r0.w = dot(r2, c12);
    r2.y = frac(r0.w);
    r1.x = 1.0f / (c[44].x);
    r1.y = 1.0f / (c[44].y);
    r1.xy = (v3.xy) * (r1.xy) + (r2.xy);
    r1 = tex2D(s3, r1.xy);
    r0.w = c[37].w;
    r1.w = (r0.w) * (c3.z);
    r1.w = frac(r1.w);
    r1.w = (r0.w) * (c3.z) + (-(r1.w));
    r2.y = (r1.w) * (c3.w) + (v3.y);
    r2.x = c2.w;
    r2 = tex2D(s4, r2.xy);
    r1.w = (r2.x) * (-(c4.x)) + (v3.x);
    r1.w = (r1.w) * (c4.y) + (-(v3.x));
    r2.x = (c[45].x) * (r1.w) + (v3.x);
    r1.w = (-(r2.x)) + (c0.z);
    r1.w = ((r1.w) >= 0.0f ? (c0.x) : (c0.z));
    r2.z = ((r2.x) >= 0.0f ? (c0.x) : (c0.z));
    r2.x = saturate(r2.x);
    r1.w = (r1.w) + (r2.z);
    r2.z = frac(abs(v3.y));
    r2.y = ((v3.y) >= 0.0f ? (r2.z) : (-(r2.z)));
    r2 = tex2D(s5, r2.xy);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = float3(((-(r1.w)) >= 0.0f ? (r2.x) : (c0.x)), ((-(r1.w)) >= 0.0f ? (r2.y) : (c0.x)), ((-(r1.w)) >= 0.0f ? (r2.z) : (c0.x)));
    r1.w = abs(c[37].w);
    r1.w = frac(r1.w);
    r3.y = ((c[37].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r3.x = c0.x;
    r3 = tex2D(s4, r3.xy);
    r1.w = saturate((r3.x) * (c[43].x));
    r3.xyz = lerp(r2.xyz, r1.xyz, r1.www);
    r0.w = (r0.w) * (c[46].x);
    r1.x = frac(abs(r0.w));
    r0.w = ((r0.w) >= 0.0f ? (r1.x) : (-(r1.x)));
    r1.x = (c[50].x) + (-(v3.y));
    r1.x = (r1.x) + (c0.z);
    r0.w = (r0.w) + (r1.x);
    r1.x = frac(abs(r0.w));
    r0.w = ((r0.w) >= 0.0f ? (r1.x) : (-(r1.x)));
    r1.x = pow(abs(r0.w), c[49].x);
    r1.z = c0.z;
    r0.w = (r1.x) * (-(c[48].x)) + (r1.z);
    r1.xyz = lerp(c[47].xyz, r3.xyz, r0.www);
    r0.xyz = (r0.xyz) + (r1.xyz);
    r0.w = c0.z;
    r1.x = dot(r0, c[39]);
    r1.y = dot(r0, c[40]);
    r1.z = dot(r0, c[41]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[38].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c0.z;

    return oC0;
}
