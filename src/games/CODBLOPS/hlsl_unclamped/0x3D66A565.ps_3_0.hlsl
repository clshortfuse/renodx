// Mechanically reconstructed from 0x3D66A565.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler3D s11 : register(s11);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD4;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    const float4 c0 = float4(1.0f, 0.0f, 31.875f, 0.25f);
    const float4 c1 = float4(-2.0f, 3.0f, 0.5f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = (v5.xyzx) * (c0.xxxy) + (c0.yyyx);
    r1.w = dot(r0, c[38]);
    r1.w = 1.0f / (r1.w);
    r1.x = dot(r0, c[35]);
    r1.y = dot(r0, c[36]);
    r1 = (r1.wwww) * (r1.xxyy);
    r2 = (r1) * (c[39].zwxy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.z = log2(abs(r2.z));
    r2.w = log2(abs(r2.w));
    r2 = (r2) * (c[40].xxxx);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = exp2(r2.z);
    r2.w = exp2(r2.w);
    r2.xy = (r2.zw) + (r2.xy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.xy = (r2.xy) * (c[40].yy);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r1.z = (r2.x) * (c[41].x);
    r1.y = (r2.y) * (c[41].y) + (-(r1.z));
    r1.z = c[41].y;
    r1.z = (r2.y) * (r1.z) + (-(c[40].z));
    r1.y = 1.0f / (r1.y);
    r3.xy = abs(r1.xw);
    r3.z = dot(r0, c[37]);
    r4.w = saturate((r1.z) * (r1.y));
    r2 = c[33];
    r2 = saturate((r3.zyxz) * (r2) + (c[34]));
    r4.x = (r2.w) * (r2.x);
    r4.yz = r2.yz;
    r2 = (r4) * (r4);
    r4 = (c[41].zzzz) * (r4) + (c[41].wwww);
    r2 = (r2) * (r4);
    r1.y = (r2.z) * (r2.y);
    r1.z = abs(c[40].w);
    r3.w = (r3.z) * (r3.z);
    r1.y = ((-(r1.z)) >= 0.0f ? (r1.y) : (r2.w));
    r1.z = dot(c[32].yz, r3.zw) + (c[32].x);
    r1.z = (r1.y) * (r1.z);
    r2.w = (r2.x) * (r1.z);
    r2.xz = c[31].xz;
    r2.x = dot(r1.xw, c[42].xy) + (r2.x);
    r2.y = dot(r1.xw, c[42].zw) + (r2.z);
    r1 = tex2D(s2, r2.xy);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r4.xyz = (r2.www) * (r1.xyz);
    r1.xy = (v4.ww) * (c[43].xy) + (v4.xy);
    r1.zw = v4.zw;
    r1 = tex2Dproj(s1, r1);
    r2.xy = (v4.ww) * (-(c[43].xy)) + (v4.xy);
    r2.zw = v4.zw;
    r2 = tex2Dproj(s1, r2);
    r1.y = r2.x;
    r2.xy = (v4.ww) * (c[43].zw) + (v4.xy);
    r2.zw = v4.zw;
    r2 = tex2Dproj(s1, r2);
    r1.z = r2.x;
    r2.xy = (v4.ww) * (-(c[43].zw)) + (v4.xy);
    r2.zw = v4.zw;
    r2 = tex2Dproj(s1, r2);
    r3.xyz = normalize(v2.xyz);
    r4.w = max(abs(r3.y), abs(r3.z));
    r1.w = r2.x;
    r2.w = max(abs(r3.x), r4.w);
    r2.w = 1.0f / (r2.w);
    r2.xyz = (r3.xyz) * (c[5].xyz);
    r3.w = dot(r1, c0.wwww);
    r1.xyz = (r2.xyz) * (r2.www) + (v6.xyz);
    r1 = tex3D(s11, r1.xyz);
    r2.w = lerp(r1.w, r3.w, c[31].w);
    r1.xyz = (r1.xyz) * (r1.xyz);
    r5.xyz = (r4.xyz) * (r2.www);
    r2.xyz = (-(v5.xyz)) + (c[28].xyz);
    r7.xyz = (r1.xyz) * (c0.zzz);
    r1.xyz = normalize(r2.xyz);
    r2.w = saturate(dot(r1.xyz, r3.xyz));
    r1.w = dot(r0, c[21]);
    r1.xyz = c[29].xyz;
    r1.xyz = (r1.xyz) * (c[30].xxx);
    r1.w = 1.0f / (r1.w);
    r2.x = dot(r0, c[10]);
    r2.y = dot(r0, c[11]);
    r6.xyz = (r2.www) * (r1.xyz);
    r2.xy = (r1.ww) * (r2.xy);
    r1.x = dot(r0, c[20]);
    r0.xy = (r2.xy) * (c1.zz) + (c1.zz);
    r0 = tex2D(s3, r0.xy);
    r1.y = (r1.x) * (r1.x);
    r8.xyz = (r0.xyz) * (r0.xyz);
    r0.w = dot(c[8].yz, r1.xy) + (c[8].x);
    r1.xy = saturate((r1.xx) * (c[9].xy) + (c[9].zw));
    r0.z = saturate(1.0f / (r0.w));
    r0.xy = (r1.xy) * (r1.xy);
    r1.xy = (r1.xy) * (c1.xx) + (c1.yy);
    r0.w = ((-abs(r0.w)) >= 0.0f ? (c0.y) : (r0.z));
    r0.z = (r0.x) * (r1.x);
    r1.w = (r0.y) * (-(r1.y)) + (c0.x);
    r1.xyz = (-(v5.xyz)) + (c[6].xyz);
    r0.w = (r0.w) * (r0.z);
    r0.xyz = normalize(r1.xyz);
    r1.w = (r1.w) * (r0.w);
    r0.w = dot(r0.xyz, c[22].xyz);
    r5.w = saturate(dot(r0.xyz, r3.xyz));
    r1.y = saturate((r0.w) * (c[23].x) + (c[23].y));
    r1.z = (r1.y) * (r1.y);
    r0 = (v5.yyyy) * (c[25]);
    r1.y = (r1.y) * (c1.x) + (c1.y);
    r0 = (v5.xxxx) * (c[24]) + (r0);
    r1.z = (r1.z) * (r1.y);
    r0 = (v5.zzzz) * (c[26]) + (r0);
    r6.w = (r1.w) * (r1.z);
    r1 = (r0) + (c[27]);
    r3.xy = (r1.ww) * (c[44].xy) + (r1.xy);
    r3.zw = r1.zw;
    r0 = tex2Dproj(s1, r3);
    r2.zw = r3.zw;
    r1.zw = r2.zw;
    r4.xy = (r3.ww) * (-(c[44].zw)) + (r1.xy);
    r4.zw = r1.zw;
    r4 = tex2Dproj(s1, r4);
    r0.w = r4.x;
    r2.xy = (r3.ww) * (-(c[44].xy)) + (r1.xy);
    r1.xy = (r3.ww) * (c[44].zw) + (r1.xy);
    r2 = tex2Dproj(s1, r2);
    r0.y = r2.x;
    r1 = tex2Dproj(s1, r1);
    r0.z = r1.x;
    r2.xyz = (r8.xyz) * (r6.www);
    r1.w = dot(r0, c0.wwww);
    r0 = tex2D(s0, v1.xy);
    r0.xyz = (r0.xyz) * (v0.xyz);
    r1.xyz = (r5.www) * (c[7].xyz);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r2.xyz = (r2.xyz) * (r1.www);
    r3.xyz = (r1.xyz) * (r0.xyz);
    r1.xyz = (r5.xyz) * (r6.xyz) + (r7.xyz);
    r2.xyz = (r2.xyz) * (r3.xyz);
    r0.xyz = (r0.xyz) * (r1.xyz) + (r2.xyz);
    r0.w = c0.x;
    r1.x = dot(r0, c[46]);
    r1.y = dot(r0, c[47]);
    r1.z = dot(r0, c[48]);
    r0.xyz = (r1.xyz) + (-(v3.xyz));
    r0.w = v2.w;
    r0.xyz = (r0.www) * (r0.xyz) + (v3.xyz);
    r0.xyz = max(((r0.xyz) * (c[45].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c0.x;

    return oC0;
}
