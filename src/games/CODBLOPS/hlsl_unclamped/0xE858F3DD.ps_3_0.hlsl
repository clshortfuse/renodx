// Mechanically reconstructed from 0xE858F3DD.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);

struct PS_INPUT
{
    float4 v0 : TEXCOORD2;
    float4 v1 : TEXCOORD3;
    float4 v2 : TEXCOORD4;
    float4 v3 : TEXCOORD5;
    float4 v4 : TEXCOORD6;
    float4 v5 : TEXCOORD7;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(1.0f, -2.0f, 3.0f, 0.5f);
    const float4 c2 = float4(0.5f, 0.449999988f, 0.330000013f, 0.0900000036f);
    const float4 c3 = float4(1.0f, 0.5f, -0.0f, 31.875f);
    const float4 c4 = float4(4.0f, -2.0f, 9.99999975e-05f, 0.100000001f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 r12 = 0.0f;
    float4 r13 = 0.0f;
    float4 oC0 = 0.0f;

    r1.xy = (v5.xy) * (c[25].xy);
    r0 = tex2D(s1, r1.xy);
    r6 = tex2D(s0, r1.xy);
    r1.xy = (v5.zw) * (c1.xw);
    r3 = tex2D(s13, r1.xy);
    r1.xy = (v5.zw) * (c3.xy) + (c3.zy);
    r1 = tex2D(s13, r1.xy);
    r3.w = r1.y;
    r4.xy = (r3.yw) * (c4.xx) + (c4.yy);
    r1.w = dot(v3.xyz, v3.xyz);
    r2.xyz = (r4.yyy) * (v1.xyz);
    r8.w = rsqrt(r1.w);
    r2.xyz = (r4.xxx) * (v0.xyz) + (r2.xyz);
    r7.w = (r0.w) + (-(c1.x));
    r2.xyz = (v3.xyz) * (r8.www) + (r2.xyz);
    r7.xyz = normalize(-(v2.xyz));
    r10.xyz = normalize(r2.xyz);
    r2 = tex2D(s14, v5.zw);
    r4.xy = (r2.xy) * (c3.ww);
    r0.w = dot(r10.xyz, r7.xyz);
    r3.xy = (r3.xz) * (r4.xx);
    r1.w = (r2.x) * (c3.w) + (-(r3.x));
    r9.xz = (r3.xy) * (c4.xx);
    r1.w = (r3.z) * (-(r4.x)) + (r1.w);
    r1.xy = (r1.xz) * (r4.yy);
    r9.y = (r1.w) + (r1.w);
    r1.w = (r2.y) * (c3.w) + (-(r1.x));
    r8.xz = (r1.xy) * (c4.xx);
    r1.w = (r1.z) * (-(r4.y)) + (r1.w);
    r8.y = (r1.w) + (r1.w);
    r4 = (-(v2.yyyy)) + (c[6]);
    r1 = (r4) * (r4);
    r3 = (-(v2.xxxx)) + (c[5]);
    r1 = (r3) * (r3) + (r1);
    r2 = (-(v2.zzzz)) + (c[7]);
    r11.xyz = (r0.www) * (r8.xyz) + (r9.xyz);
    r1 = (r2) * (r2) + (r1);
    r5.x = rsqrt(r1.x);
    r5.y = rsqrt(r1.y);
    r5.z = rsqrt(r1.z);
    r5.w = rsqrt(r1.w);
    r0.w = c1.x;
    r1 = saturate((r1) * (c[8]) + (r0.wwww));
    r3 = (r3) * (r5);
    r4 = (r4) * (r5);
    r2 = (r2) * (r5);
    r5 = (r7.yyyy) * (r4);
    r13.xy = (r6.wy) * (c0.xy) + (c0.zw);
    r6.xyz = v3.xyz;
    r12.xyz = (r6.zxy) * (v0.yzx);
    r13.xy = (r13.xy) * (c[26].xx);
    r6.xyz = (r6.yzx) * (v0.zxy) + (-(r12.xyz));
    r5 = (r3) * (r7.xxxx) + (r5);
    r6.xyz = (r13.yyy) * (-(r6.xyz));
    r5 = saturate((r2) * (r7.zzzz) + (r5));
    r6.xyz = (r13.xxx) * (v0.xyz) + (r6.xyz);
    r5 = (r1) * (r5);
    r13.xyz = (r6.xyz) + (v3.xyz);
    r12.x = dot(c[9], r5);
    r6.xyz = normalize(r13.xyz);
    r12.y = dot(c[10], r5);
    r4 = (r4) * (r6.yyyy);
    r12.z = dot(c[11], r5);
    r3 = (r3) * (r6.xxxx) + (r4);
    r4.xyz = (r11.xyz) + (r12.xyz);
    r2 = saturate((r2) * (r6.zzzz) + (r3));
    r0.w = dot(r10.xyz, r6.xyz);
    r1 = (r1) * (r2);
    r2.xyz = (r0.www) * (r8.xyz) + (r9.xyz);
    r3.x = dot(c[9], r1);
    r3.y = dot(c[10], r1);
    r3.z = dot(c[11], r1);
    r0.w = (c[27].x) * (c[27].x);
    r1.w = c[27].x;
    r1.xy = (r1.ww) * (r1.ww) + (c2.zw);
    r5.xy = (r0.ww) * (c2.xy);
    r8.x = 1.0f / (r1.x);
    r8.y = 1.0f / (r1.y);
    r1.xyz = (r2.xyz) + (r3.xyz);
    r2.w = (r5.x) * (-(r8.x)) + (c1.x);
    r0.w = (r5.y) * (r8.y);
    r2.xyz = (r1.xyz) * (r2.www);
    r1.xyz = (r4.xyz) * (r2.xyz);
    r3.xyz = (-(v2.xyz)) + (c[20].xyz);
    r1.x = rsqrt(r1.x);
    r1.y = rsqrt(r1.y);
    r1.z = rsqrt(r1.z);
    r5.y = dot(r3.xyz, r3.xyz);
    r1.x = 1.0f / (r1.x);
    r1.y = 1.0f / (r1.y);
    r1.z = 1.0f / (r1.z);
    r4.z = rsqrt(r5.y);
    r3.w = saturate(dot(r6.xyz, r7.xyz));
    r3.xyz = (r3.xyz) * (r4.zzz);
    r4.w = saturate(dot(r6.xyz, r3.xyz));
    r1.w = dot(r3.xyz, r7.xyz);
    r3.xyz = (r0.www) * (r1.xyz);
    r1.w = saturate((r4.w) * (-(r3.w)) + (r1.w));
    r5.x = 1.0f / (r4.z);
    r1.w = (r0.w) * (r1.w);
    r1.z = 1.0f / (r3.w);
    r4.xy = saturate((r5.xx) * (c[23].xy) + (c[23].zw));
    r1.xy = (r4.xy) * (r4.xy);
    r4.xy = (r4.xy) * (c1.yy) + (c1.zz);
    r0.w = dot(c[22].yz, r5.xy) + (c[22].x);
    r1.xy = (r1.xy) * (r4.xy);
    r1.z = saturate((r4.w) * (r1.z));
    r0.w = (r0.w) * (r1.x);
    r4.z = (r1.w) * (r1.z);
    r0.w = (r1.y) * (r0.w);
    r1 = tex2D(s12, v5.zw);
    r0.w = (r0.w) * (r1.y);
    r2.w = (r2.w) * (r4.w) + (r4.z);
    r1.xyz = (r0.www) * (c[21].xyz);
    r0.w = (r3.w) * (r3.w);
    r1.w = (r3.w) * (-(r3.w)) + (c1.x);
    r1.xyz = (r1.xyz) * (r2.www) + (r2.xyz);
    r0.w = ((-(r0.w)) >= 0.0f ? (c1.x) : (r1.w));
    r2.xyz = (r0.xyz) * (r0.xyz);
    r0.xyz = (r3.xyz) * (r0.www) + (r1.xyz);
    r1.xyz = (r8.www) * (v3.xyz);
    r0.xyz = (r0.xyz) * (r2.xyz) + (-(v4.xyz));
    r0.w = dot(r7.xyz, r1.xyz);
    r1.x = v0.w;
    r0.xyz = (r1.xxx) * (r0.xyz) + (v4.xyz);
    r1.w = max(c4.z, r0.w);
    r0.xyz = max(((r0.xyz) * (c[24].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.w = pow(abs(r1.w), c4.w);
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.w = (r0.w) * (r7.w) + (c1.x);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);

    return oC0;
}
