// Mechanically reconstructed from 0x180976FB.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD8;
    float4 v3 : TEXCOORD2;
    float4 v4 : TEXCOORD3;
    float4 v5 : TEXCOORD5;
    float4 v6 : TEXCOORD6;
    float4 v7 : COLOR1;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    float4 v5 = input.v5;
    float4 v6 = input.v6;
    float4 v7 = input.v7;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(2.0f, -1.0f, 0.0f, -0.5f);
    const float4 c2 = float4(0.600000024f, 0.400000006f, 8.0f, 0.797884583f);
    const float4 c3 = float4(0.959999979f, 0.0399999991f, 31.875f, 4.0f);
    const float4 c4 = float4(0.0009765625f, 0.125f, 0.25f, 0.0f);
    const float4 c12 = float4(4.0f, -2.0f, 0.797884583f, 1.0f);
    const float4 c13 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    const float4 c14 = float4(-2.0f, 3.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = tex2D(s1, v0.xy);
    r4.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r0 = tex2D(s3, v6.zw);
    r1.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r0 = (c1.xxxx) * (v7) + (c1.yyyy);
    r0.x = dot(r1.xy, r0.xy) + (c1.z);
    r0.y = dot(r1.xy, r0.zw) + (c1.z);
    r2 = tex2D(s5, v6.zw);
    r0.z = r2.w;
    r3 = tex2D(s4, v0.xy);
    r4.z = r3.w;
    r1 = tex2D(s2, v6.zw);
    r9.w = (r1.w) * (v6.y) + (c1.w);
    r8.xyz = float3(((r9.w) >= 0.0f ? (r0.x) : (r4.x)), ((r9.w) >= 0.0f ? (r0.y) : (r4.y)), ((r9.w) >= 0.0f ? (r0.z) : (r4.z)));
    r3.z = (r8.z) * (c2.w);
    r2.w = (r8.z) * (-(c12.z)) + (c12.w);
    r0 = v1;
    r0.xyz = (r8.xxx) * (v4.xyz) + (r0.xyz);
    r5.xyz = (r8.yyy) * (v3.xyz) + (r0.xyz);
    r4.xyz = (-(v5.xyz)) + (c[5].xyz);
    r0.xyz = normalize(r5.xyz);
    r9.y = dot(r4.xyz, r4.xyz);
    r6.xyz = normalize(v5.xyz);
    r2.z = rsqrt(r9.y);
    r3.w = saturate(dot(r0.xyz, -(r6.xyz)));
    r5.xyz = (r4.xyz) * (r2.zzz);
    r2.x = (r3.w) * (r2.w) + (r3.z);
    r1.w = saturate(dot(r0.xyz, r5.xyz));
    r8.w = (-(r3.w)) + (-(c1.y));
    r2.w = (r1.w) * (r2.w) + (r3.z);
    r7.xyz = (r4.xyz) * (r2.zzz) + (-(r6.xyz));
    r2.w = (r2.w) * (r2.x) + (c4.x);
    r9.x = 1.0f / (r2.z);
    r2.w = 1.0f / (r2.w);
    r2.z = (r1.w) * (r2.w);
    r4.xyz = normalize(r7.xyz);
    r7.xyz = (r1.www) * (c[6].xyz);
    r2.x = saturate(dot(r0.xyz, r4.xyz));
    r2.w = saturate(dot(r4.xyz, r5.xyz));
    r4.xy = (r8.zz) * (c13.xy) + (c13.zw);
    r1.w = exp2(r4.y);
    r2.w = (-(r2.w)) + (-(c1.y));
    r3.w = pow(abs(r2.x), r1.w);
    r2.x = (r2.w) * (r2.w);
    r1.w = (r1.w) * (c4.y) + (c4.z);
    r2.x = (r2.x) * (r2.x);
    r1.w = (r3.w) * (r1.w);
    r2.w = (r2.w) * (r2.x);
    r1.w = (r2.z) * (r1.w);
    r2.w = (r2.w) * (c3.x) + (c3.y);
    r7.w = 1.0f / (r4.x);
    r2.w = (r1.w) * (r2.w);
    r6.w = ((r9.w) >= 0.0f ? (r2.y) : (r3.y));
    r3.xy = saturate((r9.xx) * (c[9].xy) + (c[9].zw));
    r2.xy = (r3.xy) * (r3.xy);
    r3.xy = (r3.xy) * (c14.xx) + (c14.yy);
    r1.w = dot(c[8].yz, r9.xy) + (c[8].x);
    r2.xy = (r2.xy) * (r3.xy);
    r2.w = (r2.w) * (r6.w);
    r1.w = (r1.w) * (r2.x);
    r9.xyz = (r2.www) * (c[7].xyz);
    r10.w = (r2.y) * (r1.w);
    r2 = tex2D(s12, v0.zw);
    r3.xy = (v0.zw) * (-(c1.yw));
    r3 = tex2D(s13, r3.xy);
    r4.xy = (v0.zw) * (-(c1.yw)) + (-(c1.zw));
    r5 = tex2D(s13, r4.xy);
    r3.w = r5.y;
    r4.xy = (r3.yw) * (c12.xx) + (c12.yy);
    r1.w = dot(r8.xy, r8.xy) + (c1.z);
    r2.w = dot(r4.xy, r4.xy) + (c1.z);
    r1.w = exp2(-(r1.w));
    r2.w = exp2(-(r2.w));
    r1.w = (r1.w) * (c2.x) + (c2.y);
    r2.z = (r2.w) * (c2.x) + (c2.y);
    r2.w = dot(r4.xy, r8.xy) + (c1.z);
    r2.z = (r1.w) * (r2.z);
    r4 = tex2D(s14, v0.zw);
    r8.xy = (r4.xy) * (c3.zz);
    r2.z = saturate((r2.w) * (r2.z) + (r2.z));
    r5.xy = (r5.xz) * (r8.yy);
    r2.w = (r4.y) * (c3.z) + (-(r5.x));
    r10.xz = (r5.xy) * (c3.ww);
    r2.w = (r5.z) * (-(r8.y)) + (r2.w);
    r3.xy = (r3.xz) * (r8.xx);
    r10.y = (r2.w) + (r2.w);
    r2.w = (r4.x) * (c3.z) + (-(r3.x));
    r4.xyz = (r2.zzz) * (r10.xyz);
    r2.w = (r3.z) * (-(r8.x)) + (r2.w);
    r3.xz = (r3.xy) * (c3.ww);
    r3.y = (r2.w) + (r2.w);
    r2.w = (r10.w) * (r2.y);
    r2.xyz = (r3.xyz) * (r1.www) + (r4.xyz);
    r5.xyz = (r9.xyz) * (r2.www);
    r2.xyz = (r6.www) * (r2.xyz);
    r1.w = (r8.z) * (c2.z);
    r4.xyz = (r2.www) * (r7.xyz) + (r2.xyz);
    r2 = tex2D(s0, v0.xy);
    r2.w = dot(r6.xyz, r0.xyz);
    r2.w = (r2.w) + (r2.w);
    r2.xyz = float3(((r9.w) >= 0.0f ? (r1.x) : (r2.x)), ((r9.w) >= 0.0f ? (r1.y) : (r2.y)), ((r9.w) >= 0.0f ? (r1.z) : (r2.z)));
    r1.xyz = (r0.xyz) * (-(r2.www)) + (r6.xyz);
    r1 = texCUBElod(s15, r1);
    r0.z = (r8.w) * (r8.w);
    r1.w = (r8.w) * (r0.z);
    r0.xyz = (r1.xyz) * (r1.xyz);
    r1.w = (r7.w) * (r1.w);
    r0.xyz = (r6.www) * (r0.xyz);
    r1.w = (r1.w) * (c3.x) + (c3.y);
    r1.xyz = (r2.xyz) * (r2.xyz);
    r0.xyz = (r0.xyz) * (r1.www);
    r1.xyz = (r1.xyz) * (r4.xyz) + (r5.xyz);
    r0.xyz = (r3.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c2.zzz) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v2.xyz));
    r0.xyz = (r0.www) * (r0.xyz) + (v2.xyz);
    r0.xyz = max(((r0.xyz) * (c[11].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);
    oC0.w = c[10].w;

    return oC0;
}
