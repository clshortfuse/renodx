// Mechanically reconstructed from 0x6B609BE5.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s5 : register(s5);
sampler2D s6 : register(s6);
sampler2D s7 : register(s7);
sampler2D s8 : register(s8);
sampler2D s9 : register(s9);
sampler2D s10 : register(s10);

struct PS_INPUT
{
    float4 v0 : TEXCOORD1;
    float4 v1 : TEXCOORD2;
    float4 v2 : TEXCOORD3;
    float4 v3 : TEXCOORD4;
    float4 v4 : TEXCOORD6;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(4.07999992f, 4.06451607f, -2.07999992f, -2.06451607f);
    const float4 c1 = float4(0.5f, 2.0f, -1.0f, 3.14159274f);
    const float4 c2 = float4(6.28318548f, 0.25f, 0.159154937f, 0.5f);
    const float4 c3 = float4(6.28318548f, -3.14159274f, 0.5f, -0.5f);
    const float4 c4 = float4(9.99999975e-06f, 1e-15f, 1.44269502f, 0.100000001f);
    const float4 c12 = float4(0.0f, 0.25f, -1.0f, 1.0f);
    const float4 c13 = float4(9.99999997e-07f, 3.5f, 1.0f, 0.600000024f);
    const float4 c14 = float4(2.5f, -2.5f, 5.0f, 0.833333313f);
    const float4 c15 = float4(1.5f, 0.0f, 0.0f, 0.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = tex2D(s5, v3.xy);
    r0.xy = (r0.wy) * (c0.xy) + (c0.zw);
    r0.xy = (r0.xy) * (c1.xx) + (c1.xx);
    r0.xy = (r0.xy) * (c1.yy) + (c1.zz);
    r1 = tex2D(s4, v3.xy);
    r0.zw = (r1.wy) * (c0.xy) + (c0.zw);
    r0.zw = (r0.zw) * (c1.xx) + (c1.xx);
    r0.zw = (r0.zw) * (c1.yy) + (c1.zz);
    r1 = tex2D(s3, v3.xy);
    r2.xyz = (r1.xyz) * (c2.xxy);
    r2.w = lerp(r1.x, r1.y, c1.x);
    r1.x = (r2.w) * (c14.x) + (c14.y);
    r1.y = c1.y;
    r1.yz = (c[29].ww) * (r1.yy) + (r2.xy);
    r1.z = (r1.z) * (c2.z) + (c2.w);
    r1.y = (r1.y) + (c1.w);
    r1.y = (r1.y) * (c2.z) + (c2.w);
    r1.y = frac(r1.y);
    r1.y = (r1.y) * (c3.x) + (c3.y);
    r3.x = cos(r1.y);
    r1.y = (r3.x) * (c3.z) + (c3.w);
    r1.z = frac(r1.z);
    r1.z = (r1.z) * (c3.x) + (c3.y);
    r3.x = cos(r1.z);
    r1.z = (r3.x) * (c1.x) + (c1.x);
    r1.w = c12.w;
    r2.x = (r1.w) + (-(c[30].y));
    r2.x = saturate(r2.x);
    r1.z = (r1.z) * (r2.x);
    r1.y = (r2.x) * (r1.y) + (c12.w);
    r0.zw = (r0.zw) * (r1.zz);
    r0.xy = (r1.yy) * (r0.xy) + (r0.zw);
    r3 = tex2D(s6, v3.xy);
    r0.zw = (r3.wy) * (c0.xy) + (c0.zw);
    r0.zw = (r0.zw) * (c1.xx) + (c1.xx);
    r0.zw = (r0.zw) * (c1.yy) + (c1.zz);
    r1.yz = lerp(r0.zw, r0.xy, c[30].zz);
    r0.x = dot(v1.xyz, v1.xyz);
    r0.x = rsqrt(r0.x);
    r0.yzw = (r0.xxx) * (v1.xyz);
    r2.xyw = (r0.wyz) * (v2.yzx);
    r2.xyw = (r0.zwy) * (v2.zxy) + (-(r2.xyw));
    r2.xyw = (r2.xyw) * (v2.www);
    r2.xyw = (r1.zzz) * (r2.xyw);
    r2.xyw = (r1.yyy) * (v2.xyz) + (r2.xyw);
    r2.xyw = (v1.xyz) * (r0.xxx) + (r2.xyw);
    r3.xyz = normalize(r2.xyw);
    r2.xyw = (c12.www) + (-(v0.xyz));
    r4.xyz = normalize(r2.xyw);
    r0.x = dot(-(v0.xyz), -(v0.xyz));
    r0.x = rsqrt(r0.x);
    r2.xyw = (-(v0.xyz)) * (r0.xxx) + (r4.xyz);
    r1.y = saturate(dot(r3.xyz, r4.xyz));
    r4.xyz = (r0.xxx) * (-(v0.xyz));
    r5.xyz = normalize(r2.xyw);
    r0.x = saturate(dot(r3.xyz, r5.xyz));
    r1.z = (r0.x) + (r0.x);
    r0.x = (r0.x) * (r0.x) + (c4.y);
    r0.x = 1.0f / (r0.x);
    r2.x = saturate(dot(r3.xyz, r4.xyz));
    r0.y = saturate(dot(r0.yzw, r4.xyz));
    r0.y = (r2.x) + (r0.y);
    r0.y = (r0.y) * (c1.x);
    r2.y = max(c4.w, r0.y);
    r0.y = 1.0f / (r2.y);
    r0.y = rsqrt(r0.y);
    r0.y = 1.0f / (r0.y);
    r0.z = 1.0f / (r2.x);
    r0.w = min(r2.x, r1.y);
    r0.z = (r1.z) * (r0.z);
    r0.z = saturate((r0.w) * (r0.z));
    r0.z = (r1.y) * (r0.z);
    r0.z = rsqrt(r0.z);
    r0.z = 1.0f / (r0.z);
    r0.w = (r0.x) * (r0.x);
    r0.x = (-(r0.x)) + (c12.w);
    r4 = tex2D(s9, v3.xy);
    r1.z = max(c4.x, r4.x);
    r1.z = (r1.z) * (r1.z);
    r1.z = 1.0f / (r1.z);
    r0.x = (r0.x) * (r1.z);
    r1.z = (r1.z) * (c12.y);
    r0.x = (r0.x) * (c4.z);
    r0.x = exp2(r0.x);
    r0.x = (r0.w) * (r0.x);
    r0.x = (r0.z) * (r0.x);
    r2.xy = c[7].xy;
    r4.xyz = (r2.yyy) * (c[6].xyz);
    r4.xyz = (r4.xyz) * (c[36].xxx);
    r0.xzw = (r0.xxx) * (r4.xyz);
    r0.xyz = (r0.yyy) * (r0.xzw);
    r0.xyz = (r1.zzz) * (r0.xyz);
    r4.xyz = normalize(v0.xyz);
    r0.w = dot(r4.xyz, r3.xyz);
    r1.z = (r0.w) + (r0.w);
    r0.w = saturate(r0.w);
    r0.w = (-(r0.w)) + (c12.w);
    r1.z = (r3.x) * (-(r1.z)) + (r4.x);
    r1.z = (r1.z) * (c[35].x);
    r1.z = (r1.z) * (c13.x);
    r2.y = (r0.w) * (r0.w);
    r0.w = (r0.w) * (r2.y);
    r3 = tex2D(s8, v3.xy);
    r2.y = (r3.w) * (c13.y) + (c13.z);
    r2.y = 1.0f / (r2.y);
    r0.w = (r0.w) * (r2.y);
    r4.xyz = lerp(r3.xyz, c12.www, r0.www);
    r4.xyz = (r1.zzz) * (r4.xyz);
    r0.xyz = (r0.xyz) * (c12.yyy) + (r4.xyz);
    r4.xyz = (-(r0.xyz)) + (c12.www);
    r0.xyz = (c[30].xxx) * (r4.xyz) + (r0.xyz);
    r2.yzw = (r2.zzz) * (r0.xyz);
    r0.xyz = (r3.xyz) * (r0.xyz);
    r3.xyz = (r2.xxx) * (c[5].xyz);
    r3.xyz = (r1.yyy) * (r3.xyz);
    r4 = tex2D(s7, v3.xy);
    r4.xyz = (r4.xyz) * (r4.xyz);
    r2.xyz = (r3.xyz) * (r4.xyz) + (r2.yzw);
    r4.xyz = max(r2.xyz, c12.xxx);
    r2 = tex2D(s10, v3.xy);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r0.xyz = (r3.xyz) * (r2.xyz) + (r0.xyz);
    r2.xyz = max(r0.xyz, c12.xxx);
    r0.xyz = lerp(r2.xyz, r4.xyz, c13.www);
    r0.xyz = (r0.xyz) * (c[37].xyz) + (-(r2.xyz));
    r1.z = c[30].z;
    r3.x = c[38].x;
    r0.w = saturate((r1.z) * (r3.x) + (c[39].x));
    r0.w = (r0.w) * (c14.z) + (r1.x);
    r1.x = saturate((r0.w) * (c14.w));
    r0.w = (r0.w) + (-(c13.w));
    r0.w = saturate((r0.w) * (c15.x));
    r1.x = (r1.x) * (r1.x);
    r0.xyz = (r1.xxx) * (r0.xyz) + (r2.xyz);
    r1.xyz = lerp(r0.xyz, r4.xyz, r0.www);
    r0 = (v0.xyzx) * (c12.wwwx) + (c12.xxxw);
    r2.x = dot(r0, c[23]);
    r2.x = 1.0f / (r2.x);
    r2.y = dot(r0, c[20]);
    r2.z = dot(r0, c[21]);
    r0.z = dot(r0, c[22]);
    r2 = (r2.xxxx) * (r2.yyzz);
    r3 = (r2) * (c[24].zwxy);
    r4.x = log2(abs(r3.x));
    r4.y = log2(abs(r3.y));
    r4.z = log2(abs(r3.z));
    r4.w = log2(abs(r3.w));
    r3 = (r4) * (c[25].xxxx);
    r2.x = exp2(r3.x);
    r2.z = exp2(r3.y);
    r3.x = exp2(r3.z);
    r3.y = exp2(r3.w);
    r2.xz = (r2.xz) + (r3.xy);
    r3.x = log2(abs(r2.x));
    r3.y = log2(abs(r2.z));
    r2.xz = (r3.xy) * (c[25].yy);
    r2.x = exp2(r2.x);
    r2.z = exp2(r2.z);
    r2.x = (r2.x) * (c[26].x);
    r2.x = (r2.z) * (c[26].y) + (-(r2.x));
    r3.y = c[26].y;
    r2.z = (r2.z) * (r3.y) + (-(c[25].z));
    r2.x = 1.0f / (r2.x);
    r3.w = saturate((r2.z) * (r2.x));
    r0.xy = abs(r2.yw);
    r4 = c[10];
    r4 = saturate((r0.zyxz) * (r4) + (c[11]));
    r3.x = (r4.w) * (r4.x);
    r3.yz = r4.yz;
    r4 = (r3) * (r3);
    r3 = (c[26].zzzz) * (r3) + (c[26].wwww);
    r3 = (r4) * (r3);
    r0.x = (r3.z) * (r3.y);
    r0.y = abs(c[25].w);
    r0.x = ((-(r0.y)) >= 0.0f ? (r0.x) : (r3.w));
    r0.w = (r0.z) * (r0.z);
    r0.y = dot(c[9].yz, r0.zw) + (c[9].x);
    r0.x = (r0.x) * (r0.y);
    r0.x = (r3.x) * (r0.x);
    r2.xz = c[8].xz;
    r0.y = dot(r2.yw, c[27].xy) + (r2.x);
    r0.z = dot(r2.yw, c[27].zw) + (r2.z);
    r2 = tex2D(s2, r0.yz);
    r0.y = (r2.x) * (r2.x);
    r0.x = (r0.x) * (r0.y);
    r2.xy = (c[28].xy) * (v4.ww);
    r2.zw = c12.xx;
    r3 = (r2.xyww) + (v4);
    r2 = (-(r2)) + (v4);
    r2 = tex2Dproj(s1, r2);
    r2.y = r2.x;
    r3 = tex2Dproj(s1, r3);
    r2.x = r3.x;
    r3.xy = (c[28].zw) * (v4.ww);
    r3.zw = c12.xx;
    r4 = (r3.xyww) + (v4);
    r3 = (-(r3)) + (v4);
    r3 = tex2Dproj(s1, r3);
    r2.w = r3.x;
    r3 = tex2Dproj(s1, r4);
    r2.z = r3.x;
    r0.y = dot(r2, c12.yyyy);
    r0.y = (r0.y) + (c12.z);
    r0.y = (c[8].w) * (r0.y) + (r1.w);
    r0.x = (r0.x) * (r0.y);
    r0.xyz = (r1.xyz) * (r0.xxx);
    r0.w = c12.w;
    r1.x = dot(r0, c[32]);
    r1.y = dot(r0, c[33]);
    r1.z = dot(r0, c[34]);
    r0.xyz = max(((r1.xyz) * (c[31].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c12.w;

    return oC0;
}
