// Mechanically reconstructed from 0x0F71FE70.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s0 : register(s0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : COLOR0;
    float4 v1 : TEXCOORD0;
    float4 v2 : TEXCOORD1;
    float4 v3 : TEXCOORD8;
    float4 v4 : TEXCOORD5;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    float4 v4 = input.v4;
    const float4 c0 = float4(-0.5f, 1.0f, 0.0f, 8.0f);
    const float4 c1 = float4(31.875f, 4.0f, -2.0f, 0.0009765625f);
    const float4 c2 = float4(0.600000024f, 0.400000006f, 0.125f, 0.25f);
    const float4 c3 = float4(0.797884583f, 1.0f, 0.5f, 0.0f);
    const float4 c4 = float4(3.5f, -13.0f, 1.0f, 13.0f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 r7 = 0.0f;
    float4 r8 = 0.0f;
    float4 r9 = 0.0f;
    float4 r10 = 0.0f;
    float4 r11 = 0.0f;
    float4 r12 = 0.0f;
    float4 r13 = 0.0f;
    float4 oC0 = 0.0f;

    r1 = (v4.xyzx) * (c0.yyyz) + (c0.zzzy);
    r0.w = dot(r1, c[23]);
    r0.w = 1.0f / (r0.w);
    r0.x = dot(r1, c[20]);
    r0.y = dot(r1, c[21]);
    r0 = (r0.wwww) * (r0.xxyy);
    r2 = (r0) * (c[24].zwxy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.z = log2(abs(r2.z));
    r2.w = log2(abs(r2.w));
    r2 = (r2) * (c[25].xxxx);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = exp2(r2.z);
    r2.w = exp2(r2.w);
    r2.xy = (r2.zw) + (r2.xy);
    r2.x = log2(abs(r2.x));
    r2.y = log2(abs(r2.y));
    r2.xy = (r2.xy) * (c[25].yy);
    r2.x = exp2(r2.x);
    r2.y = exp2(r2.y);
    r2.z = dot(r1, c[22]);
    r0.z = (r2.x) * (c[26].x);
    r0.y = (r2.y) * (c[26].y) + (-(r0.z));
    r0.z = c[26].y;
    r0.z = (r2.y) * (r0.z) + (-(c[25].z));
    r0.y = 1.0f / (r0.y);
    r2.xy = abs(r0.xw);
    r3.w = saturate((r0.z) * (r0.y));
    r1 = c[10];
    r1 = saturate((r2.zyxz) * (r1) + (c[11]));
    r3.x = (r1.w) * (r1.x);
    r3.yz = r1.yz;
    r1 = (r3) * (r3);
    r3 = (c[26].zzzz) * (r3) + (c[26].wwww);
    r1 = (r1) * (r3);
    r0.y = (r1.z) * (r1.y);
    r0.z = abs(c[25].w);
    r2.w = (r2.z) * (r2.z);
    r0.y = ((-(r0.z)) >= 0.0f ? (r0.y) : (r1.w));
    r0.z = dot(c[9].yz, r2.zw) + (c[9].x);
    r0.z = (r0.y) * (r0.z);
    r2.w = (r1.x) * (r0.z);
    r1 = c[27];
    r1.x = dot(r0.xw, r1.xy) + (c[8].x);
    r1.y = dot(r0.xw, r1.zw) + (c[8].z);
    r0 = tex2D(s1, r1.xy);
    r0.xyz = (r0.xyz) * (r0.xyz);
    r1.xyz = (r2.www) * (r0.xyz);
    r0 = tex2D(s12, v1.zw);
    r2.xyz = (-(v4.xyz)) + (c[5].xyz);
    r0.w = dot(r2.xyz, r2.xyz);
    r4.z = rsqrt(r0.w);
    r3.xyz = (r2.xyz) * (r4.zzz);
    r9.xyz = normalize(v2.xyz);
    r6.xyz = (r1.xyz) * (r0.yyy);
    r4.w = saturate(dot(r9.xyz, r3.xyz));
    r1 = tex2D(s2, v1.xy);
    r0 = tex2D(s0, v1.xy);
    r2.w = (r0.w) * (v0.w) + (c0.x);
    r0 = (r0.xyzx) * (c0.yyyz) + (c0.zzzy);
    r1 = float4(((r2.w) >= 0.0f ? (r1.x) : (c0.z)), ((r2.w) >= 0.0f ? (r1.y) : (c0.z)), ((r2.w) >= 0.0f ? (r1.z) : (c0.z)), ((r2.w) >= 0.0f ? (r1.w) : (c0.y)));
    r5.z = (r1.w) * (c3.x);
    r10.xyz = normalize(v4.xyz);
    r4.y = (r1.w) * (-(c3.x)) + (c3.y);
    r4.x = saturate(dot(r9.xyz, -(r10.xyz)));
    r3.w = (r4.w) * (r4.y) + (r5.z);
    r4.y = (r4.x) * (r4.y) + (r5.z);
    r6.w = (-(r4.x)) + (c0.y);
    r3.w = (r3.w) * (r4.y) + (c1.w);
    r3.w = 1.0f / (r3.w);
    r4.xyz = (r2.xyz) * (r4.zzz) + (-(r10.xyz));
    r3.w = (r4.w) * (r3.w);
    r2.xyz = normalize(r4.xyz);
    r11.xyz = (r4.www) * (c[6].xyz);
    r4.w = saturate(dot(r9.xyz, r2.xyz));
    r3.y = saturate(dot(r2.xyz, r3.xyz));
    r2.xy = (r1.ww) * (c4.xy) + (c4.zw);
    r2.z = exp2(r2.y);
    r5.w = 1.0f / (r2.x);
    r3.z = pow(abs(r4.w), r2.z);
    r2.y = (-(r3.y)) + (c0.y);
    r2.z = (r2.z) * (c2.z) + (c2.w);
    r2.x = (r2.y) * (r2.y);
    r2.z = (r3.z) * (r2.z);
    r2.x = (r2.x) * (r2.x);
    r2.z = (r3.w) * (r2.z);
    r2.y = (r2.y) * (r2.x);
    r7.xyz = (r1.xyz) * (r1.xyz);
    r5.xyz = (r1.xyz) * (-(r1.xyz)) + (c0.yyy);
    r1.w = (r1.w) * (c0.w);
    r1.xyz = (r5.xyz) * (r2.yyy) + (r7.xyz);
    r1.xyz = (r2.zzz) * (r1.xyz);
    r2.y = c0.z;
    r8.xyz = float3(((r2.w) >= 0.0f ? (c[28].x) : (r2.y)), ((r2.w) >= 0.0f ? (c[28].y) : (r2.y)), ((r2.w) >= 0.0f ? (c[28].w) : (r2.y)));
    r0 = float4(((r2.w) >= 0.0f ? (r0.x) : (c0.z)), ((r2.w) >= 0.0f ? (r0.y) : (c0.z)), ((r2.w) >= 0.0f ? (r0.z) : (c0.z)), ((r2.w) >= 0.0f ? (r0.w) : (c0.z)));
    r2.xy = (v1.zw) * (c3.yz);
    r2 = tex2D(s13, r2.xy);
    r3.xy = (v1.zw) * (c3.yz) + (c3.wz);
    r4 = tex2D(s13, r3.xy);
    r2.w = r4.y;
    r1.xyz = (r1.xyz) * (r8.zzz);
    r3.xy = (r2.yw) * (c1.yy) + (c1.zz);
    r12.xyz = (r1.xyz) * (c[7].xyz);
    r1.z = dot(r3.xy, r3.xy) + (c0.z);
    r3 = tex2D(s14, v1.zw);
    r13.xy = (r3.xy) * (c1.xx);
    r1.z = exp2(-(r1.z));
    r1.xy = (r4.xz) * (r13.yy);
    r2.w = saturate((r1.z) * (c2.x) + (c2.y));
    r3.w = (r3.y) * (c1.x) + (-(r1.x));
    r1.xz = (r1.xy) * (c1.yy);
    r2.xy = (r2.xz) * (r13.xx);
    r1.y = (r4.z) * (-(r13.y)) + (r3.w);
    r3.w = (r3.x) * (c1.x) + (-(r2.x));
    r1.y = (r1.y) + (r1.y);
    r3.w = (r2.z) * (-(r13.x)) + (r3.w);
    r2.xz = (r2.xy) * (c1.yy);
    r2.y = (r3.w) + (r3.w);
    r4.xyz = (r6.xyz) * (r12.xyz);
    r1.xyz = (r1.xyz) * (r2.www) + (r2.xyz);
    r1.xyz = (r8.yyy) * (r1.xyz);
    r2.w = dot(r10.xyz, r9.xyz);
    r3.xyz = (r6.xyz) * (r11.xyz) + (r1.xyz);
    r1.z = (r2.w) + (r2.w);
    r6.xyz = (r0.xyz) * (v0.xyz);
    r1.xyz = (r9.xyz) * (-(r1.zzz)) + (r10.xyz);
    r1 = texCUBElod(s15, r1);
    r0.z = (r6.w) * (r6.w);
    r1.w = (r6.w) * (r0.z);
    r0.xyz = (r1.xyz) * (r1.xyz);
    r1.w = (r5.w) * (r1.w);
    r0.xyz = (r8.xxx) * (r0.xyz);
    r5.xyz = (r5.xyz) * (r1.www) + (r7.xyz);
    r1.xyz = (r6.xyz) * (r6.xyz);
    r0.xyz = (r0.xyz) * (r5.xyz);
    r1.xyz = (r1.xyz) * (r3.xyz) + (r4.xyz);
    r0.xyz = (r2.xyz) * (r0.xyz);
    r0.xyz = (r0.xyz) * (c0.www) + (r1.xyz);
    r0.xyz = (r0.xyz) + (-(v3.xyz));
    r1.x = v2.w;
    r0.xyz = (r1.xxx) * (r0.xyz) + (v3.xyz);
    r0.xyz = max(((r0.xyz) * (c[29].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    r0.y = rsqrt(r0.y);
    r0.z = rsqrt(r0.z);
    oC0.w = r0.w;
    oC0.x = 1.0f / (r0.x);
    oC0.y = 1.0f / (r0.y);
    oC0.z = 1.0f / (r0.z);

    return oC0;
}
