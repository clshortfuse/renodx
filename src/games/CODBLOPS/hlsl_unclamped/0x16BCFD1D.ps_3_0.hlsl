// Mechanically reconstructed from 0x16BCFD1D.ps_3_0.cso.
// HDR edit: terminal RGB upper clamp removed; zero floor retained.
// Original terminal path: saturate(linear RGB) -> sqrt encode.
// Modified terminal path: max(linear RGB, 0) -> sqrt encode.
// Intermediate clamps, alpha clamps, masks, depth, and control flow are unchanged.
// Entry point: main    Target: ps_3_0

float4 c[224] : register(c0);
sampler2D s1 : register(s1);
sampler2D s2 : register(s2);
sampler2D s3 : register(s3);
sampler2D s4 : register(s4);
sampler2D s12 : register(s12);
sampler2D s13 : register(s13);
sampler2D s14 : register(s14);
samplerCUBE s15 : register(s15);

struct PS_INPUT
{
    float4 v0 : TEXCOORD0;
    float4 v1 : TEXCOORD1;
    float4 v2 : TEXCOORD2;
    float4 v3 : TEXCOORD4;
};

float4 main(PS_INPUT input) : COLOR0
{
    float4 v0 = input.v0;
    float4 v1 = input.v1;
    float4 v2 = input.v2;
    float4 v3 = input.v3;
    const float4 c0 = float4(-28.0f, 30.0f, 31.875f, 15.0f);
    const float4 c1 = float4(1.0f, 0.0f, 0.5f, 8.0f);
    const float4 c2 = float4(0.38647899f, 0.392856985f, 0.364796013f, 0.00749999983f);
    const float4 c3 = float4(4.0f, 2.0f, 0.0399999991f, 0.782500029f);
    const float4 c4 = float4(1.00750005f, 0.0f, 0.0f, 0.0f);
    const float4 c12 = float4(81.2394867f, 17.3480244f, 37.3498383f, 59.3948402f);
    float4 r0 = 0.0f;
    float4 r1 = 0.0f;
    float4 r2 = 0.0f;
    float4 r3 = 0.0f;
    float4 r4 = 0.0f;
    float4 r5 = 0.0f;
    float4 r6 = 0.0f;
    float4 oC0 = 0.0f;

    r0 = c12;
    r0.x = dot(c[35].wwww, r0);
    r0.x = frac(r0.x);
    r0.w = c[35].w;
    r1.x = dot(r0.xwww, c12);
    r0.y = frac(r1.x);
    r1.x = dot(r0.xyww, c12);
    r0.z = frac(r1.x);
    r1.x = dot(r0, c12);
    r0.w = frac(r1.x);
    r1.yzw = r0.yzw;
    r0.x = dot(r0, c12);
    r1.x = frac(r0.x);
    r0.x = dot(r1, c12);
    r1.y = frac(r0.x);
    r0.x = 1.0f / (c[42].x);
    r0.y = 1.0f / (c[42].y);
    r0.xy = (v3.xy) * (r0.xy) + (r1.xy);
    r0 = tex2D(s2, r0.xy);
    r0.w = c[35].w;
    r1.x = (r0.w) * (c0.w);
    r1.x = frac(r1.x);
    r1.x = (r0.w) * (c0.w) + (-(r1.x));
    r1.y = (r1.x) * (c3.z) + (v3.y);
    r1.x = c3.w;
    r1 = tex2D(s3, r1.xy);
    r1.x = (r1.x) * (-(c2.w)) + (v3.x);
    r1.x = (r1.x) * (c4.x) + (-(v3.x));
    r1.x = (c[43].x) * (r1.x) + (v3.x);
    r1.z = (-(r1.x)) + (c1.x);
    r1.zw = float2(((r1.z) >= 0.0f ? (c1.y) : (c1.x)), ((r1.x) >= 0.0f ? (c1.y) : (c1.x)));
    r1.x = saturate(r1.x);
    r1.z = (r1.z) + (r1.w);
    r1.w = frac(abs(v3.y));
    r1.y = ((v3.y) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2 = tex2D(s4, r1.xy);
    r1.xyw = (r2.xyz) * (r2.xyz);
    r1.xyz = float3(((-(r1.z)) >= 0.0f ? (r1.x) : (c1.y)), ((-(r1.z)) >= 0.0f ? (r1.y) : (c1.y)), ((-(r1.z)) >= 0.0f ? (r1.w) : (c1.y)));
    r1.w = abs(c[35].w);
    r1.w = frac(r1.w);
    r2.y = ((c[35].w) >= 0.0f ? (r1.w) : (-(r1.w)));
    r2.x = c1.y;
    r2 = tex2D(s3, r2.xy);
    r1.w = saturate((r2.x) * (c[41].x));
    r2.xyz = lerp(r1.xyz, r0.xyz, r1.www);
    r0.x = (r0.w) * (c[44].x);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r0.y = (c[48].x) + (-(v3.y));
    r0.y = (r0.y) + (c1.x);
    r0.x = (r0.x) + (r0.y);
    r0.y = frac(abs(r0.x));
    r0.x = ((r0.x) >= 0.0f ? (r0.y) : (-(r0.y)));
    r1.x = pow(abs(r0.x), c[47].x);
    r0.x = c1.x;
    r0.y = (r1.x) * (-(c[46].x)) + (r0.x);
    r1.xyz = lerp(c[45].xyz, r2.xyz, r0.yyy);
    r2 = (v1.xyzx) * (c1.xxxy) + (c1.yyyx);
    r0.y = dot(r2, c[30]);
    r0.y = 1.0f / (r0.y);
    r0.z = dot(r2, c[27]);
    r0.w = dot(r2, c[28]);
    r2.z = dot(r2, c[29]);
    r3 = (r0.yyyy) * (r0.zzww);
    r4 = (r3) * (c[31].zwxy);
    r5.x = log2(abs(r4.x));
    r5.y = log2(abs(r4.y));
    r5.z = log2(abs(r4.z));
    r5.w = log2(abs(r4.w));
    r4 = (r5) * (c[32].xxxx);
    r0.y = exp2(r4.x);
    r0.z = exp2(r4.y);
    r3.x = exp2(r4.z);
    r3.z = exp2(r4.w);
    r0.yz = (r0.yz) + (r3.xz);
    r0.y = log2(abs(r0.y));
    r0.w = log2(abs(r0.z));
    r0.yz = (r0.yw) * (c[32].yy);
    r0.y = exp2(r0.y);
    r0.z = exp2(r0.z);
    r0.y = (r0.y) * (c[33].x);
    r0.y = (r0.z) * (c[33].y) + (-(r0.y));
    r4.y = c[33].y;
    r0.z = (r0.z) * (r4.y) + (-(c[32].z));
    r0.y = 1.0f / (r0.y);
    r4.w = saturate((r0.z) * (r0.y));
    r2.xy = abs(r3.yw);
    r5 = c[25];
    r5 = saturate((r2.zyxz) * (r5) + (c[26]));
    r4.x = (r5.w) * (r5.x);
    r4.yz = r5.yz;
    r5 = (r4) * (r4);
    r4 = (c[33].zzzz) * (r4) + (c[33].wwww);
    r4 = (r5) * (r4);
    r0.y = (r4.z) * (r4.y);
    r0.z = abs(c[32].w);
    r0.y = ((-(r0.z)) >= 0.0f ? (r0.y) : (r4.w));
    r2.w = (r2.z) * (r2.z);
    r0.z = dot(c[24].yz, r2.zw) + (c[24].x);
    r0.y = (r0.y) * (r0.z);
    r0.y = (r4.x) * (r0.y);
    r2 = c[34];
    r0.z = dot(r3.yw, r2.xy) + (c[23].x);
    r0.w = dot(r3.yw, r2.zw) + (c[23].z);
    r2 = tex2D(s1, r0.zw);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r0.yzw = (r0.yyy) * (r2.xyz);
    r2 = tex2D(s12, v3.zw);
    r0.yzw = (r0.yzw) * (r2.yyy);
    r0.yzw = (r0.yzw) * (c[21].xyz);
    r0.yzw = (r0.yzw) * (c[22].yyy);
    r2.xyz = (c[20].xyz) + (-(v1.xyz));
    r3.xyz = normalize(r2.xyz);
    r2.xyz = normalize(v1.xyz);
    r1.w = dot(r3.xyz, r2.xyz);
    r1.w = saturate((r1.w) * (c1.z) + (c1.z));
    r2.w = (r1.w) * (c0.x) + (c0.y);
    r1.w = (r1.w) + (c1.x);
    r4.xyz = normalize(v2.xyz);
    r3.w = dot(-(r3.xyz), r4.xyz);
    r3.w = (r3.w) + (r3.w);
    r3.xyz = (r4.xyz) * (-(r3.www)) + (-(r3.xyz));
    r3.x = saturate(dot(r3.xyz, -(r2.xyz)));
    r4.w = pow(abs(r3.x), r2.w);
    r0.yzw = (r0.yzw) * (r4.www);
    r0.yzw = (r1.www) * (r0.yzw);
    r1.w = dot(r2.xyz, r4.xyz);
    r1.w = (r1.w) + (r1.w);
    r2.xyz = (r4.xyz) * (-(r1.www)) + (r2.xyz);
    r2 = texCUBE(s15, r2.xyz);
    r2.xyz = (r2.xyz) * (r2.xyz);
    r2.xyz = (r2.xyz) * (c1.www);
    r3.xy = (c1.xz) * (v3.zw);
    r3 = tex2D(s13, r3.xy);
    r5 = tex2D(s14, v3.zw);
    r1.w = (r5.x) * (c0.z);
    r3.xy = (r3.xz) * (r1.ww);
    r2.w = (r5.x) * (c0.z) + (-(r3.x));
    r3.xw = (r2.xz) * (r3.xy);
    r1.w = (r3.z) * (-(r1.w)) + (r2.w);
    r3.y = (r2.y) * (r1.w);
    r2.xyz = (r3.xyw) * (c[40].xxx);
    r2.xyz = (r2.xyz) * (c3.xyx);
    r0.yzw = (r0.yzw) * (c2.xyz) + (r2.xyz);
    r0.yzw = (r1.xyz) + (r0.yzw);
    r1 = (c[6]) + (-(v1.yyyy));
    r2 = (r1) * (r1);
    r3 = (c[5]) + (-(v1.xxxx));
    r2 = (r3) * (r3) + (r2);
    r5 = (c[7]) + (-(v1.zzzz));
    r2 = (r5) * (r5) + (r2);
    r6.x = rsqrt(r2.x);
    r6.y = rsqrt(r2.y);
    r6.z = rsqrt(r2.z);
    r6.w = rsqrt(r2.w);
    r2 = saturate((r2) * (c[8]) + (r0.xxxx));
    r1 = (r1) * (r6);
    r1 = (r4.yyyy) * (r1);
    r3 = (r3) * (r6);
    r5 = (r5) * (r6);
    r1 = (r3) * (r4.xxxx) + (r1);
    r1 = saturate((r5) * (r4.zzzz) + (r1));
    r1 = (r2) * (r1);
    r2.x = dot(c[9], r1);
    r2.y = dot(c[10], r1);
    r2.z = dot(c[11], r1);
    r0.xyz = (r0.yzw) * (r2.xyz) + (r0.yzw);
    r0.w = c1.x;
    r1.x = dot(r0, c[37]);
    r1.y = dot(r0, c[38]);
    r1.z = dot(r0, c[39]);
    r0.w = v1.w;
    r2.xyz = lerp(v0.xyz, r1.xyz, r0.www);
    r0.xyz = max(((r2.xyz) * (c[36].xxx)), 0.0f); // HDR: removed only the 1.0 ceiling
    r0.x = rsqrt(r0.x);
    oC0.x = 1.0f / (r0.x);
    r0.x = rsqrt(r0.y);
    r0.y = rsqrt(r0.z);
    oC0.z = 1.0f / (r0.y);
    oC0.y = 1.0f / (r0.x);
    oC0.w = c1.x;

    return oC0;
}
