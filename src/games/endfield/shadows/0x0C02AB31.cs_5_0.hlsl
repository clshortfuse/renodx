// ---- Created with 3Dmigoto v1.4.1 on Sat Jan 24 06:14:41 2026
// Fixed for compilation
#include "../shared.h"

groupshared struct { float val[1]; } g0[128];

RWTexture2D<unorm float4> u0 : register(u0);
Texture2D<uint4> t1 : register(t1);
Texture2D<float4> t0 : register(t0);

SamplerState s0_s : register(s0);

cbuffer cb1 : register(b1)
{
  float4 cb1[5];
}

cbuffer cb0 : register(b0)
{
  float4 cb0[16];
}

// 3Dmigoto declarations
#define cmp -

[numthreads(64, 1, 1)]
void main(uint3 vThreadIDInGroup : SV_GroupThreadID, uint3 vThreadGroupID : SV_GroupID)
{
// Needs manual fix for instruction:
// unknown dcl_: dcl_uav_typed_texture2d (unorm,unorm,unorm,unorm) u0
  float4 r0,r1,r2,r3,r4,r5,r6,r7,r8;
  uint4 bitmask, uiDest;
  float4 fDest;

// Needs manual fix for instruction:
// unknown dcl_: dcl_thread_group 64, 1, 1
  r0.x = cmp(0 < cb1[2].w);
  r0.x = r0.x ? 0 : 63;
  r0.x = (int)r0.x ^ (int)vThreadIDInGroup.x;
  r0.x = (uint)r0.x;
  r0.y = 0.015625 * r0.x;
  r0.zw = (uint2)vThreadGroupID.yz << int2(6,6);
  r1.xy = (int2)cb1[4].xy;
  r0.zw = (int2)r0.zw + (int2)r1.xy;
  r1.xy = cmp(int2(0,0) < (int2)r0.zw);
  r1.zw = cmp((int2)r0.zw < int2(0,0));
  r1.xy = (int2)-r1.xy + (int2)r1.zw;
  r1.z = (int)r0.z + (int)r1.y;
  r1.x = -(int)r1.x;
  r1.w = (int)r0.w + (int)r1.x;
  r1.zw = max((int2)r1.zw, (int2)-r1.zw);
  r1.z = cmp((int)r1.z < (int)r1.w);
  r2.x = r1.z ? r1.y : 0;
  r2.y = r1.z ? 0 : r1.x;
  r0.zw = mad((int2)r2.xy, (int2)vThreadGroupID.xx, (int2)r0.zw);
  r0.zw = (int2)r0.zw;
  r1.xy = floor(cb1[2].xy);
  r1.xy = float2(0.5,0.5) + r1.xy;
  r1.zw = r1.xy + r0.zw;
  r1.xy = cb1[2].xy + -r1.xy;
  r2.x = cmp(abs(r0.w) < abs(r0.z));
  r0.z = r2.x ? r0.z : r0.w;
  r0.w = r2.x ? r1.x : r1.y;
  r1.x = cmp(0 < r0.z);
  r0.w = r1.x ? -r0.w : r0.w;
  r1.x = abs(r0.z) + r0.w;
  r0.x = abs(r0.z) + -r0.x;
  r0.x = r0.x + r0.w;
  r0.z = -64 + r1.x;
  r0.z = r0.z / r1.x;
  r1.xy = -cb1[2].xy + r1.zw;
  r0.zw = r0.zz * r1.xy + cb1[2].xy;
  r1.xy = r0.zw + -r1.zw;
  r0.zw = r1.zw + -r0.zw;
  r1.xy = r0.yy * r1.xy + r1.zw;
  r0.y = r2.x ? r1.y : r1.x;
  r0.y = frac(r0.y);
  r0.y = -0.5 + r0.y;
  r0.y = cmp(0 < r0.y);
  r0.y = r0.y ? 1 : -1;
  r3.x = r2.x ? 0 : r0.y;
  r3.y = r2.x ? r0.y : 0;
  r4.xyzw = floor(r1.xyyy);
  r1.zw = float2(0.5,0.5) + r4.xw;
  r4.xyzw = (int4)r4.xyzw;
  r4.xyzw = max(int4(0,0,0,0), (int4)r4.xyzw);
  r2.yz = r1.zw + r3.xy;
  r2.yz = cb1[3].zw * r2.yz;
  r0.y = t0.SampleLevel(s0_s, r2.yz, 0).x;
  r2.yz = cb1[3].zw * r1.zw;
  r1.zw = (int2)r1.zw;
  r1.zw = max(int2(0,0), (int2)r1.zw);
  r3.z = t0.SampleLevel(s0_s, r2.yz, 0).x;
  r0.y = r3.z + -r0.y;
  r2.y = cb1[0].y * abs(r3.z);
  r2.y = cmp(r2.y < abs(r0.y));
  r0.y = r3.z + -abs(r0.y);
  r2.z = cmp(0 < cb1[1].z);
  r2.w = r2.z ? 1.00000002e+20 : r3.z;
  r0.y = r2.y ? r2.w : r0.y;
  r0.y = -cb1[2].z + r0.y;
  r0.y = r0.y / r0.x;
  r5.xyzw = cb1[3].xyyy + float4(0.100000001,0.100000001,0.100000001,0.100000001);
  r5.xyzw = (int4)r5.xyzw;
  r5.xyzw = (int4)r5.xyzw + int4(-1,-1,-1,-1);
  r6.xy = min((int2)r5.xy, (int2)r1.zw);
  r6.zw = float2(0,0);
  r1.z = t1.Load(r6.xyz).y;
  r1.z = (int)r1.z & 4;
  r0.y = r1.z ? 1e+10 : r0.y;
  g0[vThreadIDInGroup.x].val[0/4] = r0.y;
  r1.zw = r0.zw * -cb1[2].ww + r1.xy;
  r0.y = r2.x ? r1.w : r1.z;
  r1.zw = floor(r1.zw);
  r1.zw = float2(0.5,0.5) + r1.zw;
  r0.y = frac(r0.y);
  r0.y = -0.5 + r0.y;
  r0.y = cmp(0 < r0.y);
  r0.y = r0.y ? 1 : -1;
  r6.x = r2.x ? 0 : r0.y;
  r6.y = r2.x ? r0.y : 0;
  r2.xy = r6.xy + r1.zw;
  r2.xy = cb1[3].zw * r2.xy;
  r0.y = t0.SampleLevel(s0_s, r2.xy, 0).x;
  r2.xy = cb1[3].zw * r1.zw;
  r1.zw = (int2)r1.zw;
  r1.zw = max(int2(0,0), (int2)r1.zw);
  r6.xy = min((int2)r1.zw, (int2)r5.xy);
  r4.xyzw = min((int4)r5.xyzw, (int4)r4.xyzw);
  r1.z = t0.SampleLevel(s0_s, r2.xy, 0).x;
  r0.y = r1.z + -r0.y;
  r1.w = cb1[0].y * abs(r1.z);
  r1.w = cmp(r1.w < abs(r0.y));
  r0.y = r1.z + -abs(r0.y);
  r1.z = r2.z ? 1.00000002e+20 : r1.z;
  r0.y = r1.w ? r1.z : r0.y;
  r0.y = -cb1[2].z + r0.y;
  r1.z = cb1[2].w * -64 + r0.x;
  r0.y = r0.y / r1.z;
  r1.z = cmp(0 < r1.z);
  r0.y = r1.z ? r0.y : 1e+10;
  r6.zw = float2(0,0);
  r1.z = t1.Load(r6.xyz).y;
  r1.z = (int)r1.z & 4;
  r0.y = r1.z ? 1e+10 : r0.y;
  r1.z = (int)vThreadIDInGroup.x + 64;
  g0[r1.z].val[0/4] = r0.y;
  GroupMemoryBarrierWithGroupSync();
  r2.xyzw = (int4)vThreadIDInGroup.xxxx + int4(5,9,13,17);
  r0.y = g0[r2.x].val[0/4];
  r1.z = -cb1[2].w + r0.x;
  r1.w = 1 / cb1[0].x;
  r1.w = min(r1.z, r1.w);
  r1.w = r1.w * r0.x;
  r1.w = r1.w / abs(r3.z);
  r2.x = r3.z * 1.00001526 + -cb1[2].z;
  r2.x = r2.x / r0.x;
  r5.x = r2.x * r1.w + 1;
  r0.y = -r0.y * r1.w + r5.x;
  r0.y = min(1, abs(r0.y));
  r2.y = g0[r2.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r2.z].val[0/4];
  r2.z = g0[r2.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r0.y = min(r0.y, abs(r2.z));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(21,25,29,33);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.z].val[0/4];
  r2.z = g0[r6.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r0.y = min(r0.y, abs(r2.z));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(37,41,6,10);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r7.xyzw = (int4)vThreadIDInGroup.xxxx + int4(45,49,53,57);
  r2.y = g0[r7.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r7.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r7.z].val[0/4];
  r2.z = g0[r7.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r2.yz = float2(0.0833333358,0.416666687) + abs(r2.yz);
  r0.y = min(r2.y, r0.y);
  r7.x = min(r0.y, r2.z);
  r0.y = g0[r6.z].val[0/4];
  r2.y = g0[r6.w].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = -r0.y * r1.w + r5.x;
  r0.y = min(1, abs(r0.y));
  r0.y = min(r0.y, abs(r2.y));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(14,18,22,26);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.z].val[0/4];
  r2.z = g0[r6.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r0.y = min(r0.y, abs(r2.z));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(30,34,38,42);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.z].val[0/4];
  r2.z = g0[r6.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r0.y = min(r0.y, abs(r2.z));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(46,50,54,58);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.z].val[0/4];
  r2.z = g0[r6.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r2.yz = float2(0.166666672,0.5) + abs(r2.yz);
  r0.y = min(r2.y, r0.y);
  r7.y = min(r0.y, r2.z);
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(7,11,15,19);
  r0.y = g0[r6.x].val[0/4];
  r0.y = -r0.y * r1.w + r5.x;
  r0.y = min(1, abs(r0.y));
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.z].val[0/4];
  r2.z = g0[r6.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r0.y = min(r0.y, abs(r2.z));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(23,27,31,35);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.z].val[0/4];
  r2.z = g0[r6.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r0.y = min(r0.y, abs(r2.z));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(39,43,8,12);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r8.xyzw = (int4)vThreadIDInGroup.xxxx + int4(47,51,55,59);
  r2.y = g0[r8.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r8.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r8.z].val[0/4];
  r2.z = g0[r8.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r2.yz = float2(0.25,0.583333373) + abs(r2.yz);
  r0.y = min(r2.y, r0.y);
  r7.z = min(r0.y, r2.z);
  r0.y = g0[r6.z].val[0/4];
  r2.y = g0[r6.w].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = -r0.y * r1.w + r5.x;
  r0.y = min(1, abs(r0.y));
  r0.y = min(r0.y, abs(r2.y));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(16,20,24,28);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.z].val[0/4];
  r2.z = g0[r6.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r0.y = min(r0.y, abs(r2.z));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(32,36,40,44);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.z].val[0/4];
  r2.z = g0[r6.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r0.y = min(r0.y, abs(r2.z));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(48,52,56,60);
  r2.y = g0[r6.x].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.y].val[0/4];
  r2.y = -r2.y * r1.w + r5.x;
  r0.y = min(abs(r2.y), r0.y);
  r2.y = g0[r6.z].val[0/4];
  r2.z = g0[r6.w].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.y = -r2.y * r1.w + r5.x;
  r2.yz = float2(0.333333343,0.666666687) + abs(r2.yz);
  r0.y = min(r2.y, r0.y);
  r7.w = min(r0.y, r2.z);
  r0.y = -cb1[0].z + 1;
  r6.xyzw = saturate(r7.xyzw * cb1[0].zzzz + r0.yyyy);
  r2.y = dot(r6.xyzw, float4(0.25,0.25,0.25,0.25));
  r6.xyzw = (int4)vThreadIDInGroup.xxxx + int4(1,2,3,4);
  r2.z = g0[r6.x].val[0/4];
  r2.z = -r2.z * r1.w + r5.x;
  r2.w = cmp(0 < r2.z);
  r5.y = cmp(r2.z < 0);
  r2.w = (int)-r2.w + (int)r5.y;
  r2.w = (int)r2.w;
  r2.z = r2.w * r2.z;
  r2.z = min(1, r2.z);
  r5.y = g0[r6.y].val[0/4];
  r5.y = -r5.y * r1.w + r5.x;
  r5.z = cmp(0 < r5.y);
  r5.w = cmp(r5.y < 0);
  r5.z = (int)-r5.z + (int)r5.w;
  r5.z = (int)r5.z;
  r5.y = r5.z * r5.y;
  r2.z = min(r5.y, r2.z);
  r5.y = g0[r6.z].val[0/4];
  r5.w = g0[r6.w].val[0/4];
  r5.w = -r5.w * r1.w + r5.x;
  r1.w = -r5.y * r1.w + r5.x;
  r5.x = cmp(0 < r1.w);
  r5.y = cmp(r1.w < 0);
  r5.x = (int)-r5.x + (int)r5.y;
  r5.x = (int)r5.x;
  r1.w = r5.x * r1.w;
  r1.w = min(r2.z, r1.w);
  r2.z = cmp(0 < r5.w);
  r5.xyz = cmp(r5.xwz < float3(0,0,0));
  r2.z = (int)-r2.z + (int)r5.y;
  r2.z = (int)r2.z;
  r5.y = r2.z * r5.w;
  r2.zw = cmp(r2.zw < float2(0,0));
  r1.w = min(r5.y, r1.w);
  r0.y = saturate(r1.w * cb1[0].z + r0.y);
  r0.y = min(r0.y, r2.y);
  r0.y = -1 + r0.y;
  r5.yw = r1.xy * cb1[3].zw + float2(-0.5,-0.5);
  r5.yw = abs(r5.yw) * float2(12,12) + float2(-4.80000019,-4.80000019);
  r5.yw = max(float2(0,0), r5.yw);
  r1.w = dot(r5.yw, r5.yw);
  r1.w = 1 + -r1.w;
  r1.w = max(0, r1.w);
  r0.y = r1.w * r0.y;

  if (AO_INTENSITY>= 1.f) {
    r0.y = r0.y * 4.0;
  }
  
  r6.xzw = cb1[0].www * r0.yyy + float3(1,1,1);
  r0.y = -cb1[2].w + r1.z;
  r1.z = cmp(0 >= r1.z);
  r1.z = (int)r2.w | (int)r1.z;
  r1.w = cmp(0 >= r0.y);
  r0.y = -cb1[2].w + r0.y;
  r1.w = (int)r5.z | (int)r1.w;
  r2.y = ~(int)r1.z;
  r1.w = r1.w ? r2.y : 0;
  r1.z = (int)r1.z | (int)r1.w;
  r1.w = r1.w ? 1.000000 : 0;
  r2.y = ~(int)r1.z;
  r2.w = cmp(0 >= r0.y);
  r0.y = -cb1[2].w + r0.y;
  r0.y = cmp(0 >= r0.y);
  r0.y = (int)r2.z | (int)r0.y;
  r2.z = (int)r5.x | (int)r2.w;
  r2.y = r2.z ? r2.y : 0;
  r1.z = (int)r1.z | (int)r2.y;
  r1.w = r2.y ? 2 : r1.w;
  r2.y = ~(int)r1.z;
  r0.y = r0.y ? r2.y : 0;
  r1.w = r0.y ? 3 : r1.w;
  r0.y = (int)r1.z | (int)r0.y;
  r0.y = r0.y ? r1.w : 4;
  r1.z = -cb1[2].w * r0.y;
  r0.x = -cb1[2].w * r0.y + r0.x;
  r2.z = r2.x * r0.x + cb1[2].z;
  r0.x = 0.015625 * r1.z;
  r0.xy = r0.zw * r0.xx + r1.xy;
  r0.xy = saturate(cb1[3].zw * r0.xy);
  r0.z = 1 + -r0.y;
  r2.xy = r0.xz * float2(2,2) + float2(-1,-1);
  r0.x = cb0[14].w;
  r0.y = cb0[15].w;
  r2.w = 1;
  r0.z = dot(r0.xy, r2.zw);
  r2.xyzw = r2.xyzw / r0.zzzz;
  r1.x = cb1[3].z * r1.x;
  r1.z = -r1.y * cb1[3].w + 1;
  r3.xy = r1.xz * float2(2,2) + float2(-1,-1);
  r3.w = 1;
  r0.x = dot(r0.xy, r3.zw);
  r0.xyzw = r3.xyzw / r0.xxxx;
  r0.xyzw = r2.xyzw + -r0.xyzw;
  r1.x = cb0[12].x;
  r1.y = cb0[13].x;
  r1.z = cb0[14].x;
  r1.w = cb0[15].x;
  r1.x = dot(r1.xyzw, r0.xyzw);
  r2.x = cb0[12].y;
  r2.y = cb0[13].y;
  r2.z = cb0[14].y;
  r2.w = cb0[15].y;
  r1.y = dot(r2.xyzw, r0.xyzw);
  r2.x = cb0[12].z;
  r2.y = cb0[13].z;
  r2.z = cb0[14].z;
  r2.w = cb0[15].z;
  r1.z = dot(r2.xyzw, r0.xyzw);
  r0.x = dot(r1.xyz, r1.xyz);
  r0.x = sqrt(r0.x);
  r0.x = 0.25 * r0.x;
  r0.x = min(1, r0.x);
  r0.x = 255 * r0.x;
  r0.x = floor(r0.x);
  r6.y = 0.00392156886 * r0.x;
  
  u0[r4.xy] = r6;
  return;
}